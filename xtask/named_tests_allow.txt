# Named tests (§16.1 / §16.2 suite-level / §16.4 / §16.6) that do not exist yet.
#
# `architecture::every_named_test_exists` is ONE-DIRECTIONAL and SCOPED (§21 item 28):
# every documented name must exist, extra tests are allowed. A name may sit here only
# while the slice that owns it has not run; the check FAILS if an entry here is
# satisfied, so the list can only shrink. One name per line, `name  # owner`.
#
# Recorded by the Slice 3 foundations correction pass. Every Slice-3-owned name in
# §16.1 now exists; everything below belongs to a later work package.

# ── 4F `components/dialog.rs` (Adjudication N1) ──
a_growing_body_resizes_the_layer_on_the_next_frame  # Slice 4F — components/dialog.rs
action_arming_is_evaluated_in_update  # Slice 4F — components/dialog.rs
confirm_is_centred_by_the_resolver_not_by_the_dialog  # Slice 4F — components/dialog.rs
convenience_constructors_render_through_the_body_slot  # Slice 4F — components/dialog.rs
draw_lays_out_against_the_height_it_asked_for  # Slice 4F — components/dialog.rs

# ── 4F `components/form.rs` (§15.1, §23 K1) ──
at_most_one_action_per_frame_in_declaration_order  # Slice 4F — components/form.rs
changing_options_between_frames_does_not_rebuild_props  # Slice 4F — components/form.rs
chooser_activation_emits_chose_with_the_field_id  # Slice 4F — components/form.rs
dirty_is_set_by_a_commit_not_by_a_keystroke  # Slice 4F — components/form.rs
enter_submits_only_when_the_focused_control_is_not_editing  # Slice 4F — components/form.rs
every_declared_field_resolves_a_value  # Slice 4F — components/form.rs
field_height_is_a_pure_function_of_spec_and_design_tokens  # Slice 4F — components/form.rs
form_action_variants_carry_no_value  # Slice 4F — components/form.rs
hidden_field_registers_no_ring_entry_and_keeps_its_draft  # Slice 4F — components/form.rs
note_rows_register_only_decorative_regions  # Slice 4F — components/form.rs
open_select_popover_dismisses_on_focus_out_and_esc_closes_only_the_popover  # Slice 4F — components/form.rs
scroll_reveals_the_focused_field_from_update_not_draw  # Slice 4F — components/form.rs
select_field_options_come_from_form_data  # Slice 4F — components/form.rs
state_holds_no_props  # Slice 4F — components/form.rs
submit_chord_is_declared_on_the_action_not_baked_in  # Slice 4F — components/form.rs
submit_commits_the_in_flight_edit_before_validating  # Slice 4F — components/form.rs
submit_skips_hidden_fields_during_validation  # Slice 4F — components/form.rs
submit_validates_every_visible_field_then_focuses_the_first_error  # Slice 4F — components/form.rs
tab_order_follows_declaration_order_skipping_hidden  # Slice 4F — components/form.rs
value_and_options_is_a_single_borrow  # Slice 4F — components/form.rs
zeroize_overwrites_every_secret_draft  # Slice 4F — components/form.rs

# ── Slices 5–7 application integration tests (`apps/**/tests`) ──
author_component_page_participates_in_focus_and_hover  # Slices 5-7 — apps/**
complete_flow_keyboard_first  # Slices 5-7 — apps/**
complete_navigation_visits_every_page_and_every_state  # Slices 5-7 — apps/**
connection_form_focuses_the_first_invalid_field  # Slices 5-7 — apps/**
connection_form_keyboard_and_mouse_reach_every_field  # Slices 5-7 — apps/**
connection_password_is_masked_and_absent_from_the_frame  # Slices 5-7 — apps/**
custom_theme_injection_repaints_every_page  # Slices 5-7 — apps/**
focus_is_restored_after_every_overlay_closes  # Slices 5-7 — apps/**
form_dialog_secret_never_reaches_the_screen_as_a_string  # Slices 5-7 — apps/**
form_dialog_toggles_visibility_and_keeps_drafts  # Slices 5-7 — apps/**
grid_adapter_keeps_every_pending_change_capability  # Slices 5-7 — apps/**
keyboard_flow_full_journey  # Slices 5-7 — apps/**
local_override_page_shows_three_distinct_buttons  # Slices 5-7 — apps/**
mouse_flow_full_journey  # Slices 5-7 — apps/**
nested_overlay_picker_inside_dialog  # Slices 5-7 — apps/**
resize_across_every_supported_size  # Slices 5-7 — apps/**
result_grid_sorts_locally_and_refuses_edits  # Slices 5-7 — apps/**
view_grid_is_read_only_with_a_reason  # Slices 5-7 — apps/**

# ── §16.6 benchmarks whose subject does not exist yet ──
frame_hintbar_derived  # Slices 4-7 — tests/perf.rs
frame_tablepro_connection_form_120x40  # Slices 4-7 — tests/perf.rs
frame_tablepro_query_editor_2k_lines  # Slices 4-7 — tests/perf.rs

# ── Slice 4 component state machines (`components/*.rs`, work packages 4A–4G) ──
cell_actions_affordance_is_painted_for_a_read_only_model  # Slice 4 — components/*.rs
click_inside_an_active_inline_edit_goes_to_the_editor  # Slice 4 — components/*.rs
edit_counter_invalidates_the_highlight_cache  # Slice 4 — components/*.rs
edit_intent_inline_cycle_external_refuse  # Slice 4 — components/*.rs
expand_collapse_is_keyed_not_positional  # Slice 4 — components/*.rs
lazy_children_do_not_reflatten_the_world  # Slice 4 — components/*.rs
query_change_emits_query_changed  # Slice 4 — components/*.rs
range_copy_is_tsv  # Slice 4 — components/*.rs
range_selection_uses_the_anchor  # Slice 4 — components/*.rs
read_only_reason_is_rendered_from_a_grid_model  # Slice 4 — components/*.rs
read_only_update_takes_a_shared_model  # Slice 4 — components/*.rs
retention_fixes_up_selection_and_caret  # Slice 4 — components/*.rs
rewind_retains_per_step_state  # Slice 4 — components/*.rs
select_all_selects_only_enabled_items  # Slice 4 — components/*.rs
sort_is_a_permutation_and_edits_stay_bound_to_the_source_row  # Slice 4 — components/*.rs
update_editable_commits_through_the_editor  # Slice 4 — components/*.rs
