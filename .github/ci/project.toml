# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 3
repository = "donbeave/terminal-components-claude"
profile = "generic"
verified = true
default_branch = "main"
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_dispatch_providers = ["github-hosted"]

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "mise-present", "rust-binaries:holla:1", "rust-binaries:jackin-preview:1", "rust-binaries:showcase:1", "rust-binaries:tablepro:1", "rust-binaries:xtask:1", "rust-features:junie-tui:3", "rust-test-targets:holla:1", "rust-test-targets:jackin-preview:1", "rust-test-targets:junie-tui:1", "rust-test-targets:showcase:1", "rust-test-targets:tablepro:1", "rust-workspace-members:7"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs."]

[workflow]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml", "perf.yml"]
notes = ["Rust verification uses Mr. Boxington 1.12.0 on providers [github-hosted]. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Local-provider jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "rust-holla"
label = "Rust crate (holla)"
kind = "rust"
root = "apps/holla"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/holla/**/*.rs", "apps/holla/Cargo.toml", "apps/holla/benches/**", "apps/holla/examples/**", "apps/holla/src/**", "apps/holla/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'apps/holla' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/holla' && mbx clippy --locked --profile test --all-targets --all-features --package 'holla' -- -D warnings", "cd -- 'apps/holla' && mbx test --locked --all-features --package 'holla'"]
full_commands = ["cd -- 'apps/holla' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/holla' && mbx clippy --locked --profile test --all-targets --all-features --package 'holla' -- -D warnings", "cd -- 'apps/holla' && mbx test --locked --all-features --package 'holla'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'apps/holla' && mbx check --locked --profile test --all-targets --all-features --package 'holla'"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-jackin-preview"
label = "Rust crate (jackin-preview)"
kind = "rust"
root = "apps/jackin-preview"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/jackin-preview/**/*.rs", "apps/jackin-preview/Cargo.toml", "apps/jackin-preview/benches/**", "apps/jackin-preview/examples/**", "apps/jackin-preview/src/**", "apps/jackin-preview/tests/**", "apps/jackin-preview/tests/perf_baseline.txt", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'apps/jackin-preview' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/jackin-preview' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-preview' -- -D warnings", "cd -- 'apps/jackin-preview' && mbx test --locked --all-features --package 'jackin-preview'"]
full_commands = ["cd -- 'apps/jackin-preview' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/jackin-preview' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-preview' -- -D warnings", "cd -- 'apps/jackin-preview' && mbx test --locked --all-features --package 'jackin-preview'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'apps/jackin-preview' && mbx check --locked --profile test --all-targets --all-features --package 'jackin-preview'"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-junie-tui"
label = "Rust crate (junie-tui)"
kind = "rust"
root = "crates/tui"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/tui/**/*.rs", "crates/tui/Cargo.toml", "crates/tui/README.md", "crates/tui/benches/**", "crates/tui/examples/**", "crates/tui/src/**", "crates/tui/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui' -- -D warnings", "cd -- 'crates/tui' && mbx test --locked --all-features --package 'junie-tui'"]
full_commands = ["cd -- 'crates/tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui' -- -D warnings", "cd -- 'crates/tui' && mbx test --locked --all-features --package 'junie-tui'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/tui' && mbx check --locked --profile test --all-targets --all-features --package 'junie-tui'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-junie-tui-testing"
label = "Rust crate (junie-tui-testing)"
kind = "rust"
root = "crates/tui-testing"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/tui-testing/**/*.rs", "crates/tui-testing/Cargo.toml", "crates/tui-testing/benches/**", "crates/tui-testing/examples/**", "crates/tui-testing/src/**", "crates/tui-testing/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/tui-testing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui-testing' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui-testing' -- -D warnings", "cd -- 'crates/tui-testing' && mbx test --locked --all-features --package 'junie-tui-testing'"]
full_commands = ["cd -- 'crates/tui-testing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui-testing' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui-testing' -- -D warnings", "cd -- 'crates/tui-testing' && mbx test --locked --all-features --package 'junie-tui-testing'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/tui-testing' && mbx check --locked --profile test --all-targets --all-features --package 'junie-tui-testing'"]
depends_on = ["rust-junie-tui"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-showcase"
label = "Rust crate (showcase)"
kind = "rust"
root = "apps/showcase"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/showcase/**/*.rs", "apps/showcase/Cargo.toml", "apps/showcase/benches/**", "apps/showcase/examples/**", "apps/showcase/src/**", "apps/showcase/tests/**", "apps/showcase/tests/fixtures/holla-page-headings.tsv", "apps/showcase/tests/fixtures/holla-shell-headers.tsv", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'apps/showcase' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/showcase' && mbx clippy --locked --profile test --all-targets --all-features --package 'showcase' -- -D warnings", "cd -- 'apps/showcase' && mbx test --locked --all-features --package 'showcase'"]
full_commands = ["cd -- 'apps/showcase' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/showcase' && mbx clippy --locked --profile test --all-targets --all-features --package 'showcase' -- -D warnings", "cd -- 'apps/showcase' && mbx test --locked --all-features --package 'showcase'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'apps/showcase' && mbx check --locked --profile test --all-targets --all-features --package 'showcase'"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-tablepro"
label = "Rust crate (tablepro)"
kind = "rust"
root = "apps/tablepro"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/tablepro/**/*.rs", "apps/tablepro/Cargo.toml", "apps/tablepro/benches/**", "apps/tablepro/examples/**", "apps/tablepro/src/**", "apps/tablepro/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'apps/tablepro' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/tablepro' && mbx clippy --locked --profile test --all-targets --all-features --package 'tablepro' -- -D warnings", "cd -- 'apps/tablepro' && mbx test --locked --all-features --package 'tablepro'"]
full_commands = ["cd -- 'apps/tablepro' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/tablepro' && mbx clippy --locked --profile test --all-targets --all-features --package 'tablepro' -- -D warnings", "cd -- 'apps/tablepro' && mbx test --locked --all-features --package 'tablepro'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'apps/tablepro' && mbx check --locked --profile test --all-targets --all-features --package 'tablepro'"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-xtask"
label = "Rust crate (xtask)"
kind = "rust"
root = "xtask"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "tools/app-inventory.json", "tools/historical-render/pins.json", "xtask/**/*.rs", "xtask/Cargo.toml", "xtask/benches/**", "xtask/examples/**", "xtask/src/**", "xtask/tests/**"]
pr_commands = ["cd -- 'xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'xtask' -- -D warnings", "cd -- 'xtask' && mbx test --locked --all-features --package 'xtask'"]
full_commands = ["cd -- 'xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'xtask' -- -D warnings", "cd -- 'xtask' && mbx test --locked --all-features --package 'xtask'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'xtask' && mbx check --locked --profile test --all-targets --all-features --package 'xtask'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-gates"
label = ""
kind = "rust"
root = "."
watch = []
pr_commands = ["mbx check --workspace --all-targets --locked", "mise run ci-gates-metadata", "mise run ci-gates-backend-free", "mise run ci-gates-examples", "mise run ci-gates-render-reachable", "mise run ci-gates-doc", "mise run ci-gates-app-inventory", "mise run ci-gates-boundary", "mise run ci-gates-bless-guard", "mise run ci-gates-allow-lists", "mise run ci-gates-doc-check"]
full_commands = ["mbx check --workspace --all-targets --locked", "mise run ci-gates-metadata", "mise run ci-gates-backend-free", "mise run ci-gates-examples", "mise run ci-gates-render-reachable", "mise run ci-gates-doc", "mise run ci-gates-app-inventory", "mise run ci-gates-boundary", "mise run ci-gates-bless-guard", "mise run ci-gates-allow-lists", "mise run ci-gates-doc-check"]
platform = "linux-x64"
trust = "untrusted-ok"
workspace_check = true
[unit.capabilities]
docker = false
nested_privileged_docker = false
buildx_compose = false
testcontainers = false
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false

[[unit]]
id = "rust-msrv"
label = ""
kind = "rust"
root = "."
watch = []
pr_commands = ["mbx check --workspace --all-targets --locked", "mise run ci-msrv-all-features"]
full_commands = ["mbx check --workspace --all-targets --locked", "mise run ci-msrv-all-features"]
platform = "linux-x64"
trust = "untrusted-ok"
workspace_check = true
[unit.capabilities]
docker = false
nested_privileged_docker = false
buildx_compose = false
testcontainers = false
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
