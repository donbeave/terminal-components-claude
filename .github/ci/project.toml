# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 2
repository = "donbeave/terminal-components-claude"
profile = "generic"
verified = true
default_branch = "main"
runners = "github"

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "cargo-nextest-policy", "mise-present", "rust-binaries:holla:1", "rust-binaries:jackin-preview:1", "rust-binaries:refactor-proof:1", "rust-binaries:showcase:1", "rust-binaries:tablepro:1", "rust-binaries:xtask:1", "rust-features:junie-tui:3", "rust-test-targets:holla:1", "rust-test-targets:jackin-preview:1", "rust-test-targets:junie-tui:1", "rust-test-targets:refactor-proof:1", "rust-test-targets:showcase:1", "rust-test-targets:tablepro:1", "rust-workspace-members:8"]
limitations = ["Manifests nested under tests/ or benches/ directories are treated as test fixtures and ignored.", "Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs."]

[workflow]
github_runner = "ubuntu-24.04"
velnor_labels = []
files = ["ci-extra-gates.yml", "ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml"]
notes = ["Rust verification uses Mr. Boxington 1.11.1 with its GitHub cache backend and bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded)."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "rust-holla"
label = "Rust crate (holla)"
kind = "rust"
root = "apps/holla"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/holla/**/*.rs", "apps/holla/Cargo.toml", "apps/holla/benches/**", "apps/holla/examples/**", "apps/holla/src/**", "apps/holla/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'apps/holla' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/holla' && mbx nextest run --locked --all-features --package 'holla' --no-tests pass", "cd -- 'apps/holla' && mbx clippy --locked --profile test --all-targets --all-features --package 'holla' -- -D warnings"]
github_full_commands = ["cd -- 'apps/holla' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/holla' && mbx nextest run --locked --all-features --package 'holla' --no-tests pass", "cd -- 'apps/holla' && mbx clippy --locked --profile test --all-targets --all-features --package 'holla' -- -D warnings"]
velnor_pr_commands = ["cd -- 'apps/holla' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/holla' && mbx nextest run --locked --all-features --package 'holla' --no-tests pass", "cd -- 'apps/holla' && mbx clippy --locked --profile test --all-targets --all-features --package 'holla' -- -D warnings"]
velnor_full_commands = ["cd -- 'apps/holla' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/holla' && mbx nextest run --locked --all-features --package 'holla' --no-tests pass", "cd -- 'apps/holla' && mbx clippy --locked --profile test --all-targets --all-features --package 'holla' -- -D warnings"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-jackin-preview"
label = "Rust crate (jackin-preview)"
kind = "rust"
root = "apps/jackin-preview"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/jackin-preview/**/*.rs", "apps/jackin-preview/Cargo.toml", "apps/jackin-preview/benches/**", "apps/jackin-preview/examples/**", "apps/jackin-preview/src/**", "apps/jackin-preview/tests/**", "apps/jackin-preview/tests/perf_baseline.txt", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'apps/jackin-preview' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/jackin-preview' && mbx nextest run --locked --all-features --package 'jackin-preview' --no-tests pass", "cd -- 'apps/jackin-preview' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-preview' -- -D warnings"]
github_full_commands = ["cd -- 'apps/jackin-preview' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/jackin-preview' && mbx nextest run --locked --all-features --package 'jackin-preview' --no-tests pass", "cd -- 'apps/jackin-preview' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-preview' -- -D warnings"]
velnor_pr_commands = ["cd -- 'apps/jackin-preview' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/jackin-preview' && mbx nextest run --locked --all-features --package 'jackin-preview' --no-tests pass", "cd -- 'apps/jackin-preview' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-preview' -- -D warnings"]
velnor_full_commands = ["cd -- 'apps/jackin-preview' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/jackin-preview' && mbx nextest run --locked --all-features --package 'jackin-preview' --no-tests pass", "cd -- 'apps/jackin-preview' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-preview' -- -D warnings"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-junie-tui"
label = "Rust crate (junie-tui)"
kind = "rust"
root = "crates/tui"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/tui/**/*.rs", "crates/tui/Cargo.toml", "crates/tui/README.md", "crates/tui/benches/**", "crates/tui/examples/**", "crates/tui/src/**", "crates/tui/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui' && mbx nextest run --locked --all-features --package 'junie-tui' --no-tests pass", "cd -- 'crates/tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui' -- -D warnings"]
github_full_commands = ["cd -- 'crates/tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui' && mbx nextest run --locked --all-features --package 'junie-tui' --no-tests pass", "cd -- 'crates/tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui' && mbx nextest run --locked --all-features --package 'junie-tui' --no-tests pass", "cd -- 'crates/tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui' && mbx nextest run --locked --all-features --package 'junie-tui' --no-tests pass", "cd -- 'crates/tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui' -- -D warnings"]
depends_on = ["rust-junie-tui-testing"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-junie-tui-testing"
label = "Rust crate (junie-tui-testing)"
kind = "rust"
root = "crates/tui-testing"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/tui-testing/**/*.rs", "crates/tui-testing/Cargo.toml", "crates/tui-testing/benches/**", "crates/tui-testing/examples/**", "crates/tui-testing/src/**", "crates/tui-testing/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/tui-testing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui-testing' && mbx nextest run --locked --all-features --package 'junie-tui-testing' --no-tests pass", "cd -- 'crates/tui-testing' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui-testing' -- -D warnings"]
github_full_commands = ["cd -- 'crates/tui-testing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui-testing' && mbx nextest run --locked --all-features --package 'junie-tui-testing' --no-tests pass", "cd -- 'crates/tui-testing' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui-testing' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/tui-testing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui-testing' && mbx nextest run --locked --all-features --package 'junie-tui-testing' --no-tests pass", "cd -- 'crates/tui-testing' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui-testing' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/tui-testing' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/tui-testing' && mbx nextest run --locked --all-features --package 'junie-tui-testing' --no-tests pass", "cd -- 'crates/tui-testing' && mbx clippy --locked --profile test --all-targets --all-features --package 'junie-tui-testing' -- -D warnings"]
depends_on = ["rust-junie-tui"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-refactor-proof"
label = "Rust crate (refactor-proof)"
kind = "rust"
root = "tools/refactor-proof"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "tools/refactor-proof/**/*.rs", "tools/refactor-proof/Cargo.toml", "tools/refactor-proof/benches/**", "tools/refactor-proof/examples/**", "tools/refactor-proof/src/**", "tools/refactor-proof/tests/**"]
github_pr_commands = ["cd -- 'tools/refactor-proof' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/refactor-proof' && mbx nextest run --locked --all-features --package 'refactor-proof' --no-tests pass", "cd -- 'tools/refactor-proof' && mbx clippy --locked --profile test --all-targets --all-features --package 'refactor-proof' -- -D warnings"]
github_full_commands = ["cd -- 'tools/refactor-proof' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/refactor-proof' && mbx nextest run --locked --all-features --package 'refactor-proof' --no-tests pass", "cd -- 'tools/refactor-proof' && mbx clippy --locked --profile test --all-targets --all-features --package 'refactor-proof' -- -D warnings"]
velnor_pr_commands = ["cd -- 'tools/refactor-proof' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/refactor-proof' && mbx nextest run --locked --all-features --package 'refactor-proof' --no-tests pass", "cd -- 'tools/refactor-proof' && mbx clippy --locked --profile test --all-targets --all-features --package 'refactor-proof' -- -D warnings"]
velnor_full_commands = ["cd -- 'tools/refactor-proof' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/refactor-proof' && mbx nextest run --locked --all-features --package 'refactor-proof' --no-tests pass", "cd -- 'tools/refactor-proof' && mbx clippy --locked --profile test --all-targets --all-features --package 'refactor-proof' -- -D warnings"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-showcase"
label = "Rust crate (showcase)"
kind = "rust"
root = "apps/showcase"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/showcase/**/*.rs", "apps/showcase/Cargo.toml", "apps/showcase/benches/**", "apps/showcase/examples/**", "apps/showcase/src/**", "apps/showcase/tests/**", "apps/showcase/tests/fixtures/holla-page-headings.tsv", "apps/showcase/tests/fixtures/holla-shell-headers.tsv", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'apps/showcase' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/showcase' && mbx nextest run --locked --all-features --package 'showcase' --no-tests pass", "cd -- 'apps/showcase' && mbx clippy --locked --profile test --all-targets --all-features --package 'showcase' -- -D warnings"]
github_full_commands = ["cd -- 'apps/showcase' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/showcase' && mbx nextest run --locked --all-features --package 'showcase' --no-tests pass", "cd -- 'apps/showcase' && mbx clippy --locked --profile test --all-targets --all-features --package 'showcase' -- -D warnings"]
velnor_pr_commands = ["cd -- 'apps/showcase' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/showcase' && mbx nextest run --locked --all-features --package 'showcase' --no-tests pass", "cd -- 'apps/showcase' && mbx clippy --locked --profile test --all-targets --all-features --package 'showcase' -- -D warnings"]
velnor_full_commands = ["cd -- 'apps/showcase' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/showcase' && mbx nextest run --locked --all-features --package 'showcase' --no-tests pass", "cd -- 'apps/showcase' && mbx clippy --locked --profile test --all-targets --all-features --package 'showcase' -- -D warnings"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-tablepro"
label = "Rust crate (tablepro)"
kind = "rust"
root = "apps/tablepro"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "apps/tablepro/**/*.rs", "apps/tablepro/Cargo.toml", "apps/tablepro/benches/**", "apps/tablepro/examples/**", "apps/tablepro/src/**", "apps/tablepro/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'apps/tablepro' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/tablepro' && mbx nextest run --locked --all-features --package 'tablepro' --no-tests pass", "cd -- 'apps/tablepro' && mbx clippy --locked --profile test --all-targets --all-features --package 'tablepro' -- -D warnings"]
github_full_commands = ["cd -- 'apps/tablepro' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/tablepro' && mbx nextest run --locked --all-features --package 'tablepro' --no-tests pass", "cd -- 'apps/tablepro' && mbx clippy --locked --profile test --all-targets --all-features --package 'tablepro' -- -D warnings"]
velnor_pr_commands = ["cd -- 'apps/tablepro' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/tablepro' && mbx nextest run --locked --all-features --package 'tablepro' --no-tests pass", "cd -- 'apps/tablepro' && mbx clippy --locked --profile test --all-targets --all-features --package 'tablepro' -- -D warnings"]
velnor_full_commands = ["cd -- 'apps/tablepro' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'apps/tablepro' && mbx nextest run --locked --all-features --package 'tablepro' --no-tests pass", "cd -- 'apps/tablepro' && mbx clippy --locked --profile test --all-targets --all-features --package 'tablepro' -- -D warnings"]
depends_on = ["rust-junie-tui", "rust-junie-tui-testing"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-xtask"
label = "Rust crate (xtask)"
kind = "rust"
root = "xtask"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "tools/app-inventory.json", "xtask/**/*.rs", "xtask/Cargo.toml", "xtask/benches/**", "xtask/examples/**", "xtask/src/**", "xtask/tests/**"]
github_pr_commands = ["cd -- 'xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'xtask' && mbx nextest run --locked --all-features --package 'xtask' --no-tests pass", "cd -- 'xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'xtask' -- -D warnings", "mise run boundary", "mise run bless-guard", "mise run doc-check", "mise run rustdoc", "mise run lychee"]
github_full_commands = ["cd -- 'xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'xtask' && mbx nextest run --locked --all-features --package 'xtask' --no-tests pass", "cd -- 'xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'xtask' -- -D warnings", "mise run boundary", "mise run bless-guard", "mise run doc-check", "mise run rustdoc", "mise run lychee"]
velnor_pr_commands = ["cd -- 'xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'xtask' && mbx nextest run --locked --all-features --package 'xtask' --no-tests pass", "cd -- 'xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'xtask' -- -D warnings", "mise run boundary", "mise run bless-guard", "mise run doc-check", "mise run rustdoc", "mise run lychee"]
velnor_full_commands = ["cd -- 'xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'xtask' && mbx nextest run --locked --all-features --package 'xtask' --no-tests pass", "cd -- 'xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'xtask' -- -D warnings", "mise run boundary", "mise run bless-guard", "mise run doc-check", "mise run rustdoc", "mise run lychee"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
