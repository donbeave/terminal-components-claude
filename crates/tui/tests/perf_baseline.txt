# perf baseline: name ns allocs bytes [hits ring]
# regenerate with PERF_BLESS=1 in a release build; review like a snapshot
# the `#` header is preserved across a bless, so the reasons below survive
#
# Re-blessed by the Slice 3 foundations correction pass (docs/reviews/
# slice3-foundations-review.md F18, adjudications 4, 6 and 8). What changed and why:
#
#  fit_10k_grapheme_line_to_80        7 -> 0 allocs. Adjudication 4: the benchmark now
#                                     drives `RowUi::label` (the ellipsis path it is
#                                     named for) over a corpus whose symbols fit
#                                     ratatui `Cell`'s inline storage, so the painter's
#                                     own allocation count is asserted at exactly 0.
#                                     `<= 8` was rejected as a magic constant.
#  fit_10k_grapheme_line_to_80_wide   added. The ZWJ-emoji corpus, reported: the 7
#                                     allocations are ratatui `Cell` heap symbols, a
#                                     property of the buffer, and are asserted equal
#                                     between a 10 k and a 100 k line and <= 80.
#  measure_is_allocation_free         added (Adjudication N2): `Ui::resolve` is the
#                                     uncached `&self` path `Measure::measure` uses.
#  paint_spans_500_rows_is_...        added (BL-4): the span painter no longer collects
#                                     a `Vec<RawSpan>` per call.
#  style_downgrade_theme_all_levels   1034 -> 1079 allocs, 165008 -> 170904 bytes.
#                                     MA-6/F14: `Recipes` now carries the neutral
#                                     fallback recipe a `Family::custom(..)` resolves
#                                     through, and `Theme::downgrade` clones it once
#                                     per level (4 levels x 12 row-like parts); and
#                                     MI-13 applies the mono state-rule fallbacks to a
#                                     family's variant maps as well as its base parts,
#                                     which is what keeps `pressed` distinguishable
#                                     without colour under a variant that re-declares
#                                     PRESSED. This is a one-shot theme operation,
#                                     asserted never to appear in a `frame_*` profile.
#
# Added by the Slice 2 prototype package (components), all five §16.6 rows that
# needed a component to exist:
#
#  list_1k_rows_render                the control arm of R1.
#  list_100k_rows_render              < 500 allocs/frame (measures 0) and ns <= 1.5x
#                                     the 1 k control; a 5 000-key selection is live so
#                                     the per-visible-row `KeySet` lookup is in the
#                                     measurement.
#  list_100k_select_all               < 100 allocs (measures 0): `ToggledAll` is a
#                                     set-level operation and never materialises
#                                     100 000 `ItemKey`s (R7).
#  event_dispatch_is_not_o_n          0 allocs; the raw 100k-vs-100 ns ratio is
#                                     reported and asserted only under PERF_STRICT=1.
#  frame_showcase_buttons_120x40      the migrated showcase Buttons page: nine buttons,
#                                     two disabled, 0 allocs/frame, 9 hit regions and a
#                                     7-entry ring.
#
#  every ns column                    re-measured on the blessing machine; ns is
#                                     reported always and asserted only under
#                                     PERF_STRICT=1, so these are a snapshot, not a
#                                     contract. The allocation and byte columns are the
#                                     hard assertions.
#
# Recorded by Adjudication O (docs/reviews/adjudication-o-foundations-followups.md):
#
#  style_resolve_10k_parts            memo hit rate 0.997, measured on the shipped
#                                     two-way set-associative memo (hits=907088,
#                                     misses=2912; 32 keys over 128 two-way sets,
#                                     PERF-CACHE line). O1: a one-way table of the same
#                                     256 entries measures 0.872 on this key set and a
#                                     broken key ~0.003, so the >= 0.90 gate is a
#                                     key-correctness floor, not a performance bound --
#                                     diagnose the geometry from PERF-CACHE before
#                                     suspecting the key. O4a moves the binding style
#                                     budget here, the low-noise pure-resolution loop:
#                                     under PERF_STRICT, ns / 10 000 x 2 000 <= 32 000,
#                                     i.e. <= 16.0 ns per query (blessed at 12.0).
#  style_resolve_per_frame            deliberately carries NO row below. It reports a
#                                     difference of two medians (styles resolved per row
#                                     minus hoisted), and a baselined ns for a
#                                     differential invites a meaningless x1.2 regression
#                                     check (O4a correction 3). Its strict net
#                                     extrapolates the 160-query difference -- 200 in
#                                     arm A minus 40 in arm B, not 200 -- to 2 000
#                                     queries against the same 32 us ceiling.
#  viewport_100k_lines_reindex        added by the exact-layout adjudication: report-only
#                                     cold prefix storage plus width-change and explicit-
#                                     invalidation O(document) index work. Warm work stays
#                                     in viewport_100k_lines_render.
event_dispatch_is_not_o_n 135185 0 0
fit_10k_grapheme_line_to_80 156980 0 0
fit_10k_grapheme_line_to_80_wide 333847 7 224
focus_tab_traversal_ring_200 381966 0 0
frame_form_update_draw 36005 0 0
frame_hintbar_derived 2384 0 0
frame_showcase_buttons_120x40 8275 0 0 9 7
frame_testbackend_empty_120x40 24886 0 0
fuzzy_10k_grapheme_label 323778 15 262232
hit_registry_size_is_bounded 4040 0 0 80 40
intents_drain_is_o_1_when_the_queue_is_empty 632 0 0
list_100k_rows_render 130740 0 0
list_100k_select_all 63000 0 0
list_1k_rows_render 131116 0 0
measure_is_allocation_free 7619 0 0
mouse_move_over_1000_regions 7261112 0 0
paint_spans_500_rows_is_allocation_free 113159 0 0
picker_100k_borrowed_domain_render 35372 0 0
style_backdrop_full_screen_120x40 41866 0 0
style_downgrade_theme_all_levels 65004 1079 170904
style_resolve_10k_parts 120141 0 0
style_resolve_10k_parts_with_two_overlays 121787 0 0
textbuffer_offset_at_10k_line 287795 0 0
textbuffer_pos_of_10k_line 339752 0 0
truncate_10k_grapheme_line_to_80 320007 6 288
truncate_middle_10k_to_40 328753 1 128
viewport_100k_lines_push 552000 4 400644
viewport_100k_lines_render 98597 0 0 6 1
viewport_layout_10k_grapheme_line 1737908 0 0
width_10k_grapheme_line 341420 0 0
wrap_10k_graphemes_to_80 457620 1119 82416
