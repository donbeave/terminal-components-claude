[package]
name = "tui-next-testing"
description = "Test harness, digest scene, conformance driver and perf allocator for tui-next"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "tui_next_testing"
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
tui-next = { workspace = true, features = ["testing"] }
ratatui-core.workspace = true
bitflags.workspace = true
