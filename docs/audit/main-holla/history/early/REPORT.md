# Early semantic history audit — indices 0–6

Scope: reversed `tree-audit.json.patches`, inclusive indices 0–6, pinned MAIN_BASE `c12cad8728755cd2d03eefdd8e02891143fca86d`. Read-only source audit. External artifacts only. Indices 7–8 delegated by parent to history_api; their exact byte-application checks here do not claim semantic review.

## Method and precise coverage

Read the complete initial REFACTORING_GOAL creation (index 0) in contiguous chunks ≤18,000 characters; all three subsequent goal patches (1–3); complete initial COMPONENT_ARCHITECTURE creation (4); every added/deleted line of its extension (5), in contiguous chunks ≤18,000 characters. For index 6, reviewed all whitespace-token deltas against the exact parent, with old and new tokens and token offsets (`inline-6.txt`), plus the complete appended Adjudication J (`append-6.txt`). Unchanged context inherits its previously reviewed semantics; it was not reread as whole snapshots. Word-delta normalization ignores whitespace only, not wording, code tokens, punctuation, additions or deletions. Original exact patches remain authoritative for formatting.

Every original patch byte was read programmatically and SHA256 hashed. A strict unified-patch interpreter checked every context/deleted line against Git's exact parent blob, applied every added line, then compared the complete output bytes with Git's exact child blob. All 9 applications passed. For reviewed indices 0–6, each parent blob equals the most recently reviewed child of the same path or explicit absence; no unseen parent snapshot is used by induction. Seven child snapshots reviewed inductively; both path creations start from absence. No rename/deletion in these seven patches. None is formatting-only. Goal index1 includes trailing blank-line removal in addition to semantic changes. Identity evidence and hashes: `coverage.json`.

This is semantic document history evidence, not code implementation evidence. No build, current runtime reproduction, visual judgment, or behavioral bisect was performed by this worker. Historical [F] annotations and WP-0 measurements below remain reports until independently reproduced. Public API examples were read, not compiled.

## Revision decisions and supersessions

| Index / commit | Semantic change | Surviving implication / supersession |
|---|---|---|
| 0 e48137f | Creates 30-section implementation goal: reusable components, complete application migration, no permanent legacy, Junie parity, real customization, runtime ownership, safety, tests and independent review. | Still core obligations; current user adds Holla as fourth app, pins Holla product reference and main implementation base. |
| 1 dce91d5 | Removes JACKIN_REFERENCE/GOAL/FEEDBACK from historical authority/read lists; Jackin semantics reference current source/tests; removes final blank line. | Does not remove Jackin behavior preservation. Current user explicitly requires GOAL and reference/recovery documents, superseding this reading-list reduction. |
| 2 2d81eec | Adds mandatory Claude Fable coordinator/builders and Opus-only judgments/reviews; durable REFACTORING_STATE ledger. | Model restrictions explicitly superseded by current user. Independent review, evidence, sequencing and clear ownership survive. |
| 3 25ea92b | Replaces mandatory external-source checklist with flexible architecture research; removes mandatory specific primary URLs/version recording; broadens prior-art wording. | Evidence-backed alternatives remain. Current user separately mandates inspection/pinning of verification tools; system/developer browsing requirements still apply. |
| 4 2e45302 | Creates accepted architecture §§1–15, Adjudications A–I: two phases; caller state; runtime interaction; stable keys; Response; themes; open composition; domain split; secure text. | Architectural invariants survive except explicit amendments; exact impossible signatures were subsequently corrected. |
| 5 e8d053c | Adds §§16–20, twelve examples, full migration tables, exact test inventories/perf budgets, visual exceptions, sequencing, workspace/name/facade decision. | Explicit tests and disposition coverage cannot disappear silently. Some flawed staging/API statements corrected by J and later revisions. Historical exception list does not automatically authorize drift from newly pinned Holla. |
| 6 95ab652 | Applies full Slice2 review: 34-item J plus inline changes/examples. Fixes Rust ownership, action loss, Esc ordering, field duplicate registration, diagnostic semantics, missing APIs, conformance/provenance and staging. | J supersedes conflicting A–I details narrowly; Form shape and Grid update bound explicitly remain open here, later resolved by K/L/M worker. |

## Concrete obligation ledger from reviewed history

The following are requirement groups, not assertions that candidate code satisfies them. Parent integrator must map every group and named artifact to current implementation, gap and executed proof; exact expanded test lists remain in the reviewed original patches (especially §16).

| ID | Normative contract | Concrete acceptance evidence required |
|---|---|---|
| E01 | One coherent public component API; applications consume curated facade or public author layer; no permanent legacy/compatibility system. Compiler/package boundary preferred over scanner tricks. | External consumers/examples compile; dependency closure excludes apps; no `#[path]`/`include!` escape; controlled legacy deletion with every disposition mapped. |
| E02 | Caller owns durable state, app domain values/effects; short-lived props borrow configuration; update/draw separate. J moves collection items/model to phase arguments to avoid retained shared borrow blocking action mutation. | Compile examples where update returns before source mutation; Grid mutable model only in update; no data-owning collection props forcing frame clones. |
| E03 | Draw may paint/register/cursor/report derived layout but never commit/cancel/validate, change selection/focus/domain, navigate, emit effects, close layers. J permits only runtime-owned derived cache, keyed by `(Id,TypeId)`, invalidated by resize/theme/generation. | Repeated draw exact cells/registry and equal semantic state, effects unchanged; adversarial interior mutation coverage. Shared references alone do not prove purity (current user strengthens early claim). |
| E04 | Response consumption independent of Paint/Layout invalidation; semantic typed actions; J restricts `BitOr` to `Response<()>` so action folding cannot silently lose actions; optional source id; `is_consumed/is_changed` readers. | Boundary wheel consumed with no repaint; compile-fail action-response folding; map/erase preserve flow/invalidation; concrete layout invalidation behavior per current user. |
| E05 | Runtime exclusively owns focus, hover suppression, valid press-release activation, capture, flash, routing, cursor, modal scopes, scheduling. | Full keyboard/mouse equivalence; disabled skipping/read-only reachability; hover never focuses; suppression until pointer moves; release outside does not activate; exactly one global machine. |
| E06 | Stable namespaced component/item identity; item keys in collection actions; no positional logical selection after insertion/removal/reorder. | Reorder/insert/delete/filter/sort preserve chosen/edit target and close keys; collision diagnostics; test `separator_prevents_concatenation_collision`, `item_key_pair_is_order_sensitive`, `id_equality_ignores_debug_label`. J distinguishes child Id from subregion PartRef. |
| E07 | Geometry runtime-owned, authoritative painting/registration/clipping; empty/tiny rects safe. Layout facts available only through read services. | Every component 0×0…3×3, out-of-clip sentinels, stale-region click after empty draw, capture resize/removal, first-input/route/queued-input freshness strengthened by current user. |
| E08 | Focus scopes trap/restore; modal barriers active even when too small to draw. J delivers Esc to focused editor before app Bubble/layer dismiss/product ladder. No duplicate input on focus reruns. | `esc_reaches_the_focused_editor_before_the_layer`, `restore_target_receives_keys_before_the_next_draw`, convergence/no diagnostic, nested menu/picker in dialog, captured owner layer closes. |
| E09 | Runtime layer stack defines z-order independent of draw call order; outside click based on top interaction; duplicate layer drawing diagnosed; cursor only legitimate active owner/layer. | Reverse layer draw order produces same cells/routing; no background input; hidden cursor; no-op undrawn modal trap; focused-editor cancellation before dialog close. |
| E10 | Explicit scheduling/elapsed time; animation/flash/status design tokens. Runtime timer deadlines replace app frame heuristics. | Current user exact Buttons 2199/2200-ms tests, 1000 unrelated inputs no time progress, idle completion and repeated-draw invariance; reduced/paused live timing. |
| E11 | Junie exact approved semantic tokens, state grammar, surfaces and spacing; substantially different Paper theme; truecolor/256/16/mono for user themes. | Full cell/style/cursor and viewed captures across components/apps. Semantic glyph/modifier differences when hue absent; J adds pressed BOLD plus bracket glyphs, CHECKED precedence over SELECTED. |
| E12 | Deterministic override levels: base part → variant → matching states by specificity/declaration → global family/variant → nested scopes outer-to-inner → instance/part; roles resolve after merge against live surface/capability. Clear/remove semantics explicit. | Sentinel overrides affect intended cells only; merge identity/associativity/clear/modifier symmetry; every declared part actually queried AND resolved glyph painted; family/variant/subtree/instance/state/slot proof. |
| E13 | Ordered surface ladder, no color-equality elevation/dimming; contextual inheritance replaces raw bg parameters; all glyph/spacing/motion conventions semantic. | Duplicate-color theme and light theme preserve role distinctions; downgrade all tokens exhaustive; backdrop footer contract; custom colors via Role::Custom only through resolution. |
| E14 | Borrowed keyed collections, renderer hooks, distinct cursor/selection/activation and rich family-specific features; common scroll ownership/drag. | Borrowed domain row/cell consumers; no per-frame duplicated strings; wheel doesn't steal cursor; tree hierarchy/lazy state, tabs strip key, steps frontier, completion owner-focus, picker modal, real row hooks. |
| E15 | Grid generic mechanics only; TablePro adapters own SQL/null/default/FK/primary/pending/undo/query safety. DataTable absorbed only with every former capability preserved. | DOM audit 22-capability mapping; sorting permutation keeps edit source; named fixtures reach production renderer; real edit/validation/undo/filter/sort workflows and simulation effect counts. |
| E16 | Open dialog body/parts, convenience constructors same path; reusable Form/Help/Wizard/PickerChain/Completion controller; product FS/provider/Capsule/rain remain adapters. | Composed-vs-convenience digest equivalence; nested overlays, wizard rewind/per-step retention, staged retry/back, editor completion splice; no closed body enum or app generic duplicates. |
| E17 | Unified text lifecycle, controlled values, Unicode grapheme handling, validation closures, disabled/read-only distinctions, secure masks. Secret Debug/Display redacted, no Clone/Eq/Serialize; synthetic tail; memory wipe requirement historical. | Begin/commit/cancel/blur policy tests including validation once; secret invariant across views/debug/errors/captures/logs; J allocation-free `write_mask`; external validation test; zeroize proof where supported. |
| E18 | Every current module and app-side reusable control disposition explicit: original 31 widget modules + 23 app-side controls plus foundations. | §18 exhaustive map checked against source; Showcase 22 pages/shell/footer/inspector; TablePro and Jackin full scenarios; current user adds all Holla eleven scenarios and branch-only changes. |
| E19 | Conformance/render/behavior/process layers complement each other. Existing app tests retained; test identities enumerated and checked (not only count); reviewed baseline classification; no candidate-as-expected. | §16 unit names, suite-level names, per-component applicable capabilities; zero missing named tests; required feature/doc/binary tests; expected/actual/diff and provenance. J removes nonexistent SecretInput, Empty, Scrollbar cases and adds PropsList rather than faking coverage. |
| E20 | Before/after performance, deterministic allocations/bytes binding; all gates and docs complete, independent API/visual review. | Exact below; meaningful runtime/input sizes, retained baseline source/provenance; required user toolchain matrix; review committed exact candidate and artifacts; all 12 external examples and component rustdoc contract. |

## Original performance obligations requiring surviving mapping

At index5 §16.6/§20.9: Showcase lists <20 allocations/frame; TablePro 500×12 grid <100, ≤320 hits; Jackin manager100 <60; Capsule4panes <200; query-editor2k <40; core cursor/list/manager/wheel/focus/mouse/style paths zero allocations; zero allocations for unchanged derived HintBar added by J. TablePro grid load <8000 (one preformatted conversion); list100k render <500, ≤1.5× list1k timing; bytes/frame <64KiB for list100k and viewport100k; select-all <100 via AllExcept; tree toggle/flatten allocation <10×viewport and count-independent render; viewport push/render count-independent, layout10k zero allocations; code spans sorted/edit-counter keyed; no full tree scan per ordinary event. Styles ≤2× original and overlay path ≤2× empty path; backdrop ≤1×; explicit wall-time policy advisory except PERF_STRICT. Timing evidence must distinguish configured strict mode and current user binding correctness/allocations.

J narrows the impossible component-count intent-drain test to `intents_drain_is_o_1_when_the_queue_is_empty` (empty 500-v20 frame ±10%; two-intent total probe ≤500×5ns, zero allocation). J records historical debug/release tolerance ≤1 optimizer-elided allocation, not carte-blanche widening. J changes Showcase hit count from ±10% to classified counts/no unexplained >25% growth. J replaces clone-benchmark deletion assertion with exact line absence and preserves replacement Capsule frame obligation. WP-0 `07cb2c9` reported harness/44 perf lines, but two pre-refactor app digests and perf CI were expressly still owed at J. Reports do not prove current artifact availability.

## Important amendments and contradictions to preserve

1. J fixes real architectural specification errors: same-name Response constructors/readers, generic folding silently losing actions, collection props retaining data borrow through action closure, Grid mutator unreachable behind shared model, invalid builder generic transitions, Cx iterator borrowing all mutable services, Field registering duplicate id, const bindings carrying runtime actions, binary-only apps with unlinkable integration tests. These fixes must not regress while restoring product.
2. J's layer outside-hit and focus-restore wording is a historical contract that later runtime adjudications may supersede; verify later patches. Current user requires one coherent routing/layout contract and no stale incompatible geometry; the early “last frame, one-frame latency” allowance is insufficient evidence for queued input/route changes.
3. Initial architecture's “draw purity is a compile error” claim is overstrong: Fn/shared references and public author closures can use interior mutability/effects. Current user explicitly requires repeated-draw state/effect tests and API boundary enforcement.
4. Initial `(len,first,last)` collection stamp cannot detect internal-only reorder without explicit invalidation; requirement is stable identity, not blind adherence to a cache shortcut. Current user expressly disallows durable logical selection keyed only by position. Later amendments must be checked before implementation decisions.
5. J's `Ui::cache<T>` permits derived cache only, not arbitrary durable state hidden from equality tests. A type-name scanner alone cannot prove semantic purity.
6. J records Form phase-driving API and Grid mutable update bound as unresolved; this report does not resolve them. Parent history_api owns later L/K/M resolutions.
7. Initial Appendix B rationale incorrectly equates same-package bins with same crate; J correctly requires app libs for integration testing. Preserve enforceable public API outcome; don't reproduce incorrect prose as fact.
8. Historical exceptions (radio arrow cursor/commit split; picker secondary affordance; hints; status priority drops; mono; focused ring sizes) still require source/capture classification against the user's pinned Holla product contract. Listing an exception is not a parity waiver. No new Junie token/spacing/glyph change authorized by historical review alone.
9. Historical staged app exclusion was superseded by J's compiling legacy root plus temporary tui-next before migration; current task starts from main's existing architecture. Do not reintroduce legacy or redo old temporary naming. Current user overrides shared append-only multiwriter conventions with single ownership for exports/manifests/runtime/baselines.

## Explicitly out of this worker's proof

No code root cause was reproduced or bisected. Initial architecture identifies enabling defect classes and source citations (position ids, ad hoc responses, duplicated runtime, mutating render, color equality, owned collections/overpainting, domain leaks); those are historical reports. Current implementation mappings and executed acceptance remain integrator work. All index0–6 semantics reviewed; index7–8 semantic coverage must come from history_api. No claim that full user §3 or the implementation goal is complete.
