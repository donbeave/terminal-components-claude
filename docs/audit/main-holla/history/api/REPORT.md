# Historical API adjudication patch review

Scope: reversed tree-audit patches 7 and 8 only. All added/deleted lines and hunk headers read, not commit-title inference. Exact parent pairs, original patch SHA256, line counts and hunk coverage: `coverage.json`. Full reviewed semantic input: the two `*.delta.txt` files. Exact test/command-bearing added and removed lines: `*.test-and-command-lines.txt`. No builds, runtime reproduction, current implementation compliance audit, or source edits performed. Earlier snapshot reconstruction is parent-provided evidence, not rerun here. These are both semantic changes, not formatting-only changes; neither patch creates/deletes/renames the document.

## 27bd918e3a8a0a7fdba14fb10643139340d6281f relative to f2d30b654e5c6bea154392a8be6e9f4b7d4dbcb1

Adds Adjudications L (§22) and K (§23), inline corrections, full Form API (§15.1), example 13, and updated manifests/gates. Every 987 added and 127 removed line reviewed.

| Obligation | Amendment/supersession | Required proof (not executed here) |
|---|---|---|
| L dependency/backend boundary | ratatui umbrella and SmallVec removed; core + optional crossterm backend; apps facade-only; exact normal dependency set and core features; resolver 3; testing dev-cycle isolated | Cargo metadata/tree exact edges/features and singleton Unicode/bitflags versions; backend-free standalone build without feature unification; external applications/examples compile |
| L width/paint | Single width function delegates CellWidth, including FF9E/FF9F; original-label grapheme fuzzy indices; clipping writer replaces fit; set_line span writer; paint_style; wide paint_cell resets shadowed cells; paint marks written-cell bitset | width_matches_ratatui_cell_width corpus, fuzzy original grapheme tests, CJK wide shadow tests, paint clipping/canonical cell comparison, zero-alloc row tests |
| L theme/layout | Final Style::patch layering with symmetric modifier removal; role merge remains separate; Style::new and semantic resolution; typed border/scrollbar/line sets preserving Junie glyphs; Rect geometry reuse; Track integer layout with Start/End and spacing; no solver | Modifier law differential tests, glyph/border exact output, geometry tests, sentinel semantic overrides, sorted state rule tests |
| L runtime | Typed terminal mode commands, chained panic hook before first mode change, reverse restore and idempotent leave; one cursor write; exhaustive mouse kind mapping/modifiers/horizontal wheel; key accessors; release dropped; no enhancement flags or process globals | panic_hook_restores_before_delegating; release normalization; mouse cases/modifiers; terminal restore integration; cursor ownership; harness isolation |
| L language/gates | Rust 1.88/2024 actual compile; documentation/external README tests; narrow non_exhaustive policy; unsafe forbidden in library/apps, documented allocator exception in testing; no material stubs or suppressed warnings; source/graph scanners and empty legacy allow-list | Real 1.88 and stable matrix; rustdoc -D warnings; all examples/README/doc-check; forbidden patterns/metadata; meaningful allocator tests |
| K1 Form | A real composition component, not layout helper/overlay. Caller values per phase; declaration once. Nine field kinds; required/help/span/group/plain; two-column pairs, sections, chooser/note rows, nested popovers, scrolling, per-field/cross-field/server errors and dirty state | All F1–F13 tests, production connection and Jackin forms; every real FormData resolves every declared field; keyboard and mouse reachability; tiny rect conformance |
| K1 submit/update | Declaration-order focus; hidden fields have no geometry/focus contribution but retain drafts; scroll owned by update; first semantic action wins; commit in-flight edit then validate visible fields, validate_all, focus/reveal first error or emit submit; Enter only idle non-typing; dirty on committed change | submit order/error/hidden/chord/Enter/dirty tests; nested Esc trap; draw twice does not commit/cancel or mutate model; action selection deterministic |
| K1 secrets | No values() cloning channel; typed Secret borrow; action carries Id/ActionKey only; synthetic mask; Secret not Clone/Eq/Serialize; redact FormState/drafts and zeroize on close/dismiss | Secret compile-fail, debug/screen/effect sentinel leakage tests, zeroization, production password masked; connection rebuild by reference |
| K2 Grid capability | update(&M: GridModel), update_editable(&mut M: GridEditor), draw(&M). read_only_reason and actions moved into GridModel. GridCellActions and editable(bool) deleted. One shared navigation implementation; reconcile both entry points before action | read-only compile-fail, editable commit exactly once/error leaves editor open; reason and actionable glyph/hotzone on GridModel-only model; both entry points full conformance and reorder tests |
| K2 domain preservation | Runtime read-only-with-reason editor adapters still supported; display-only grids need no Editor trait; database semantics move into TablePro adapter; all 22 DOM capabilities retained | TablePro pending-change inventory, view read-only reason, result local sort/refusal, inline click routing, external edit begins no inline edit |

Performance obligations preserved/added: connection form 120x40 <40 allocations/frame; sorted Vec KeySet contains proven binary-search comparison count; 100k-row selected rendering within prior 1.5× bound; HintBar unchanged focus 0 allocations; original pre-refactor perf baseline immutable with Vec size changes explained. Semver gate explicitly deferred until end-of-refactor v0.1.0 baseline, binding from v0.1.1—not a falsely shipped mid-refactor gate.

Named new tests/commands and retained renamed mappings are reproduced verbatim in `27bd918e3a8a0a7fdba14fb10643139340d6281f.test-and-command-lines.txt`; do not substitute a few examples above for that complete required-name ledger.

Open items this revision deliberately leaves: foreign Size/Span collisions and author/root export choice; ASCII border placement; generic choice controls versus closed FieldKind. Next reviewed patch resolves these.

## 87ab93d4d21b75ce975c3a5cfecd987a0b935d2f relative to d57ed9ff43a8f6845eaa298969826c9e14b8a122

Adds Adjudication M (§24) and inline/example corrections. Every 484 added and 79 removed line reviewed.

| Obligation | Amendment/supersession | Required proof (not executed here) |
|---|---|---|
| M1 complete public facade | Our Size and role Span retain names; role Span moves to text/span.rs, avoiding collection→component dependency. Root + author re-export Buffer/Cell/Position/Rect/Color/Modifier/Style and own Span; Frame root-only; foreign Line/Span/Text qualified author::raw only, not flat exports; foreign Size absent | every_foreign_type_in_the_public_surface_is_re_exported rustdoc graph plus truly external author example; exact facade dependency edge; author cannot access runtime internals |
| M1 public span paint | Ui::paint_spans uses own role vocabulary resolving live theme/surface into same set_line writer as RowUi | ui::paint_spans_matches_row_ui_label_spans differential cells and column count over full text corpus; custom renderer sentinel role override |
| M2 ASCII border retained | Const Set in theme/border.rs (not newtype/inherent impl); + corners, - horizontal, pipe vertical. Junie ROUNDED, Paper PLAIN. Opt-in only; Capability exactly color; full Unicode fallback remains explicitly deferred | ascii_border_set_is_pure_ascii; builtin_border_sets_are_ratatui_sets; border-only ascii_theme_renders_without_box_drawing_glyphs; capability_has_no_unicode_field; example 2 compiles |
| M3 collections/data | Select/RadioGroup/ChipBar keep generic T/K/R, new(id), borrowed per-phase items and public key/row hooks. Closed FieldKind with one lifetime/zero type parameters holds LabelSelect/LabelRadio/LabelChips default aliases. No props stored in state | standalone_select_takes_items_per_phase with non-string/key/custom row; field_kind_has_no_type_parameters; state_holds_no_props plus actual state inspection |
| M3 option borrowing | options(&self) for draw and value_and_options(&mut self) for update make one data borrow; arrays stay unchanged when options change; choice props contain no slices | select_field_options_come_from_form_data; changing_options_between_frames_does_not_rebuild_props; single-borrow compile pass and two-call E0502 compile fail |
| M3 richer choices | Positional Choice index channel forces ByIndex aliases; historically caller remaps reordered options or uses Chooser with keyed layer. Chooser emits only field Id; standalone controls keep stable keys | reorder/insert/delete property tests at real adapter; chooser activation and keyed popup behavior. Current user §5 now forbids durable logical selection keyed only by display position, so historical bare-index concession cannot excuse unstable identity |
| M action vocabulary | Select Chose/Opened/Closed; Radio Chose; Chip Toggled/Closed/Activated; cursor ≠ controlled value; explicit parts inventories | exact semantic actions, focus/value distinction and per-part styling tests; changes require current architecture judgment rather than obsolete model-specific process |

M adds/retains exact test identities in `87ab93d4d21b75ce975c3a5cfecd987a0b935d2f.test-and-command-lines.txt`. All 13 external examples remain required. doc-check scope expands through §24. No prior K Grid or Form F1–F13 obligation is dropped by M.

## Audit cautions for normative consolidation

1. Current user instructions supersede historical Opus-only adjudication routing, three-app scope, and any weaker first-frame/stale-geometry concession. Historical API details may need evidence-backed correction; preserve the behavior and boundary obligations.
2. These patches claim shared references make model mutation unrepresentable. That does not exclude interior mutation/effects; user explicitly requires repeated-draw state/effect invariance. Add actual adversarial tests, not type-only proof.
3. M's options()/value_and_options() defaults can disagree (draw sees labels while update default sees empty labels). Historical claim this is immediately visible is insufficient; test both phases with real adapters and mismatch mutation.
4. K example 13 retains form_props(&self) despite the same patch requiring constructor arguments never &self; treat examples as compile obligations and investigate borrow behavior, not unquestionable valid implementation.
5. Historical direct backend-optional dependency plus unconditional key vocabulary creates a backend-free design issue; actual no-default-feature isolated compile must settle it. Do not claim manifest prose proves backend freedom.
6. Named test identity lists, scanners, command syntax, and form method-count prose are evidence of obligations—not executable proof. One historical cargo test command passes two positional filters; actual command must be corrected into separately discovered/executed tests. M says four negative greps though its block contains more. Never accept aggregate counts or zero-match filters.
7. ASCII border test scope is border cells only: scrollbar/rule glyphs may remain Unicode, so whole-frame ASCII promise would be false. Full glyph fallback is deferred by M, not silently implemented.
8. Historical source-line bug claims (password plaintext, draw-mutated form scroll/disabled, grid reason trait reachability) are documentary/source-design leads here. No current runtime reproducer or behavioral bisect was run.

Current implementation mapping remains the parent integration audit's responsibility; this report establishes historical obligations and supersessions only.
