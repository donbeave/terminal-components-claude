# Current normative contract from reachable architecture history

Pinned MAIN_BASE `c12cad8728755cd2d03eefdd8e02891143fca86d`. User attachment governs main-based implementation, pinned-Holla product parity, four applications, available Codex routing and exact verification/integration. This external synthesis does not edit or replace repository source documents. It is not implementation-completion evidence.

## Authority and continuity

Original REFACTORING_GOAL capabilities survive its historical header: one reusable API; caller durable state; borrowed collections; runtime-owned interaction; semantic customization; rich components; all application behavior; no permanent legacy; testing/performance/security and independent review. Named agent/model restrictions, old three-app cardinality, obsolete absolute paths and original slice staging are superseded. Holla-first/tree-replacement recommendations are investigation leads only. Existing accepted main safety fixes survive even when reference leaks; exceptions require narrow reproduction/test/review.

All exact original commands, API sketches, test names and baseline identities remain recoverable through INDEX.md and immutable snapshots. The report groups below preserve semantic obligations; `historical-named-obligation-inventory.json` separately indexes 1,483 literal API/test/path/command leads, with first revision and MAIN_BASE presence. Literal absence never automatically discharges an obligation.

## Current architecture

1. Public components use caller-owned concrete durable state and short-lived borrowed props; collections/models borrowed during phase calls so semantic action handling can mutate owner data afterward. Components expose typed semantic actions, keyed for existing items; collection-wide add/request actions do not invent keys. Response separates consumption/paint/layout; folding restricted to actionless responses. Application/component binding identities use separate ranges; stable logical item identity and namespaces remain mandatory.
2. Update alone changes semantic state and emits domain requests; draw may paint and report runtime-owned derived geometry/state/cursor/cache facts. Shared references do not prove effect/state purity: production repeated-draw tests and adversarial author callback tests required. Derived caches cannot hide durable domain state; geometry supplied once to paint/clip/hit/focus/scroll/cursor and refreshed before incompatible queued input. Historical first-paste/one-frame allowances cannot override current request.
3. Runtime alone owns global focus, hover suppression, press/release/flash, capture, routing, cursor, modal layers, bootstrap and scheduling. Latest accepted hit priority highest layer then registration; decorative regions do not receive controls. Disabled top hit absorbs activation without lower fallthrough; disabled hover presentation suppressed; wheel policy explicit. Active capture dynamic disable remained historically unspecified and must now be decided/tested. Runtime effective binding resolution governs handlers and displayed hints; text/paste precedes inappropriate global shortcuts.
4. Layer stack owns barriers/z-order/placement; Fill distinct from Fixed zero-size. Components supply pure measured size and resize open layer during update, resolver clamps/flips. Popover loses focus and dismisses without restoring opener; modal traps and restores appropriately, including zero-size. Draw traversal of Panel/Dialog exactly once returns bare R even empty; clipped malicious body cannot escape. SplitPane sole two-rect one-body exception preserves seam and logical order.
5. Runtime time explicit monotonic, exact earliest persistent wakeup deadlines; Bootstrap once before first draw/input, Tick once per timer event followed only by Settle reruns. Application simulation delta/motion ownership preserved without event-count elapsed behavior. Actual 2199/2200-ms Buttons and 1000-input proof mandatory. Pure app status projection preserves reference content/priorities/tones; no duplicated mutable shell cache.
6. Themes use semantic roles, ordered surfaces and common accumulation; family+variant state rules merged by specificity/declaration before author override layers. Built-in token/palette values preserved; neutral recipe makes unknown custom families usable. Mono fallback final implementation is resolver-level static fallback after merged state rules and before author overrides, not recipe-vector mutation; latest documented count20 supersedes18/19. Full truecolor/256/16/mono and independent Paper evidence required. ANSI16 categorical reference mapping survives; terminal runner only detects/narrows environment, --color ceiling never floor.
7. Customization includes partial tokens, family/variant, subtree, instance/part/state, row/cell/slot renderers. Actual intended cells must change under sentinels and siblings/theme remain unchanged. PARTS equals actual component-owned resolution over meaningful fixture sweep, caller rows remain row-owned; current StyledQuery lacks provenance, so this remains proof gap. Declared SlotFn-addressable set equals visible replacement set. Readiness spinner glyph Slot cannot suppress required affordance; SlotFn substitution still supported with geometry/interaction retained. TextArea covered FIELD query fallback is not visible-cell evidence.
8. Central Ui::reference paints an inert subtree with optional exact owner/part receiving only runtime interaction flags; semantic flags remain caller-owned. It supersedes component-local state_override/inherit_forced APIs. Reference suppresses registration, scopes, layers, layout,cursor and bindings even in arbitrary callbacks; nested restores outer target. Inert background still paints but cannot register interaction.
9. Borrowed efficient collections preserve rich behavior and renderer hooks. Grid model owns domain ordering/SQL semantics; library emits sort/edit requests, stable-key state, schema-driven Option cell holes; ragged cells never reach invalid hooks. Tree runtime cache stores only keyed derived indices, incremental subtree/query behavior, steady viewport work. Steps lifecycle skipped is inspectable read-only; frontier derived cached with explicit invalidation. Viewport exact wrapped prefix and append suffix cache, retention fixup, Unicode selection/caret and bounded warm work. No losing capability to satisfy a smaller API.
10. NavList separates Chose from EnterContent; only shell focuses content. Sectioned/collapsed policy and any reference compact policy must share the same geometry for paint and hit. ChipBar CHECKED marker uses reserved pad, payloadless AddRequested; Radio value controlled separately from cursor; StatusBar only matching keyed live label hovered, blank/static regions clear it. Preserve all real custom rows, badges, metadata and delegated scrollbar overrides.
11. Form/editing values stay owner-side; current owner sensitivity reconciled before visibility filtering. Hidden unchanged fields retain eligible drafts, shape/sensitivity transitions erase prior drafts. Public state errors generic; detailed plain render errors only through current-owner-aware private path. Secret constructors/masks/debug/cancel ownership explicit; owner zeroizes on cancel/dismiss. Safe-Rust overwrite is best-effort, never a claimed guaranteed erasure. No secrets through any view/Debug/error/log/trace/artifact, no simulated real effects.
12. Workspace backend-free public core/test boundary; no app dependency/domain vocabulary in library. Terminal session feature separation must be tested without workspace unification; backend event dependency exception does not imply backend packages absent from dependency graph. Exactly one target per four public executable names, app libs unpublished and independently linkable tests, no root detached/legacy duplicates. All31 original widget-module dispositions and23 app-side controls survive in mapping, plus later composed families/Holla additions.

## Current proof contract

Pure real production views with complete canonical cells/styles/cursor and image review; in-process real update/runtime behavioral reachability; independent terminal binary CLI/input/resize/exit restoration. Required test identities enumerated before run; default/all/isolated features, MSRV1.88 and stable all declared gates. Source scanners must fail on missing subjects, exact named examples, wrong signatures/private API, skipped registered components, stale deferrals and mutation probes. Doc-check currently excludes18–20 and is name-based; correcting description does not satisfy original coverage capability.

Historical root captures/digests/perf remain immutable namespace distinct from pinned-Holla reference. First-generation pins are not approvals and may be spent once; no laundering via revert/base. Guard real preceding base, all changed/added keys accounted, no live declared blocker, no self-diff or stale output. Capture exact argv, transactional outputs, source+binary+tool+environment/clock/font/viewport provenance and hashes; expected distinct from candidate. User extends historical96 startup cells and132Holla matrix; neither is complete interaction/product coverage.

Deterministic allocation/byte and correctness budgets binding, benchmark timing as explicitly configured. Preserve exact workload identities and old-to-new benchmark mappings; isolated collection binaries avoid unrelated perturbation. Current latest limits/counter contracts enumerated in late/AUDIT.md§12 and original-to-amended budgets in early/REPORT.md and middle reports. No lowering thresholds to pass.

## Detailed traceability

- Original goal/A–I/J: early/REPORT.md (20 groups).
- L/K/M: api/REPORT.md (dependency/public facade/Form/Grid and all named test/command lines).
- Foundations F1–F26/N/O/P/Q–39: middle/early-large-report.md and middle/report-11-21.md.
- §§40–59 and late reconciliations/security: integrator-semantic-review.md (every revision disposition;10 component groups).
- §§60–74, package/capture reversals/merges: late/AUDIT.md (22 groups).
- Current source corroboration: current-source-gaps.md.
- High-level implementation/gap/tests crosswalk: AUDIT.md H01–H20; implementation acceptance remains OPEN until executed evidence exists.

No archive deletion or architectural replacement is authorized by this document itself; user permits implementation after complete historical/contract review. All68 changed edges now have semantic coverage; independent integrity audit passes (`late/coverage-integrity.md`). Broader user§3 additionally includes pinned DESIGN/Holla-only disposition and source-obligation mappings owned by the root/app reviewers.
