# Late history semantic audit — patch indices 35–53

Read-only source audit. Owned output: this directory only. No builds, source changes, runtime reproductions, snapshot approvals, or implementation-completion claims.

## Coverage and method

`coverage.json` pins 19 parent-relative patches and both endpoint snapshots by SHA256. Every full patch was applied to authoritative `git show parent:path` contents and the reconstructed bytes equaled authoritative `git show commit:path`. 2,054 +/- lines: 841 novel nonblank texts reviewed, 1,072 exact-repeat texts mapped to first occurrence, 141 blank lines. All novel +/- semantics were read in `compact-deltas.txt` in contiguous bounded chunks covering characters 0–153560, without tool truncation. Long similar lines were reviewed through all word-level old/new opcodes and local context; whitespace-only changes are formatting. Unchanged context and repeated text are not claimed freshly read. Earlier first-occurrence indices 0–34 require the other auditors' coverage: this is explicit induction, not standalone complete-history proof. `novel-deltas.txt` retains every full novel line; `review-deltas.txt` is an intermediate reduction, not separate evidence.

The Git-proven snapshot equality independently validates context, path identity and complete delta shape. All 19 edges modify COMPONENT_ARCHITECTURE.md; no REFACTORING_GOAL.md edge in assigned range. No path rename/deletion in these patches; commit subjects saying archive do not mean document rename. Merge-parent differences are indexed separately, especially both e524ceaa parents.

## Per-edge disposition

| Index / commit | Semantic disposition |
|---|---|
| 35 a1759b2 | Large closure amendment adds §§60–72 and rewrites frontmatter/contracts; central exact-target references, mono resolver, borrowed viewport prefix, stable bindings, collection/model/form fixes. Status says through 70 despite §§71–72 additions; section content wins. |
| 36 ecc1337 | Precise §54 addendum (bootstrap, four causes, persistent deadlines, Jackin delta/status/fanout/dimming) and §74 D1–D3 (color ceiling, executable capture matrix, per-app baselines). |
| 37 cae32f8 | Adds executable capture-matrix architecture check. |
| 38 a573eba | Fail-closed app manifests; exact 96-cell capture axes/script contract; 22-page structural/live-body guard; §16.5 named tests become visible. Historical counts updated, not fresh execution proof. |
| 39 37f9031 | Tightens page ownership: each of 22 named page modules owns production draw/update roots; shared mod.rs wrappers cannot discharge coverage. |
| 40 246f7e4 | Entire snapshot is EXACT tui-next→junie-tui / tui_next→junie_tui substitution, programmatically proved. No other change. Corrupts historical staging prose into self-renames and duplicate package names; cannot reinterpret as a new naming decision. |
| 41 9b31166 | Bless guard now claimed implemented/fail-closed on absent/invalid base; app-baseline nonempty and virtual-workspace guards; items 32/33 added to visual/perf ledger. Claims require current reruns. |
| 42 0defae2 | ARGS shell interpolation replaced by exact argv after --; per-run identity and binary/Git/environment/tool provenance. |
| 43 067869c | Exact +/- inversion of 42, programmatically proved. Temporary reversal, not final authority to use unsafe interpolation. |
| 44 e45fde1 | Status through 74; checked-in TSV/JSON hashes validate all 96 cells/480 artifacts, empty stderr, successful capture, one resolved source revision; worktree-scoped tmux and fail-closed lock. |
| 45 383af57 | Item 34 classifies exactly 144 Showcase visual repairs: 88 Paper/ANSI16 foreground keys + 56 Grid/Table keys. Requires regenerated capture and independent review; no unrelated drift authority. |
| 46 8cfb810 parent87bfbf | Merge delta replays package substitutions and app/gate/evidence/visual changes already introduced above. Every line exact-repeat mapped. |
| 47 1f01b83 | Historical Overrides references corrected to PartStyle in five paragraphs: styled attribution and forced-state two-half operator. No resurrection of component-local forcing superseded by §72. |
| 48 87dae98 parent34cf02 | Merge delta replays 46 plus five PartStyle paragraph corrections. Every changed text already reviewed. |
| 49 ec3d760 parent34cf02 | Reintroduces exact argv and run provenance; now safe capture name publishes a transactional directory containing ansi/txt/cursor/html/png. |
| 50 e524cea parente6ca52 | Merge applies verified argv/transactional-directory/provenance contract to branch with checked-in hash validation. |
| 51 e524cea parentec3d76 | Other merge parent supplies architecture/package/app-gate/visual corrections; same child as 50, distinct full parent-relative patch, all deltas mapped. |
| 52 c493a2c | Historical missing trap tests marked superseded by implementation claim at 5d17cc0: modal iff TRAPS_FOCUS, nonempty and zero-area traps, forward/reverse wrapping, FocusOut dismissal only popover with restoration suppressed. Explicitly not gate/bless proof. |
| 53 15ecde1 | Extensive reference sanitization: stale symbols changed to prose/current names (frame-state geometry/roles, register_control diagnostics, FormData::options, PartEdit::when, PARTS, Color::from_u32, StyledQuery). §72 obsolete forcing explicitly withdrawn; stale suppression checks claimed implemented. Claims do not weaken retained behavioral obligations or replace required rustdoc-json/signature/example verification. |

## Surviving obligations and concrete acceptance

1. Runtime pointer Move delivers exactly one top-live nondecorative target; hover changes request paint, never focus/activate. Captured Move becomes Drag solely to capture owner with captured geometry/origin; resize/disappearance/generation mismatch release capture; nested captures rejected. Test phase/action counts, captured drag outside then release, decorative overlap, modal barriers and hover suppression after keyboard.
2. Explicit effective Tab/BackTab component bindings precede unbound traversal; only ignored raw input reaches Bubble. Stable ActionKey binding identity, duplicate diagnosis, owner/action overrides and one effective resolver govern routing, displayed chords and hints. Structural keymap snapshot/revision, not fingerprints, invalidates hint cache; focused dynamic descriptors borrow data with reused storage. Test remap/remove/latent/hidden bindings, duplicate chords and unchanged warm zero-allocation hints/routing.
3. Reference rendering is central Ui::reference over whole inert subtree, including arbitrary slots/callbacks. Optional exact owner/part receives only focused/focus-visible/hovered/pressed; semantic selection/editing/disabled/readiness stay caller-owned. No component state_override/inherit_forced. Test zero registrations, bindings, cursor, layers, layout side effects and exactly one target across Dialog/Form/collection/scrollbar cases; external public API absence and callback adversaries.
4. Mono fallback resolves once after merged family/variant state rules and before author overrides; no stored recipe-vector mutation during downgrade/clone. Generic manifest reaches neutral/custom families. Final count is 20 (LOADING ICON underline distinguishes BUSY); historical 19-count sentences remain stale and must be reconciled. Viewport selection explicit canvas/primary inversion plus underline; Grid decorated cell error underline; Picker pressed label inversion+BOLD+underline; Select pressed field inverse+BOLD and existing bracket cells. Never terminal REVERSED or geometry drift. Test symbols/modifiers ignoring hue, all themes, fallbacks once, recipe storage unchanged and override precedence.
5. Tree chosen stable key paints chosen marker in reserved cell after disclosure without moving label or registering new hit target; Set replaces, Clear/Inherit blank. Selection follows reorder. ChipBar marker Checked, caller META preserved, automatic container/label patches only, AddRequested uses no fabricated item identity. Distinct focused/current/selected/pressed semantics cannot be conflated.
6. Grid columns sole schema; three required model methods row_count,row_key,cell; Option<CellRef> holes never reach decor/action/editor hooks, copy empty TSV, retain rectangular geometry. Optional alignment distinguishes explicit Left from inheritance. Calls bounded by row_count and min(columns,MAX_COLUMNS). Test external three-method model, panic invalid hooks, ragged editable/read-only update/draw/copy/sort/navigation and tiny nonzero-origin rectangles. No zero-width shown/registered columns; exact clipped update/draw geometry.
7. COLLECTION means stable identity, SELECTS means selection. Grid single click moves; double-click/Enter activate same stable key. Mono fixtures establish real Space selection/F2 edit; no synthetic semantic flags. Steps lifecycle rail does not select. Conformance reveal chords must make reordered offscreen stable target available before click, otherwise fail. Captured/pressed PartRef styles only owned target row/cell/actions or scrollbar thumb.
8. Grid has no whole-grid Status prop/ICON/REPORTS_STATUS; empty loading/error via EmptyState; pending fetch belongs explicit row/model and loaded errors decor. Removing global status must not delete actual reference loading/error/fetch-more behavior.
9. Picker/FilterList/Completion filter and measure semantic AsItem label/matches/key, not painted text or Display. Borrowed domain values need not Clone/Display; RowFn only changes paint; Completion uses insert fallback label. Test distinct insert, original-grapheme indices, stable action keys, compile-fail RowFn-only model and borrowed allocation gates.
10. Form effective disabled ORs inherited/configured state; sealed private TextTarget supports String/Secret without cloning/leak. Read disabled before single mutable value/options borrow. Fields reconcile by stable Id, preserving reordered drafts and zeroizing removed slots. Dirty only committed changes, not draft keystrokes. Test dynamic disable, non-Clone secrets, reorder/removal, draft/commit and repeated-draw purity.
11. Exact wrapped viewport prefix is derived cache keyed width/wrap/generation plus count. Cold/reflow/invalidate O(document), append indexes suffix only, warm frame zero prefix visits/allocations, painting visible content. Same-length edits require invalidate; front retention fixes positions; saturated generation disables reuse. No owned grapheme matrix. Test brute-force prefix, offscreen same-length rewrap, saturation, 1k/100k equal visible work, long-line grapheme allocation and independent caller-owned testing probes.
12. Preserve binding perf limits: viewport warmed bytes <64KiB; warmed 100k render 0 allocs/index visits, strict ratio <=1.5; reindex report-only separately; 500x12 grid load <8000 allocations and render <100; style downgrade ceiling 1079; warmed borrowed Picker 19 visible rows/38 AsItem calls, 0 bytes, strict ratio <=1.5; Form Tick+draw 0 allocations/bytes. Isolate collection benchmarks in perf_collections so codegen does not perturb unrelated measured subjects; one Counting allocator per process, unchanged thresholds, serial release execution both targets.
13. TooSmall named family raw34, existing ids unchanged; title primary bold/detail secondary/help muted/actions faint, four-line exact copy/geometry. Test bidirectional Panel override isolation, faint q Quit, completeness and 20 conformance cases. SelectClosed/Open appended 39/40, glyphs ▾/▴ one cell, field right-2, Set/Clear authoritative; closed field excludes semantic SELECTED while popup has exactly one Chosen. Full future ASCII conversion v/^ obligation remains distinct from historical partial conversion.
14. Runtime exactly one Bootstrap before first draw or event, no input/clock advance. UpdateCause only Cx, four causes; one timer delivery yields Tick then Settle on reruns. Earliest outstanding repaint deadline persists through unrelated events; only delivered expiration clears it; no unsolicited ticks. Headless/terminal share lifecycle. User goal now additionally requires explicit monotonic elapsed time and busy at 2199ms/completion >=2200ms independent of event counts; historical route timer-count wording cannot authorize event-count busy semantics.
15. Jackin route.tick_ms owns simulation delta (33 Intro/Outro/Handoff/Cockpit; Capsule80; other80/200), paused freezes simulation; no added App tick hook. Product status projection pure draw-time from current model, dynamic text app-owned fixed buffers, no stale Jx cache/seventh Screen method; preserve status facts/tones/priorities/actions. Manager/Accounts inherent apply_msg preserve inactive-screen fanout. Test every named §54.8 case, historical frame282/rain/ticks fixtures and 36 responsive digest keys; user four-app scope adds Holla analogues without real effects.
16. Ui::dim_layer zero byte-identical; success/warning/danger/info step muted,faint,ghost then erase into backdrop at >=4; ladder role arithmetic, never reverse color lookup. Test role collisions, all color levels and handoff monotonicity; independently classify actual changed frames.
17. --color ceiling in shared run cannot exceed detected capability; from_env force rules remain and TERM=dumb wins. Full 4x4 weaker-of matrix, NO_COLOR/dumb/redirected tests; parsers pass request, no duplicated detection. Historical public Ord versus private rank was open: inspect implementation/current later decision rather than silently pick historical prose.
18. Capture matrix exact original axes 3 apps×4 sizes×2 themes×4 colors=96 cells, five artifacts each; user supersedes app cardinality with four binaries and separate full product recipes. Capture BIN explicit, exact argv after --, owned run/session, transactional safe output directory; binary/source/environment/tool provenance and all artifact byte/hash checks, empty stderr, successful result, fail on missing/stale output. Both capture producer and boundary verify. Never treat startup-only matrix as every page/scenario/journey proof.
19. App packages publish=false, expected single lib/bin targets, no reverse dependency into core; missing due source/manifest fails. Root virtual and no detached root sources. Showcase exactly22 ordered identities/navigation/dispatch/named modules and reachable actual draw/update use; scanner-satisfying inert calls forbidden by user. Public consumer and behavioral/render evidence remain necessary.
20. Per-app visual/perf files newly created, checked in/nonempty, frozen historical root captures/perf untouched; first-generation is location provenance, not permission for differences against existing before captures. Items32/33 require exact captures/independent review and measured perf comparison; item19 component namespace cannot classify apps. Missing/invalid BLESS_GUARD_BASE fails. Named-test scan includes architecture §16.5; expected identities, not aggregate historical364/386 claims, must execute.
21. Visual ledger closed sets: new components256 Panel/Split/Viewport/Tree +64 TooSmall +256 ScrollRegion/NavList/Steps/Grid +128 Diff/Code +576 overlay/composition=1280 additions; selection16 retained keys, Dialog28, central-reference177, remaining accepted movements75 total accounting are historical scratch claims requiring original exact hashes. Item34 separately144 Showcase repairs (88+56). No reusing first-generation authority for later movement, no candidate self-blessing. Full exact frame/cell/style review and product reference still required.
22. Parts conformance equals component-owned resolved parts over meaningful fixture sweep, caller RowUi styling attributed separately. No allow-list for missing paints: paint or remove truly unsupported declared part, preserving product capabilities. patch_part identifies always-resolved default part; sentinel patches must change actual cells. Doc-check heuristic name existence is not signature checking or compiling inline examples: owed rustdoc-json validation and actual external examples remain; stale/no-hit/duplicate suppressions must fail.

## Authority corrections

User overrides historical model names, three-app restriction, machine paths, holla-first/tree-replacement recovery prose and baseline autoapproval interpretations. This audit supplies historical requirements, not permission to replace main architecture, delete unproved legacy obligations, accept stale test counts, or mark the integration goal complete. Later patches beyond53 may amend items above; integrator must reconcile them. The source-prose sanitization at53 and mechanical rename40 are particularly unsafe to treat as evidence that an API/behavior ever worked.
