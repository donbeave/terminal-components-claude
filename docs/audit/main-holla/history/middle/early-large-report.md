# Semantic delta audit: chronological edges 9–10

Scope: COMPONENT_ARCHITECTURE.md additions and deletions at 69fcdcad413375a181c72a144b776e08a7b2b5fd relative to fa87a59c6d1028bc08515132e1f7719624bf9a08, and 587c53bdfaa6f391b686f2383b5675194d9ba9f8 relative to 1129ab1c0225548f3a2bd20cae5e33aa87fbc5a5. Complete novel extracts read in contiguous character intervals of at most 18,000: edge 9 [0,84983), edge 10 [0,136188). No output truncation in those reads. Counter comparison against original patches found edge 9 omitted exactly one `-}` and one `+}`; those brace-only lines were separately inspected. Edge 10 contained every +/- line. Immutable original/extract SHA256 and exact identities: `early-large-coverage.json`. Parent-snapshot/context induction belongs to parent auditor; this report proves full semantic +/- review of assigned edges, not review of all prior history.

Edge 9 is the partial inline amendment; edge 10 finishes its signature/example updates and appends §§25–26, explicitly accepting the Slice 3 review and Adjudication N. These are semantic amendments, not formatting-only edits. No rename/deletion in these two edges. Neither document assertion of implementation nor historical review verdict is fresh implementation evidence. No source edits, builds, behavioral tests, or baseline approvals performed here.

## Binding amendments reconstructed

1. Runtime: hit priority is highest layer, then latest registration; a lower region registered last cannot shadow popovers. Wheel delivery requires target on top layer, including popovers without inert lower registration. Live capture converts Move to Drag and uses actual press position, never area origin. Resize must deliver pending focus transitions rather than discard them. After fifth focus pass, diagnose, apply last target and stage FocusOut/FocusIn for next handle; test delivery, not merely nonempty focus. Latest scope on highest layer wins. Reconcile gains first reachable entry anywhere before None. Esc cancellation intents only for Esc dismissal; editor then application bubble/Esc hook precede layer Esc dismissal. Keymap conflict analysis once per change/debug, not quadratic every event.
2. Response: unit-only BitOr remains; action-bearing folds must fail compilation. Consumption dominates; invalidation max; id now first Some (`lhs.id.or(rhs.id)`), state remains lhs. `App::on_esc` and `Cx::command` fill previously missing routing channels.
3. Identity: structural derived equality/hash/order supersede manual hash-only implementations because const Id match patterns require structural equality. Corpus test compares equality/order to hash equality/order across ~200 derived ids; old debug-label test retained only debug-only. Historical rationale wrongly writes equal hashes imply equal segment chains before separately acknowledging FNV collisions: do not promote this as a mathematical collision guarantee. Current stable logical key requirement survives.
4. Draw purity: permitted runtime frame facts explicitly include editor registration and next-frame declared state. D-6 deliberately records an edit/paste same-handle miss because editing flags are last-frame data. **Current user queued-input/stale-geometry and paste contract supersedes acceptance of that defect**; this historical exception cannot justify missing first paste. Draw has no semantic app mutation; derived cache heuristic was acknowledged weak. Current repeat-draw state/effect invariance needs actual tests, beyond shared references.
5. Painting/composition: Ui raw escape marks full clip; internal CellUi alignment/RowUi raw must use buffer_in(area), intersected, marking only written cells and accurate roles. Paint spans with Buffer::set_span per span, inherited LABEL base style, no intermediate Vec; compare full glyph/padding corpus against verbatim legacy grapheme fit, no non-ASCII/control/zero-width skips. Cell roles drive dimming; painting queried resolution recorded as (Id,Family,Variant,Part,Resolved), and harness resolves actual component family instead of hardcoded BUTTON.
6. Theme: family base → variant base → stable merge of both pre-sorted state lists by specificity, family first on ties → global/scope/instance. Sentinel colors distinct under both themes must prove precedence. Custom unknown family inherits neutral row-like CONTAINER/GUTTER/MARKER/LABEL/META recipe until defined. Resolved adds align. SecretMask becomes distinct glyph role from Dirty, though same Junie bullet. Mono disabled removes modifiers then adds DIM; fallbacks also append to variant maps so variant PRESSED cannot override bracket fallback. Driver default all ten states; component may narrow only with Caps coverage validation.
7. ANSI16: reject CIE76 nearest color and restore exact categorical legacy algorithm: spread <40 → BT.601 gray thresholds 30/110/200; otherwise dominant RGB channel, Yellow special r-dominant/g>120/b<80, brightness max>180. Explicit expected LightGreen accent, LightRed error/danger_soft, Yellow warning, Black subtle border, Gray fg[1]. No rebless allowed for restoration. ANSI256/Mono arithmetic unchanged; Lab remains needed by token derivation. Historical numeric estimates require recalculation before evidence use.
8. Layout: Auto unmeasured gets one cell beside Flex, equal remainder share absent Flex; measured rows/columns consume natural sizes. Example 9 two-row Props fixed to measured Auto. distribute_into allocation-free, RowUi max 16 columns explicitly documented (extras ignored). Current rich collection/customizer contract may require revisiting this cap rather than hiding it.
9. Layers N1: LayerSize::Fill/Fixed supersedes misleading min_size and (0,0) full-screen sentinel; any zero Fixed dimension → empty. Resolver alone sizes/clamps/anchors/flips; components supply size and lay inside resolved clip. Point anchors flip above/left near edges instead of covering pointer, recorded visual-change item 17. Only size/anchor mutable while open; setters repaint only on change. Lifecycle kind/inert/focus facts immutable. Dialog/Select/Picker/MenuBar/ContextMenu compute own pure props+tokens size and reassert during update. Dialog body_rows supplied by caller; width override or token; height border/title + shared wrapped description + optional body/actions spacing; shared zero-allocation wrapped_rows prevents measure/draw divergence. Bare Fill remains primitive constructor default, not valid unsized dialog implementation. Dynamic descriptions/errors/theme changes must resize on next draw. Historical “no component computes rect” refers overlay placement, not ordinary internal layout.
10. Measurement N2: Measure keeps &Ui. Ui::resolve/glyph_str query live surface/scopes without cache access, roles, styled-part records or allocations. Ui::style/style_patched remain mutable painting path. Theme::metrics returns glyph/size/align without color/surface or draw scopes; one accumulation path shared with resolution. Full differential every built-in family/variant/part × themes × state sweep ± overlay; 1,000 measures leave roles/parts/cache untouched; 2-cell themed gutter increases natural button width one; 10,000 measures zero alloc. Scoped/instance sizing discrepancies are not excused by metrics lacking scopes; current authoritative geometry/customization requirements still apply. Ui::surface_style + Resolved::over use inherited.patch with modifier add/remove semantics. with_part binds resolution once, pushes no scope or clip. Ui::scroll_region remains explicitly OPEN, blocking original 4E, not decided by N.
11. Secret: synthetic masking; redacted Debug on every state/props wrapper; Secret not Clone/Eq compile-fail; fill bytes then black_box + compiler_fence before releasing storage. Historical safe-Rust zeroization limitation explicitly recorded; observable test empty exposed value and released capacity does not prove optimizer-resistant erasure. Current no-leak contract survives all channels.
12. Public facade: theme/layout are explicit public-module exceptions; text becomes crate-private with curated reexports, author names Id/ItemKey/Part/PartRef instead of module/glob. author::raw INLINE nested module, not file; whole-file grep forcing different structure explicitly rejected and replaced by syn-scoped check. secret/validate/field_control foundation files at crate root. Public foreign-type coverage eventually rustdoc-json; substring reexport check acknowledged inadequate deferred upgrade.
13. Dependencies: ratatui-crossterm nonoptional for unified event vocabulary; feature gates only session/run/DefaultTerminal. --no-default-features proves session separation, not absent transitive backend package. Backend confined session; vocabulary event.rs; exact two-file policy. Banned umbrella/widgets/macros and critical-section/palette over whole normal closure; smallvec/parking_lot/lock_api/scopeguard/libc/mio/signal-hook allowed only below ratatui-crossterm, positive every-path proof, no direct smallvec/crossterm. Exact normal library/app deps and single unicode-width/segmentation/bitflags versions remain historical obligations subject later amendments; do not prune subtree and claim total absence.
14. Gates: scan all non-test items, never truncate file at first cfg(test); broad palette regex with explicit builder/downgrade path exceptions instead of silent narrowed matching. No infinite spin loops added to satisfy lint; use structural infallibility or justified expect. Domain scanner deliberately code-only. Four missing checks must exist: every_named_test_exists, conformance_covers_every_public_component, state_override_used_only_apps_fixtures, all_examples_compile/external-consumers. Missing compile-fail tests and suite-level parts/binding-conflicts/focus-settling/zero-geometry tests must run. Names identity checked, not total counts. Case12 sets cursor/check keys before reorder/insert/delete and proves survival plus click identity. doc-check range extends through §§24–26; old foreign-name heuristic explicitly deviation, legacy references move printed allowlist; rustdoc-json upgrade deferred to original Slice8. Current user mandates real gate functionality, forbids treating deferral as complete.
15. Performance: deterministic allocations/bytes binding. Span paint 500×3 zero alloc; inline grapheme ellipsis 10k through RowUi::label zero alloc; ZWJ buffer-symbol storage separate 10k/100k same counts ≤80 for 80 columns, never arbitrary relaxed ≤8. Intent queue empty 500/20 consumers zero probes/alloc; two intents 500 drains exactly 500 probes/no alloc. Style 10k zero alloc and ≥90% cache hits. Old ≤2× trivial field-read baseline explicitly superseded by frame share ≤5% and recorded microcost; strict ns baseline×1.2, empty-intent ratio×1.25 under PERF_STRICT. PERF_TARGET removed; only STRICT/BLESS. Role-level cache excludes Surface; binds color afterwards; generation-stamped 256 slots, painting-only. Prose evolution references both embedded array and later boxed cache one construction allocation; exact final storage should follow latest amendment, invariant no per-frame/query alloc survives. Historical rebless-in-same-commit is no current automatic approval; user requires immutable reference and independent review.

## F1–F26 obligation continuity

F1 precedence; F2 spin-loop removal; F3 written cells/roles; F4 span allocations; F5 ANSI16 restoration; F6 focused cursor candidate; F7 actual family resolution; F8 layer-first hits; F9 whole-file scanner; F10 exact grapheme/padding reference; F11 compile-fail safety; F12 executable named-test and architecture inventory; F13 four suite-level cases; F14 neutral custom-family fallback; F15 live capture origin; F16 resize/fifth-pass focus intent delivery; F17 ten-state mono and true stateful reorder proof; F18 perf benchmark split/probes/cache/frame contracts; F19 positive dependency path validation; F20 backend two-file boundary; F21 curated facade; F22 secret mask/zeroization/palette exceptions; F23 full doc range and eventual semantic resolver; F24 layer sizing discharged as decision by N1, not evidence of implementation; F25 remaining minor focus/wheel/Esc/columns/fuzzy/fingerprint/mono/perf/measurement corrections; F26 architecture and state-ledger amendments. All explicit tests/commands remain present in immutable full patches; this mapping preserves their semantic acceptance obligations, does not imply those commands passed.

## Current-authority and verification disposition

User attachment read first. Current main-based four-application integration and actual Codex delegation supersede historical opus/Claude routing, three-app scope, fixed slice ownership/machine paths. Product contract stays pinned-holla + DESIGN; N1 Point flip exception needs reconciliation against that actual reference, not historical permission to accept any drift. These two edges establish historical root-cause reports at 18afddd (seven blockers plus major/minors); no independent source reproduction or introducing-commit proof made in this assigned audit. Parent integrator must map every surviving obligation above to current code, gap and concrete executed proof; historical document claims alone cannot close any implementation requirement.
