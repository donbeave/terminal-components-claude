# Semantic history review — indices 11–21

Scope: reversed `tree-audit.json.patches`, indices 11–21 inclusive, all COMPONENT_ARCHITECTURE.md. Exact patch/novel hashes and complete code/test-name token inventory: `coverage-11-21.json`. Reviewed 331,716 novel characters in bounded ≤17,000-character reads without truncation. Every added and removed semantic line was reviewed. One identical moved `FixtureRow` declaration at index13 was separately read; its shape remains `{ key: ItemKey, label: String, meta: String, disabled: bool }`. Full raw patch context was not repeatedly reread; preceding unchanged contract belongs to parent audit induction. Parent owns patch→snapshot reconstruction proof. This is historical obligation review, not present implementation verification or execution of quoted commands. Claims that historical code/tests passed remain historical reports until reproduced.

Current user overrides historical model-routing names, three-app scope, ownership dates, and acceptance shortcuts. Independent judgment/review remains required using available agents; four applications and pinned Holla production parity govern. Historical weak first-generation review is explicitly insufficient for current full canonical-cell/style and independent image/interaction review. Later sections outside this slice may supersede exact API spellings or decisions; integrate with their audit, never drop a surviving requirement merely because current implementation violates it.

## Per-revision semantic map

| Index / commit | Amendment | Surviving obligation and acceptance consequence |
|---|---|---|
| 11 / 4aabceb | §27 O1–O4, foundations followups | Correct cache shape, ASCII whole-set coupling, ANSI16 categorical fidelity, honest deterministic/performance instruments. Detailed below. |
| 12 / dc3e0fa | §28 P1–P6, prototype corrections | Runtime-addressed lifecycle delivery diagnostics; unconditional layer-owner update; prompt sizing; forced-state composition; real mono readiness and disabled readability; complete render target execution. |
| 13 / 70dacec | §29 Q1–Q3, §30–32 | Slot glyph clear/inherit distinction; geometry-preserving pressed affordances; paired fixture state; overlay/trap separation; ChipBar marker; neutral recipe participates in mono passes; reject false textual proof. |
| 14 / 3ed377e | §29.8 and measured inventory corrections | Popover focus-out dismissal without restoring old focus; modal capability and non-empty-trap guards; corrected retained test/perf evidence. |
| 15 / 9486b65 | §33–34 | PARTS means actual own styled parts with explicit attribution; driver fixture sweep; all registered cases covered; capability detection belongs to terminal runner; deterministic constructors; idempotent narrowing. |
| 16 / afc6067 | §35 | Strike Ui::scroll_region, retain direct two-phase ScrollRegion composition; typed/stale suppression discipline; whole ASCII table owned by themes. |
| 17 / 838f25e | §36 | First-generation digest namespace and truthful limited review; enumerate every added/moved key; guard missing; first-error panic hides other cells; scratch capture/classification. |
| 18 / 8ee9977 | §37 | Structural AST gates and adversarial demonstrations; missing subject and dependency-path vacuity remain open. |
| 19 / 6ec2917 | §37 followup corrections | Add exact named tests; preserve original and live performance files as distinct namespaces; corrected measurements without lowering targets; StatusBar hover primitive exists but consumer incomplete. |
| 20 / 739754c | §38 | Readiness capability implies actual BUSY/ERROR paint; retained-state reasoning depends on reachability, not forced differences; stale narrowing reasons cannot authorize omission. |
| 21 / 7f7cc6a | §39 invariant Q | Forcing substitutes runtime state, never props-derived state; explicit Option fixture forcing; repair before baseline generation; rule-match coverage and Empty override gap remain owed. |

## O: cache, ASCII, ANSI16, measurement

O1 preserves ≤256 statically sized cache entries, one construction allocation, zero per-frame allocations/growth, generation reset, painting-only memo and measurement bypass. Replaces direct-mapped/Option/Resolved/by-value sketch with boxed 256 `(u64,u32,StylePatch)` slots in 128 two-way sets, nonzero key sentinel, MRU promotion/shift. Key covers family/variant/part/flags/overlay stack hash; surface excluded because role binding happens afterward. Generation wrap must clear slots before stamp returns to1. Acceptance: deterministic hit/miss/reset and wrap tests, no stale style; zero alloc and ≥90% benchmark floor. Historical ~99.7% rate and ~12ns are reported, not new measurements. Floor is cache health, not universal hash guarantee.

O2 `borders_set(ASCII)` must also call public idempotent whole-set `ascii_glyphs`; four box-drawing roles and every typed line/scrollbar field including caps/seams must change, without adding glyph enum variants. Quiet `-`, active `=`, track/caps `|`, thumb `#`; whole-frame and component-free scans plus positive Unicode control prevent vacuity. Last-write-wins/sticky behavior is intentional: later glyph override wins; changing borders back does not restore glyphs. No automatic Unicode capability axis. Full ASCII table remains required, later §35 reassigns theme ownership and whole-GlyphSet ASCII width1 acceptance; old ~31-role estimate and 4E coupling are corrected.

O3 preserves categorical hue/brightness ANSI16 algorithm and exact tokens. Corrects fabricated `border_subtle→Black` into `#262626→DarkGray`; `surfaces[1] #111111→Black`, fg #b3b3b3→Gray, primary→White, accent→LightGreen, danger/danger_soft→LightRed, warning→Yellow, info→LightBlue; dark accent→Green and dark danger→Red. Do not retune implementation to satisfy erroneous prose or regenerate unrelated baselines. CIE76 arithmetic corrections are rationale against the rejected perceptual-distance metric, not new normative tokens.

O4 keeps exactly0 style allocation, adds historical strict per-query ≤16ns and differential `resolution_ns×2000/160≤32000`, not erroneous ×10 over200. Real Showcase style-resolution share≤5% is deferred only until real app frame exists; reinstate with actual frame then report substitute. Baseline-free differential must be named in header. Intent probe count is cumulative; assert empty queue0 probes/allocs and nonempty 500-vs20 difference480 for one update pass, not absolute500. Raw O(n) app-loop ratio is reported; per-control ratio `(ns500×20)/(ns20×500)≤1.25` strict. Do not relax equality to multiples or redefine counters per-frame.

## P/Q: lifecycle, geometry, glyphs, forced state

Layer-owner update must run even after close; caller opening/resizing/closing may guard `is_open`, component delivery may not. Runtime finishes by diagnosing undrained runtime-addressed Layer/Cancel/FocusIn/FocusOut buckets regardless of decorative-only registration. Pointer decorative exemption survives. Tests require deliberately gated owner produces diagnostic; unconditional owner receives dismissal with zero diagnostics; decorative click remains nondeliverable. Opening dialog uses its measured layer spec to avoid one-frame Fill flash.

Dialog height must account for border2+title1+wrapped description+input rows+optional body separator/body+optional separator/action row. Input rows0 / field_height / field_height+1 for none/prompt/acknowledge; title row reserved even empty. Draw and measure share wrapped-row and token arithmetic. Prompt body cannot be caller-guessed. Verify prompt field height, themes, tiny bounds, resolver ownership and no component recentering.

Forced reference rendering is noninteractive at every composed depth. Internal inherit_forced propagates from Dialog to Buttons and Field through FieldControl to TextInput; none registers IDs, focus stops or decor. Boundary gate forbids exposing internal propagation as a public alternative bypassing state_override restrictions. Temporary example/matrix split expires when Showcase has a lib/test surface; preserve runnable demo and registration assertion. Both render and render_components targets must run until any later consolidation preserves every named test identity; --test render alone was incomplete.

Mono DISABLED must remain readable, not black-on-black: primary foreground + DIM, remove other modifiers on LABEL/MARKER/FIELD/TEXT before ERROR rules so UNDERLINED survives. No geometry-changing disabled brackets. BUSY/LOADING uses real spinner icon cells rather than inert state rule. Driver required states are union over capabilities, not if/else chain. Fixture force pairs flags with readiness/disabled/checked props so rendered affordance can actually appear. Readiness-capability refinement below supersedes weaker icon-slot wording.

Q1 resolved glyph becomes `Slot<GlyphRole>` in Resolved and PartMetrics. Cell-owning gutter/marker/part distinguish Inherit/Set/Clear while preserving reserved geometry. Marker Inherit uses caller glyph; Clear leaves blank; part Clear reserves width rather than becoming width0. Text-run labels do not automatically insert glyphs. Live borrowed-row/dynamic-tab example callers need regression coverage. Pressed brackets paint existing Button/Tab/Chip pad cells via one helper, never steal label/ellipsis/close columns; RowUi rows use container reverse+BOLD. Historical Choice/Brand separate bracket questions remain explicitly UNRESOLVED by this section; do not infer exemption or resolution.

Q2 private paired fixture state makes force sole writer; BUSY>LOADING>ERROR>Ready precedence. Q3 reason-string containment replaces impossible grep, but later sections prove it only checks textual shape; not sufficient behavioral justification. OVERLAY means layer opening, TRAPS_FOCUS means actual trap. §29.8 corrects original skip-all trap gap: every overlay opens/closes/restores appropriately; modal must declare trap, declared trap must actually arm one; trap non-empty; wrap unconditional even one stop; tiny rect still traps. Select is nontrapping Popover and must dismiss on focus-out; runtime walks stack after focus transition; FocusOut must not restore opener or Tab becomes no-op; Modal excluded. Esc still reaches Select before outer dialog. Rename historical form test claiming trap to focus-out dismissal test.

ChipBar exact PARTS retains OVERFLOW and gains MARKER `[CONTAINER,MARKER,LABEL,CLOSE,OVERFLOW]`; selected marker in existing pad, checked canonical role corrected from nonexistent Check to Checked. Width/label/close geometry unchanged; cannot narrow SELECTED away. §33 found live CHECKED vs forced SELECTED mismatch: forced picture alone did not prove genuine chip checked affordance. Chip add action naming, RadioGroup controlled value documentation, FieldControl item-channel composition, StatusBar per-item hover, Choice/Brand bracket remain unresolved in Q; later audit must trace decisions.

## Neutral recipe and truthful style testing

§31 every mutating recipe pass covers `by_family ∪ neutral`; neutral is substitute family recipe, not seventh precedence/base layer under all families. Apply same mono rules and variants to all resolvable recipes; unknown custom family and public default Recipes are reachable. Test both themes against still-undeclared family: rule count and actual resolved bold/error glyph/readable DIM. Original test repeated buggy iterator and missed neutral. Sparse declared family replacing neutral is intentionally distinct from undecided get_mut/override_family/override_variant/define_variant/define_family insertion behavior, which silently loses neutral; keep that separate root cause.

§33 invariant P: PARTS exactly own-draw resolution set across driver fixture sweep, including same-ID composed components; caller RowUi arbitrary custom parts attributed Row, not Component, retaining real owner/family. Instrument every RowUi style resolution including container/gutter/label_patched/trailing. Remove extra allowlist; Field explicitly includes composed parts, containment test. Sweep default, forced mono states, empty rows, disabled, narrow, activation-open paths; closable props activate CLOSE. Unpainted declared part must paint or be removed, no reason-string exemption. patch_part hook removes hidden ordering dependence; registry-wide parts coverage must match actual registered cases including Probe/Dialog/Props. Zero recorded resolutions must fail, but interim nonzero check misses partial under-recording. Owner-scoped automatic recording on bare public Ui author surface remains owed; existing note_styled opt-in does not prove complete customization.

§34 terminal I/O boundary run/TerminalSession detects capability; Theme constructors, Runtime, Harness, Scene remain environment independent. Explicit for_level narrowing never widens; downgrade same level idempotent before runner applies it. Pure from_env table: TERM=dumb wins even force; nonzero/nonempty CLICOLOR_FORCE overrides NO_COLOR and tty; nonempty NO_COLOR mono, empty not; redirected stdout mono unless force; unset/empty TERM ANSI16. Test pure inputs, no unsafe set_var. Capture script previously unsets NO_COLOR and hardcodes truecolor, so mono live review never executed; fix both runner and capture mechanism. Headless mono digest labels were explicit and not mislabeled—do not confuse missing review with corrupted evidence.

## Gate/provenance amendments

§35 Ui constructs no components: strike Ui::scroll_region everywhere; direct ScrollRegion update/draw returns content rect and owns registration/scrollbar/capture. Seven duplicate scrollbar implementations were library widgets grid/table/panel/viewport/code/props/list, not app code. Doc-check is name-existence only, not signature or inline-example compilation; compiler-backed external examples remain necessary. Suppressions need kind (future/foreign/rejected-open), owner/expiry and stale-hit failure. Present executable claims must actually exist or be explicit open obligations. Full ASCII assertion must inspect every glyph, one-byte ASCII width1, not merely reject U+2500..257F; independent design review still required.

§36 first-generation item19 covers exactly first capture per missing key, never subsequent movement; 14 named components×8 states×2 themes×2 colors×2 sizes=896 historical lines. Missing branch must print frame text. Historical review admitted glyph-only and no style review; current user requires full cells/styles/cursor plus images. Scratch generation identifies all added/moved cells because first-error panic skips rest; classification set must equal changed key set. Guard at that revision does not exist, cannot be claimed run. Historical guard forbids moving truecolor keys and requires citation/completeness; current allowed exception must be explicit independently reviewed, never automatic bless. Inherited Button effects include Dialog actions, not just leaf snapshots.

§37 AST gates replace substrings for conformance registration, forbid unsafe attribute, unsafe impl and generic/multiline editable signatures; each must fail deliberate broken input and pass fixed. Missing Grid made gate vacuous; due missing subject must fail. Dependency-tree substring cannot prove every path is beneath backend; multi-root structural proof owed. no_todo self-name line exemption remains loophole. No source scanner should force meaningless production calls.

§19 corrections preserve frozen pre-refactor root tests/perf_baseline.txt and distinct live library file; rows disjoint. Updated historical Before values: Showcase213 allocations; TablePro frame1030/176hits; Jackin manager499; move132; Capsule1080602allocs/74131917bytes; grid load61005; sort399998 per two-sort iteration; viewport15949816 per1000pushes+frame; truncate6, fit7, middle23. Targets unchanged: Showcase<20/ring≥4/hitgrowth classified; TablePro<100/hits≤320; manager<60; move0; Capsule<200; load<8000; viewport cost independent of length; truncate1; RowUi fit0 (inline grapheme corpus excluding ZWJ); middle≤1. Correct retained app tests26 Showcase/23 TablePro/28 Jackin plus10 module tests; current inventory by identity overrides historical counts, never drop tests to hit count.

## Readiness and force semantics

§38 status acceptance must paint ICON for BUSY/LOADING and ERROR; declare REPORTS_STATUS and force BUSY/ERROR into required mono states. TextArea/List were real missing-affordance cases; Progress/Meter/StatusBar/HintBar/Empty actually paint and must keep states; Spinner always spins via frame prop so no distinct entered BUSY state; ScrollRegion production capture implies PRESSED and mono thumb BOLD. Dialog launcher-only fixture says nothing about actual dialog status; own unmet readiness remains explicit, not excused by widening forced fixture. Reason strings cannot prove reachability; named capability cross-check plus independent review still needed for differently named readiness props such as EmptyState.

§39 invariant Q: `flags(runtime,derived)=forced.map_or(runtime|derived, |f|f|derived)`, always superset derived. Forced state replaces runtime hover/etc but never erases error/busy props. Separate two arguments make ownership decision explicit at all21 historical sites/18 files. Slot<StateFlags> rejected: Option already represents forced/nonforced registration; explicit force-empty must remain noninteractive. Fixture forced returns Option so unconditional misuse fails compilation. Fix before first-generation baseline: disabled Progress/Meter/HintBar wrong content/styles otherwise become pinned; historical24keys/12truecolor are not current measured diff. Empty three-of-four parts bypass overrides remains owed; PROGRESS ICON CHECKED dead rule needs real done fixture or deletion; recipe-match coverage must enumerate rule coordinates and use stale-inverting exemptions, not just part/state coverage. Tests must deliberately disagree runtime, forced and derived flags; old force paired props made union and replacement indistinguishable. Review actual cells/geometry, not pairwise hashes alone.
