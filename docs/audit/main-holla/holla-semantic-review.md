# Holla semantic review — code and prose coverage complete; visual/runtime proof pending

Pinned reference 794b095c196562d38f1b6f7ce379c128af2a023d. Source read in bounded nontruncated chunks; no source mutation, test execution or live reproduction. Findings below source-proven unless stated otherwise. Main remains implementation base. Recovery-tree replacement directives remain explicitly superseded (AUDIT.md).

## Coverage ledger

Complete initial implementation files read: app.rs 1–1130; main.rs 1–125; scenario.rs 1–112; clock.rs 1–147; screens/home.rs 1–1236; screens/plan.rs 1–359; screens/activity.rs 1–250; screens/modals.rs 1–143; domain/action.rs 1–167; domain/plan.rs 1–289. Additionally complete: all remaining domain/*.rs (including fixtures 1–949), sim/*.rs (catalogue 1–667, plans 1–696, world 1–251, mod 1–7), screens/mod.rs 1–211, app_tests.rs 1–993. Thus all initial Holla Rust files have now received full semantic reading. Complete project/recovery prose: holla-project/GOAL.md 1–280, PROMPT.md 1–18, notes/design-note.md 1–94, decisions.md 1–296, interaction-models.md 1–665, main-vs-holla-refactor-analysis.md 1–227, HOLLA_REFACTOR_RECOVERY_PLAN.md 1–1458. Deleted GOAL.md 1–1552 and FEEDBACK.md 1–946 now complete. JACKIN_REFERENCE.md 1–2409 fully read in chunks 1–115,116–420,421–700,701–1000,1001–1350,1351–1720,1721–2080,2081–2409 (205046 bytes total), exported under deleted/. Final historical agent configurations and README additive patch fully read; model-specific restrictions superseded. Both DESIGN texts complete: Holla 1–1255 read in bounded chunks 1–340,341–660,661–970,971–1255; exact MAIN_DESIGN→HOLLA_DESIGN comparison proves main 1–1242 shared text identical outside four fully read hunks. All 12 unique commits now have semantic disposition: initial Rust/prose addition and later README, agent metadata, keyhint, design, notes, capture-script, recovery and ignore patches read. Capture-only patch: paths/bytes/hashes inventoried, images not visually inspected. Final README 1–127, Cargo addition, full p6 script and amended keyhint patch additionally read.

## New actionable findings

H-S01 app.rs 604–631: mouse modal result always text=None; keyboard extracts input/ack. Alias save with mouse loses payload. Centralize result extraction for keyboard/pointer; test identical semantic action/text and domain effect.

H-S02 home.rs preview_dialog gives Blocked secondary button without disabled state; on_modal preview re-finds action but checks risk only, not availability. Revalidate availability and bound identity at effect boundary, keep blocked explanatory preview. Test blocked action via pointer, keyboard, stale dialog, direct forged action.

H-S03 home.rs PG dialog tag stores no PID/query; terminate reselects current first blocker instead of verifying displayed target. A different blocker or reused PID can be killed. Bind target snapshot (PID+query+host/revision) to typed semantic intent; revalidate exact identity before in-memory mutation. Test replacement, PID reuse, changed query, removed blocker, cancellation.

H-S04 home.rs implements no on_paste; default Screen ignores. Custom modal paste also consumed without forwarding in app.rs. Route paste to active query/picker/input through public edit component. Test q/? pasted as text versus plain shortcut behavior.

H-S05 mouse action row invokes run directly, while keyboard Enter on gh.clone opens clone picker. Unify product intent dispatch before modality-specific path; test same clone collection/review flow.

H-S06 app draw mutates too_small, size, focus, ring/hits, modal pop/push, menu hover (draw_host_menu). Plan draw adjusts scroll; Activity draw inserts/clamps scroll. Move durable changes to update/layout preparation, commit presented geometry separately. Repeated draw invariance must check UI/model/effect state, not only world.

H-S07 Plan keyboard focus ring includes visible steps only; keep_visible runs in draw after focus choice. Offscreen steps cannot necessarily be reached because Down wraps visible ring. Plan draw also overpaints last registered step with “… N more steps”, so visible label and hit target disagree. Model-level stable step cursor plus authoritative viewport required; test every step at 80x24 with output pane active. Plan has no on_click action or wheel override: pointer selects via app focus only; no pointer path opens confirmation. Add accessible equivalent controls without losing accepted geometry; independent exception review required.

H-S08 Activity on_wheel exists but render registers no scroll hit region, so app hit_scroll dispatch cannot reach it. Add runtime-owned viewport area and test wheel at actual reference output coordinates. Preserve per-activity scroll; keyboard works independent of focus.

H-S09 PickerModal maps payload by displayed label, not stable item key; duplicate labels can select wrong payload. Carry opaque stable ID through borrowed picker items and result.

H-S10 plan on_modal trusts tag kind and action index alone; ignores tag action ID, text, ran state. Domain run is public and apply_effect can be repeated; front-end disabling alone insufficient safety. Typed validated approval bound to plan/host/revision, one-shot commit, effect sink tests needed.

H-S11 CLI unknown args silently ignored, help/errors exit before runtime; preserve known aliases/options unless explicit reviewed correction. Ctrl+C directly exits when no modal; Ctrl+Q/q uses confirmation. Too-small q/Ctrl+C exits directly. These are product exceptions to ordinary quit confirmation, to characterize not accidentally remove.

## Migration slices

1. Pure app domain and deterministic fixtures into apps/holla with UI-free fact adapters, exact fixture tests, no process capability.
2. Shared runtime contracts for focus, modal identity/result, pointer capture, paste, clock, prepared layout; app shell stores product route/status/intent only.
3. Home production view and keyed ranked actions with single dispatch; query/picker/preview/trust/clone/ranking journeys and exact state cells.
4. Plan DAG public custom rows + viewport; single typed approval/effect boundary; complete narrow-viewport keyboard/mouse reachability.
5. Activity public custom entity strip/log viewport with stable identity, tones, retained scroll, route timing.
6. CLI/process harness and immutable 132 matrix + ANSI16/state extension; all original tests mapped to executing binary target; independent artifacts/review before integration.


## Domain/simulation/test findings after full Rust review

H-S12 catalogue recent_actions invents Risk::ReadOnly/Ready for every stored pin/recent command, bypassing canonical broad/bounded/trust semantics. Ranking memory must reference canonical action or explicitly typed custom simulated command policy; never downgrade risk. Pin broad cleanup and untrusted task then activate through every route.

H-S13 domain Plan::run only considers Failed/Skipped dependencies, not PolicySkipped or Pending. Public Vec<usize> deps assume earlier valid indices without validation; cycles/out-of-bounds can recurse/panic. Use validated DAG construction and success-only dependency predicate. Preserve fixture declaration order and parallel independence. Test policy-skipped dependency, cyclic/self/forward/invalid edge, duplicate ID, repeated run.

H-S14 Docker apply_effect intentionally preserves partial failure effects (web/cron removal on failed containers step); do NOT simplify to success-only effects. It leaves container_bytes/images/networks accounting unchanged even when output claims removals, while images bytes halves on every repeated apply. Model per-step typed effects and consumed application; reconcile counts/bytes with exact output. Add effect idempotence, failed partial removal, excluded branch, changed-target tests.

H-S15 Git plan claims checkout primary but apply_effect only clears behind for successful pulls; branch/upstream state never changes. Diverged children are advertised untouched although fetch/checkout report success before pull fails. Need reviewed truthful partial-effect model, preserve per-child independent failure. Debian output similarly hardcodes tool versions/held/reboot text across differing RemoteHost world; generate truthful lines from fixture state and actual effects with narrow reviewed changes.

H-S16 Disk catalogue recoverable count includes active-today candidates; plan will_change excludes them (existing test only checks removal count, not exact bytes). Keep inspected bytes versus policy-removable bytes separate and explicit. apply_effect matches paths only, so changed freshness after review is not revalidated. Validate same target/freshness before mutation.

H-S17 World::seek rounds frame milliseconds up to multiples of 80 and casts u64 to i64; frame=100 =>160, >i64::MAX becomes negative, huge legal values create enormous loops. Characterize accepted normal --frame semantics and implement bounded deterministic seek preserving those outputs. Reduced motion currently only affects tick_interval indirectly (paused handled, reduced not special); discovery spinner still animates. Treat claimed reduced contract as needing explicit corrected implementation/tests.

H-S18 Alias “Change alias” only appends; lookup first wins, so replacing existing alias may be inert or duplicate. Enforce alias/name ownership and actual replacement. Reset ranking clears aliases globally for command, pins/hides only cwd, leaves usage intact; preserve documented scope or explicitly reconcile label with effects. Scope detection for mise uses raw starts_with cwd (sibling prefix confusion); use path-component semantics.

H-S19 App tests all instantiate Theme::junie and compare symbol text. paused_frames_are_byte_identical is text-only, not bytes/style/cursor. PG revalidation test never changes target. merged_logs_scroll_retained_per_activity uses short fully visible stream and asserts only route/title, so scroll retention isn't proved. No process-spawn, paste, original coords, ANSI modes, first-input-before-draw, queued input, draw-state invariance, pointer-gate payload, or effect-replay guards. Existing 70 named declarations must survive migration but need stronger independent proofs.


## Prose review and supersession decisions

H-P01 all holla-first implementation directives are superseded, including recovery title/base/§1/§4.1/H-01 step 3/§12.1/§12.3 line-of-development, and analysis §1/§8 Phase 0,2,3. Main workspace already exists and must remain base. Consequently recovery “delay package cutover/public facade” instructions apply only as historical failure leads, not authority to restore root monolith or duplicate public API. Safe bounded integration and independent acceptance clauses survive.

H-P02 retain recovery H/V/A/R/APP/PKG/FINAL requirement IDs as traceable requirements, adapt sequencing to main repair + Holla forward-port. Pure draw, prepared authoritative geometry, keyed collections, bounded public customization, clocks, all-family coverage, exact discovered tests, external consumers, three verification layers, artifact hashes, anti-circular expectations, failure aggregation, and exact merge-result reruns remain valid. Historical pass counts/CI statements are leads, not current evidence.

H-P03 analysis §8 suggests gate decrement on Tick as one timing fix: insufficient for explicit elapsed-time contract. Deadline/monotonic source wins; do not replace event count with tick count. Also reject advisory-first correctness gates and ≥269 floors as final proof; exact identity plus meaningful executed coverage required. Preserve stated intent that empty/missing/stale evidence fails.

H-P04 interaction-models is brainstorm/semantic sketches, explicitly not pixel specification. Later decisions supersede initial two-row activity tabs (actual tonal row1), p/o plain shortcuts (actual Ctrl+P/O), Tab scope (Ctrl+S), home Esc exit (consumed), and global digits (query text on Home; Ctrl+A global). Do not resurrect discarded geometry to satisfy old sketch. Yet stable selected identity under streaming, target-bound revalidation, cancellation truth and keyboard access are enduring product requirements, not mere sketches; current source violates several.

H-P05 notes' claims “blocked secondary disabled”, “pin always beats alias+urgency”, “every action keyboard reachable”, “scope always visible” require proofs: source blocked button is enabled; score includes differing ring/usage weights, so constant 150 alone doesn't guarantee ordering; clipped ring makes navigation incomplete; narrow rows drop scope below 10-cell budget. Preserve exact accepted visuals while resolving safety/identity/accessibility separately with bounded decisions.

H-P06 P5 decision explicitly confirms Reset clears pin/alias/hide, NOT usage (history truth). Retain usage; do not treat H-S18 as permission to erase it. Global alias replacement semantics still broken. P5 also explicitly accepts policy-skipped rows labelled “required” meaning non-excludable; preserve unless reviewed clarity change.

H-P07 design-note's claim “nothing runs without a gate” is overbroad relative to explicit ReadOnly direct behavior; simulation remains absolute. Destructive task reset-db is classified ReadOnly after exact-file trust by current source; product safety contract needs typed task risk instead of name/command inference before any future generic adapters. Do not introduce real effects.

H-P08 p6 decisions record acceptance of cursor on placeholder first cell, reason dropping with two-cell-gap policy, header crumb reserve +10, initial seeded launch failure, bold Warning ▲ and body discovery ▲. Preserve these precise conventions; earlier provisional +8 reserve and two-row strip are superseded. Historical claims of two critics and settled captures are historical records only, not this review's own evidence.


## Deleted obligation retention

D-GOAL deleted by 49961e0 is the complete original Jackin preview goal, not dead boilerplate. Retain 13 top-level product families and §34 forty-step keyboard journey; original exact 11 launch stages; 5 Editor/5 Settings tabs; 8 scenarios; create prelude; boundary arbiter (concurrent claim/winner/idempotence/discovery failure); failure/no-survivor outro; durable detach/reconnect/takeover; all Capsule modes/dialogs; account/usage distinctions; secret service boundary. Pure rendering, deterministic ticks, single terminal-session guard, all four sizes, mouse/keyboard equality, public generic controls already existed as obligations before architecture refactor.

D-FEEDBACK explicitly amends D-GOAL rather than replacing entire product. Retain all 13 numbered amendments: reusable distinctive status bar, canonical accent brand, bottom HintBar precedence, anchored tab Rename/Close, selected tab background+underline and no selected left gutter, semantic meter line/block (default low 0–59/medium60–84/high85–100), compact+advanced change inspector with standard/TUICR simulated view, actual picker wheel motion, every scrollable surface audit, Capsule functional menu bar replacing old identity strip, no unconfigured agent options, global registry→workspace effective account set→session zero/one/multiple resolution, sparse configured-role scopes scaling to100+. These supersede original per-provider single-account override/Auth duplicate credential workflow. Do not restore obsolete original workflow during parity repair.

D-FEEDBACK test identities are conceptual obligations (tabs style; picker wheel + no render reset; meter modes/status; inherited/disabled/additional/two same-provider accounts; agent zero/one/multiple;100+Roles;tab context menu;HintBar context/location;both diff layouts/modes/wheel). Requires actual image inspection and runtime keyboard/mouse evidence, not code presence. Simulated Jackin/Git/provider/TUICR boundary remains absolute.

D-JACKIN_REFERENCE initial contract: historic source archaeology pinned external real Jackin 8d161b3b41c64da0de3ab5f4aef1969316c193d1, semantics not old visuals. Its developer-machine path/source-only research restrictions describe the historical research task, not authority to stop this migration or invoke installed real Jackin. Original real-product semantics must be reconciled against later feedback and accepted Holla preview, not copied wholesale as UI design. All pages now read; retained obligations and source/document drift are recorded below. Historical source citations were read as document content, not independently re-executed against real Jackin.


## Complete deleted reference reconciliation

D-REF01 retain all 87 atomic child IDs (§atomic-child-inventory) and 19 WF step sheets (§21.1) as the exact semantic coverage registry, alongside 14 aggregate specs (§11) and modal invocation matrix (§15.6). Do not treat aggregate labels or enum presence as executable child-flow proof. Original Editor/Settings Auth children migrate to the later FEEDBACK global account registry/workspace effective set model while preserving typed source/secret validation, cancellation and scope; obsolete single-account override is explicitly superseded.

D-REF02 preserve host selection stable across discovery, current-directory/saved/instance/new sentinel identities, hidden purged/superseded rows, manifest sessions versus live daemon snapshots, preview/list focus and independent horizontal/vertical scroll. Actions include prewarm, reconnect/restore/new-session/shell/inspect/stop/purge/delete, not only launch/edit. Refresh failure clears invalid derived live data and reports a deduplicated error; retain explicit no-sessions/no-tabs/unavailable distinctions.

D-REF03 create prelude source→destination same/edit→workdir→name→Editor retains readonly, default destination/source path and basename name, last browser cwd, cancellation rewind determined by edited destination. Editor pending/original transaction isolation, save validation+effective diff, running isolated-mount/source-drift cleanup gates, save-error parent recovery persist. Mount Shared/Worktree/Clone cycles and strict path validation, role allowed/default/trust/not-in-registry identity, reserved env rejection/masking/source/scope remain semantic obligations. Settings is a full global stage with independent coauthor/DCO flags, global mounts/env/trust and dirty/save/discard handling.

D-REF04 usage axes stay separate: provider, agent runtime, usage surface, stable account subject/handle, quota window, lifecycle, freshness, confidence, session token telemetry versus subscription quota/spend. Retain nine registry surfaces Claude/Codex/Amp/Grok/Z.AI/Kimi/MiniMax/OpenCode/Unsupported. Missing/unsupported is not zero; presence-only credential evidence is not authenticated identity. Host startup projection plus r refresh and Capsule scoped read-only refresh preserve last-good data with freshness/error metadata. Simulate opaque capability relay boundary; never read real host credentials/projection or call providers.

D-REF05 historical reference internally contradicts itself: §25 scenario matrix calls host Usage renderer partial while §§11.2,16.4,29.1 and Final Verification call broker-backed startup+r refresh current. Retain current startup/refresh semantics; stale partial row is superseded by specific corrected evidence. OpenCode ordering gap remains a gap (provider exists), not permission to drop it. Recorded dirty-exit source/docs drift requires explicit preservation decision and tests; do not silently adopt stricter old design text or invent live feature completeness.

D-REF06 launch all 11 ordered identities, queued/running/done/skipped/failed and modeled-only blocked distinction; run-bound bounded 5000-line ANSI build log with wrapping/tail/manual scroll; safe failure summary/stage/run ID/next step; only run ID copyable from failure; full failure diagnostics not public copy. Container info absent-field honesty and explicit copy, failure freeze, hard abort versus quit/cancel, exact terminal handoff and final Construct outro persist through simulation. Historical real docker-exec handoff is semantics to simulate, never a process capability to port.

D-REF07 Capsule retain five mode priorities dialog→drag→select→prefix→normal; stable pane/tab/session targets across picker/confirm; split .05–.95/default .5 and .05 resize steps; nearest directional focus, zoom, tab overflow, label16 cap unless later accepted design changes. Preserve keyboard/palette/pointer parity, prefix routes, rename/export/context/usage/spawn failure/split/close/exec/dirty exit dialogs; terminal bytes/paste/focus/mouse/protocol routing through simulation only. Cursor hidden under dialog/scrollback/no output/no pane/agent-hidden; typing snaps scrollback live; selection drag/autoscroll/word-copy semantics; durable detach, single attach takeover, last-live-session drain stay distinct.

D-REF08 exact old phosphor colors/rain/row glyphs/split percentages/modal widths/footer geometry are historical presentation, not current parity authority. §20.7/26.5 redesign freedom does NOT authorize changing accepted main/Holla fixtures under this task. Later FEEDBACK menu/header/tabs/status/footer/meter/diff requirements and current DESIGN own presentation. §26.6 historical exclusion of captures/fixtures/refactors belongs to archaeology task, explicitly superseded by current user implementation+verification scope.

D-REF09 planned Apple parity, host event stream, Desktop bridge, new provider/auth features remain out of scope. Historic evidence tags and final verification statements are documentation provenance, not this session's test/image/runtime evidence. Existing tests/baselines must be independently inventoried and proved against candidate; real Jackin source path/commit cited in deleted document is not a migration dependency.

H-P09 final README verification patch 4a46bca fully read. Retain three layers, eleven scenario checklist, exact hidden-command reveal, frame0 launch failure, remote quit identity, resize and color/motion. Replace process-list observation as sole simulation proof with structural no-process boundary plus deterministic effect tests and adversarial runtime harness. Preserve original capture namespace; run candidate evidence externally with strict failure propagation and stale-artifact rejection. Claims every flow tested/mouse everywhere/reduced motion are hypotheses contradicted by H-S findings, requiring reproduction and narrow fixes.


## Full DESIGN reconciliation

D-DESIGN01 semantic tokens, exact theme values, single accent with explicit safety/brand/activity exceptions, bold/glyph/underline/reversed-cell state grammar, whitespace/card/frame hierarchy and spacing remain authority. Four Holla deltas are additive shared warning/error glyph weight, failed/unavailable glyphs, one-row tonal entity strip exception, and content-driven decision-dialog width. Main shared text is byte-equivalent outside those deltas. Keep main public component architecture; use bounded custom entity rows to implement Holla exception, not legacy generic Tabs or private app runtime.

D-DESIGN02 retain minimum72×20, four viewports, prioritised metadata/hint/segment dropping, no overlap, clipping and own viewport scroll, wheel under pointer without moving selection/focus, modal wheel barrier, keyboard-triggered ensure-visible only, hidden reachable stops, matching press/release, hover suppression after key, 140ms activation and4–5s status. Render-rebuilt ring wording and guardrail2 historical draw-and-register imperative do not authorize durable draw mutations or private runtimes: reconcile into prepared authoritative geometry with read-only paint and shared runtime registration. No metadata-scrolling reset on draw.

D-DESIGN03 contradiction: final composed-pattern sentence says no context menu exists despite full ContextMenu/MenuBar catalogue and accepted product implementation. That absence claim is obsolete; retain single public anchored-menu primitive and canonical menu/hint ownership. Picker catalogue mentions internal faint hint row while later HintBar rule explicitly forbids private modal/menu/picker hints: shell resolves exactly one fixed bottom layer. Intro says two applications and several old global keyboard descriptions likewise scope showcase/TablePro; Holla's accepted always-edit query/chords are explicit app exceptions, not permission to regress text routing.

D-DESIGN04 preserve shared component contracts: button busy/disabled refusal; text edit commit/revert versus multiline keep; select owner popup/cancel; keyed list/tree/tab/table selection; document tabs selected-plane+edge-to-edge underline without gutter; full viewport selection/tail/copy; minimum-clamped splitter; ordered step rail; one quota meter Line/Block with60/85 thresholds and explicit domain status; masked input raw-value lifecycle; canonical lockup/status/hint/menu; data grid pending and safety; generic code editor receives language hooks; facts typed target moves focus before activation; no domain knowledge in library.

D-DESIGN05 preserve deterministic boundary ritual phrases/glitch/starfield/outro with tick/seed/motion semantics, sparse registry policy and effective account set, secret-safe session zero/one/multiple selection. Do not port real secret/process capabilities. New generic conventions require showcase coverage at120×40 and80×24; no baseline refresh to hide regression. Full source semantic reading is complete for this subtask. Runtime reproduction, executed test mapping, public consumer compile checks, capture rendering/image inspection and narrow exception approval remain separate pending evidence, owned by parent/integration work.
