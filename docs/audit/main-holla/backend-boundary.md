# Backend-free input boundary

Base: 761f6d5. Branch: codex/backend-free-input. This slice implements the user-authorized experimental API amendment: backend-free core/testing consumers use framework-owned KeyCode, MediaKeyCode, ModifierKeyCode and KeyModifiers. Matching variant names remain; these types are no longer raw crossterm aliases. Crossterm normalization and bidirectional conversions exist only with the optional crossterm feature. Default library features retain the terminal adapter; workspace dependencies disable implicit defaults and terminal apps explicitly opt in.

Root cause: compile-only no-default checking could pass while an unconditional backend dependency and public aliases retained the backend. The gate now resolves and runs two standalone one-member workspaces, traverses their actual dependency closures, requires core/testing subjects, and rejects backend crates. Fixture lockfiles prevent accidental unlocked dependency drift. Synthetic graph mutations and an actual temporarily injected crossterm feature demonstrate rejection (backend-mutation.log); the fixture was restored before final verification.

Conversion proof covers every backend key/media/modifier variant, F boundary values, Unicode and null chars, named modifier bits and all 256 bit patterns. All key variants normalize press/repeat and filter release. Round trips preserve key identity and modifier bits. Existing event state policy remains: key press/repeat coalesce, releases are ignored, mouse behavior unchanged. Paste Debug redaction and drop wiping remain untouched.

Verification: commands.json records 13 passing commands and exact logs: all-feature and backend-free event tests; strict no-default library and xtask Clippy; isolated backend gate; two-file adapter gate; Rust 1.88 core/full-workspace checks and both isolated executables; documentation. Additional strict rustdoc all-feature/no-default logs final-1.log and final-2.log; final diff check final-3.log; final targeted formatting format-final.log. Initial final-0.log exposes fixture formatting subsequently corrected. Earlier all-target all-feature library Clippy and workspace check passed; their output was inspected live, not preserved here. New fixture formatting does not alter behavior.

Scope: manifests, owned keys/event adapter/public exports, isolated consumers and xtask gate; approved runtime.rs extension only cfg-gates session-only take_repaint_request. No application behavior, runtime routing, theme, or Paste handling edits. Parent integrates, independently reviews and pushes this slice.
