# Independent TablePro CLI review

Candidate: 967f7e72ec44d5e4991c690c1d7975b5d79d67a7. Reference: 794b095c196562d38f1b6f7ce379c128af2a023d. Read-only source review of all five changed files and pinned reference main.rs; clean candidate status verified. Independent reviewer build used stable rustc 1.98.1 (48a229cea 2026-09-01), isolated target under this directory.

Disposition: PASS for bounded CLI extraction and redacted argument errors; not full application acceptance. No new slice-specific correctness defect found. Help short-circuits before runtime, prints original description/key copy plus additive Paper theme option; error paths retain exit 2 without argument-value disclosure. Ignored unknown arguments and absent --connect preserve reference behavior. Existing additive case-insensitive colors, ansi256/ansi16 and Paper remain accepted. Theme/color order is independent in parser; actual explicit-color versus NO_COLOR remains deferred shared runtime defect, not proven by parser tests.

Independent execution: cargo +stable build --locked -p tablepro --bin tablepro passed (initial invocation using nonexistent package tablepro-app failed; corrected using manifest package tablepro). cargo +stable test --locked -p tablepro --test cli_contract passed 3/3. CARGO_TARGET_DIR points to this directory/target. PTY review script executed 16 reference/candidate help/error cases: expected 0/2 exit, no ESC bytes, unchanged terminal attributes, bounded completion. Candidate invalid connection/color synthetic-secret-sentinel absent from output; pinned reference echoes values. This is a reproduced narrow security exception accepted by review.

Additional startup evidence: default args, dangling --connect and ignored --unknown value enter alternate screen for both binaries. Reference q exits 0 and restores terminal. Candidate q does not quit and was killed after bounded 5 seconds; forced-kill lack of restoration is harness consequence, not independent cleanup finding. Follow-up Ctrl+Q candidate exits 0 and restores termios/alternate screen for all three cases. Source confirms pre-existing app.rs208 only binds Ctrl+Q, whereas footer app.rs1976 already advertised q Quit. Restored reference help now also exposes this known application mismatch. Track root application keyboard restoration obligation; do not claim all CLI/live acceptance complete or change reference help to bless app drift. No app interaction binding changed in this commit.

No source edits or commits performed by reviewer. Root integrator owns committing/pushing this external report into durable audit if desired. Artifact hashes in manifest.json bind observations to exact reviewed source and executable.

Source-confirmed separate safety gap: pinned reference app.rs274–315 request_quit counts pending rows and dirty query tabs, opening destructive confirmation before quitting. Candidate app.rs2157–2160 QUIT directly sets quit and calls cx.quit, bypassing those guards. Reference Ctrl+C cancels an active query before requesting safe quit. This pre-existing app behavior is outside CLI extraction but blocks full application acceptance. Proposed next bounded ownership: apps/tablepro/src/app.rs plus new quit-contract tests; implement one guarded quit intent for nonediting q and additive Ctrl+Q, Ctrl+C cancellation-or-guarded-quit, and modal barriers. Root must settle writer assignment and base after integrating this CLI commit. No speculative source changes made.
