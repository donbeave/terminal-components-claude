# ADJ-13 finite broker corpus author handoff

Added an independently executable source/AST corpus, not production broker or scanner implementation. TASK-072 R-002 / AC-011 / CHK-013 now mandatorily runs the submitted dispatcher through the new frozen group; the DEC ADJ-13 and HIST EARLY-046 edges name that exact lane. EARLY-AMEND-046 is an unrelated ActionKey amendment and was deliberately not changed. Coordinator assembly must project the new edge into the trusted source-obligation copy.

The corpus contains 45 distinct source snapshots: eight accepted shapes and 37 required rejections. Base snapshots preserve all 95 pinned main core Rust files, not just session.rs; other-file mutants add their explicit path. A real syn parser observes every byte, even inactive/unreferenced source. Parsed facts retain hierarchical guards, types/generic arguments, aliases/imports, visibility, field inventory, function/method depth and macro tokens. The observer emits no verdict. A future submitted checker must resolve these facts/source and pass the complete corpus with fresh positive recovery after each negative. A whole-file exemption, old regex, constant pass/reject checker or self-reported digest cannot satisfy the acceptance contract.

After the shared actual-compiler repair, normal session70486 and optimized session42292 both completed `broker-bootstrap-driver.py --self-test`, exit0:45 cases,37 negatives; all per-case AST hashes matched between modes. Each also exercised ten real-parser IPC controls: positive and recovery accepted; always-pass, always-reject, zero observation, retained-digest forgery, recomputed-digest forgery, boolean exit substitution, malformed JSON and duplicate-key JSON rejected for their exact intended reasons and required private execution counts. `--transport-self-test` exposes these focused controls independently. These are actual parser/source-premise and transport results, explicitly `submitted_checker_qualified: false`, not policy verdicts or future dispatcher acceptance. The earlier sessions34930/47798 covered the initial premise corpus only and are superseded here. Rustfmt passed and TASK072 taskfmt lint passed with zero warnings using the canonical experiment configuration. No production library API, rule18, singleton lifecycle or backend dependency was changed. Rust correctness guidance kept the observer fallible at external input boundaries and separated observation from policy verdict.

| Evidence path | Lines | SHA-256 |
|---|---:|---|
| evidence/broker-bootstrap-driver.py | 235 | ab112498f58ccd357e955f628435b01780c8af5d62a4f9f38d1db5eca703e152 |
| evidence/broker-bootstrap-observer.rs | 142 | ab260be9aa91f3ac9dd284e817118b9d24f8c2e645b5a33c1f5efa81427d1278 |
| evidence/broker-bootstrap-protocol.md | 13 | 8f4d117ca6de6ba0ddd0be91575484bf827c23c3c7cf999db5964aa519523f38 |

These runs bind shared main driver fd6e1f240cd09367b8414041302fa926d6d939502577d2ff8f3269c9cde1cd49 and source driver b1a74b78f6a13d0ba2430e016aa25471314a31301b814adbec878428a38f9ed6. Freeze closure is the three broker assets plus the existing complete ARCHITECTURE and RUNNER lists; runner-bootstrap-cases.tsv is not imported by this path and is not an invented dependency.

Review required before freezing: independent source-policy interpretation and adversarial checker runs against this authored corpus; coordinator transitive-asset freeze and source-obligation projection. Backend-free dependency/compile/symbol absence and production singleton installation failure/retry, poison, concurrent lease, cleanup and real signal controls remain separate mandatory TASK009 proof. They were not executed by this corpus. Existing 193 source cases remain unchanged and provide no ADJ13 coverage credit. Whole planning goal remains open.
