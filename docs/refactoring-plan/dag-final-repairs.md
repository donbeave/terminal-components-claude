# DAG and executor-contract repair record

Planning-only repair, 2026-09-11. This records contract changes, not approval of unimplemented production tools.

- DAG-FINAL-01: TASK-071 now separates host-selected preparation accounting (TASK-002–008, independent pinned-source discovery/expectation register, proposed products untrusted) from production accounting (accepted inventory plus disposition receipts). Every preparation README binds the mode. Producer-before-receipt and self-approved/omitted/forged-mode cases are assigned to the independently frozen runner qualifier. No dependency edge can conceal a future-receipt requirement.
- DAG-FINAL-02: TASK-007 explicitly discovers/reconciles all inline/external/generated tests. TASK-008 now lists the 146 pinned-main inline-source locations in its exact file scope and trusted source-scope contract. Only independently approved test assertion spans may change; outside bytes, cfg/ignore/module structure, retained assertions and original archives remain fixed. Known Holla hint/Escape/world-count assertions are explicit. Independent span/omission/archive counterexamples are assigned to TASK-071/072 qualification. The location search is not claimed to replace complete parser/compiler discovery.
- DAG-FINAL-03: Baselines TASK-002–006 own observation schema/identity mapping. Existing production tasks explicitly own untrusted extraction seams within existing writable source/completion-test paths; no new accepted candidate-adapter receipt exists. TASK-070 owns independent source/action/state qualification; TASK-072 owns real-production/extraction-only checks. Protected reference adapters and judges remain forbidden. No TASK-074 is needed because extraction is a bounded part of each already assigned production outcome.
- DAG-FINAL-04: Proof command examples now pass the host-bound immutable `/proof/bootstrap/experiment.toml`; configuration cannot come from the executor repository.
- DAG-FINAL-05: TASK-007's authoritative historical union count is 620. Task index imports actual TOML scopes/dependencies; coordinator owns regenerated graph/synthesis metrics.
- INT-02: Every README binds `campaign-executor-protocol.md`, which explicitly supersedes only canonical executor-authority/empty-progress steps while preserving AGENTS template bytes. Actual operator handoff uses existing future host commands, complete frozen progress and host-authentic results, not an invented scheduler. Initial TASK-001 qualification has an explicit manual trusted-operator exception using currently available pinned taskfmt and frozen independent drivers rather than trusting its own submitted host.

Validation after these edits: canonical taskfmt project lint passes all 73 packages with zero errors/warnings. DAG and integrated source-to-contract validation remain coordinator-owned. Preparation/seam/span counterexamples and host feedback/index/overlay qualification require the assigned evidence owners' final tests and independent rereview before these findings can be closed. No new runtime operation is claimed available today.
