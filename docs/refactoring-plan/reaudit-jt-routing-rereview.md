# Independent rereview of Filter, ChipBar and Inspect trace repairs

Bounded verdict: the repaired TP-039–042 and JA-060 routing premises are supported by actual oracle source and replay. This is not full application or frozen baseline acceptance. The coordinator did not author these scenario repairs or their five replay tests.

Read `reaudit-jt-routing-repairs.md` completely, all five repaired canonical scenario rows, both disposable Rust harnesses (`lib.rs`, 32 physical lines; `jackin.rs`, 16 physical lines), their Cargo manifest, and the reused Jackin Harness definition through its input/render methods. Minified source lines were read in full, not treated as one-line coverage shortcuts.

TablePro's separately imported App hashes to Git blob `80cb1e66cf8ed5a8fce97b75ea842f7a4793cfdb`, identical to immutable `02f5294b:src/bin/tablepro/app.rs`. An independent `git diff --exit-code` against the immutable oracle produced no changes for the imported shared library/core/widgets/UI/theme/runtime, all Jackin source, or TablePro connections/db/model/sql/tabs/workbench. Unrelated Showcase and unused TablePro App clock experiments in the same disposable tree are not imported by these probes. They were observed and excluded explicitly, not described as an entirely clean tree.

Executed `rtk proxy cargo test --offline --locked --manifest-path /tmp/jt-routing.fLfB8q/Cargo.toml --target-dir /private/tmp/tui-snap-audit.656lHG/oracle-clock/target routing:: -- --nocapture`. Four TablePro tests passed, covering all four sizes: premature Enter applies, Tab permits actual Add/Cancel/Update, invalid numeric text is accepted into a no-results filter, upper paste is consumed unchanged while typed text works, no-value NULL lacks a Value focus stop, and pointer lead plus group Space/Delete/X follows the source routing. The fifth test passed for both Inspect modes at all four sizes: selected Diff emits Copy, while clipboard/generation, full rendered buffer and modal remain unchanged.

The TablePro harness initializes the supported Production connection directly before driving real orders navigation; it does not independently prove the canonical CLI/connection prefix. Every later event uses actual handlers and real rendering. Pointer activation verifies the published hit identity and uses Move/Down/Up. Focus selection uses bounded ordinary Tab; the repaired ChipBar path deliberately uses a real lead click because registered focus alone did not make the Grid-to-group Tab route reachable. The copy test separately requires a Changed result from nonempty selection/copy, rejecting the earlier empty-selection false premise.

The immutable F08a/F09 proposal documents were also read completely; both explicitly remain Deferred. Their desired improvements therefore cannot supply current parity expectations. The historical semantic prefixes now state actual upper-paste no-op and Inspect copy discard, while source titles preserve the original proposals. `sync-app-scenarios.py --write` updated the remaining JA-060 copy and application matrix; `sync-derived-obligations.py --write` updated TASK-005/069 contribution copies from the corrected primary donors.

Residual obligations: full source seed prefixes, all required profiles/palettes, PTY routing, pointer selection of Inspect text, frozen flow assets, final exact source payload joins and whole-app closure remain future task evidence. This rereview neither authorizes OS clipboard effects nor promotes these diagnostic frames into acceptance baselines.
