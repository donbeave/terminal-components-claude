# Later amendments to architecture sections 0–29

## Scope and completion

This planning ledger directly reconstructs amendments to the prefix of `COMPONENT_ARCHITECTURE.md` before the top-level §30 heading. The prefix includes the original appendices and sections 21–29. It covers every indexed parent edge after `3ed377e3`, which is the last edge owned by the early reader; the parent assignment's `3ed5c26f` spelling was corrected against Git. The early reader owns `70dacec1` and `3ed377e3` themselves. The historian owns the initial J/L/M inline reconciliation. Later numbered sections belong to the middle and late readers, even when their decisions amend the earlier prefix.

Main is pinned to `7b27732a8c3c131760ec3438f641cb3c11343a42`. The controlling UX oracle is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. References to the older `794b095` fixture source below are historical evidence, not a substitute oracle.

All 85 owned parent edges are classified in [the exact coverage table](history-early-amendments-coverage.tsv): 58 distinct changed before/after prefix pairs, 15 unchanged pairs, and 12 duplicates of previously inspected exact pairs. No material older-section amendment remains unread in this scope. This closes a historical-reading gap, not implementation, parity, or task-mapping gates.

Original Git parent deltas were read directly, using complete line deltas for substantive contracts and exact word deltas for long paragraphs and nomenclature reconciliations. `a1759b2a` required 724 lines of context-bearing delta; it was read in bounded overlapping ranges, including the complete visual-item additions. Word deltas retain every inserted and removed word and identify the corresponding original hunk. Duplicate before/after prefix hashes prove content identity; repeated merge deltas were additionally compared by their exact zero-context changed blocks. [The hunk ledger](evidence/early-amendment-hunks.tsv) records 549 original hunks, 382 distinct changed payloads, and the first exact source edge for every reused payload. The initially supplied early report was used as navigation, not as a substitute for these reads.

[The read-only reproducer](evidence/early-amendment-extract.cjs) reads the 96-edge canonical index, asserts the ownership boundary, obtains both original Git blobs, and hashes the exact prefix. Run `rtk proxy node docs/refactoring-plan/evidence/early-amendment-extract.cjs inventory` to reproduce the table's hashes; `hunk-inventory` reproduces the exact hunk ledger. Pass one or more commit prefixes to reproduce the corresponding original hunks. `AMENDMENT_WORD=1` selects exact word changes; `AMENDMENT_NOVEL=1` groups previously encountered zero-context change payloads; `AMENDMENT_START`/`AMENDMENT_END` bound output without changing the inventory. Context may cross the §30 boundary; no later-section coverage is claimed from that incidental context.

## Authority corrections the execution plan must preserve

`a1759b2a` replaces per-recipe copied mono rules with a private static resolver layer, after the merged family/variant state rules and before author overrides. The generic manifest has 20 entries after `ICON + LOADING` is added; contemporaneous references to 19 in §16.1/§28 are stale figures. Requested-family lookup must reach neutral-backed custom families, and fallback application must leave recipe storage unchanged. Targeted Viewport/Grid/Menu/Help/Picker/Select rules are additional to the generic manifest. A historical count of 18 or 19 must not become the current contract.

The same revision replaces component-local forced-state builders and `inherit_forced` with exact-target `Ui::reference`. The entire subtree, including arbitrary callback bodies, is inert. Only the four runtime flags may be injected into one owner/part; selection, readiness, editing, disabled and read-only state remain genuine caller data. `Fixture::forced()` returns `Option<StateFlags>` so `Some(empty)` remains distinct from a live fixture. The broad prohibition against a Ui reference scope in old §28 is explicitly superseded. Later `15ecde1e` and `0f018368` make that historical status explicit throughout the original decision text.

Choice geometry is still an unresolved historical obligation. `43a147f0` named `choice::mono_pressed_choice_keeps_the_label_geometry`; `15371443` explicitly retracts that nonexistent current test and keeps Choice's in-run bracket outside the accepted reserved-pad helper contract. Brand and menu title have named padding proofs. Do not infer Choice closure from their implementation. Separately, `15371443` records that `ebfa8b8` closes TextArea's covered-part false negative by checking the actual recorded resolution when the digest is unchanged. A future `Conformance::patch_part()` hook is an optional ordering improvement, not unfinished mandatory work.

The viewport target is an exact full `usize` prefix of wrapped-row counts plus visible-content painting. Cold/reflow/invalidation is O(document); same-key append extends only the suffix; unchanged warm frames do no prefix work. Editing an existing line requires `ViewportState::invalidate()`, even when length is unchanged. Generation saturation disables reuse. The former claim that an exact global index could itself be window-only is superseded. Warm zero-allocation and visible-work proofs are distinct from report-only cold reindex measurements.

Form cancellation belongs to the enclosing owner; `Form::cancel` and `Reconcile for FormState` are withdrawn. Private Id-keyed reconciliation preserves eligible drafts but zeroizes removed or shape/sensitivity-transitioned drafts before visibility filtering. Public errors are always generic because state cannot observe current dynamic owner sensitivity; only Form's private render path may display current plain detail. Direct secret String lifecycles must construct sensitive state before `begin`. Safe-Rust zeroization remains best effort; the plan must not promise guaranteed memory erasure.

Bindings gain stable `ActionKey`, optional chords, owner-scoped overrides, and one resolver shared with derived hints. An explicit Tab/Shift+Tab/BackTab component binding runs before fallback navigation traversal. Hints select the first non-empty priority layer and cache on focus/state/layer/table/map revision. `ActionKey::application` and `custom` occupy disjoint ranges; applications must not put product commands in component identity space.

Publication amendments remove the old immediate restored-opener exception. Closing-owner FocusOut remains immediate, but restored FocusIn and retained input wait for successfully published geometry proving eligibility. Scene projection and aborted paint never grant live authority. Explicit typing fallback and simulation feedback are independent declared policies, not consequences of navigation focus or wall time. Their later inline corrections are preserved in the obligations ledger.

## Gate and integration history

`9486b654` corrects the false statement that style resolution records styled parts automatically. `8ee99771` replaces the formatting-sensitive Grid editable grep with a syntax scan and explicitly identifies its then-vacuous missing-file success. `444ae3d9` strikes a nonexistent application dispatch allow-list. These are evidence of a recurring architectural risk: declarations or reported green checks are not proof that the claimed path executes.

`0f018368` gives the current documentation check an honest scope: an open-ended numbered-section source scan using regex and syn inventory, not compilation, signature checking, visibility checking, or rustdoc JSON. The broader foreign-type/API architecture checks remain separate. `33eeb99e` and the independently based `03697429` add the same fail-closed shared-Props scan, including library composites; neither claims cross-file call-graph proof. `cd70747d` synchronizes the actual conformance registry, including Probe and DerivedHintBar, while removing stale entries. `15371443` corrects Cargo filters: the integration target exposes `<component>::<case>`, not an automatic `conformance::` prefix. `2a0cf299` fixes malformed `Option<StateFlags>` grep syntax and labels the 934-test number as evidence from tested `f713ccb`, not an evergreen HEAD claim.

Capture history is not linear acceptance. `cae32f88`, the equivalent `0c6c45df`/`3296f55e`/`a573eba8`/`08cabdf0` blobs, and `612ff7af`/`37f9031e` tighten matrix, app-manifest and per-page ownership checks. `df83c5ed` combines those previously inspected contracts. `2f20bea2`, `0defae22`, `ef15e28b` and `737ed7de` carry the identical exact-argv and owned-session amendment. `130b85a4` and `067869c7` revert that amendment to the prior ARGS contract. `557331b6` adds transactional per-shot artifact directories. `ec3d760d` combines the hardened transport content; both `e524ceaa` parent edges show its union with artifact hash validation, app baselines, virtual root and the other branch's checkpoint. The current merged contract retains explicit argv, owned run/session, provenance and hash validation; the intervening revert does not make unsafe shell interpolation authoritative.

`246f7e40` is exactly a global prefix replacement of `tui-next` with `junie-tui` and `tui_next` with `junie_tui`. This equality was checked against the complete original prefix. It also corrupts historical sentences into a rename from a name to itself; those sentences are not fresh architectural decisions. `b60c8280`'s substantive sequencing requirement survives: remove each root binary/default-run in the same commit that adds its application replacement, avoiding duplicate binary ownership. Current integration strategy must use actual manifests and frozen SHAs, not replay obsolete dates or temporary package names.

`aabe0b5f` is an archived unowned variant, inspected relative to both parents. It mixes legitimate name corrections with damaging prose substitutions (for example, saying `Dialog::facts` supersedes itself) and removes historical resolver-upgrade language. It does not outrank the accepted `15ecde1e`/`0f018368` reconciliation. `d4715f8e` replaces specific agent names with generic reviewer/builder roles; it changes no architecture or visual acceptance. `e94c1443` changes three app perf references from the 500×12 to the 500×14 TablePro frame; it does not change the separate generic 500×12 load/render benchmark obligations.

`c3b51b96`'s two parent edges preserve the accumulated runtime/theme/Grid amendments and add visual items 35–39: inherited empty-region styling, row-only hover, and merged Showcase/TablePro/Jackin/component baseline unions. The final `7b27732a` edge has the same prefix pair as the second merge parent. Historical permissions for hundreds of changed or new digest keys are not acceptance under PLANNING_GOAL. The exact immutable oracle controls every observable state, including added coverage, headings and shell spacing.

## Required joins

[The granular amendment ledger](history-early-amendments-obligations.tsv) supplies current authority, supersession and deterministic proof obligations. It supplements [early obligations](history-early-obligations.tsv), [middle obligations](history-middle-obligations.tsv) and [late obligations](history-late-obligations.tsv). The parent plan must join these rows to actual main implementation, component/app scenarios, and eventual task IDs. This report deliberately does not infer current implementation success from a historical test count, checkpoint, commit title, or baseline blessing.
