# Historical TASK-001 CI requirements

> Superseded. These old CI and host-qualification requirements are not
> current inputs.

Current Rust validation uses `cargo nextest`; current task validation uses only
latest standalone taskfmt `lint`/`verify` in isolated subagents. See
[`subagent-only-policy.md`](subagent-only-policy.md).
