# Actual style timing qualification re-audit

Author-owned preparation; independent re-review pending. This report does not
close TASK073, certify TASK067's five-percent product bound, or authorize a
production edit. Root owns frozen assets, manifest synchronization and acceptance.

## Architectural finding

TASK073's normative `trusted/style-timing-contract.md` requires actual entry
timing, complete invocation census, three-mode equality and a source-uninstrumented
whole Showcase frame. TASK067 consumes that same seam. A digest-only workload,
synthetic frame, candidate-supplied scalar, or source-only timing estimate can
pass a superficially plausible percentage without measuring style resolution.
The original proof hole is missing independent provenance at the numerator,
calibration and denominator boundaries—not merely a missing assertion.

Repair is a source-copy-only executable qualifier before TASK073 implementation:
the protected host compiles immutable pinned main, instruments its existing Ui
entry bodies in a disposable copy, runs its actual Showcase App with the existing
Harness, recomputes arithmetic from private observations, and challenges the
submitted external dispatcher over the existing protected FD ABI. No production
source, canonical AGENTS, project commit or oracle source is changed.

## Source and evidence ownership

Source: main `7b27732a8c3c131760ec3438f641cb3c11343a42`, exact existing
`evidence/main-source.tar.gz`. Original compiler/archive validation remains in
`architecture-bootstrap-main-driver.py`; transport invariants remain in
`runner-bootstrap-driver.py`. New files are restricted to:

- `evidence/style-timing-bootstrap-driver.py`
- `evidence/style-timing-bootstrap-probe.rs`
- `evidence/style-timing-bootstrap-frame.rs`
- `evidence/style-timing-bootstrap-protocol.md`
- This report.

All injected observation methods are in temporary source copies. They expose
App/ListPage durable state and actual style-cache contents after drawing; they
do not replace rendering or mutate semantic state. Probe storage is caller-owned
and testing-gated, inherited only through shorter Ui reborrows. The actual App
trait forwarding retains update, draw, keymap, minimum size, quit and Esc behavior.

## Verification ledger

First preliminary real positive (before later strengthening; **withdrawn as a
style-share measurement because its numerator census was incomplete**): complete4800cell
frame/cursor/state equality, allocation/byte/cache-count equality, actual
raw-minus-calibration upper ratio `0.03389023329992303`. This is historical
diagnostic evidence only; not the final artifact's result or product acceptance.

Independent reviewer found the missing reached leaf: pinned main
`collection/rowui.rs:658`, `CellUi::drop` calls `resolve::bind` after nonempty text.
At full width120, `app.rs:612–638` leaves a94-column page; Lists' compact layout
at `pages/lists.rs:432` calls `compact_file_row` and its META CellUi at151–174.
Timing only the eight Ui methods therefore omitted real final-cell binding.
Repair wraps only the exact existing bind expression, not Drop's paint/alignment
work. Status's direct bind (`components/status.rs:716`) and Grid's direct bind
(`components/grid.rs:1979`) are registered too; their reached/zero counts are
explicit. Closed Lists shell Status items are Plain with no tone (`app.rs:47–50`),
so its delta bind returns early. Unreached `dim_layer` color-binding paint paths
are guarded fail-closed, not charged wholesale as style work.

Reviewer also found that protocol qualification's deliberately expensive causal
positive needed a separate executable strict-performance branch. The repair adds
an actual over-budget source negative under strict acceptance; protocol-mode
causal qualification remains distinct from product certification.

Effective build configuration must not be inferred from the word `--release`.
Inherited Cargo/Rust overrides are removed, real RUSTC and release knobs fixed,
and actual compiler artifact profile plus source inventory recorded.

Corrected11-entry diagnostic subsequently passed actual compiler/fullFD replay:
12 RowUi binds reached, raw `[10953,11467,11365,12247,12075,11165,10494,11540,10910]`
ns, calibration `[6290,5830,6127,6288,6510,5256,6577,5205,5623]`ns,
corrected `[4663,5637,5238,5959,5565,5909,3917,6335,5287]`ns;
real source-uninstrumented denominator
`[164083,162000,160417,158417,159500,160541,166125,160625,163458]`ns.
Conservative upper ratio `0.03998939507754849`. This remains diagnostic evidence,
not TASK067 certification.

A later full preparation passed the actual ordinary protocol positive,
18 negative cases and both real cost-causality source fixtures, then the ordinary
real-clock **strict** positive exceeded0.05 and correctly failed. No successful
threshold is substituted for that failure. Protocol qualification cannot depend
on defective pinned main already meeting the future product threshold. The
initial correction kept actual Instant protocol recovery after every negative.
That was insufficient: a later fixed diagnostic series of12 fresh actual
Instant replays produced11 structurally valid measurements and one negative
raw-minus-calibration duration. A source-positive case cannot guarantee a
timing-positive sample on this host. The negative correction is `INVALID`, not
zero, and is not discarded or retried to obtain a pass.

The final structural separation uses deterministic-clock actual Rust
protocol/strict-arithmetic positive and rejection controls, with a fresh actual
Rust arithmetic recovery after every negative. Separately, three mandatory
actual Instant attestations retain every raw, calibration, corrected and
source-uninstrumented denominator sample. The protected host computes their
fresh quality: any negative correction means `INVALID`; otherwise upper>0.05
means `REJECT`; otherwise `PASS`. The external consumer must report that exact
acceptance outcome. An always-pass real expensive-resolution attestation is
also attacked. Neither a valid source nor a successful instrumentation qualifier
waives actual measurement quality or TASK067's product bound.
Real strict acceptance rejects injected clocks. The measurement schedule and
compiler policy were not tuned after the failure.

The source-replay representation is complete archive plus byte overrides,
explicit deletion lists and exhaustive reconstructed inventories for both
control and subject. Independent review verified original5029-file source-map
digest `f3ad683ff3aae86eb530c23ed0986a688bb891ec41c77e64e433ae2d69d8b881`.
This avoids creating/deleting5029duplicate source files for every isolated FD
replay; full reconstructed source is still compiled before dispatch.

Root explicitly authorized narrow TASK073 scope additions after independent
leaf review. Updated073 `verify.toml`, README and timing contract assign
`ui/paint.rs`, `components/status.rs`, `components/grid.rs` alongside existing
`collection/rowui.rs`; no component behavior repair is authorized there.

First complete compiler/execution preparation pass reached valid plus all14
negative classes with their intended causes. The subsequent external-adapter
startup failed while source files were still being edited. That run does not
qualify the dispatcher. A later full run includes cache-content equality,
normal/unwind restoration, actual cost-causality controls and feature-disabled
compilation. Final results and hashes must be appended only after execution.

## Exact synchronization required

TASK072: protected CHK012 and corresponding R001/AC010 bind the actual timing
qualifier. Canonical dispatch:
`python3 -B /task/trusted/style-timing-bootstrap/style-timing-bootstrap-driver.py --runner /work/tools/refactor-proof/bin/tc-proof`.

TASK070: require the same independently accepted qualifier receipt before
accepting capture/timing plumbing; it cannot use a TASK073 completion receipt as
the qualifier's prerequisite. TASK073 later implements the one production seam;
TASK067 later measures the restored product through that accepted seam.

Protected group must contain the four new evidence files plus existing actual
architecture/main bootstrap helpers, runner/host observer helpers and exact main
archive with all transitive imports resolving. Root copies and hashes these;
this author does not modify shared baseline or task acceptance source.

## Final execution and independent review

Final normal and optimized Python full dispatcher records will be appended
below. Required coverage: all14 actual negative classes plus expanded direct
leaf/strict controls, fresh arithmetic-clock actual Rust recoveries,
forged/zero/always-pass/typed-output external rejection, separately retained
actual Instant quality and resolver-versus-paint cost causality, feature-disabled
build and complete source-to-invocation census. Author does not close the
independent findings.

Pre-Cargo-repair executable/protocol inventory for the retained normal run:

| Path relative to repository | Lines | SHA-256 |
| --- | ---: | --- |
| `docs/refactoring-plan/evidence/style-timing-bootstrap-driver.py` |676|`b5dc42aa51310524c68a8f6c5decda325eeddd3423edd4a2c1e632b33015ab96`|
| `docs/refactoring-plan/evidence/style-timing-bootstrap-probe.rs` |75|`5a0465b5b224dbc32ec41a1112fbecab95c41ae6535ba9bd205683fbfb34fc23`|
| `docs/refactoring-plan/evidence/style-timing-bootstrap-frame.rs` |64|`b10c13a407b27a19cd53961638dfe98ffcbbc03f76e98ad79981ab5c856a6b96`|
| `docs/refactoring-plan/evidence/style-timing-bootstrap-protocol.md` |193|`b734d80bfc8b43798fd6c9c7fb0f5de99420c1a9f333adc2aba575d5b01bf2ce`|
| `refactoring-tasks/terminal-components/completion/073/README.md` |152|`f7658220c69b26a7cda6eb1eaa6497d5d391e5ea149ee7f222334ce9872ba07f`|
| `refactoring-tasks/terminal-components/completion/073/verify.toml` |36|`6ff7b7967ba32fa9cd1b38e08bd821015c41c74e0ae65fb1fb84f2ff823322c5`|
| `refactoring-tasks/terminal-components/completion/073/trusted/style-timing-contract.md` |23|`a48c04d0c75d81464c71965ca1a44e03e5f393add9c30d64b55f638c7dae6da9`|

Driver coverage includes source reconstruction/compiler identity, all11 leaf
boundaries, paired real execution, independent membership/interval/cache/frame
judgment, nineteen negative source variants, private FD authority and six
malicious external-consumer classes. Probe coverage includes disabled/calibration/
measured ownership, interval membership/depth, caller-owned storage and scoped
restoration. Frame coverage includes actual App/Lists, full4800cell semantics,
single-draw physical clock boundaries, separately labeled arithmetic ledger,
fixed12warmup/9batch/3mode schedule and untimed actual branch consumers. All
listed bytes were read; the inventory identifies author-reviewed preparation,
not independent acceptance.

Final normal command:
`rtk proxy python3 -B docs/refactoring-plan/evidence/style-timing-bootstrap-driver.py --self-test`
exited0. It prepared26 actual source/compiler fixtures, rejected19 negative
source cases covering14 required classes, executed34 positive arithmetic
controls and30 fresh recoveries, and rejected6 malicious external consumers.
The complete JSON receipt, including every pre-dispatch and dispatch Instant
sample, source inventory/binary identities and50 main execution event digests,
is `evidence/style-timing-bootstrap-normal-result.json`.

| Actual Instant dispatch | Quality | Conservative upper ratio |
| --- | --- | ---: |
| Ordinary Lists |PASS|0.043768265215583836|
| Expensive actual resolver |REJECT|1.0617298545370584|
| Paint-only actual writes |PASS|0.006670343813200956|
| Expensive resolver, always-pass external attack |REJECT|0.8152544462888309|

These observations do not erase earlier ordinary `REJECT` or negative-difference
`INVALID` outcomes. This final run followed structural clock-domain separation
and additional provenance attacks, not a retry policy seeking a favorable
number. Injected arithmetic results are in the explicitly nonperformance
`arithmetic_control_not_performance` field; they are not included in this table.
The final source census is355 calls:258 `style`,1 `style_inherited`,1
`paint_patch`,28 `style_patched`,55 `surface_style`,12 RowUi binds; the remaining
five registered entries are explicitly zero on this frame and independently
exercised by actual branch consumers. No TASK067 product certificate is issued.

Independent follow-up found a shared compiler-identity gap after this normal
run: the inherited main helper hashes the PATH Cargo launcher, which on this
host is a mutable MBX shell shim, instead of binding the resolved actual Cargo
toolchain executable. Actual RUSTC and artifact profile are bound, but that does
not close Cargo selection. Foundation owns the shared structural repair:
resolve/directly invoke actual Cargo and Rustc, then revalidate their identities
around compilation. The normal receipt above is retained as pre-repair
execution evidence, not final compiler-provenance acceptance. Timing must rerun
against the repaired shared helper before frozen qualification is accepted.

The corresponding optimized Python run was deliberately interrupted with
SIGINT/exit130 after preparing the omitted-Grid control, before dispatcher
qualification, to unblock shared repair. It is not a successful optimized run.
Timing now calls the shared `validate_compiler_identity(identity)` API before
and after every build and the feature-disabled check; execution awaits the
owner's repaired helper. A separate scalar hardening also makes injected
controls' `strict_performance_pass` always false; their arithmetic success uses
`strict_arithmetic_pass`. Thus even an extracted scalar cannot mislabel an
injected clock as physical performance evidence. These changes require new
final hashes and normal/optimized execution receipts.

## Compiler-bound final campaign

Shared compiler repair is now stable: `architecture-bootstrap-main-driver.py`
SHA `fd6e1f240cd09367b8414041302fa926d6d939502577d2ff8f3269c9cde1cd49`.
It resolves the selected rustup Cargo/Rustc executables, hashes their bytes,
binds verbose versions/toolchain, directly invokes them, and revalidates both
before/after compilation. Foundation independently exercised21 controls under
normal and optimized Python. Timing's same pre/post API calls include its
feature-disabled build; PATH launcher identity is no longer accepted.

Timing source stayed unchanged across the final two campaigns:

| Evidence file | SHA-256 |
| --- | --- |
| `style-timing-bootstrap-driver.py` |`eca42a9c12c249d670b949adce07eb7679955ea8da1e4d308df586d09e365ae1`|
| `style-timing-bootstrap-protocol.md` |`977f1656f0d4f08e979be3b1516954b648df18f2813df65859c7e8221fffb723`|
| `style-timing-bootstrap-probe.rs` |`5a0465b5b224dbc32ec41a1112fbecab95c41ae6535ba9bd205683fbfb34fc23`|
| `style-timing-bootstrap-frame.rs` |`b10c13a407b27a19cd53961638dfe98ffcbbc03f76e98ad79981ab5c856a6b96`|

Normal session29273 exited0:26 prepared fixtures,19 negatives/14 classes,
34 arithmetic positive controls,30 fresh recoveries,6 malicious consumers
rejected and50 main execution receipts. Every injected-positive result keeps
`strict_performance_pass=false`; arithmetic acceptance is a distinct scalar.
`evidence/style-timing-bootstrap-qualified-normal-summary.json` records the
counts and all7 physical sample sets with recomputable upper ratios. It is
explicitly a transcription summary, not the original full50-event JSON; the
complete original JSON remains in the execution transcript. Summary SHA:
`1ee846b862fcadf2ae1d12c38b3883f1ba8ffda924a45a6f04cd0baa284b8b76`.

Normal actual dispatch upper ratios: ordinary0.048425 PASS;
expensive0.7240464383596235 REJECT; paint-only0.0022167132266104317 PASS;
expensive always-pass attack0.7249811617596377 REJECT. All three pre-dispatch
measurements are retained too. Parent released a quiet execution slot after
its decode/build batches; no simultaneous root expensive workloads were
authorized during these fixed schedules. This coordination is not a controlled
benchmark-host certificate, does not erase earlier quality failures, and does
not certify TASK067's future completed product. No favorable-sample retry,
discard, threshold or fixed-schedule tuning occurred.

The unchanged optimized session74064 exited0 with the same26/19/34/30/6
coverage, using `python3 -O -B`. Its complete original JSON (all50 main
execution identities and all7 physical sample sets) is
`evidence/style-timing-bootstrap-qualified-optimized-result.json`, SHA
`f47b36b6afefd27a48e0a154b3ca50f96f724c1028497237207901f382309026`.
Actual dispatch upper ratios: ordinary0.03976797791230781 PASS;
expensive1.0755435983665627 REJECT; paint-only0.002416023564142491 PASS;
expensive always-pass attack0.9893152643566899 REJECT. All arithmetic-positive
strict-performance scalars remain false. The execution slot was released to
the independent reviewer and other qualifier owners after completion.
Independent rereview remains pending; author does not self-close findings.
