# Historical TASK-001 operator evidence draft

> Superseded. This draft is retained only as provenance and cannot supply
> current verifier-subagent evidence.

Current evidence must be produced by a verifier subagent with latest
standalone taskfmt `lint`/`verify`, then independently reviewed.
