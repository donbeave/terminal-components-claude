# Independent style timing bootstrap rereview

## Current boundary

This is a bounded review of the newly authored style-timing-bootstrap driver/probe/frame and the corresponding TASK-073/TASK-067 measurement contracts. Verify-and-stop applies. It does not certify restored Showcase parity, a final five-percent product result, all component attribution, or a general-purpose profiler. Final bounded compiler-bound execution passes below; earlier invalid checkpoints remain recorded as provenance.

## STR-01 — reporting a strict failure is not enforcing it

Initial `style-timing-bootstrap-driver.py` judge computed `strict_performance_pass = upper_ratio <= 0.05` but returned normally regardless of that value. Only the special injected straddling case rejected for threshold uncertainty. A real over-budget source could therefore be reported as an accepted observation with a false metric flag. TASK-067 `trusted/obligations.md:17` instead requires the protected strict judge to enforce the upper/lower bound.

The legitimate distinction is between instrumentation protocol qualification and final strict performance acceptance. A deliberately expensive source is useful as a positive causal timing witness but cannot become a strict performance pass. Requested repair: freeze separate protocol/strict acceptance profiles, apply an actual strict bound check, reject the real expensive-source measurement under the strict profile, and rerun a fresh valid recovery without relabeling protocol acceptance as product certification. The author acknowledged this repair; final bytes and execution will be checked below.

## STR-02 — Ui-only entry census omits an actual Lists binding

The initial eight boundaries were Ui::style/style_inherited/style_defaults/paint_patch/style_patched/resolve/surface_style/bg. Independent pinned-source search found a binding outside Ui in `7b27732a8c3c131760ec3438f641cb3c11343a42:crates/tui/src/collection/rowui.rs:658`: CellUi::drop directly calls resolve::bind after nonempty painted content. This is not a hypothetical future caller. `apps/showcase/src/pages/lists.rs:174` paints a META CellUi, selected by the compact file-list branch when body width is90..130 (`draw_files:432`). At the fixed120-column fixture, `app.rs:612–638` computes main width120-24-2=94 with inspector closed; `pages/mod.rs:99` preserves that width, so the compact branch is reached. A Ui-only timing sum omits that actual work even when its delta is neutral.

The same source census also found direct binding at `components/status.rs:716` and `components/grid.rs:1979`. Their exact applicability must be frozen: closed Lists shell StatusItems are plain with no tone (`apps/showcase/src/app.rs:47–50`), so Status's empty-delta early return matters; the Grid page is not the selected page. The author's separate dim_layer finding is also valid: `ui/paint.rs` binds backdrop/ladder/accent roles outside the eight entries, though the closed Lists fixture has no active layer. An unmeasured reached branch must fail closed rather than count as zero.

Root condition: a census of public Ui wrappers was treated as a census of all actual source binding work. Structural repair requires independently enumerated responsible leaf bindings and reached/unreachable source guards, with the same caller-owned seam propagated to those leaves. Timing the whole CellUi::drop would incorrectly include alignment/move/paint work; the interval must surround the existing binding expression. The author agreed to add exact leaf boundaries and a RowUi omission mutant, and explicitly withdrew the earlier3.389% ratio as having an incomplete numerator.

The repair must also respect production ownership: TASK-073 verify.toml initially permits ui/mod.rs and rowui.rs but not ui/paint.rs or components/status.rs/grid.rs. If the accepted production seam inserts guards into those additional files, their exact narrow instrumentation responsibility must be assigned to TASK-073 before dispatch, as its own timing contract requires. Alternatively, a genuinely unreachable path can retain an explicit frozen guard without claiming it was exercised. Disposable instrumentation permission alone does not grant future task write scope.

## STR-03 — inherited build overrides are not the recorded release policy

Initial build() inherits os.environ, then sets only CARGO_TARGET_DIR, CARGO_NET_OFFLINE and RUSTFLAGS. Its recorded build_policy says release/offline/locked and `--cap-lints=allow`, but does not bind potentially inherited CARGO_ENCODED_RUSTFLAGS, CARGO_PROFILE_RELEASE_* or compiler-wrapper settings. Those can change effective optimization/compiler execution while the receipt still describes the same declared policy. Requested repair: sanitize build-affecting overrides or explicitly capture and bind the effective environment/config before dispatch; add an override control proving the declared profile cannot silently diverge. This concerns measurement reproducibility, not a claim that the submitted candidate currently controls the protected compiler environment.

## Inspected measurement and isolation design

Read all initial driver517 lines, probe75 lines and frame63 lines, plus TASK-073's entire style-timing-contract.md and TASK-067's measurement clauses. Also read the imported archive/source verifier and the inherited runner's FD launch, snapshot and report-binding paths.

The design genuinely compiles pinned-main source copies in release/locked/offline mode and executes compiler-produced control/subject binaries. The control draw is the production Showcase Lists120x40 frame, not a synthetic time budget. Both binaries are warmed and the host admits alternating control/subject draws for each fixed disabled/calibration/measured slot. Runtime animation time is held by the real harness; timing uses std::time::Instant. Observation formatting occurs after the measured draw. Every cell's symbol/colors/modifiers plus cursor, focus/hover, App and Lists state, effects, capture, top layer and quit state are compared; cache contents and allocation/query counters are separately checked.

The probe is caller-owned, inherited through Ui reborrows and feature-gated. Independent invocation visits are outside timed intervals; measured guards include resolver work, while calibration clocks are adjacent and resolution still runs outside the empty interval. Depth/order checks reject wrapper/leaf overlap and missing timing intervals. Negative corrected values are not clamped. The fixed observed-range bound is conservative over the sampled batches but is not a statistical guarantee for every machine or future execution.

The inherited protected runner gives the submitted adapter only request-write and response-read descriptors, validates nonce/tree/source/operation and one-shot execution, checks immutable-input hashes, and checks exact observation digests/results. The actual subject subprocess receives only its own standard streams and no inherited observer descriptors. Its private binary is compiled before adapter dispatch; the candidate does not choose the command or return fabricated measurement rows as an observation. These source/isolation checks are preparation facts, not a replacement for running the final attack suite.

An independent `python3 -B` load of the pinned archive verifier succeeded: complete archive SHA-256 `732e40d9cf82e98e3d6375aaeecee349e8cb774abc5094dbbff145555201633a`, 5029 file entries, canonical sorted path-to-SHA256 map digest `f3ad683ff3aae86eb530c23ed0986a688bb891ec41c77e64e433ae2d69d8b881`. The author's planned packed source representation must reconstruct this entire base plus every explicit source override, not replace it with a reduced hand-selected source subset.

## Execution and final checkpoint

Stable repaired bytes independently reread: driver676 lines SHA-256 `b5dc42aa51310524c68a8f6c5decda325eeddd3423edd4a2c1e632b33015ab96`; probe75 `5a0465b5b224dbc32ec41a1112fbecab95c41ae6535ba9bd205683fbfb34fc23`; frame64 `b10c13a407b27a19cd53961638dfe98ffcbbc03f76e98ad79981ab5c856a6b96`; protocol193 `b734d80bfc8b43798fd6c9c7fb0f5de99420c1a9f333adc2aba575d5b01bf2ce`; TASK073 style contract `a48c04d0c75d81464c71965ca1a44e03e5f393add9c30d64b55f638c7dae6da9`.

STR-01 is structurally repaired. Fixed arithmetic clocks qualify deterministic controls while actual compiled Rust work still executes. Separate mandatory Instant ordinary/expensive-bind/paint attestations preserve all raw samples and derive fresh quality: negative correction INVALID, upper bound>5% REJECT, otherwise PASS. `StyleFixture.launch` computes the private fresh category, and validate requires the runner's matching rejection or exact event output. Neither a source-positive label nor an earlier preparation verdict predetermines a later timing pass. The strict profile refuses injected clocks; strict-arithmetic is explicitly not product performance. The always-pass real-attestation attack covers the former false-success mechanism. This meets the clarified qualification boundary without waiving TASK067's unchanged controlled-host bound.

STR-02 is structurally repaired. Repeated independent source search confirms the11 named entries plus unreachable dim_layer guard; `ui/layer_buf.rs:236` is a test-only bind, not an omitted production leaf. Added leaf guards time only existing bind expressions. Separate branch consumers cover otherwise-zero Status/Grid calls, while the live Lists census requires RowUi. TASK073 verify.toml now explicitly assigns ui/paint.rs and components/status.rs/grid.rs alongside ui/mod.rs/rowui.rs. No later task must create a second probe to reach those files.

STR-03's concrete environment override mechanism is repaired: all inherited CARGO_/RUST-prefixed variables removed, resolved RUSTC pinned, wrappers disabled, exact release settings supplied, actual artifact opt_level/debug_assertions/overflow_checks checked. The reused compiler identity helper identifies Cargo's executable path/hash (which may be a rustup proxy) and version, not a separately resolved Cargo implementation binary; actual rustc binary identity remains pinned. This is an attribution limitation, not evidence of a candidate-controlled compilation bypass.

Independent `/tmp/style-rereview-check.py` (SHA-256 `cdc2e66ad7b39f91172673afbbab927eb0edaf3e75da24afa01915b59dbb3fb1`) passed16 explicit-require checks under both `python3 -B` and `python3 -O -B`. Cases include exact5% acceptance, over-budget strict rejection, negative correction INVALID/rejection, injected-as-real strict refusal, threshold-straddling uncertainty, zero/forged denominator, cache alteration, constant numerator, missing interval, nested interval, changed full frame and inherited profile/encoded-flags/wrapper/toolchain override removal. These are independent arithmetic/build-environment checks; they do not pretend to replay production Rust or replace the full adversarial compiler/FD campaign.

Subsequent author/Foundation handoff confirms the Cargo executable is a mutable MBX shim, not a fully bound toolchain implementation. Compiler-helper repair plus pre/post identity verification is therefore a concrete remaining acceptance item, beyond STR-03's now-tested environment cleanup. The author's completed normal campaign is pre-repair diagnostic; its optimized campaign was stopped to repair this root condition. The earlier stable driver hash is a reviewed checkpoint, not the final freeze.

The preceding checkpoint is superseded by the final independent execution below. Only this rereview report and disposable diagnostics have been edited by this reviewer for the timing subtask.

## Final compiler-bound independent execution

Current bytes fully reread: driver SHA-256 `eca42a9c12c249d670b949adce07eb7679955ea8da1e4d308df586d09e365ae1`, protocol `977f1656f0d4f08e979be3b1516954b648df18f2813df65859c7e8221fffb723`; probe/frame hashes remain those above. Imported compiler helper `fd6e1f240cd09367b8414041302fa926d6d939502577d2ff8f3269c9cde1cd49` now resolves actual Cargo and rustc through one explicit rustup-selected toolchain, requires a shared binary directory, hashes executable bytes and verbose versions, and revalidates identity before/after each timing build. Timing compilation invokes this resolved Cargo, not the mutable PATH MBX shim. The earlier compiler-binding blocker is repaired; no generic compiler sandbox completeness claim follows.

`rtk proxy python3 -B /tmp/style-rereview-check.py` and `rtk proxy python3 -O -B /tmp/style-rereview-check.py` each pass17 checks. Current script SHA-256 `31f09bf93077e2133b1531535d42dc138f916fc0128bce4fc62342794005b86a`. Added explicit accepted-injected-control assertion: `strict_performance_pass` must be false and `strict_arithmetic_pass` true. Exact5%, over-budget, negative and uncertainty checks remain independent of the author's self-test runner.

`rtk proxy python3 -B /tmp/style-independent-actual.py` passes6 newly compiled source preparations and12 protected private-FD replays, including three attacks and three fresh recoveries. Script SHA-256 `0a2275c0faf6c0cc1e85701fa85bb523d0e345218bd72256d08266c9537cbdbf`; complete diagnostic result `/tmp/style-independent-actual-result.json`, SHA-256 `2e0436d80098d560a84f2a4da9443a3ff25a56c13b4727c264bcec943eb8f84a`. Root/Foundation held expensive work during this measurement window. No sample discarded or retry selected for a better result.

Cases: actual valid compiled Lists work under explicit arithmetic clocks; omitted live RowUi bind rejected; injected clock refused by strict product profile; fresh actual Instant ordinary/256-real-bind/64-real-paint attestations. Actual raw numerator increases with repeated resolver binds; paint-only raises the uninstrumented denominator without moving its resolver numerator into the expensive-bind range. Full-frame/state/cache equality is checked before timing quality, not substituted for it. Private attacks reject forged typed-bool observation bytes, always-pass omitted-RowUi source, and always-pass real expensive-source attestation, each followed by a fresh accepted arithmetic control.

| Fresh private attestation | Conservative upper ratio | Required outcome |
| --- | ---: | --- |
| Ordinary actual Instant | 0.05097457023589092 | REJECT |
| Repeated actual resolver binds | 0.7355566971942655 | REJECT |
| Repeated actual paint | 0.0026131795850420237 | PASS |
| Expensive-source always-pass attack | 0.7381350451885617 | forged acceptance rejected |

Ordinary source legitimately rejects above5%; the qualifier passes because its runner honestly returns that fresh protected rejection. This directly disproves the former source-valid-implies-performance-pass mechanism. Ordinary receipt digest `ddbf04f7958ad677c5a9a38c2cca0fd146623995be574793e3f02573518543c5`; expensive attack digest `292c98a6b9b2e763cff2298ad3647686ac67295d9b941d297f2f3258d2b71c1f`. The accepted arithmetic ratio0.01 is never a product timing receipt.

Bounded rereview conclusion: STR-01/02/03 and the later mutable-compiler-path blocker are structurally repaired and discriminated by these independent checks. The author separately reports complete normal/optimized26-preparation campaigns; those are not relabeled as reviewer executions. Remaining product obligations stay TASK067's controlled-host actual Instant≤5%, restored frame ownership and allocation ceilings, plus TASK073's eventual production-seam integration. This rereview does not claim every mutant was independently rerun or all-component parity.
