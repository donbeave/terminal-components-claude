# Branch-diff history review

Scope: all188 history-partition paths from `branch-diff-inventory.tsv`, Holla `2e2401393c47360741ebd321679de08982dca50a` against main `7b27732a8c3c131760ec3438f641cb3c11343a42`. Immutable product oracle remains separately `02f5294bfdbf38004cc49130d0aff1d01f31434c`. These revisions are not interchangeable: Holla adds PLANNING_GOAL.md after the oracle. No production/task payload changes or execution probes are authorized by this review.

`branch-diff-history-ledger.json` is the per-file source manifest. A deleted source remains historical evidence; deletion does not cancel an accepted requirement. An added historical PASS report remains a claim to inspect, not candidate execution proof. Current goals and explicit amendments control old execution, blessing, framework and live-OS proposals.

Initial coverage:60/188 files have a complete same-blob source read reused from `history-semantic-reaudit.md`: all33 F pages, all23 HP pages, the improvement-reference page and GOAL/GOAL2/REFACTORING_GOAL. These are A/D entries, so the entire changed side is the entire changed hunk content.128 files remain unread or not yet recorded as fully read. Partial architecture/STATE source-clause reads do not count as full-file branch coverage. All nonzero source-side blob identities were checked directly against Git.

The final clause audit remains independently useful: global196 source/disposition rows are complete, but that count cannot certify the188-path diff. F20's lost Holla/Jackin current/Later distinction is reported in the semantic audit. A129's nonexistent ledger locator was repaired in source/canonical only with coordinator authorization; no branch source was changed.

Final coverage:188/188 full files recorded;0 unread. The per-file ledger distinguishes direct bodies, exact-blob reuse, full-body-plus-all-delta reconstruction, combined complete range coverage, and lossless dictionary-normalized reads with exact reconstruction or full-record Git validation. All190 nonzero inventory-side Git blobs match the two pinned branch trees;the57 reused deleted improvement pages also match the separate immutable oracle. Truncated portions were not credited until independently reread. Source-disposition completion is not execution proof.

Source-derived dispositions from this batch:

- Superseded CONTINUE/RESUME/execution prompts correctly preserve navigation to the newer goal. Current PLANNING_GOAL still controls their old baseline and execution permissions.
- Consolidation part1–3 preserve exact rejected patch provenance. Rejected item-row threading, manager rewrite and Grid/progress WIP do not waive accepted capabilities or authorize moving new oracle digests; HIST:A129 retains015/034/051 owners.
- Main-Holla component report's bounded C1–C5 are leads, not current defects automatically: backend-free migration and later readiness/custom-cell decisions must be applied. Its syntactic booleans expressly do not prove capabilities;031/065/072 need actual registered painters and external compilers.
- The earlier REFACTORING_AUDIT_REPORT claims historical HTML/PNG exist; the later baseline report explicitly establishes they were absent and permits only labeled regeneration. Preserve the earlier report as historical evidence, reject that stale existence claim as authority for current proof.001/069/070 own independent current artifact qualification.
- History AUDIT/current-source-gaps have stale capture-disable and covered-FIELD interpretations.715ee077 and accepted EARLY-AMEND-044 qualify them; old reports cannot reopen a settled exception or erase its narrow limits. The history coverage-integrity report explicitly proves only its two-path graph, not whole repository or current task semantics.
- Holla domain/memory reviews preserve private reviewed-plan construction, consume-once approval, exact target facts and bounded Unicode alias/count safety. Their external tests and794b095 seek claims are not current full-app parity approval. Existing source-oracle differences and actual event schedules remain separately required by040–050.
- Initialization review explicitly leaves first-input geometry, pending publication and timing work outside its slice, and records six real pre-existing failures. Jackin CLI review preserves parser security but explicitly leaves intro Ctrl-C, startup drain, color and layout open.009/051/057/066/069 cannot treat these bounded ACCEPT headings as closure.
- Paint-provenance decision identifies ambient last-style authority and same-color collisions as the enabling class. Preserve value-bound per-channel provenance and all painter/continuation-cell transport;011/013/031/065 must not replace this with RGB equality or a token call counter.
- TablePro CLI review explicitly records q not quitting and direct Ctrl+Q bypassing pending/dirty guards outside its accepted parser slice.058/063/064 retain guarded source actions; restoring help text is not actual quit parity.
- Historical regeneration approval, full license/provenance, all five gate logs, gate manifest and mutation-results were read. The exact998 additions remain regenerated, not recovered originals; three visual sample pairs are not499 visual inspections. Preserve licenses and source hashes. No new installation or baseline blessing is authorized; the original installation inventory itself remains unread in this branch pass.
- Deleted scope/goal/O04/O05/O07 pages preserve the simulated-product boundary and unselected optional features. O07's old imperative to correct F18 does not override the same scope's explicit standalone F18 deferral or current ADJ-09. No new matrix importer, OS clipboard or enabled transcript is authorized.
- PLAN-FINDINGS and laneB/C statuses preserve historical accepted adapter sorting, explicit focus-only registration, product clock deltas and total RunId access. Old slice-order blockers and baseline-blessing instructions are superseded. PLAN-FINDINGS' arithmetic phrase “5.4×” relative to480000 is erroneous (1080602/480000≈2.25); the recorded exact1080602 measurement and unchanged<200 target control, not that explanatory ratio.
- Deleted tui-audit-editor retains real Unicode/source-index/find-paste and readonly eligibility fixes. Deleted ecosystem report's two-click acknowledgement account is superseded by53b8212f/e4866ce4's exact one-completed-click assertions; its competitor comparison explicitly does not authorize speculative components/frameworks.

Whole-partition source reading and disposition review is complete. Historical execution claims, render correctness and current implementation acceptance are not certified by this read-only review.

## Additional complete-source findings

The full main architecture blob is now covered: §§3–16,18–20,22–75/tail reuse exact-source reads documented in `history-semantic-reaudit.md:13,50,169`; this pass read complete gaps1–117,2294–3863 and4146–4949. The truncated4188–4205 interval was reread separately. Both DESIGN blobs were fully read/reconstructed in `history-source-continuation.md`; the complete exact branch diff was freshly read. Deleted oracle clauses remain authority for current product behavior despite their absence from main DESIGN.

- `docs/guides/quickstart.md` says `run` does not detect color capability, contradicting accepted architecture§34/§74. TASK009/068 must preserve automatic detection/ceiling and correct documentation; the stale guide is not a permission to duplicate detection in applications.
- Architecture§17 example11:3533–3570 keys Roster rows by display name, permits duplicate names, and deletes all matching names. Concrete counterexample: add two distinct people named Alex, select one, confirm removal; both disappear. Root enabling condition is treating a compiled illustrative example as identity proof. TASK068 must require duplicate-label/distinct-key behavior through actual exported library paths; preserve the old illustration as history, not a valid current dynamic-identity recipe.
- Architecture§17 example12 declares capabilities its body does not exercise and uses positional Segmented state; example13 uses application actions from `ActionKey::custom` despite the later disjoint application namespace. TASK030/031/065/068 must resolve current source-qualified APIs and exercise the full claimed author boundary, not copy obsolete illustrative signatures or count thirteen filenames.
- Architecture AppendixB:4309 inaccurately describes same-package binary/library targets as the same crate; they are distinct crate targets. The accepted workspace/facade boundary still stands; this old rationale does not establish private reachability. Actual independent external compilation and dependency edges own proof.
- API/K/M, late history and integration reports expressly contain stale accepted-count/gesture/bootstrap/ICON/covered-FIELD statements. Current canonical amendments and oracle dispositions control; no historical PASS, capture count or constructor sketch replaces actual protected execution.

Independent reader reuse is explicit, not a self-join: `reaudit-history-inputs.md:148–169` records full bodies for API/research/interaction/domain/performance/modern/M/N/O/P/Q; app/K/Slice2/Slice3 main bodies are reconstructed from fully read originals plus every exact later delta. The registry preserves differing main blob identities rather than falsely asserting equal bodies.

## Historical evidence identity and inventory limits

- Installation manifest `a24e784c109753b8a32b8d5a8db79523bda9ccb2` is now fully inspected through all499 normalized stems and full-record hash validation. All998 `.html`/`.png` outputs exist at pinned main and every SHA256 matches; none existed at declared source base `1318e854ae1e814fcf829fd8bd2e7f88377bd382`. All1716 original `baseline/before` entries at archive `c12cad8728755cd2d03eefdd8e02891143fca86d` retain identical Git blobs at source base and pinned main. This proves installation integrity, not visual correctness, original-render recovery, or oracle acceptance. Earlier unread-installation progress notes above are superseded only for read coverage.
- `docs/audit/main-holla/holla-path-disposition.tsv:19` exemplifies all965 proposed `evidence/holla-reference/shots/...` destinations absent at pinned main. Every1010 nondeleted source blob/SHA pair is valid and matches declared historical `794b095c196562d38f1b6f7ce379c128af2a023d`; source loss is not inferred. The table is an old proposed forward-port map, not proof that files were installed. Structural repair: TASK068/069 current evidence index must distinguish retained Git revision/path from an existing filesystem destination and verify either locator. Preserve original proposal text.
- `component-inventory.json` syntactic `borrowed_slice` booleans, duplicate Tabs `TAB`, empty Diff parts and builder booleans cannot certify author capability. `app-test-source-inventory.json` inventories only TablePro/Jackin; enum excerpts terminate at nested braces (including `Statement`), and bare names cannot bind assertion bodies/profiles. TASK007/008/031/065/071/072 require compiled/source-qualified complete inventories and real externally exercised capabilities. Root enabling condition: treating a lexical extraction as the same authority as executable semantics.
- Historical1483 named-obligation entries deliberately say semantic mapping is required; they mix normative APIs with rejected names, examples, shell commands and test tokens. Exact reconstruction confirms all records, not1483 implemented obligations. Current source-disposition owners must remain separate from token presence.
- Current README omits Holla in its three-app architecture/library sections, removes prior Diff CLI/Alt+D and newer Holla variants/workflow detail, and carries stale one-click/second-click and baseline instructions. TASK068 requires complete body reconciliation; existing owners017/032/040/051/058/063/064 supply behavior evidence. Secret-tail text is not disclosure approval; ADJ13 controls. The comparison is both exact README sides, not an excerpt-only audit.
- Deleted external Holla evidence/matrix are a separate roadmap: `fca7d0cc41e139014900e1876a4ab30f126cddda` plus local preview `215990ef2d682151bc0b16bd1697da3cbdbd7d9e`, not immutable prototype02f5294. All226 matrix rows were inspected, including216 exactly reused old-contract cells,10 changed cells, every source and all new-evidence/disposition/gap/target fields. No useful capability is silently rejected, but real scanners, OS processes/Trash, durable stores, package managers and `list/run/doctor` are not authorized additions to this simulated four-app refactor. Preserve HP01–23 research under its explicit separate scope.
- Complete STATE confirms historical main item39 baseline approval and refusal to rewind were consolidation-specific decisions. They do not override the current user's chosen source-only oracle. Its own repeated corrections show why declared test counts, guarded fixture substitutes, registry self-enumeration, named-test existence and first-failing-cell summaries cannot close parity. TASK070–073 independent discovery/execution authority remains essential; no historical PASS is reused as current execution proof.

## Final visual-ledger findings and coverage boundary

Pinned `docs/visual-changes.md` blob `eb980bbfb681ccf792a7cb9c9776daf9409aae01` was read completely: all3387 lines, including all prose and nonstandard records;1734 strict old/new digest rows were losslessly grouped into432 key/section groups with all36 axes read. Every strict original line reconstructs exactly, both digests are16 lower-hex characters and no listed pair is unchanged. Independent Git lookup against all3960 digest records in seven pinned-main baseline files finds1652 new pairs;82 older/superseded pairs do not match current main. This is full record inspection and identity checking, not rendering recomputation or visual approval. All594 item39,168 item37,64 item38 and512 strict item36 new pairs match current main; item36's remaining160 multiword-page rows were read directly in the prose stream.

- `docs/visual-changes.md:343–354` contradicts `REFACTORING_STATE.md:2088–2091` (blob `3d4f655c829952b0b4fa2130a03340ac65b72366`) about the tree-empty control: visual ledger says reverted-source still fails the new baseline; STATE says it passes the new baseline. Concrete counterexample: a new-baseline failure cannot, by itself, establish that old baseline was already stale. Root enabling condition: narrative experiment summaries omit exact source/baseline pair and accepted observer result identity. Preserve both original claims as disputed history; TASK001/069/070 must require immutable source/tree/baseline/run bindings before using a control to classify movement. This audit did not rerun either experiment.
- `docs/visual-changes.md:1126–1134` retains the280-moved scratch total, but its six rewritten final entries enumerate4+6+9+4+6+64=93 rows. Item36:1793 claims208 movements before its rewritten672-row list; item37:2500 declares truecolor-only despite84 truecolor+84 mono rows; item38:2692 similarly omits its32 mono rows; item39:2778–2784 claims mono/ANSI16/ANSI256 but its594 listed rows are54 truecolor+540 mono, with no ANSI16/256. These are stale scope descriptions, not proof that extra frames exist. Root enabling condition: freehand narrative totals survive a mechanically rewritten record set. TASK068/069 current reports must derive counts/axes from typed, source-qualified records and preserve original prose only as historical context.
- `docs/visual-changes.md:115–150` interleaves the1c Select and1d Loading headings/fences. A heading-only parser assigns Select's four pressed digests to Loading; a fence-only parser can omit Loading's fields. Root enabling condition: Markdown layout doubles as an authorization schema. Current classification inputs must use strict independently validated typed records; presentation prose must not grant baseline authority.
- Item19:653–660 expressly says first-generation digests pin drift, not approved appearance, with frame text covering glyphs but not recovered style. Item20:721–745's historical PASS inspects six after-frames and points to ephemeral `/tmp` evidence; exact22-key scope is not proof of excluded within-key cell changes. Item36:1799–1801's reference is794b095, not the user's02f5294 oracle. TASK002–006/031/065/069/070 must preserve full-cell/cursor, source-only reference and protected execution requirements; neither old hash membership nor source-matching current-main baselines can replace them.
- Deleted `docs/tui-audit-inventory.md` blob `33bca30616798b1db17f141cc5ef6756c4498d9d` was fully read, including all1573 rustdoc export rows and572 derived contracts by exact dictionary reconstruction. It explicitly withdraws its older849 lexical count. Rustdoc exports exclude private/test items and are not proof that every old storage shape or example API remains the accepted design. Preserve capabilities and source behavior through TASK007/008/031/065/071/072; source-qualified amendments govern owned-versus-borrowed representation.

No source files, baseline files, task payloads or historical fixtures were changed. Remaining execution/parity obligations stay with their canonical tasks and explicit pending adjudications; this report closes only the188-path source-review assignment.

## Independent bounded artifact-report rereview

Reviewed root's complete `branch-diff-artifacts.md` and259-line census implementation. A separate read-only checker independently matched all7019 unique endpoint/path/blob records against both pinned Git trees, recomputed all5399 unique Git object identities and every endpoint SHA256/byte count (218438713 total), rejected duplicate keys across all605 artifact JSON documents, and checked all1214 PNG IHDR dimensions without decoding pixels. No census implementation function was reused. This confirms integrity/count claims, not the separate original PNG-decoding execution claim.

Independently recomputed all499 recipe/source joins and2495 artifact locators;112 matrix and560 provenance hash/size bindings;302 old ANSI hash and302 fidelity geometry joins (one approximate/one missing-glyph sidecar, other reported categories zero). Verified all1214 five-format memberships and cursor triple syntax. ANSI control finals are exclusively SGR `m` in1209 nonempty-control records, so the report's SGR terminology matches this actual corpus despite its general CSI-stripping regex.

Recomputed exact ANSI/text617 and HTML/text1214 differences and both diagnostic17 nonpadding sets from original Git bytes. Every saved unified-diff record equals the independent reconstruction; all17 complete diffs were read. The changed spinner, progress, time and one-row log-window observations support BA-ART-01's atomic-frame warning. Padding normalization remains diagnostic, never parity approval. Checked dirty provenance exactly:112 main records at61b6f8f9;290 Holla records at92d916298 and12 ate4866ce41. The manifest's0cd1bf7 header versusd5e7075 body discrepancy is directly visible. No contradictory integrity or use-boundary finding remains in the bounded report review.

Limits: no PNG decode, font measurement, renderer run, recipe execution or manual1214-raster review. The three Markdown records were used for their relevant prose; the large499-row Markdown presentation was not independently reread in full here because its typed recipe/source relationships were independently checked and root owns its complete source review. This bounded rereview is not additional188-history coverage or a declaration that historical manifests satisfy current four-app oracle acceptance.
