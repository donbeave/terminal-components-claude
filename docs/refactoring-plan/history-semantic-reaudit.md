# Historical semantic re-audit progress

This is a source/disposition audit, not execution approval. Pinned main is `7b27732a8c3c131760ec3438f641cb3c11343a42`; product oracle is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. A source clause matching a canonical row does not prove its owning task's verifier works. Prior PASS reports, matching row counts and traceability joins are not substituted for source reads.

## Finite scope and remaining work

The canonical620 rows partition by original ledger: global196, early60, early-amendments54, inline13, API/app/research44, input audits66, late60, middle71, other56. Itemized source/disposition passes below cover middle71, late60, inline13 (with the explicitly adjudicated F10 correction), other56 and all global196:396 rows. The six earlier authority repairs and exact related rows are separately documented in `reaudit-history.md`; they do not certify unrelated clauses. Early114 and API/input110 have independent reviewers. No family is marked executable-complete. The newly assigned188-path branch-diff history pass is recorded separately and is not implied by these clause counts.

Historical blob/edge discovery and complete linked-source reads are in `history-source-continuation.md`, `history-revision-index.tsv` and `history-inputs-read-index.tsv`. Their counts are intentionally not a semantic completion metric.

## Middle71 source/disposition pass

Main `COMPONENT_ARCHITECTURE.md` §§30–55 was read completely, including both distinct §47 records and the §54 addendum. The current tail §§56–75 and unnumbered Meter/Scroll amendments was also read completely to check superseding authority. Earlier full LaneB/C, Q residuals and documentation-findings reads provide the independent linked input. Main `xtask/src/main.rs:7307–7335` was read directly for HM25; it still selects `3..=17 | 21..=u32::MAX`, confirming the live scope gap rather than merely repeating the historical report. `ecc13378:REFACTORING_STATE.md:1546–1558` directly says the fresh640-frame FAIL supersedes the earlier PASS. `ecc13378:REFACTORING_STATE.md:827–840` directly distinguishes the accepted configured Form bridge from future trait widening.

`docs/audit/legacy-test-disposition.md` was also read in full (818lines), including every historical test row and the closing caveats; both subsequent patches568e1ef6/9df371d5 were read fully and all three edges added to the linked read index. Its seven sections cover the247 partition,14 explicit gaps,76 library assertions,30 perf subjects,33 Showcase,41 TablePro and67 Jackin test identities. The document itself says many application assertion summaries were inferred from names and the18 same-name perf successors were not workload-rederived. Those admissions forbid treating this historical file as the approved current inventory/disposition register. Its grouped-integer `position_label` and removed `set_rows` workload destinations were explicitly inferred, so007/066/071 must resolve actual assertions/workloads; a deleted API name alone is not permission to discard their behavior. Current GAP rows retain these obligations rather than adopting the historical `DIES` labels as proof.

| Exact canonical rows | Source clauses and disposition checked | Owner proof still required |
|---|---|---|
| HM01–HM02 | §31 missing-family neutral versus sparse present family; §70 static fallback supersedes materialized rules. F5 broader sparse-family redesign remains unselected. | 011/031: neutral-backed custom family real resolution; defined-empty family does not acquire a new cascade level. |
| HM03–HM04 | §32 Clear blank/reserved width versus Inherit; shared reserved-pad brackets, Choice geometry explicitly separate. No global bracket rewrite from this history. | 015/018: adjacent cells/label rectangle and actual mono oracle Choice mapping. |
| HM05–HM09 | §33/§41/§45: exact owned PARTS, same-id composition, arbitrary row ownership, generated registry cases, no extra whitelist, no silent measure-to-paint escape. ebfa8b8 FIELD recorded-query exception and optional patch hook are not visible-paint proof. | 031/073: actual production resolution attribution, exact union, true conditional branches, query/paint separation and deliberate mutants. |
| HM10–HM12 | §34/§40/§74: pure constructors, terminal-only detection, pure decision-table precedence, runtime request ceiling; actual capture must execute requested effective capability. | 009/070: real PTY, contradictory environment and effective metadata; no flags-only evidence. |
| HM13–HM15 | §35 rejects Ui scroll convenience, requires scoped live suppression metadata and whole-set ASCII conversion. §71's partial conversion defer does not erase the conversion obligation; current ADJ ASCII table is separately selected. | 014/011/068/072: absent rejected API, every glyph/set field, stale/duplicate/satisfied/expired suppressions. |
| HM16–HM19 | §§36/49: generation is committed history; all cells/mismatches enumerated; immutable before-images; genuine parent/base; added/moved sets exact. Current oracle is stricter than historical visual-change permissions. | 008/066/069: independent reference bytes and full mismatch inventory; same-tree base must reject. |
| HM20–HM21 | §37: parsed structure, missing due subjects fail, every dependency root path, no self-naming TODO exemption. | 065/072: real-code mutants, including mixed legal/illegal paths; not comments or token matches. |
| HM22–HM24 | §§38/39/49 plus §§64/72: actual readiness/selection capability, real semantic setup, runtime-half forcing only, actual rule matching and invertible exemptions. | 010/031: conditional/default/empty branches, exact target, all status axes; never fabricate props. |
| HM25–HM27 | §§40/42: complete section scope, gate-table/dispatch/wrapper agreement and complete downstream lint outcomes. Main doc scanner's omitted18–20 range is independently confirmed. | 068/072/069: inserted bad references in every section shape, deleted dispatch/table/wrapper and complete successful all-target lint. |
| HM28–HM30 | §§41–43: exact13 named examples, executable assertions, specificity counterexample order, exact public signatures and testing-crate API coverage. Root/author facade rules are deliberate lists, not count or ident existence. | 068/072: deleted named example, private/wrong-arity return type, compiled external consumers, missing testing API. |
| HM31–HM33 | §§42–43: design/current architecture reconciled explicitly; independent preregistered rejection conditions; seed cascade remains unselected; literal colors allowed only in actual custom-theme authorship. | 011/068/069: untouched-token equality; application literal fails; separate architecture/visual reviewer. |
| HM34 | §44 counts247 historical tests by target and assertions, not only87 app names. Historical54/183/10 partition is not a current disposition register. | 007/066/071: full compiled inventory and assertion/workload mapping, including domain secrecy tests. |
| HM35–HM37 | §§44/47: plain Brand inert versus clickable branch; disjoint-slot hover tests need colliding sentinel patches; later§73 central disabled-pointer rules supersede local stripping convention. Focus plane invariants remain oracle-qualified. | 010/011/027/031: actual overlapping owner, capture transition, plain negative and foreground/background/modifier facts. |
| HM38–HM45 | §44 GAP4–12 and §51: HintBar painted ellipsis, Meter alternate modes/tones, StatusBar actual coordinates and keyed hover, Tabs reserved geometry. Keep-mask/hash inequality alone cannot prove these. | 020/028/029/053: painted-coordinate/role and actual runtime input proofs; all alternate painters. |
| HM46–HM47 | §§44.4/.6/47: dormant PERF_TARGET is not strict execution; deletion follows complete successors and preserved before-images. Qualified historical_revision repaired without erasing original GAP names. | 065–067: both100k workloads below64KiB, threshold perturbation, no duplicate source or removed unaccounted assertion. |
| HM48–HM51 | §45/§50: exact nonempty documented SlotFn set, busy ICON substitution versus inert single glyph, narrowly scoped same-id forwarding, clear_glyph convenience still optional. | 015/031/068/073: sentinel actual painter changes; child/custom row isolation; Set/Clear versus SlotFn distinction. |
| HM52–HM54 | Both§47 records and §48: completed historical atomic rename is not permission to recreate staging; bin multiset and due roots/manifests fail closed; real per-page update/draw reachability; diagnostic failures nonempty. | 032/065/072: missing/duplicate app, autodiscovered duplicate bin, dead/test-only page helper, actual failing tool diagnostic. |
| HM55–HM56 | §§30/50: Chip checked membership, payloadless AddRequested, exact six parts; Radio value caller-owned, existing cursor not externally retargeted. Architectural controlled-state separation does not authorize changing the oracle's arrow-selected product behavior. | 018/020: real-key collision, Add input, external value/reorder, actual app arrow adapter and frame proof. |
| HM57–HM59 | §52 and full LaneB: Grid requests versus adapter comparison, typed narrow readers, explicit focus-only registration. | 010/022/060: external API witness and real reordered model; no geometry cache/ordinary-zero-area exception. |
| HM60–HM61 | §53 strict match-plus-ancestors projection; runtime-owned structural index, viewport work and change-local scans. Main tree.rs:504–533/1045–1050 explicitly makes saturated generations non-reusable. R8 supplies generation-gap cache eviction. | 020/067: deterministic accessor counters, interior change with invalidate, filtered expansion restoration and cache-clear lineage. |
| HM62–HM67 | §54 addendum and full LaneC: once-only bootstrap/tick, exact deadlines, route delta, semantic dimming parity-qualified, pure fixed-buffer status and inactive fan-out. | 009–010/011/017/028/051/053–055: real runtime and domain transitions; no declaration or zero-allocation total substituting for status facts. |
| HM68 | Full LaneC and Slice7§7: all RunId producers/consumers total, visible token format must reconcile oracle. | 051/054/055: empty/nonASCII/one-char/current fixtures; no newly hashed visible token solely from the old proposal. |
| HM69 | §55 bareR and exactly-once body under anchored empty clip; §56 separate total two-slot SplitPane. | 023/027: malicious callback, zero/tiny rectangles, sentinel return, actual generic signature. |
| HM70–HM71 | ecc13378 state:1546–1558 superseding FAIL; state:827–840 configured choices accepted, wider FieldControl item channel still future; main§30 shorthand is not authority to widen. | 069 disposition;018/019 production bridge and original app behavior. |

### Findings versus already-owned gaps

No new canonical authority repair is required by this middle pass. HM25's scope defect remains real but is explicitly assigned to068/072; HM05–09/48–50 require the planned031/073 observation foundation and cannot be certified by current source logs. HM28–30 require actual external compilation and negative gates, not names. HM64/HM66/HM67 retain explicit app proof gaps; the selected tasks own them, but a task row is not their execution evidence. Most dangerous false closure would equate a completed source/disposition pass with any of those proofs.

## Late60 source/disposition pass

The current§§54–75 and Meter/Scroll tail were read completely. Special amendments were compared directly:30924105/aacb7cb3 detailed gutter/header ICON; c62ec8d3 actual Grid trait and Meter pin diff; d52ee38b→29ed8e5e authored Meter policy;4afca16f→4a9dcab1 last-projected capability eligibility;da4b1364 hidden scrollbar;efada044 historical-goal authority. Main ColorLevel declaration and narrow_to implementation were independently read at tokens.rs:525–540/638–650. The complete historical regeneration approval and independent installation review were read; neither approves current frames. Their exact manifest binding and absent-only scope agree with HL-75-ARCHIVE.

| Exact canonical rows | Source disposition checked | Owner/proof consequence |
|---|---|---|
| HL-54-CAUSE/DOMAIN/DIM/STATUS/FANOUT | §54 addendum supersedes abbreviated/proposed LaneC APIs. Pure fixed-buffer status, route timing and inactive message fan-out are mandatory; visual fade changes remain oracle-constrained. | 009–010/011/017/028/051/053–055; same middle-family runtime and actual app obligations, not duplicate evidence. |
| HL-56-TRAVERSE | §56 is the sole two-slot total container; shared clipped closure once, bareR, logical ordered rectangles, seam first. | 012 actual malicious-body/tiny/offset/sentinel proof; no public SplitAreas workaround. |
| HL-57-ENTRY/PARTS | §57 separates spatial entry from choice; shell owns focus; exact owner patch/slot sets and collapsed group separators. | 020/032 source-oracle navigation and owner-isolated RowUi; no arbitrary META propagation. |
| HL-58-LIFECYCLE/PARTS | §58 Skipped is readonly terminal lifecycle, not disabled; stable seed, physical movement, boundary no write; component META differs from optional row META. | 020/054 real lifecycle/actions, exact slots and ScrollRegion forwarding. |
| HL-59-CACHE/INVALIDATE | §59 exact cache stamps and forward frontier work; explicit invalidation for retry/interior changes, saturated revision recomputes. O(1) probes are allowed outside visible rows. | 020/067 actual accessor counters outside PERF_STRICT; no allocation-only complexity claim. |
| HL-60-MARKER | §60 chosen-key SELECTED in reserved cell, no new hit region or moved label; glyph Slot semantics differ from SlotFn. | 020 source-oracle marker and logical identity proofs; no fixture-owned default painter. |
| HL-61-SCHEMA/RAGGED/ALIGN/NUMBER | §61 three required methods, one columns schema, structural holes and nullable alignment; c62ec8d3 removes extra row_number and reads RowDecor.number. | 022/060 compiler witness, invalid-hook traps and sorted source-number output. |
| HL-62-SELECTION | §62 geometry-neutral explicit semantic swap/underline. Historical rejection of REVERSED is target architecture, not permission to normalize oracle backend modifiers. | 021/011 exact source-derived range cells plus accepted new semantic roles. |
| HL-63-FAMILY/KEYS | §63 named family34 and isolated recipe; historical64 first-generation keys do not authorize application changes. | 027/031 actual too-small source frames and cross-family override isolation. |
| HL-64-CAPS/GESTURE/CLIP | §64 separates collection identity/selection, actual semantic setup and clipping. Historical Grid gesture clause already superseded by the actual oracle same-current-cell click rule. | 022/031 preserve gesture metadata; source current-cell/different-cell trajectories, real selected/editing state and tiny clipped geometry. |
| HL-65-LOCAL/PARTS | §65 no whole-grid status; later detailed gutter and header ICON are real optional surfaces, not readiness revival. | 022 loaded data unchanged, local empty/error and optional/header slot branches; no stale exact-nine-parts gate. |
| HL-66-ISOLATION | §66 one allocator per measured executable; isolate codegen-dependent domains, unchanged1079 style ceiling. | 067 same workload/toolchain/profile measurement; no threshold reset after added benchmark. |
| HL-67-SEMANTICS/MOVE/FORM/SECRET | §67 actual semantic AsItem data drives filter/measure/key/insert; runtime Move distinct from capture Drag; private configured Form bridges and sealed String/Secret target. | 010/019/023–025 actual source handlers, inherited OR configured disabled, one mutable borrow, nonClone secret path. |
| HL-68-ID/MAP/PUBLISH/HINTS | §68 stable action identity, structural KeyMap snapshot, contiguous additive descriptors and common resolver; full hint cache key, stable reused storage. | 013/028 actual remap/removal/hidden conflict/owner transitions and warmed zero allocation; no raw-key fallback for declared commands. |
| HL-69-REVEAL/PRESS/PERF | §69 absent target must be revealed and proved present; exact pressed owner/part; actual80×24 borrowed picker38 AsItem reads/zero allocation. | 010/014/022/024/031/067; omitted target cannot skip, root press cannot color unrelated children. |
| HL-70-MONO/PROBE/FORMPERF | §70 static manifest once; caller-owned private work counters; exact prefix semantics; actual Runtime Form Tick+draw zero allocation. | 011/019/021/031/067 real production work observation, independent probes and untouched recipe vectors. |
| HL-71-DISCLOSURE/ASCII | §71 roles39/40 appended, marker at right−2, explicit Set/Clear wins, one popup chosen marker. Partial ASCII defer is historical, not abandonment. | 018/011 all glyph mappings under selected ADJ; actual closed/open geometry and flags. |
| HL-72-BOUNDARY/MATRIX/VISUAL | §72 supersedes local forcing; entire subtree inert, exact target/None; only four runtime bits, real props. Historical576/177/280/1280 counts are not frame approval; state FAIL controls historical closure. | 010/031/073 truthful runtime capture and root/child distinction;069 disposition, fresh oracle frames. |
| HL-73-HIT/EXCEPTIONS/CAPTURE/PROPS/INERT | §73 current source gaps separated from later implemented capture reconciliation. Private non-method Props constructor scope remains intramodule. No fallthrough, observable disabled Move/wheel exceptions and central inert sinks all retained. | 010/065/072 actual overlapping owner/capture-publication tests and AST receiver mutant, no cross-file helper mandate. |
| HL-74-COLOR/ORDER/CAPTURE/BASELINES | §74 requested color ceiling; existing public narrow_to solves stale ordering question without Ord. Historical96-cell capture expanded by current four-app manifest. Before-images stay immutable and app/perf evidence has independent provenance. | 009/070 capture execution/completeness;008/066–069 preserved history. No new color-order API or old capture count as completeness. |
| HL-75-ARCHIVE | §75 plus complete approval/review: exact499+499 absent-only reviewed generated bytes; existing empty blobs immutable; original binary unknown; not original images/current acceptance. | 008/069 narrow disposition only. Do not repeat the old installation or derive current expected output from it. |
| HL-METER-POLICY/PROJECTION/PINS | d52→29ed changes semantic-enum lift to ordered resolved-color authored policy, preserving typed provenance.4a9 replaces declared source with actual last projection. c62 changes seven integers despite unchanged-assertion prose. | 011/029 canonical before/after cells and typed73 slots;008 recorded pin conflict not silently reaccepted. |
| HL-SCROLL | da4 visibility false preserves wheel/reveal, returns full area, removes pointer chrome, releases active thumb; measure1×1. | 014 actual hide-mid-drag and stale bar-part input, NavList forwarding. |
| HL-GOAL | efada and current PLANNING_GOAL distinguish preserved architecture from obsolete execution order/visual-polish permissions. | All task authority; no completion from historical red disposition or accepted old frame edits. |

All60 late rows have a current source/disposition match; historical-count-only, optional-API and stale-implementation exceptions remain explicit. This is not execution qualification. Current primary ownership and available source-obligations were inspected for the affected gate/accounting rows;007 explicitly requires source-qualified assertions and actual complete execution,066 explicitly reconciles all3211 historical identities, and071 must independently enforce that product. These clauses address the historical names-only shortcut; the future accepted output cannot be inferred now.

Current full-family source/disposition passes therefore cover131 canonical rows (middle71+late60), plus only the earlier individually named authority repairs. Still pending: global196, early114, inline13, API/input110 and other56 (489 rows before subtracting explicitly itemized repairs). Actual owner-proof completeness remains a separate outstanding dimension for every family.

## Inline F1–F13 source/disposition pass

Read the original `27bd918e3a8a0a7fdba14fb10643139340d6281f:COMPONENT_ARCHITECTURE.md:1465–1505` completely and current§15.1 invariants, surrounding data/API/acceptance/rejection clauses. Read the actual e49de3f8/3f26ab14 security/lifecycle amendments and current Form submit implementation/tests, then019's complete finite source-witness table and inline obligation rows. The original thirteen invariants are preserved; later privacy, current-owner sensitivity, best-effort zeroization, configured choice commit and nontrapping Select qualifications apply.

| Exact row | Verified current meaning and proof owner |
|---|---|
| INLINE-F1 | Configuration-only props, private shared non-method constructor; per-phase borrowed data.019 plus065 Props gate; no captured screen/value in FieldKind. |
| INLINE-F2 | Visible eligible fields register in declaration order; runtime traverses.019 reordered fields/sections, no duplicated form traversal. |
| INLINE-F3 | Hidden no geometry/registration/measurement; eligible same-id/shape/sensitivity drafts retained.019 removal and sensitivity transitions clear before visibility filtering. |
| INLINE-F4 | Pure spec/design/width height, reveal from published geometry; first-frame absent area remains documented no-op.019 first-publication/resize proof cannot substitute draw-time mutation. |
| INLINE-F5 | Only Id/ActionKey payloads, no values bundle.019 actual all-kind actions; caller keeps owned secrets. |
| INLINE-F6 | Draw commits/cancels/validates nothing; FocusOut update applies declared control policy.019 repeated draw and exact callback counts. |
| INLINE-F7 | First declaration-ordered semantic action, all flow/invalidation folded, action-row last.019 aggregation and no lost later layout/paint. |
| INLINE-F8 | Select owns popover lifecycle; Esc inner before outer; current focus policy applies.019 nested source-real layer, no repaint/re-registration ordering hack. |
| INLINE-F9 | Form composes bare/Panel/Dialog; parent owns layer/trap/chrome, decorative background ordinary miss.019 same field engine and clipping. |
| INLINE-F10 | ADJ-12 clarifies total visible-field validation: commit once, validate all visible fields in order, store all safe errors, cross-validator only after all fields pass, first failure controls reveal/focus/Invalid. Current fail-fast implementation is incomplete; see original-source analysis below. |
| INLINE-F11 | Idle-only Enter under configured policy; editing key cannot accidentally submit; submit action declares chord.019/013 routing and hint share identity. |
| INLINE-F12 | Dirty on admitted commit or immediate choice/toggle, not draft text; later explicit oracle navigation policy can admit equal-key commit.019 repeat/cancel/mark-clean and callback order. |
| INLINE-F13 | Redacted Debug/actions, generic public state error; private render reads current owner sensitivity; owner clears cancel/dismiss and removed/changed drafts best-effort.019 current-data transitions, no Form layer lifecycle API. |

### H-F10 — finite witness does not distinguish validation short-circuit

`refactoring-tasks/terminal-components/completion/019/trusted/source-witnesses.md`, W-019-10, selects Avalid/Binvalid/Chidden. Main `crates/tui/src/components/form.rs:1183–1204` returns at the first visible field error; `validate_all` is reached only after that loop. Its trait comment confirms cross-validation runs after all per-field checks pass. The named test at2282 has one field, so its "every visible field" name is not multi-field proof.

Counterexample: a mutant continuing after B's error, or invoking cross-validation despite B's error, can preserve the existing A/B/hidden-C witness's visible first-error result while violating current callback semantics. The enabling condition is a first-error fixture with no later visible validator and an observation phrased only as order, not the exact permitted call sequence. This does **not** select a new collect-all-errors behavior from the original F10 wording.

Component owner repaired019's finite witness with first/middle/last invalid fields, three visible validators and a hidden fourth, plus cross-invalid and all-valid branches. This closes the missing distinguishing input, but the callback expectation itself needs the authority clarification below; canonical remains unchanged.

#### F10 authority qualification discovered during original-source reread

The original accepted ADJ-K is not an unambiguous mandate to stop later per-field callbacks. `27bd918e3a8a0a7fdba14fb10643139340d6281f:docs/reviews/adjudication-k-form-grid.md:148` explicitly says cross-validation runs after every per-field check passes. Its F10 at214 (also original architecture1480) says every visible field is validated in declaration order, followed by the first-error result. That establishes cross-validation's success precondition, but leaves a tension over later per-field callbacks after a failure. The cited product source at the same revision, `src/bin/tablepro/connections.rs:246–250`, executes `let a = self.name.validate(); let b = self.port.validate(); a && b`: both callbacks run, even when the name fails. Lines704–711 subsequently focus the first error. The immutable oracle retains exactly that two-validator implementation at the same source location. Therefore `a && b` cannot be cited as short-circuit callback execution: its operands are already evaluated.

The first modern Form implementation, `a1759b2a21adeac08222f2438243033fabd77512:crates/tui/src/components/form.rs:878–904`, returns immediately from the per-field loop. Current main preserves it. That is implementation continuity, not a discovered accepted amendment of F10. A history search for the original F10 phrase finds27bd918e and87ab93d4; the latter changes the Form data/options channel, not an explicit validation-loop decision. No later explicit fail-fast adjudication was established in this reread.

Parent accepted ADJ-12 after this source review: validate every visible field in declaration order, store every per-field failure through existing sensitivity-safe storage, then reveal/focus/emit Invalid for the first failure; cross-validation runs only if no per-field failure occurred. No public validation policy is added. The oracle paints name and port separately at `connections.rs:1145–1153`; its tab error at1131 reflects either error. `TextInput::validate:177–185` stores each error and its renderer consumes error state at295/455. Modern Form already has per-id `set_error_with_sensitivity` storage and private error resolution at `form.rs:566/1336–1339`, so this is a narrow correction using existing machinery, not a new public error aggregation framework. Canonical A82 and INLINE-F10 now cite ADJ-12 and preserve their original historical summaries. The distinguishing witness must reject current fail-fast behavior, not bless it. Parent owns task019/058 and authority-ledger synchronization.

Parallel allocation: component-B reviewer owns all110 API/input rows; component-A reviewer is assigned early114 after its terminal review. Historian continues global196 and other56. Together with middle71/late60/inline13,144 canonical rows have an itemized source/disposition pass; no whole-plan or executable completion is claimed.

## Other56 source/disposition pass

Read all1692 lines of pinned-main `REFACTORING_GOAL.md`, then the complete original-to-main diff and each of its five individual post-creation patches. Exact creation is `e48137f1852ea8f1b2885478a3f3312749443831` (1683lines); subsequent revisions are `dce91d51c848cf07eaad4058296775e644940bd5`, `2d81eec4358e88ef3a37c52466d612ba9e493238`, `25ea92b08f41838896af58ab062834cab606ba9b`, `efada04419f72ecada75cfffb4b1bc6014a05ba2`, `d4715f8e453fa8078a7a51dc73c63931bd53af03`. Main blob is `859bf16e` (abbreviation from Git diff, not a newly computed full hash). Thus removed clauses and intermediate routing instructions were read, not inferred from the final text. Every RG01–RG56 row was read against its own source sections.

| Exact rows | Source disposition and current proof consequence |
|---|---|
| RG01–RG08 | §§1/15/23G require all customization layers and genuinely external authoring. Later accepted recipe/typed-token authority controls implementation: optional hooks and FIELD query-only descriptors are not universal painted overrides.011/031/068/073 need real global/variant/scope/instance state isolation and public downstream compilation. |
| RG09–RG13 | §§2/5/9/21 require one public system and application domain ownership, prohibit live database/provider/process integration and superficial universal Widget/builders-only redesign.065/068 require whole-source boundaries and real app consumers; historical framework research is not permission for a second facade. |
| RG14–RG20 | §§10–12 require phase borrowing, typed modes, seven distinct state owners, pure draw and stable identity.016/019/020/022/065 need real non-Clone consumers and post-mutation behavior. Generic no-panic/stable-identity intent cannot silently redefine an oracle application's reachable defect; deferred F03/F06 need explicit authority treatment below. |
| RG21–RG26 | §§13–14 require runtime-owned focus/pointer/scroll/layer lifecycle with meaningful key/mouse equivalence and arbitrary bodies. Later nontrapping Popover and exact source product exceptions qualify the earlier broad overlay examples.009/010/013/014/017/023/028 need actual ownership and nested transitions; F01 paste proposal does not create universal modal-first behavior. |
| RG27–RG32 | §§15–17 require semantic themes, explicit patch/reset order, meaningful parts, total geometry and safe empty regions. Later authored Meter provenance and static mono fallback govern.011/012/031 cannot substitute a changed app layout or corrected historical stray pixel for oracle equality. |
| RG33–RG40 | §§18–20 require borrowed/custom collections, cursor/value separation, bounded work, generic Grid/domain adapter boundary, shared editing, secrecy and ergonomic composition.019/020–026/060/067 need component proofs plus exact app behavior; F04/F18 are not authority to relabel or enable a source-disabled app fixture. |
| RG41–RG45 | §§21–24 preserve runnable names, deliberate package/API docs and all-component demonstrations. Later accepted2024/MSRV1.88 and backend-free split supersede the original preserve-edition presumption. New custom/semantic fixtures must not manufacture app-oracle states or rewrite existing pages under RG42's broad demonstration goal.032/068/073 own separate public fixture/API coverage and original app journeys. |
| RG46–RG50 | §§25–26 require actual conformance, source/compiler boundaries, complete app tests, measured performance, strongest applicable quality gates and justified warnings. Later no-unsafe library/app rule is stronger than the original 'unavoidable unsafe' caveat.031/065–072 must prove failures with genuine mutants and qualified full accounting; names, counts and preserved old PASS logs are insufficient. |
| RG51–RG56 | §§27–30 require independent reviews and truthful complete evidence. Fixed old model routing and immediate eight-slice implementation are superseded execution instructions; old defect-fix/bless permissions are superseded by immutable parity. The research-tool changes25ea do not delete architectural acceptance obligations.069 needs exact candidate-bound integrated closure, not historical signed commits or a matching ledger. |

Other56 source/disposition pass is complete. Owner proofs remain future execution obligations, and the global deferred-product findings below remain open until their specific source dispositions are repaired. Combined source/disposition coverage is now200 itemized rows (71+60+13+56), not620 and not executable completion.

## Global deferred-product authority findings (ongoing196-row pass)

### H-F08a / H-F09 — deferred fixes became current parity requirements

`historical-obligations.tsv:139/143` and canonical equivalents retained `later_open` while task060/056 source-obligations made proposed corrected behavior the primary CHK-004 comparison requirement. The exact immutable documents `02f5294b:docs/improvements/f08a-between-upper-value-paste.md` and `f09-deliver-inspect-copy-without-closing-the-modal.md` were read fully; both explicitly say Deferred and pending implementation. Actual oracle `src/bin/tablepro/app.rs:237–246` handles only `FilterEditor.value` paste and otherwise consumes. Actual oracle `src/bin/jackin_preview/screens/inspect.rs:250/343` discards the DiffView event in compact-open and advanced Diff routing. Those source paths confirm the historical defects remain, without relying on the old reports as execution proof.

Credible counterexamples: routing paste to upper `value2`, or delivering selected Inspect text to preview clipboard, would satisfy the old task requirement while changing oracle state/output. Root enabling condition is promoting a deferred proposal's desired acceptance text into a primary parity requirement without an explicit current authority disposition. Structural repair: preserve historical text but mark F08a/F09 `deferred_not_selected_oracle_preserved`, bind actual unchanged owner/state/clipboard behavior, and prohibit inferred fixes or new effect APIs. Authorized canonical/source changes are complete; root owns downstream synchronization. This does not suppress valid paste/copy in other source owners.

Further deferred rows under source review: F03 Settings range-anchor panic, F06 local-sort retargeting, F07 malformed direct-API ingestion without proven app producer, F08c modified document-selection keys, F16 Showcase unreachable sections, F17 stray zero-area headers and F18 misleading disabled/read-only fixture. Their complete immutable proposal files were read; unlike F08a/F09, their exact current source/task authority resolution is not yet certified. Accepted modern safety/identity APIs must not be weakened merely to reproduce an old implementation defect, while successful post-defect application continuation must not be called oracle parity without evidence and authority.

### F03 actual production-page reproduction; disposition still pending

Compiled `/tmp/tc-history-f03.2Y42qp/main.rs` against the existing oracle library and unchanged oracle `SettingsPage`, using the original Page/PageCtx definitions with unrelated module declarations omitted. The observer sends normal page key events: focus Environment; Down three times; Shift+Down; focus Remove selected; Enter; refocus Environment; Shift+Up. No Settings/ListBox state, range anchor, removal method or algorithm was replaced. Actual Settings emits `Status("Removed 2 variables")`; the final event panics at `src/widgets/list.rs:99:31`, `index out of bounds: the len is 3 but the index is 3`. The observer catches that panic to distinguish it from a missing execution path. This is a production-page/input reproduction, **not** a full-App/PTY routing or frame proof; focus is supplied through normal PageCtx.

Checked actual compiled page source against Git: Settings blob `8123f7f319a13c8e5a3ecbc2d19586c61ef684d2`, List source blob `44cbbab4fdd3a04f67a2712f2be241264a966ee1`, both equal immutable02f5294b. Compiler `rustc1.98.0 (88d9e12ae 2026-08-18)`. Probe source SHA256 `5576f6a288794b306d8755b787f2093ee97973de8e538c1895a68455b7f5d270`; executable `da8aba525468ef6d1e3e35a9bfff8649fc254ff63eace12a95823c10d0bc9a24`; linked library `43236589e8cda7a2d7cabf74c974bad6c9e85767bebdd4bcb0c87b1f6810a1dc`. Existing library build provenance is not elevated into frozen baseline authority; this diagnostic confirms the exact source failure already visible in unchanged code.

Source enabling condition: Settings `remove_selected_vars:577–590` changes items/checks/cursor independently but cannot reset ListBox's private anchor. ListBox `move_cursor:93–102` clamps the new cursor only, then indexes an inclusive interval reaching the stale anchor. Settings `handle:362–375` forwards focused list keys and removal button activation to those methods. HIST:F03 currently asks for successful delete-then-ShiftUp; SC-SETTINGS-ENV chooses other nonpanicking removal paths and does not cover this trajectory. A postpanic frame cannot be manufactured from the oracle. Parent notified to decide explicit failure/disposition scope while retaining safe accepted modern identity storage; no canonical F03 change yet.

### F06 / F08c / F16–F18 exact-source checks

* F06: oracle `grid.rs:1178–1188` changes `order` through local sorting without remapping cursor/range anchor. TablePro `tabs.rs:2074` enables local sorting for actual query result grids. Thus the historical target-change defect is not merely a new malformed-model API. The task's stable-key mechanism and actual application post-sort cursor must be separately reconciled; the deferred effective-pending-value sort proposal is not selected automatically.
* F08c: oracle `field_common.rs:78–79` routes Ctrl+Home/End with `false` even when Shift is present. Actual Showcase TextAreas `pages/textareas.rs:90–99` forwards the key to its focused TextArea, whose editing path uses this shared translator. Consequently an all-modifier 'extends selection' requirement would change a reachable oracle edit path. This is not evidence to weaken ordinary Shift+Home/End or Ctrl+Shift+Left/Right, whose separate translator branches preserve Shift.
* F16: oracle TextAreas `pages/textareas.rs:64–82` allocates fixed13/1/10 rows and paints the second card through the resulting clipped allocation. The current SC-TEXTAREA-EDIT scenario already explicitly checks absent lower controls at narrow size. Adding section switching/reveal under historical F16 would change the product.
* F17: oracle Buttons `pages/buttons.rs:151–159` writes matrix headers with width checks but no height check, while the following row loop checks bottom. Modern total component clipping is accepted independently; its existence cannot prove the oracle's outside-allocation header never appeared.
* F18: oracle TextAreas `pages/textareas.rs:37–40` labels a `.disabled(true)` TextArea 'Read-only transcript'. Changing its label/control or making it navigable is a deferred proposal, not immutable parity. Current SC-TEXTAREA-EDIT correctly preserves inert behavior.

These are source-derived findings, not new baseline captures. Parent owns the authority decision; affected canonical rows remain unchanged until explicitly authorized.

## Global architecture anchors A01–A130: source/disposition pass

All130 global A rows have now been compared with their named substantive source clauses, not merely with a task join. Pinned-main architecture §§3–16,18–20,22–29 and the previously fully read §§30–75/tail supply the source; truncated output segments were reread before counting them. The thirteen-example/API review remains the separate middle-family proof obligation, not an assertion that those examples execute. Full current GOAL.md (380lines), full db5c41a6:GOAL2.md (376lines), main STATE2230–2283 and the relevant rejected rows/appendix435–480 of `docs/audit/consolidation/disposition-ledger.md` were read directly. The full original REFACTORING_GOAL and amendments are already recorded above. Backend supersession was checked directly in `4e84b744f9b3a1e007aa9080e7ab924ed8ca7246:crates/tui/Cargo.toml` and the complete164-line `xtask/src/backend_free.rs`: optional terminal dependency and isolated one-member consumers are concrete, not a commit-title inference.

This is130 additional itemized source/disposition reviews, making330 in this historian's itemized report (200+130). It does not establish330 executable passes. The66 global F/HP/O/P rows remain in progress below; independently reviewed early114 and input/API110 are not credited as this reader's work.

| Exact rows | Source interpretation and required owner proof |
|---|---|
| A01–A05 | §§3–6/25: borrowed two-phase configuration, caller-owned semantic state, derived caches only, orthogonal flow/invalidation/action and unit-only fold.010/031/065/067 need actual shared-borrow compilation, repeated production draw and callback/interior-mutation detection, not signature-only purity. |
| A06–A07 | §§7/25: keyed domain identity, structural hash equality/order, duplicate diagnostic rather than mathematical collision prevention.010/015/020 need same-target reorder/remove/insert and debug-label-independent comparison. Index-only app behavior requires separately explicit oracle adapters. |
| A08–A16 | §§8/25/73: one published registry owns focus, hit order, capture and wheel; top disabled pointer absorbs, hover never focuses, boundary wheel consumes without repaint.715ee077 is implemented capture invalidation, not an unresolved proposed API.010/014 must distinguish removal/disabled/decorative/empty/layer blocking and retain physical-input-once/focus-settle diagnostics. |
| A17–A21 | §§9/26/29.8: editor-first Esc, lifecycle-owned barriers, modal traps, nontrapping popover focus-out dismissal without restoring opener. Fill is not Fixed zero; only size/anchor mutate while open.012/017/018 need real lifecycle and same-frame resolver changes. Old universal modal-first paste is not implied; ADJ-09 controls actual app exceptions. |
| A22–A30 | §§5/10/22/25/26/55/56: uncached side-effect-free measurement shares accumulation, full geometry agreement, empty leaves versus once-only containers, bareR, SplitPane exception, explicit Auto/flex, bounded RowUi columns, one grapheme/cell writer and base-style spans.012/013/015/023/027 must prove actual offset/tiny/wide-cell output and allocation bounds; no silent clipping of required oracle content. |
| A31–A40 | §§11/25/27/31/70 plus Meter amendments: stable family/variant state merge, all override levels, clear/reset symmetry, semantic surface roles, categorical ANSI16, authored palette/provenance and two-way generation-stamped memo.011/029/031/067 need colliding role sentinels, all73 slots, generation wrap, measured no-cache measurement and source-matched mono geometry. A38 provenance correction belongs to component-B reviewer; old gutter/header commits are not palette amendments. |
| A41–A47 | §§28–29/33/38–39/50/72–73: reachable semantic readiness, fixed runtime-only forcing, exact owner PARTS and inert reference boundary. FIELD's recorded-resolution exception is retained. Choice's old unresolved in-run bracket now needs explicit ADJ-13, not invented generic geometry. Configured Props enforcement is intramodule, not a cross-file call graph.010/015/018/031/065/072/073 own distinct compiler/production-painter/mutation proofs. |
| A48–A51 | §§13/68: stable ActionKey remaps, component Tab before traversal, shared routing/hint resolver and structural cache revisions; every public component has all15 documentation sections.010/028/030/067/068 require actual routing/hint pairs and compiled public examples, not enum/string presence. |
| A52–A60 | §§14/18–19/23/52/61/64–65: all31 original widgets/all23 app controls have intentional dispositions; no legacy facade; Grid mechanics generic, domain ordering/effects in adapter; readonly shared model and mutable editable entry points share navigation; schema holes omit invalid hooks; readiness local.022/060/065/066 need finite per-item ownership and readonly actions/reasons. A58's readonly current-cell Activated correction belongs to B; old single-click-Moved blanket is not current oracle authority. |
| A61–A69 | §§50–53/57–60: keyed derived Tree index/filter/reveal, reserved marker, controlled Chip/Radio value, keyed Status hover, NavList EnterContent separation, inspectable Skipped Steps and incremental frontier.018/020/028/030/032/067 need real identity and source interaction; bounded allocation alone does not prove bounded accessor work. |
| A70–A79 | §§62–63/67/69–71: semantic AsItem independent of renderer, Completion insertion/source grapheme indices, enabled Move-only menu hover, nonempty reveal before identity click, exact pressed PartRef, authoritative Select glyph Set/Clear, geometry-neutral viewport selection, exact wrapped prefix and caller-owned probes, TooSmall hierarchy.010/014/018/021–024/027/031/067 must reject unobserved/no-op fixture hooks. |
| A80–A87 | §§15/23–25/67: caller-owned Form data, hidden retained drafts versus shape/sensitivity erasure, total validation per ADJ-12, single mutable option/value borrow, disabled before borrow, sealed String/Secret target and shared lifecycle.016/018/019/025/043/052 need secret-sensitive real state, no implicit Clone/serialization or guaranteed-erasure claim. Historical fail-fast implementation is not F10 authority. |
| A88–A96 | AppendixB/§§22–25 and4e84b744: curated public facade, unpublished app lib/bin, backend-free isolated core/testing, Rust2024/MSRV1.88, unsafe prohibition, complete dependency paths, semver only after baseline, terminal reverse restoration and exhaustive input normalization.009/065/068/072 require actual foreign-signature/feature-graph/external compiler and terminal proof. Old nonoptional backend and own-KeyCode rejection are superseded, not active contradictions to restore. |
| A97–A105 | §§54/68/73–74 and explicit publication/typing/feedback amendments: exactly-once Bootstrap, persistent deadlines, product-owned simulation delta, fixed-buffer status, inactive fanout, role-based dimming, successful-presentation authority, explicit typing owner, color ceiling and hidden-scrollbar interaction.009/010/011/014/017/028/051/053–055 need actual state/time/source trajectory; captures or self-reported clocks do not prove it. |
| A106–A115 | §§16/36–49/74–75: complete named tests/conformance, real semantic fixture setup, whole-source architecture/document scope, real per-page roots, full matrix provenance and immutable before artifacts.007/008/031/032/065–073 must distinguish capture completeness, owned parity and overall closure. Old 998-artifact installation authorization is manifest-specific, not permission to replace any current reference. |
| A116–A126 | §§16.6/20.9/25/27/53/66/69–70: actual Showcase style share≤5%, exact app allocation/load thresholds, normal versus ZWJ cell storage, one-intent delta480, bounded collection/accessor work, non-Clone Picker19rows/38calls, warm versus dirty viewport, FormTick+draw and isolated allocator binaries.010/013/019–021/024/060/067 own actual source workloads and non-self-reported measurements. A121's outdated two-intent summary remains a reported source-disposition defect, despite correct supplemental/task067 prose. |
| A127–A130 | GOAL parity generation, STATE2230–2283, GOAL2 consolidation and REFACTORING_GOAL§§27–30: restore old composition through current architecture; inherited60exact/379mismatch/60runfail is unresolved evidence; rejected stale patches do not waive capabilities; integrated independent review required.032/034/039–040/050–051/057–058/064/066/069 own current closure. Historical permissions to bless/add/fix products are superseded by current PLANNING_GOAL; consolidation cleanup commands have no present authority. |

### New global findings from this pass

* **A121, source-disposition drift.** `historical-obligations.tsv:122` calls the nonempty workload “two-intent” while accepted main architecture§27.4(b) explicitly corrects it to one intent and `probes(500)-probes(20)==480` in one update pass. `completion/067/trusted/obligations.md:33` and HI-059 already retain the corrected delta, but both010/067 primary HIST source obligations still repeat the obsolete summary. Counterexample: two enqueues or an absolute500 assertion can satisfy that summary while violating the accepted construction-cumulative workload. Root enabling condition: amended authority label without its amended current meaning. Repair current disposition/proof to one intent, exact480, zero empty probes/allocations, strict normalized ratio≤1.25; preserve original historical quotation separately.
* **A129, stale nonexistent locator.** `historical-obligations.tsv:130` / canonical historical_revision still cites `docs/consolidation/ledger.md`; that path is absent at pinned main and has no `git log --all -- <path>` history. Correct source_document already names `docs/audit/consolidation/disposition-ledger.md`. Relevant rejected rows are166–167,175,254–255,259; appendix468/476–477 explains item-row, manager, Grid and progress distinctions. Counterexample: an executor follows the stale revision locator, finds no source and substitutes the short canonical summary for the actual rejection rationale. Repair pinned source path/line qualification. No new behavior decision is required.

Both findings were sent to the coordinator. No canonical/task payload was edited during this read-only pass. F03/full-App deferred-behavior probes remain assigned to the coordinator; the earlier page-only diagnostic above is not upgraded into that evidence.

## Remaining global F/HP/O/P clauses: completed source/disposition pass

All66 remaining global rows have now been read against their substantive source documents. This completes this reader's global196 source/disposition pass, not its executable proof or resolution of every finding. Together with middle71, late60, inline13 and other56, this report itemizes396 source/disposition reviews. Early114 and HI/HAR110 are independently assigned and are not credited to this reader.

The complete immutable `02f5294b:docs/improvements/f*.md` corpus (33 files), `hp01` through `hp23` (23 files), and the209-line `docs/improvements-plan-reference.md` were read. Earlier recorded full tracker/scope/shared-contract reads remain separate evidence. The HP source pages include their complete acceptance, evidence tables, deferred remainders and limits, not only checked headings. HP22/23 bodies were completely visible before a subsequent oversized commit-stat output was truncated; no truncated commit stat is counted as a full source read. P01 uses actual53b8212f widget changes and e4866ce4 exact assertion changes, P02 the complete final `src/ui/fade.rs`, and P03 the complete final `src/core/scroll.rs`, together with the four full commit messages and the previously source-qualified app requirements. This is not a claim to have reread every call site in those commits; the new branch-diff pass will record per-file coverage separately.

| Exact rows | Source disposition and concrete owner obligations |
|---|---|
| F01/F04/F08a/F09 | Deferred proposals remain unselected; actual oracle owner behavior and accepted modern API safety are separate.017/022/023/056/060 must preserve the specifically qualified paste/copy paths, not introduce the proposed corrected app behavior. Earlier findings above remain the authority record. |
| F02/F05/F08b/F08d | Shared/Holla implemented eligibility, lazy-insertion identity, grapheme/paste and exact modifier paths are current. TablePro and other app additions explicitly remain Later where the source says so.020/024/028/030/043/046/059 require actual eligible target, filter-reset policy and owner-route observations; an empty/disabled list must not choose a fallback target. |
| F03/F06/F07/F08c/F16/F17/F18 | Full proposals are deferred, not selected merely because modern APIs can implement them.020/022/060 must distinguish reachable Settings failure, local-sort behavior and malformed direct-API ingestion;013/016 preserve actual modified-key routes;032/039 retain clipped source sections;012/031 distinguish modern containment from legacy stray paint;016/022 must not enable the source-disabled transcript. Root owns pending app-level evidence/authority, with no successful postpanic oracle frame invented. |
| F10 | Complete logical grapheme segmentation precedes style assignment; split styled clusters retain source geometry/copy.013 must prove cluster-boundary output, not only valid UTF-8. This F10 is distinct from Form's INLINE:F10/ADJ-12. |
| F11/F12/F13 | Every retained-output mutation enforces limits, rebases anchors and invalidates derived facts.021/044/055 must cover append/replace/prune, full versus partial eviction and same-length replacement. Old owned-content revision authority does not imply magical detection of arbitrary mutations in modern borrowed models; the accepted caller invalidation contract still applies. |
| F14/F15/F19 | Tab/control display, original-source copy, keyboard browse selection and dirty append work are distinct.013/021/026/067 need source-index/cell/copy agreement, selection inherited by Diff, fresh-layout equality and actual dirty counters. A safe file preview sanitizes before storage, so raw unsafe escape bytes are not its copied-source contract. Warm redraw alone does not qualify append complexity. |
| F20 | Holla Activity and file search are current; Jackin pane/Inspect search and a generalized controller are explicitly Later.021/025/026 must not add Inspect search and call it oracle preservation. Existing CodeEditor find is a different current owner. See H-F20 below. |
| F21/F23b |009 owns actual preview terminal acquisition/restoration, supported suspend/resume and declared failure paths. Child-task PTYs and reaping remain HP15 Later; source does not promise restoration after SIGKILL. Fixture termios snapshots are not native execution evidence. |
| F22/F23f/F23g |001/069/070 separate integer cells, emitted ANSI, rasterizer fidelity and real terminal observations; bind exact source/tools/binary/environment. TrueColor/Mono/NO_COLOR remain separate, including first-process SGR. A manifest/hash or old inspected frame alone proves neither semantics nor new candidate equality. |
| F23a/F23c/F23d/F23h | All-app current scope supersedes the old limited Holla proof inventory.009/010/032/039/040/044/050/051/057/058/064/066/069 need actual event reachability, draw between queued stages, current resize geometry, bounded drain and exact Paste versus Type/modifier/drag/reset transcripts. The source's256-event drain budget is not permission to advance simulated time per event. |
| F23e |067 separates dirty/idle actual production work and collection workloads. Historical sample timings are observations, not substitute thresholds or proof that all missing collection work already passed. |
| HP01/HP02 |041/044/049 preserve simulated discovery provenance, exact stable action IDs and learned ordering; aliases, caps,21-score ceiling and source selection remain explicit. Live PATH probes, durable usage store, locking and restart I/O are Later. Untested UI variants in the original evidence are not implicitly passed. |
| HP03/HP04 |042/044/048 preserve selected native path identity, exact Open/Reveal/Copy/Analyze routes, bounded browser generation, previews and diagnostic states. Real home indexing, OS clipboard, file traversal/opened-descriptor safety remain Later. Preview limits256KiB/2000lines/4096chars describe the retained fixture behavior, not a new live scanner project. |
| HP05/HP06 |047 preserves exact Git command/cwd, root and peer independence, distinct pull modes, safe branch-selection semantics and represented outcomes. No real git/remotes/worktree deletion is selected. A generic successful batch does not prove each standalone operation or failure barrier. |
| HP07 |047 preserves fixture-manifest package/Just/Make/Taskfile/mise distinctions, exact argv and diagnostics. Node string-only/sorted/30cap and lock precedence differ from ordered uncapped mise output; unsupported Make syntax is rejected without discovery execution. Source admits unasserted Just/Make caps, child cwd and trust-cancel variants; do not promote prose into observed outcomes. |
| HP08/HP09 |047/048 preserve Cargo build/test/clippy/clean and Docker/Compose standalone/compound variants, target-bound review, permanent tool-native disclosure and typed outcomes. Dry run is not deletion; discovery failure differs from empty daemon; stop failure blocks remove, independent prune branches remain independent. No host process, daemon or filesystem mutation is selected. |
| HP10/HP11/HP12 |047/048 preserve service schema/30actions/10services, cache-age and status distinctions; Gradle wrapper versus installed routes, depth-five shared traversal, no symlink/node_modules descent and prerequisite failure; IDEA lowercase `.iml`, selected-directory dedup and visible log health. Real service cache, Gradle/JVM/Trash execution remain Later. The HP12 test name says case-insensitive but its assertions correctly reject `Upper.IML`; the assertion/source rule controls. |
| HP13 |049 preserves each represented manager alone and aggregate typed stages, exact Brew flags, macOS casks, ZSH override and standalone mise completion. The document explicitly lacks several host/manager variants and actual aggregate execution proof; a plan containing steps does not prove running those steps. Real upgrades remain Later. |
| HP14/HP15 |044/047 preserve exact normalized output and final fragments before completion, retention4000/dropped500, queued/parallel state, independent versus dependent failure and virtual-clock prompt/cancel ownership. Secret input is redacted; ordinary stdin bytes route once. Real streams, children, signals750ms/reaping, headless runner and terminal error paths are not established by fixture strings or virtual ticks. |
| HP16 |040 preserves existing preview scenario/color/motion/frame options only. Full list/run/doctor/JSON/exit-code/headless CLI, including simulated expansion, is explicitly entirely deferred; do not implement it as a refactor requirement. |
| HP17 |043/049 preserve fixture configuration schema, exact argv, global-first collisions, whole-file/path/cwd trust, changed-review rejection and save-failure no launch. Explicit `sh -c` is not implicit shell interpolation. Live config, atomic persistence and trust CLI remain Later; fixture action exit127 is actual missing simulated outcome, not permission to manufacture success. |
| HP18/HP19 |045/046 preserve allocated/apparent distinctions, hardlink counting, symlink leaves, partial/dataless/error states, generation/cancel/cache hints, sort/fold/keyed selection and distinct Top files scope. Seven-day/depth-two v3 cache,100MiB/top50 and timeout describe source models; real block accounting, Spotlight concurrency, bounded worker channels and restart I/O remain Later. Fold totals, cache replacement selection and post-cleanup rescan need independent observations, not checked headings. |
| HP20/HP21 |046 preserves all18 represented categories and strict shared eligibility, inherited protections, exact mode/target/revision review, Cancel default, Trash/no permanent fallback and truthful dry run. Three collision attempts and explicit audit-log writes are different from cleanup effects. Operational pathname race, real process probes and OS mutation remain Later; policy tests must not become authorization to delete user files. |
| HP22/HP23 |045/046/048/049/050 preserve world-owned cleanup completion before quit, every requested path's typed outcome, log health and Trash-versus-physical capacity, explicit macOS/Linux capability fixtures and honest unsupported states. Virtual log mirrors do not prove durable restart; macOS fixture `Os::Debian` does not prove Linux kernel/Trash/subreaper behavior. Actual terminal/color behavior remains separately qualified through F21–F23. |
| O01/O02/O03/O06 | Retired generic worker/theme/collection/container proposals do not authorize new frameworks.044/065/020/017 still own the accepted specific lifecycle, semantic theme, identity and layer contracts under current architecture. Retirement removes this proposal, not every underlying capability. |
| O04/O05/O07 | Matrix paste, terminal/OS clipboard extensions and a proposed readonly-field API remain conditional and unselected.022/009/016 must not manufacture new app behavior or external effects from these rows; independently accepted modern APIs and actual source behavior retain their own authority. |
| P01 |53b8212f removes the prior-focus condition from TextInput/TextArea/CodeEditor; disabled remains inert and readonly CodeEditor only moves caret. e4866ce4 strengthens Holla exact insertion `/XUsers/alex/work/big` and TablePro acknowledgement editing after one completed click.016/022 must distinguish completed text click from press-only and Grid's select-then-act current-cell rule; the generic label “click-to-edit” does not mean every grid click edits. |
| P02 | Final oracle fade uses55% outer/80% inner contrast, height12 for two rows, no fade below4, majority container plane, reversed/other-plane and hardware/explicit cursor-row exemptions, DIM outer-only for non-RGB.014 must preserve those cells/styles at top/middle/bottom and resize through accepted shared rendering; old allocation-heavy implementation is not required. |
| P03 | Final ScrollState reports actual movement, clamps extent/viewport, minimally reveals, rounds thumb/inverse and preserves grab offset.014 and the app tasks must prove actual drawn track, boundary consumed/no repaint, current labels, tail capability and resize/hover/horizontal wheel routes. The final commit's full source call-site coverage is assigned to branch-diff review, not inferred from the commit message. |

### H-F20 — deferred Jackin search promoted into a current parity summary

`historical-obligations.tsv:154` (F20; locate by ID if rows move) labels component scope “Holla and Jackin find” and asks for real search/navigation/copy journeys, although `02f5294b:docs/improvements/f20-find-in-read-only-output-through-existing-composition.md` explicitly keeps Jackin pane/Inspect search and the common controller in Later. Actual oracle `src/bin/jackin_preview/screens/inspect.rs:250/343` forwards to Diff without a query/find owner. Adding a search field/controller to Inspect could satisfy the generic summary while changing source output and input ownership. Root enabling condition is loss of the per-app current/Later distinction when flattening the source into one comparator obligation. Structural repair: retain the quoted proposal but qualify current Holla owners and separately current CodeEditor find; record Jackin Inspect search as unselected, with no primary parity requirement for invented functionality. Coordinator notified; no canonical mutation by this read-only pass.

The A121 correction is now visible in the shared ledger (one intent/delta480); its earlier finding above records the reviewed pre-repair state. A129 locator and deferred-app decisions remain coordinator-owned. Global source review is complete, while current source-owner proof gaps and pending explicit dispositions remain open. No execution probe, fixture, task payload or production edit was made during this final66 review.
