# Continue main without losing Holla behavior

## Authority and current status

This is the working proposal for the user's additional whole-branch review. The implementation base is pinned main `7b27732a8c3c131760ec3438f641cb3c11343a42`; the compared Holla branch is `2e2401393c47360741ebd321679de08982dca50a`. The immutable behavior oracle remains separately pinned at `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Identical old files do not make those authorities interchangeable.

The exact no-rename inventory contains 7,885 changed paths: 866 source, test, tooling and historical-document paths, plus 7,019 generated-artifact paths. Full source reading, finding integration, compatibility adjudication and independent rechecks are still in progress. Decoding historical images and parsing manifests is not visual parity approval. This proposal does not claim the entire review is finished.

No production implementation, branch integration, baseline blessing, commit, merge or publication is authorized here. Required changes belong in the existing completion task graph; they are not a competing rewrite from the Holla branch.

## Preserve completed main work

Keep the workspace/package split, backend-neutral event types, shared runtime, typed actions, controlled values, stable keys, shared text engine, pure drawing, attributed semantic styles, retained lifecycle ownership, sensitive-buffer protections and checked arithmetic. Keep exact-target validation and opaque, non-cloneable, one-shot reviewed plans. Do not copy back old owned widgets, raw app input engines, draw-side mutation, unchecked calculations or replayable authorization objects.

Main tests proving those properties remain useful. Main tests asserting obsolete product behavior must be classified and replaced by source-qualified expectations; a green self-baseline is not evidence of oracle parity.

## Required continuation

| Area | Change required within main | Existing owners and proof |
| --- | --- | --- |
| Text and rendering | Centralize normalization and mutation outcomes; enforce grapheme boundaries after edits; restore word/fuzzy/wrap semantics; handle graphemes split across styled spans and `Display` fragments; clear previous wide-cell ownership. | TASK-013/015/016, direct counterexamples and real public consumers. Streaming allocation contract needs its measured compatibility decision. |
| Runtime and layout | Restore cursor visibility before the prior panic hook; complete safe suspend/resume; avoid replaying one physical key during Bubble/Esc follow-up passes; unify effective chord identity; restore horizontal minimum-collapse choice. | TASK-009/010/012, actual terminal/input trajectories and narrow geometry cases. |
| Shared controls | Restore distinct source Status/Segments policies, wrapped Empty geometry, correct reverse menu navigation, completion geometry and keyless Picker submission. Reconcile Select, Steps and Grid/DataTable source behavior through explicit shared configuration while preserving accepted modern defaults. | TASK-020–024/027/028, source-family-specific fixtures and actual app consumers. No application-local replacement engine. |
| Holla shell and worlds | Restore 34 scenarios, source CLI/help/exit grammar and no-history opt-out, full route/tab graph and bindings. Interpret `--frame` as source tick count; prove any fast-forward equivalent for every restored state machine. | TASK-040/041 and domain owners; TASK-050 proves the full union. |
| Holla execution | Replace `Would run`/static-output placeholders with source in-memory command outcomes, gates, streams, prompts, cancellation, retry, effects and cleanup progression. Preserve safer modern review identity and exact target revalidation. | TASK-043–049, intermediate state/effect/output observations and full-parent acceptance. |
| Holla policy | Restore exact alias transfer/reset, open-F10 quit-chord precedence, and Files/Activity find including the Files outer-query paste fork. Do not add deferred search to unrelated facts pages. | TASK-040/041/042/044/050; explicit named branches, not broad “search works” assertions. |
| Jackin | Restore missing configuration/world state, resolver candidate/explanation policies, takeover/discovery progression, exact change fixtures and post-input PTY transcript timelines. Preserve typed editor-write and manager-operation tokens. | TASK-049/051–056; complete branch review and exact scenario refinements are still pending. |
| TablePro | Restore connection validation/fields, missing live command/modal handlers, original filter SQL/evaluation semantics, history fixtures and source paste recipients. Keep keyed pending edits, generation ownership and safe async effects. | TASK-057–063; use real save/cancel/execute effects, not marker surfaces or boolean-only tests. |
| Showcase | Remove unintended Overview interaction/geometry while preserving public-author conformance elsewhere; restore exact CLI grammar, source layouts and interactions. Explicit color override versus generic capability detection needs a bounded decision. | TASK-032–039/068 and relevant foundation owner; source environment/exit cases and full frames. |
| Proof and documentation | Replace obsolete claims, examples and product expectations; bind executable compiler identity, actual observations and exact protected asset membership. Historical captures remain historical until independently regenerated from the immutable oracle. | TASK-001–008/065–073; canonical task lint, joins, negative qualification and immutable provenance. |

These rows summarize required work, not permission to omit the detailed contracts. Exact findings, locations, source distinctions and ledger coverage live in the partition reports linked below.

## Decisions that must remain explicit

Source compatibility does not automatically override accepted modern generic APIs. Source-specific Select navigation, Steps presentation/actions, Grid sort and click behavior, editing chords and explicit color selection require narrow shared policies and consumer configuration. Preserve generic defaults unless a separately reviewed decision changes them.

An actual immutable-oracle crash was reproduced in Showcase Settings after removing selected environment entries and extending selection. A successful post-crash baseline cannot be invented. Safe continuation requires an explicit authority decision; until then this branch remains unresolved. Other deferred source proposals are not automatically accepted refactoring work.

Universal allocation or timing promises must match measurable behavior. Long transient grapheme fragments require bounded-by-content storage; borrowed-input guarantees remain separate. Timing qualification and a real production performance pass are distinct. Failed or invalid measurements remain visible.

## Detailed evidence

- [Foundation](branch-diff-foundation.md): complete 67-path source review.
- [Components A](branch-diff-components-a.md): complete 41-path source review.
- [Components B](branch-diff-components-b.md): complete 35-path source review.
- [Holla](branch-diff-holla.md): complete 86-path source review.
- [Jackin and TablePro](branch-diff-jackin-tablepro.md): ongoing combined review; no whole-partition completion claim here.
- [Exact inventory](branch-diff-inventory.tsv) and [artifact decode census](branch-artifact-census.json): coverage inputs, not acceptance certificates.
- [Re-audit plan](reaudit-plan.md): remaining completion conditions and independent ownership.

Final handoff must update every affected task, scope, dependency, canonical source row and proof branch, independently recheck the repaired contracts, and finish the remaining whole-file reads. This document remains a working synthesis until that work is complete.
