# Independent actual-source qualification rereview

Status: **in progress; no whole-FND-01 acceptance**. This review is independent of the source-driver author. Planning instruments only; no production edits, commits, baseline blessing or dispatcher acceptance. `verify-and-stop` and `investigate-first` govern evidence scope and stopping. Holla's separately requested idle MenuBar paste correction is recorded in `holla-trace-corrections.md`, not treated as source-qualifier evidence.

## Read scope and authority

Read the complete initial source-driver339 lines, source-protocol112 lines, TASK-072 README196 lines, verify76 lines, trusted obligations and all20 source-obligation rows. Read inherited transport `Fixture.run`, strict JSON loader, protected snapshot/source binding, actual-source extraction/build and compiler identity paths. Subsequent driver385 lines, source-policy118 lines and source-observer79 lines were read completely; the author is still extending them, so these are not final hashes.

Actual architecture source authority remains main `7b27732a8c3c131760ec3438f641cb3c11343a42`; immutable UI authority remains `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Historical controls were compared with the exact TASK-072 source obligations and `git show` main/444ae3d9 source where needed. No source obligation closes merely because it shares CHK-006.

Fresh independent `git archive 7b27732a8c3c131760ec3438f641cb3c11343a42` bytes equal the entire decompressed protected `main-source.tar.gz`, not merely selected file pins. Both SHA256 values are `732e40d9cf82e98e3d6375aaeecee349e8cb774abc5094dbbff145555201633a`; archive contains5029 regular files. Source archive provenance is verified in full.

## Findings delivered during authorship

1. **Initial HM30 mapping was false.** Initial source-protocol coverage table mapped Button/Id-only core-library consumers to HM30. `history-middle-obligations.tsv:31` requires actual tui-testing Harness/Scene/Fixture/Caps and private-name/arity/absent testing-API controls. Counterexample: remove or privatize a testing API while keeping Button intact; all original29 controls are unchanged. Root condition: broad historical-row mapping from a neighboring public-API witness. Structural repair: compile actual testing crate consumers, preserve exact compiler errors and same-testing recovery; emit and verify the testing library digest. Author has added these controls; final actual compilation still requires revalidation.

2. **Source-policy `dispatch-source-owns` initially labeled a harmless helper forbidden.** At the inspected118-line revision, source-policy.py:87 appended only `fn owns(_id: junie_tui::Id) -> bool { false }`. Main `xtask/src/main.rs:2600` rule25 and4476–4479 dispatch patterns target `.owns(...)`, `.locate`, `.child(...)` and `scrollbar::id_for`, not an unused free function's spelling. Counterexample: an architecture checker following actual call syntax accepts this fixture while a broad name ban rejects it. Root condition: mutant name substituted for the forbidden source operation. Structural repair: actual method-dispatch negatives and a harmless same-name helper positive; separately cover all promised dispatch forms. Sent immediately to author/root; pending final rereview.

3. **Dependency map initially overclaimed A93.** Three real Cargo graphs discriminate legal/direct/transitive library-to-application paths, but A93 (`source-obligations.tsv:6`) specifically includes umbrella Ratatui/widgets/macros/backend misuse, feature/version graph and duplicate Unicode versions. Counterexample: a checker handles app paths correctly but ignores the banned dependency identities and duplicate versions; original graph corpus accepts it. Root condition: generic graph execution treated as all dependency-policy semantics. Structural repair: independent actual Cargo graph mutations for each promised policy, or explicit unqualified dispatch blocker. Not whole-A93 closure.

4. **Other broad-row limits remain explicit until independently witnessed.** HM21's todo self-name clause was absent from initial29; new actual AST todo/comment cases address that mechanism. HM28 named13 inventory/public compilation does not by itself execute the required reversed-specificity assertion. HM26's scoped one-gate join does not establish whole-registry exhaustion or one-sided extra rows. EARLY-AMEND-034/HM53 inventory is distinct from actual per-page update/draw reachability. Parent/author informed; final clause/control manifest must distinguish source syntax, compiler evidence and actual application execution.

5. **Three original dependency cases did not discriminate permitted-root behavior or build-edge traversal.** `dependency_source` introduced permitted bridge→showcase only in overall negative cases. A broken checker forbidding application reachability from every root therefore returns all three expected categories despite ignoring each root's policy. No build-only dependency case detects a checker skipping build edges. Structural repair: actual permitted-only bridge positive plus restricted shared-subgraph negative, and build-only path negative. Keep root-local visited sets. An independent real-transport adversary is prepared in `/tmp/source-qualification-rereview.IH2g4A/graph_overreject.py` for replay after fixture preparation stabilizes.

6. **New global dependency-policy expansion initially masked intended negatives and lacked a positive.** In the inspected expanded driver, every bridge-based forbidden-closure/duplicate-version/app-transitive mutant also violated the exact five-direct-dependencies policy. A shallow checker testing only direct dependency names and core features could reject these without traversing the graph or checking duplicate versions. `graph_violations` also selected every package of a name when *any* same-name path was reachable, incorrectly including disconnected versions. Structural repair: independently frozen scoped policy profiles/positive subjects, exact intended violation checks, package-ID reachability and an unreachable-duplicate positive. Preserve a truthful whole-main negative separately. Actual attempted transport replay on driver `e3b74a0b9a124726698a1ed6220b892fbd929312de5293b09182b924431eac9c` failed before any candidate with `actual dependency mutation ineffective: valid ['forbidden-palette', 'core-features']`; that revision is not an executable qualifying corpus. The author received the exact failure and structural proposal.

7. **Every-root graph qualification also needed a testing-only negative.** Every initial app-path negative reached the application from junie-tui, so a checker ignoring junie-tui-testing entirely survived. The author added a testing-only path with clean core and updated private traversal to the policy's library-root set. Source reread confirms the intended mechanism; final measured replay pending.

8. **Page order/phase inventory needed source roles, not directory counts.** Initial policy omitted frozen PageId order and order mutants. The author added PageId::ALL's22 identities plus swapped/duplicated/omitted-order cases and phase-removal cases. An interim exact22-module assertion excluded only mod.rs, but actual `git ls-tree` shows24 files:22 pages plus mod.rs and author.rs. The author helper is not a page; the complete source inventory must retain it while the page-phase subset excludes it. Source reread confirms that correction. Presence of update/draw signatures still does not prove runtime reachability; actual production witnesses remain separately required.

## Measured checks

Initial29-case source qualifier passed both:

```text
rtk proxy python3 -B docs/refactoring-plan/evidence/architecture-bootstrap-source-driver.py --self-test
rtk proxy python3 -B -O docs/refactoring-plan/evidence/architecture-bootstrap-source-driver.py --self-test
{"always_pass_rejected":1,"forged_rejected":1,"positive":2,"source_cases":29,"zero_rejected":1}
```

Initial driver SHA256 `29ee21ca37b57361e0d596115012893f369d77ac61f0df572e39fad7e967e7f4`; initial protocol SHA256 `715ffe2050b02d7356e5aa3b62d1cbef12816a0e64b5452a98a6fdc1e10ba904`. These results do not transfer automatically to later expanded source-policy code.

Independent actual FD transport attack harness: `/tmp/source-qualification-rereview.IH2g4A/transport_attacks.py`. Command `rtk proxy python3 -B /tmp/source-qualification-rereview.IH2g4A/transport_attacks.py` exited0. It recorded driver SHA256 `f87216a16d3ef9a4cbaf9aa7a7139ca03f28a3c0c7ee43fb98555163b3c0b387` and prepared88 rows, then selected actual external Button compiler cases. It accepted a valid positive, a genuine negative with exact ARCHITECTURE/nonzero/observer digest, and a fresh positive recovery. It rejected13 adversaries for their expected exact failure class: missing result, malformed JSON, duplicate key, wrong run, unknown field, wrong output type, zero observer, forged records, wrong requested tree, replayed request, negative with zero exit, negative missing its observation digest, and positive with nonzero exit. No malicious candidate supplied the compiler program/source or observer verdict. Preparing88 rows is **not** proof that all88 semantic premises passed.

## Acceptance boundary

The new instruments genuinely execute pinned Rust compiler/Cargo processes, consume actual source copies, preserve raw output and externally held observations, and reject the tested transport forgeries. Their self-tests intentionally use transport doubles rather than a reference production checker. This proves fixture premises/transport only; the future TASK-072 dispatcher must still pass the full protected corpus, shared-path source review and exact-tree host gate. Final hashes, all expanded actual premises, optimized execution and repaired semantic controls remain pending while the author edits.
