# Jackin and TablePro whole-branch comparison

## Scope and current status

Completed source-read and bounded semantic comparison:92 Jackin paths and45 TablePro paths,137 exact branch blobs. The companion `branch-diff-jackin-tablepro-read-ledger.tsv` records every commit/blob/SHA-256/line count, complete read coverage, retained architecture, restoration responsibility and finding/task mapping. Reading means all current bytes plus all changed old bytes and exact equal-line mapping where recorded; hashing alone never counted. This is not an implementation/parity completion claim. Open disposition and proof additions remain explicit below.

Architecture baseline: main `7b27732a8c3c131760ec3438f641cb3c11343a42`. Compared branch: holla `2e2401393c47360741ebd321679de08982dca50a`. UX authority remains the separate immutable oracle `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Every deleted legacy Jackin/TablePro path in this partition was byte-compared against that oracle and is identical. This equality is scoped to these exact app files, not the branches globally or their shared dependencies.

Git reports the legacy-to-workspace moves as additions/deletions. Reviewing each added file as a new feature or each removed screen as a removed capability would be incorrect: app composition, domain models and tests were redistributed. The review pairs logical modules and follows relocated responsibilities before classifying a behavior gap.

## Initial architectural evidence

Main TablePro `src/domain.rs` now owns stable row records, pending baselines, monotonic row keys, schema-independent generic Grid adapters, application SQL generation and undo snapshots. Its `PendingRow` keeps current/original/lifecycle/key together; reorder moves whole records, invalid permutations leave them unchanged, and undo never rolls the key allocator backwards. Retain this completed architecture. Restore missing source interactions through its public reusable Grid boundary, not by copying the old state-owning widget or reintroducing positional identity.

Main `src/grid_model.rs` composes the same result adapter for structure rows. `src/quick_switcher.rs` owns an app index and weak workbench owner token while shared Picker owns query/navigation state; its app projection retains scoped item filtering, row keys and matched spans. `src/filter_editor.rs` owns all18 domain predicates and SQL projection. These are architectural progress, not replacements for exact source UI proof. Cross-branch findings and task/proof mappings follow; source policy conflicts remain explicitly open where not adjudicated.

## Verification boundary

This pass has not executed all new main tests or proved complete app parity. Existing source-replay findings in `reaudit-jt-routing-repairs.md` remain separately qualified; their oracle tests are not a main-branch implementation certificate. No production source or golden was edited.

## Source deltas found during TablePro review

### BD-JT-01 — control-shaped paint does not own source actions

Main `apps/tablepro/src/app.rs:2720–2862` paints Connect/Edit/Duplicate/Delete labels without separate control registration. The only registered details target is the whole card at1662–1664; any completed card click calls connect at3024–3036. Oracle `src/bin/tablepro/connections.rs:488–549,772–788,1093–1114` has four independently owned button paths. Counterexample: clicking Edit connects in main; clicking blank details space also connects. Root cause is replacing a control composition with app-owned paint while retaining a coarse click proxy. TASK-058's separate reusable Buttons/Form/Tree proof must remove that proxy, not merely adjust hit coordinates. TP-004/006 and the complete Connections source inventory provide existing proof owners; blank-card non-action and each separate button need exact identity assertions.

Main's connection tree also keeps separate interaction and visual TreeState (`app.rs:475–476,1989–1999,3038–3045`), deliberately initializing one at the first connection and the other at a group. Retain keyed Tree composition but remove mirrored behavioral state through the planned shared selection/focus contracts. A matching initial frame alone cannot prove the next key acts on its painted owner.

### BD-JT-02 — conversion and validation changed source outcomes

Main `connections.rs:80–85,253–309` accepts0 as a u16 port, requires a PostgreSQL Database despite the source having only help text, fails conversion for SQLite's blank default port, preserves name whitespace, and emits Some(empty) for enabled SSH with an empty host. Oracle `connections.rs:89–105,213–250,621–632,689–735` validates only name and port1–65535, accepts blank port, converts it to5432, trims name and omits empty SSH. These are app domain policy deltas, not reasons to restore old state-owning TextInputs. TASK-058 must preserve each source branch through controlled FormData and its shared validation collector. TP-008 already exercises port0; TP-009 observes SQLite's blank port but switches away before saving, so conversion is not proved by that checkpoint. Additional explicit finite variants must bind blank-Database acceptance, SQLite blank-port save, trimmed name and empty-SSH conversion. Existing source paragraphs are authority, not a substitute for those executable cases.

Main also drops the Clients group, SSH-user and Local-only fields (`connections.rs:60,89–159`) and hides SSH-host instead of showing it disabled (`:396–400`). Oracle declares them at168,197,205 and renders disabled SSH controls at1202–1208. TASK-058/TP-011 owns their restoration and exact focus/disabled geometry. Its controller must retain Secret ownership guarantees while independently honoring the frozen password-display disposition; this report does not authorize changing that adjudication.

### BD-JT-03 — surface names and bound keys overstate implemented flows

Main `app.rs:152–165,337–432` declares/binds TAB_LIST, FILTER, PREVIEW, SAVE, EXPLAIN, CLEAR_QUERY, COMPLETE and PALETTE, but the entire command match at2913–2984 has none of their handlers. HELP merely assigns Surface::HelpDialog. Actual draw at3151–3159 renders only QuickSwitcher and destructive layers. The Surface enum and set_surface seeds are not executable substitutes for TablePro's Filter, typed safety, completion, history, plan and help flows. Main QueryTab uses a single TextInputState (`tabs.rs:240–260`); plan/error/history content is reduced to labels/first six lines (`app.rs:1799–1856`); no app handler invokes its domain completion model. TASK-059/060/061/062/063 own restoration through reusable runtime controls. Keep their parent scenarios intact, not one marker-only frame per Surface. This source result is stronger than test names or a 21-surface census, but does not yet close the pending old app/tabs/workbench read.

### BD-JT-04 — history fixture and displayed vocabulary were reduced

Oracle `model.rs:218–369` seeds15 complete records, including multiline SQL and two failure records. Main `model.rs:117–188` retains7, omits the multiline record, alters the EXPLAIN trailing space, and adds literal backslashes around the ordres error quotes. Oracle first_line uses U+22EF at175; main uses U+2026 at75. Both push at the front; oracle's reversed input followed by final reverse (`:370–385`) must be followed literally when restoring IDs/order, not normalized from the misleading newest-first comment. Main's bounded history ownership and typed SwitchTarget::OpenTab(TabKey) are completed structural progress. TASK-061 must restore exact fixture bytes/IDs/order; TASK-061 history and TASK-063 switcher must prove consumers, including full multiline/error details and path-only matches. Main's current seven-record tests cannot establish those properties.

### BD-JT-05 — database fixture migration largely preserves source

Full main db source plus all76 non-equal old lines and1036 exact-equal mapped lines were read. Catalog values, generated row algorithm, enum options, row counts, six connections and their outcomes are retained. The added app-level Value::Default supports replacing the old Grid's default-value ownership without importing database semantics into shared Grid. Full main sql plus all149 non-equal old lines and1366 exact-equal mapped lines were read. SQL tier/gate, supported subset, deterministic executor, row cap and plan tree remain source-derived; UTF-8 cursor/token iteration and guarded formerly-unwrapped catalog lookup are structural hardening. Keep these implementations and their tests while TASK-062 restores the missing live queue/acknowledgement consumer. No final semantic equivalence claim is made for unreviewed consumers.

### BD-JT-06 — dormant filter evaluator cannot become the parity implementation

Oracle `tabs.rs:183–259` emits `LIKE`, `!=`, and a Between pair of `>=`/`<=` comparisons; NotContains and NotIn return no predicates, while Regex is approximated by substring `Cmp::Like`. `TableTab::select` at459–474 ANDs every enabled predicate regardless of `match_all`; the lead toggle in `workbench.rs:751–754,1075–1077` changes its label and reloads but does not enable OR. Main `filter_editor.rs:188–276` changes emitted SQL to ILIKE/<>/BETWEEN, escaping, quoting, and implements its own all-operator evaluator. Counterexamples: a source NotContains filter leaves the result set unchanged, while the new evaluator excludes matching rows; toggling ALL to ANY does not change source conjunction; quote-containing text has different preview bytes. Main's evaluator is presently dormant behind the missing Filter handler, so this is a wrong future restoration path, not evidence that its current live UI already exposes all operators. Root condition: a newly extracted domain abstraction silently upgrades the label-implied semantics. TASK-060 must retain app ownership but reconstruct the actual source predicate/preview policy, with separate operator-by-type, no-predicate, quote, NULL, case, wildcard and conjunction witnesses; TASK-063 closes full frames. TP-039–042 alone do not exhaust those branches. JSON operator ordering also differs (`oracle tabs.rs:136–141`); numeric/bool chip labels are unquoted in source at253–259, unlike main's generalized formatter.

### BD-JT-07 — Picker paste compatibility has more than one underlying owner

Oracle `app.rs:236–254` special-cases only Dialog and Filter, then delegates every Picker paste to the underlying screen. `workbench.rs:1194–1202` first uses an editing explorer filter, otherwise delegates to Table Grid, Query or History search. The History TextInput receives paste even idle; its source on_paste begins editing. Ctrl+O/Ctrl+G are admitted while editing (`app.rs:638–644`), so active explorer filtering, an inline Table cell, and History-search branches are reachable, not fabricated hidden-focus states. SafeMode uses the same Picker branch, although its opening chord requires !editing at668. Counterexample: open History, open TabList, paste nonempty text; source changes its underlying search draft without changing the Picker query. A Query-only configured CodeEditor helper would preserve one branch but lose the others. Root condition: the compatibility exception was inferred from one representative consumer instead of the complete app router. TASK-059/060/061/063 and ADJ-09 need explicit source-owner cases and narrow existing-owner helpers without LayerSpec passthrough, fake focus, or duplicated app editing. This report requests adjudication; it does not authorize widening the shared default modal route.

### Read checkpoint

Legacy TablePro app2518, tabs2531 and workbench1330 lines have now been read in full in addition to the production pairs listed above. This closes their earlier read-pending qualification, not their remaining test/task-proof mapping. Distinct source state machines include a seven-tick first query and five-tick subsequent statements, pinned-result retention, cancellation and failure-stops-batch, worst-tier batch safety, five-tick commit, three-tick lazy Explorer, drawer-only focus stops below100 content columns, TabList dirty-close bypass, ignored alternative-open flag, read-only history editor and copy-status-only callbacks. Main keyed owner/generation records must survive restoration of those observable paths. Tests and the Jackin partition remain pending in the ledger.

### BD-JT-08 — surviving test names hide deleted journeys

All45 TablePro inventory blobs are now fully read, including21 new test/baseline files and legacy899-line app_tests. New `app_tests.rs:108–121` cancels by setting a bool true then false;147–161 checks confirmation then runs an unrelated SELECT, never types a token;213–223 commits a model cell then discards rather than saving;248–271 requires only a generic Production/Connections header for every named Surface. The old same-name tests at `app_tests.rs:365–381,438–494,609–645` drive real cancellation, typed/mouse acknowledgement and reviewed save. Main `perf.rs:162–168` compares two Debug strings under the name debug_and_release_alloc_counts_match; its 2k-line editor benchmark at186–202 merely looks for TablePro after drawing a single-line input. Root condition: preserved labels were treated as preserved acceptance meaning. TASK-061/062/063/064 must restore actual public event sequences and observations, TASK-067 binds measured allocation/time work, and TASK-073 must reject marker/flag substitutes. Keep useful domain tests under accurate names, not as substitutes for deleted scenarios.

The same weakness is byte-visible in main `tests/baselines/tablepro.txt`: all eight FilterEditor digests at50–57 equal TableGrid at154–161; all eight HelpDialog digests at66–73 equal WorkbenchDefault at162–169; GridCellEditing58–65 equals PendingChangeBar90–97. `visual.rs:18–27` seeds a Surface then treats that digest as the whole sanity guard. These are not hash collisions inferred from absence: production draw and seeds corroborate identical missing states. TASK-005's oracle fixture checks and TASK-064/073 frame+semantic conjunction remove the enabling condition. Do not bless these marker fixtures as source parity.

### BD-JT-09 — preserve the new ownership tests without inheriting their UX expansions

Main close_contract, destructive_generation, replacement_contract, inline_draft, tab_ownership, tab_identity_guard, row_identity, row_actions_contract, undo_contract, switcher_contract and quick_switcher_contract prove substantive mechanisms: stable record keys, stale generation rejection, whole-workbench owner rejection, same-key payload replacement, scoped destructive target, draft survival, keyed reorder/undo, retired-key nonreuse, and redacted Debug. These are completed main architecture and must remain covered. Their seeded/replacement adversaries are legitimate architecture tests, separately identified from executable source app traces.

However several fixtures rely on editable SELECT result grids (`row_actions_contract.rs:7–15,18–60`, `replacement_contract.rs:5–16`, `destructive_generation.rs:6–14`). Oracle `tabs.rs:2030–2070` unconditionally makes Query rows read-only with the source reason even when the SQL engine identifies an editable table. Main permits typed result edits via ResultSet.editable. Retaining the reusable guard architecture does not authorize exposing those added Query editing actions. TASK-061 must configure source read-only Query composition; TASK-060 should carry editable Table-path runtime ownership witnesses, with direct generic model fixtures retained for non-UX authorization adversaries. Likewise QuickSwitcher `quick_switcher_contract.rs:68–83` expects Ctrl+C blocked, while oracle App handles Ctrl+C before modal routing (`app.rs:255–265`): route-specific compatibility needs explicit adjudication rather than using newer tests as immutable UX authority.

### TablePro per-file semantic disposition

Production ownership: Cargo/lib/main/cli →057 package and064 CLI integration; connections →058; db →057 fixture executor and062 live consumer; domain/grid_model →060 reusable Grid adapters plus057 domain policy; filter_editor →060 with BD-JT-06; model →057 fixture/search and061/063 consumers; tabs →059 ownership,060 Table,061 Query/History and062 execution; workbench →059 Explorer/tab lifecycle; quick_switcher →063; app →058–064 composed routing/frames. Restore source clauses while retaining main keyed/weak-owner/generation APIs.

Tests/baselines: app_tests →064 genuine journeys (BD-JT-08); cli_contract →064 process exits/bytes; close_contract/tab_identity_guard/tab_ownership →059,063 preserved ownership; completion_contract →061 model proof plus future Popup path; destructive_generation/replacement_contract/quit_contract/inline_draft →059–063 preserved guards with source path separation; row_identity/row_actions_contract/undo_contract →060; switcher_contract/quick_switcher_contract →063; footer_unicode →058/063 shared Unicode projection; paint_contract →058 readable paired styles but not action ownership; perf/perf_baseline →067 measured restored work, not Debug equality; visual/baselines/tablepro →005 oracle baseline and064 full-frame+semantic closure, never implicit blessing. Each exact path/blob/line count is independently listed in the read ledger. No execution of these main tests is claimed by this read/mapping checkpoint.

### BD-JT-10 — domain preservation does not imply World policy preservation

Main Jackin account/agent/usage/workspace/onepassword domain models retain their source algorithms (640/280/590/607/37 exact-equal old lines respectively; all remaining old lines also read). Preserve added documentation, checked indexing, redacted EnvValue/OpField/Secret Debug, typed RunId and captured workspace-save records. In main `sim/world.rs:179–280`, World owns expected/proposed payloads, due time, fail-once consumption, identity allocation and exactly-once completion independently of the active screen. Manager operation sequence/review watermark at163–177 likewise removes stale UI replay. These are completed main ownership mechanisms, not code to replace with legacy callbacks.

World policy has nevertheless been deleted: oracle `sim/world.rs:19–85,157–178` includes global coauthor/DCO, mounts/env/role-env/auth modes, typed filesystem inventory, takeover deadline, session account choices and first-use discovery state. Main `:29–34,103–150` retains only trust in GlobalConfig and omits those fields. Main `:402–404` hardcodes Sync, while oracle `:324–330,387–394` reads each configured mode and suppresses Ignore launches. Counterexample: Settings → Agents → Ignore → return to session picker must retain the source disabled reason, which cannot be represented by the current World policy. Restore domain fields and typed completions under TASK-051/053/054/055 and their actual source consumers, keeping the new operation authority. JA-026–029 and JA-045/048 require joined state+frame proof, not Settings labels alone.

Additional exact World differences require source-qualified consumer proof: main `:305–310` ticks every daemon; oracle `:213–220` only ticks Running instances. Main `:381–382` overwrites arbiter discovery whereas oracle `:289–292` preserves a discovery error. Main `:385–388` replaces the visible instance-id allocator (`oracle :309–312`); typed opaque RunId does not authorize changed public instance IDs. Main lacks the timed takeover producer at oracle222–235. Restoration owners TASK-051/055/056 must prove stopped-daemon immobility, failed-discovery retention, exact newly launched IDs and JA-057 takeover producer separately from a seeded takeover view. This checkpoint records source differences; whole-app reachability and task payload coverage remain under review.

### BD-JT-11 — extracted diff fixture quietly removes hunks and source text

Main `sim/changes.rs:13–248` correctly moves app-owned data out of the retired widget and supplies a reusable projection seam. Keep that architecture. But `hunks_for` at275–347 now emits exactly one hunk for every extension. Oracle `sim/changes.rs:65–103` gives retry.rs three hunks; main278–287 keeps only the first. Config loses its default-backoff function and shifted start; Markdown, TSX, YAML and Terraform snippets are shortened/rewritten; added/deleted/renamed fixtures lose lines (`oracle :104–263`, main288–404). The old test's exact three-hunk assertion at318/328 was removed in main445–460. Counterexample: Inspect retry.rs, jump next hunk twice; source has content and line numbers that the extracted fixture cannot supply. Root condition: fixture data was treated as disposable sample text during renderer decoupling. TASK-056 must restore exact fixture payloads into the new owned model; TASK-056/JA-060 proves compact/advanced file selection, hunk navigation, full text/counts and the already-adjudicated dropped-copy event. Do not reintroduce widget-owned domain data or interpret F09 Deferred as permission to drop content.

### Jackin service read checkpoint

Main launch527/provider558/onepassword847/changes496/world624 and their full paired legacy sources have been read. Launch retains the11-stage timeline, build log, credentials hold/retry, network failure and modeled-only blocked branch while adding typed RunId and safe indexed access (410 exact-equal old lines,22 non-equal). Provider retains every old line (542 exact-equal) and validation/error/usage policy. 1Password preserves lookup gates, fixtures, masked description, source classification and closure-only material access (673 exact-equal,16 non-equal), adding redacted Debug and replacing a prior expect with a typed missing-field error. No actual app consumer parity is implied by these retained services; payload wiring and UI flows are still being reviewed.

### BD-JT-12 — account precedence extraction drops effective-set policy and explanations

Oracle `domain/fixtures.rs:84–208` constructs ordered candidates constrained by the workspace effective set; includes enabled-without-preference fallback; tests enabled status separately from later launch readiness; retains every shadowed decision and exact explanation. Main `domain/fixtures.rs:118–186` returns early, checks explicit selections only for provider/enabled/Available, omits effective-set membership and final enabled fallback, then accepts provider default without readiness filtering. Counterexample: an enabled Available session account excluded by workspace policy is selected by main125–133 but skipped by oracle91–106; an enabled but unavailable source choice is retained for source explanation/readiness UI but skipped by main. Main's typed optional precedence is useful; its changed selection algorithm is not source UX authority. TASK-051 must restore ordered source candidates and diagnostic payload under that typed representation; TASK-052/054/055 must prove workspace disabling, session selection, lifecycle readiness and shadowed explanations as separate concerns, including JA-023/045/048. `World::offer_for` filtering a later selected ID against ready accounts does not repair `account_for` consumers or missing explanation data.

Both main fixture files (824+1296 lines) and every old fixture line were read (1154 exact-equal mapped against pinned.rs,662 non-equal old lines). Production `world_for` uses pinned graph, not compact `fixture_accounts`/`fixture_workspace`; retain that separation and do not mistake extra compact test aliases for production duplicates. Main pinned preserves most account/window/instance fixture payloads and explicitly canonicalizes role identities across allowed/default/last, role-env, account preference and instance fields at1191–1253. Preserve coherent keyed identity while proving source-facing label/why projection separately. HardCases main world initializes daemon health Healthy (:533), whereas oracle fixtures1652 initializes Stale; first-use discovery and45s hard-case takeover are omitted (oracle1627/1657). TASK-051 fixture proof must assert these source states before app replay, not merely counts or buildability.

### BD-JT-13 — PTY model preserves boot, rewrites interactive transcripts and deadlines

All2099 main PTY lines and1740 legacy lines were read (1246 exact-equal,494 non-equal). Preserve app-owned transcripts/revision/retention and safe public SplitModel delegation, stable pane identities, recursive topology, source boot scripts and keyboard nearest geometry. But main `sim/pty.rs:1258–1278` collapses three Claude/two Codex/two other-agent reply rotations to one shortened response. Oracle `:927–1011` has full styled lines and later permission prompt. Main `:1439` echoes input immediately; oracle1174–1184 queues the echo at700ms then each reply350ms apart. Main permission deadlines350/750/1100 and rejection250 (`:1416–1429`) replace source400/900/1500 and300 (`oracle :1140–1164`); styles, text and final changed-file count also differ. Oracle1186–1188 restores awaiting_permission/Blocked when a rotating reply contains y/n; main removes that path entirely. Shell git push/log/cargo responses are shortened (`oracle :1213–1238`, main1466–1479).

Counterexamples: immediately after ordinary input source shows Thinking until700ms but main already shows quoted echo; send enough Claude inputs to reach the third reply and source blocks for PR permission while main repeats the first response; shell git log loses its second commit. Root condition: only boot transcript and coarse end-state tests bracketed simulator extraction. TASK-055 must preserve exact output spans and virtual deadlines; TASK-055/JA-050 must exercise each rotating reply, permission accept/reject, due-minus-one/due boundaries and shell command branches, then full viewport/cursor state. Keep source-owned safe simulation and reusable viewport; do not copy the retired widget implementation back. New snapshot reconstruction at1690–1769 and saturating pane allocation at1796–1799 need separate ownership tests; they are not immutable evidence of reconnect fidelity.

### BD-JT-14 — timing constants survived; choreography did not

All main rain577 and legacy951 lines were read (214 exact-equal,737 non-equal). Main preserves phase constants, duration wording and12-frame handoff stage enum, but replaces the source mixer,78-glyph pool, radial xorshift/f32 starfield, streaks, respawn and dimming with unrelated sparse integer cells (`main rain.rs:26–45,156–200`; oracle22–47,275–416). Its ladder maps zero to Primary instead of Ghost and dim subtracts toward brighter roles (`main80–97`; oracle54–85); dim_buffer loses source glyph removal/background/semantic-color branches. Paused intro skip jumps directly to end in main335–337, while oracle561–569 still jumps phrases→warp. These are not rounding-only render differences.

Actual app consumers confirm missing restoration: `app.rs:3944–3988` draws whole phrase chosen by tick/P1_LEN at top, no typewriter/glitch/pill/warp;4933–4975 paints diagnostic handoff/outro labels instead of cross-fade/field;5571–5672 draws launch stage labels and clipped log lines without atmosphere. Source counterpart `rain.rs:589–637,758–848` supplies the omitted frame functions. TASK-051 owns source choreography and TASK-054/055/056 joins lifecycle handoff/outro; JA-002/003/040/041/062 must compare full exact frames and skip/resize behavior, not phase labels or self-determinism. Keep the new App/Cx scheduler and semantic painter ownership; restore the source algorithm through the public painter rather than retain the temporary integer redesign.

Main `app/historical_paint.rs`569 lines were also read completely:32 explicit semantic fg/bg pairs, targeted Mono rules preserving intentionally invisible fills, public Ui style-default resolution, and equal-RGB distinct-origin tests are substantive architecture. Preserve that token provenance and collision proof. This palette contains no choreography and cannot establish source animation parity; pair contrast tests do not cover interaction or full frames.

### BD-JT-15 — service-backed modal flows replaced by hardcoded branches

All6954 main App lines and2455 legacy modal lines have now been read. Main `app.rs:1314–1373,2040–2070` supplies exactly one account, one vault, five items and one field, advancing immediately; only the final reference consults `world.op`. The retained `screens/op_flow.rs`365-line model is not imported by App. Oracle `screens/modals.rs:1577–1955` enumerates actual service accounts/vaults/items/fields, stages loading until `now_ms + latency_ms`, labels locked/authorization/denied states, preserves error guidance, searches each stage, disables nonconcealed fields, and rewinds/retries. Counterexample: open the actual picker, select Shared/no-access vault or a locked account; main cannot reach either source branch despite retaining those simulator fixtures. TASK-051's shared PickerChain composition and TASK-052's account consumer must restore the actual service-driven stages; other callers TASK-053/054 require their own bindings. Preserve reference-only metadata and typed state; do not restore bespoke widget ownership. JA-032/034/037/038 and every other OpFlow caller must distinguish keyboard completion, simulated loading boundary, retry, back, cancellation and direct seeded error evidence.

FileBrowser is similarly disconnected: main `screens/file_browser.rs`215-line keyed model is not imported; `app.rs:2570–2597,3990–4033` advances fixed Prelude strings and creates only a host mount with source==destination. Oracle `modals.rs:167–449` browses the actual filesystem (directories before files, parent row, keyboard Enter/Right/l open versus Space/s choose, Backspace/Left/h parent, path edit/validation, current-row click open, wheel/scrollbar, read-only checkbox, URL toggle/lookup); `prelude.rs:140–369,400–477` owns host/git destination, optional edited destination, mapped child workdirs, precise name/path errors and rewind. TASK-051 owns this shared app composition and actual Prelude journeys JA-012–015; TASK-053 consumes its editor/settings browsers. Keep the keyed projection but restore the source producer and full callback flow. The optional `resolving` field/tick is not evidence that ordinary URL Choose is asynchronous: oracle231–243 resolves synchronously; timed resolving remains a separately identified seeded state unless a producer is found.

### BD-JT-16 — keyed component events discarded by application selection shortcuts

Main `app.rs:1966–1975` handles `PickerAction::Chosen(key)` but, when adding an editor role override with a nonempty query, ignores that key and takes the first role whose text contains the query. Counterexample: query matching two roles, move to the second, choose it; the first receives the override. Root condition: app reimplements filtering instead of resolving the component's stable chosen identity. TASK-053 must resolve the returned key against the current catalog (with stale/removal rejection), retaining the public Picker and qualified role identities; proof must choose a nonfirst filtered match and reorder/remove the catalog. This supplements parent JA-020/022/024 environment/role journeys; a model-only key test or first-match UI replay cannot close it.

### BD-JT-17 — captured operations exist, but live app still substitutes first workspace

Test distinction: main `tests/editor_save_journey.rs:39–42` checks only the first match of svc-01, so it preserves canonical identity downstream but misses BD-JT-16's second-match substitution. Keep its complete captured-save assertions and add the nonfirst-selection adversary. `tests/manager_repository.rs`137 lines and `tests/world_cwd.rs`58 lines prove exact current-directory mount association, catalogue payloads, reordering and stale-target rejection; these are model-level progress, not evidence that App's first-workspace shortcuts use that authority. `tests/support.rs`136 lines correctly routes keys/mouse through the public Runtime and separates app_mut fixture setup; retain that boundary. Its200ms wake helper coalesces one product tick and is not a generic virtual-millisecond advance.

Main `screens/manager_actions.rs`908 lines retain captured target/payload, due-time, review acknowledgement, world operation watermark and stale completion guards. App never stores a ManagerActions session and explicitly discards ManagerOperation messages at `app.rs:3871–3875`. Its live launch chooses `world.workspaces.first()` at1435, editor open does likewise at3441–3444, cockpit labels prefer first at784–796/5616–5622, and Capsule header uses first at5949–5965. Counterexample: select a second workspace, edit or launch; durable target and labels still belong to the first. The source route contract carries explicit workspace/pending/instance identity (`screens/mod.rs:145–164`), and source Prelude sends its actual pending workspace (`prelude.rs:351–368`). TASK-051 manager and TASK-054 launch must wire retained captured-target authority into actual actions; TASK-053 editor and TASK-055 Capsule must project that same target, not infer identity again from list position or first fixture. Direct manager ownership tests remain useful but cannot count as executable app closure.

### BD-JT-18 — frozen screenshot painting masks a different interactive composition

Main `app.rs:4036–4704,5016–5364` paints hardcoded Capsule, Editor and Manager strings/coordinates; draw calls it after the live controls and layers at6324–6326/6397–6429 only for selected120×40 paused states. Manager live rows start with workspaces (`1043–1088`) while the overwritten image starts Current directory (`5240–5258`); the displayed Editor general fields have no corresponding update controls (`2745`,4912–4930). Counterexample: click the visible Current directory row or Name field in that frozen image, then resize to another supported size; underlying actions/geometry and display are unrelated. Root condition: separate visual imitation accepted as parity while the real app remained incomplete. Preserve HistoricalPalette's semantic origin resolution, remove the screenshot-specific second composition as source behavior is restored through reusable components. TASK-051/053/055 and JA-005/006/016/017/046 require exact full-frame plus focus/hit/action invariants across sizes, including first action and resize from the previously matching frame. This is not authority to delete main completed ownership or to bless its generic replacement layout.

### BD-JT-19 — dirty-exit labels and effects are inverted; source branches absent

Main `app.rs:3580–3625` paints three status choices Stay/Exit/Cancel; Down clamps2, and only choice>=2 exits, marking the instance CleanExited. Thus its displayed Cancel exits; displayed Exit does nothing. The378-line CapsuleInteraction model's four enum options are neither source choices nor the app's live reducer. Oracle `screens/capsule.rs:1080–1117,2296–2333` has Start new agent, Inspect changes, Exit&keep, Exit&discard, plus a separate Cancel button; discard opens a typed-confirmation facts Dialog. TASK-056/JA-059 owns restoration, with TASK-055's shared session operations retained. Require each exact source branch, Cancel/Esc no mutation, kept dirty state, discarded nested confirmation and correct other-running/last-instance arbitration. Do not use main labels or its model count to derive the expected source choices. Source modal is an opt-in ChoiceDialog/Form bridge consumer under ADJ10, not permission to alter the shared default choice behavior.

### BD-JT-20 — live UI omits behavior even where isolated models support it

App-only counterexamples now source-read: main mount Edit unconditionally sets first mount readonly+Clone (`2693–2699`), Roles sets one fixed default (`2707–2710`), General has no update (`2745`); environment save pushes directly and bypasses the pending model's duplicate-name checks (`2660–2677`). Capsule menu/palette Split/Zoom/Focus/Detach reach `update_command` without prefix, but their effect arms require `capsule_prefix` (`3481–3537`); tab close only writes a status (`3414–3416`), rename changes an app string rather than daemon title (`3126–3131,5710–5714`), and fake Shell/docs tabs are added to shorter daemon tabs (`5704–5716`). Inspect paints fixed src/main.rs/lib.rs text (`5882–5910`) instead of its retained Changes fixtures. Restore through TASK-053/055/056 actual reducers and reusable component composition; component/model unit tests remain prerequisite mechanisms, not source app proof. Exact source-screen read and complete per-action task mapping for these families remain in progress; this checkpoint does not claim whole Jackin parity coverage.

### BD-JT-21 — HardCases launch plans have executable producers

All2659 legacy Manager lines now read. Oracle `screens/manager.rs:361–386,2253–2293` captures selected workspace/default role, then chooses CredentialsLocked for matching locked account identities and otherwise BlockedSidecar for role sre in HardCases. Fixtures359–392 define workspace2 infra-control-plane with default sre;1651 starts locked. Main App1393–1398 keeps only LaunchFailure versus Clean, losing both producers. Do not equate a modeled-only blocked *stage* (no halt) with a seeded-only launch *plan*.

Disposable real-App replay `/tmp/jt-routing.fLfB8q/jackin.rs::routing::hard_cases_real_manager_selects_blocked_sidecar_without_seeded_plan` passes at80×24,100×30,120×40,160×50: fresh HardCases Reduced0, settle the initial route, Home Down Down Enter Enter produces Cockpit workspace2, role sre, plan BlockedSidecar. No scenario/model/plan mutation. Exact imported App, Manager, fixture and Launch bytes were independently checked equal to oracle. Command: `rtk proxy cargo test --manifest-path /tmp/jt-routing.fLfB8q/Cargo.toml --target-dir /private/tmp/tui-snap-audit.656lHG/oracle-clock/target --bin jackin-routing-audit hard_cases_real_manager -- --nocapture`;2 tests, four sizes each PASS. This is functional diagnostic evidence through the existing Cargo shim, not the compiler-identity timing attestation.

TASK-051/054 must preserve these real plan producers and join JA-011/043 with exact selected target/account and stage observations; existing JA-043 seeded LaunchPlan fixture still has value but does not exhaust executable creation. A second independent real-App test in the same module also passes all four sizes: HardCases Reduced0 → settle initial route → Home Down Enter Down Enter creates workspace1/Codex Primary with CredentialsLocked, without mutating fixtures or plan. Both producer classes are now replayed; stage recovery remains a distinct obligation. Source Manager also confirms narrow drawer<100, wide roster>=150, selectable pane attach, source ordered rows, five-second last-good refresh, per-status action availability,1800/2200/2400ms stop/purge/prewarm and selection survival. TASK-051/JA-005–011 owns those actual behaviors while retaining main's captured operation reducer; no missing app integration may be closed by model-only tests.

### BD-JT-22 — retained performance row names do not prove retained workloads

Main `tests/perf.rs`181 lines and five-line baseline fully read. The Capsule fixture really constructs four panes with2000 lines each (66–105), and the benchmark draws actual App, but its comment149–150 says no scrollback clone while App5823–5840 allocates owned Strings for every line on every draw; App1804–1813 independently repeats whole-line projection during update. Baseline text claiming17 allocations and test126 asserting a removed clone-row name cannot prove this cost disappeared. Manager benchmark name says100rows, but only expands existing14-workspace/17-instance HardCases fixture (53–60) and logs counts instead of asserting100rows (137–141). TASK-055 must provide borrowed styled transcript projection through the reusable viewport, preserving source spans/caret/selection; TASK-067 must measure the exact declared workload and reject incorrect row/line counts before timing. Keep allocator and actual public-runtime benchmark boundaries; do not retain misleading legacy row labels as workload evidence. No performance run or measured allocation regression is claimed here.

Main `tests/timing.rs`164 lines fully read: exact early/coalesced/paused/monotonic deadlines, input/draw nonaging, source frame-seek/world-clock separation, dynamic job acceleration, dismissed deadline and active modal continuation are real scheduler-contract tests. Preserve them and connect restored source busy/browser/validation producers to the same admission owner. They prove timing ownership, not omitted source animation frames or service state production.

### BD-JT-23 — migrated tests sometimes ratify lost behavior

All main Jackin test files now fully read. Pairing old `app_tests.rs`1238 lines with main1327 maps981 equal lines and257 explicitly read changed/deleted old lines; chrome186→207 maps176 equal and10 changed. Source `app_tests.rs:538–551` explicitly opens masked environment values with `m`, asserts the plain value and “plain · shown”, then masks again. Main `tests/app_tests.rs:521–538` changes this to always-masked, and its visibility-named test591 merely calls that same test. This is a changed source requirement, not stronger source parity. TASK-053 must preserve intentional source reveal only for the source-enabled value type while retaining sensitive Debug/redaction and masked default; add exact reveal/remask and no-unrelated-secret assertions rather than preserving the altered expected result.

Old `app_tests.rs:1114–1126` registers a sole agent account in a first-use world and Enter launches from Current directory. Main `tests/app_tests.rs:1192–1213` inserts a workspace before opening Launch, hiding the newly introduced prerequisite; TASK-051/054 must replay the zero-workspace producer without model insertion. `tests/app_tests.rs:275–331` now asserts one-account/one-vault picker sizing and cannot certify the source multi-account 1Password service described in BD-JT-15. Existing64 main digest rows cover8 scenarios×2 sizes×2 themes×2 colors and establish only main's own baseline; they are not immutable-oracle comparisons. Keep all independent Runtime lifecycle, diagnostic, clock, durable identity, captured-save and stale-operation tests (`preview.rs`, `manager_identity.rs`, `world_fixtures.rs`, `editor_save_ownership.rs`, `manager_actions.rs`); TASK-057 closes Jackin validation and must preserve these completed domain/ownership contracts, not discard them. TASK-004/056 must join source full-frame and semantic assertions, not rename these weaker tests as oracle receipts.

### BD-JT-24 — global settings requires real scoped state, not selected labels

Old `screens/settings.rs`1288 lines fully read. Its two independent commit toggles, scoped Mounts/Environments, per-agent supported auth-mode cycle/reset, Trust current-row toggle/source-open status, bracket tab switching, dirty/error badges and source focus navigation all act on one pending GlobalConfig. Preview155–314 rejects invalid environment keys, displays blockers and sensitive paths, distinguishes Clean up & save / Save anyway / Save; save316–364 freezes input, completes after900ms, retains draft on permission failure and publishes only on success. Unsaved exit366–389/1114–1138 has Cancel/Discard/Save; save failure returns focus to Save. Counterexample against main's reduced global/state paths (BD-JT-10/20): changing an agent mode to Ignore must alter subsequent session availability only after successful save; changing selected trust row must not unconditionally toggle row0. TASK-053 must integrate retained Config/Settings models with reusable controls and the World save owner, restoring these exact branches and JA-026–029 Settings families. The source's odd trust propagation guard349–357 is parity input, not permission to silently replace it with a redesigned policy.

### BD-JT-25 — Usage, Cockpit and Inspect carry independent scroll/focus/error contracts

Old Usage963, Cockpit1012 and Inspect1016 lines now fully read. TASK-052 Usage must retain grouped sorted account identities, nonselectable headings, Overview, independent list/detail offsets, list wheel versus detail wheel and scrollbar tracks; width<100 swaps regions, Enter opens detail, Esc returns list before Manager, `m` preserves selected account. Refresh128–160 admits enabled accounts once with provider duration plus120ms per index; outcome866–888 preserves last-good/retry for rate-limit/provider/missing-file failures. This is distinct from generic “Limits” text or refreshing every record. Require JA-039 and the source stage contributions with exact account/freshness/offset observers; main `tests/app_tests.rs:383–395` text-only navigation cannot close these branches.

TASK-054 Cockpit must preserve source credentials Retry / plain-text alternative / cancel740–772 (plain-text is a source fixture shortcut, not an actual input Form), failure run-id copy that reopens its failure acknowledgement701–706, cancel versus Ctrl+Q quit versus Ctrl+C abort, log-before-output status605–618, wrapped styled build spans509–528, independent log follow/scroll/drag,11-stage state/duration and frozen atmosphere. Captured target/account fixes must preserve complete effective account set99–113, not just the first agent's account. TASK-056 Inspect must compose retained real ChangeSet with reusable Diff/Tree/Splitter: compact current-row click opens, unpushed row does not; Advanced cursor movement previews but pointer activation follows Tree's own event; width<90 stacks and disables seam drag; Tab switches region; Esc clears diff selection before returning tree, then returns exit choices or closes according to entry. Unified/review and compact/advanced are independent toggles. Source compact250 and Advanced343 discard Diff copy events: preserve F09 no-clipboard branch and do not infer delivered clipboard from the visible `y Copy` hint. JA-060 plus earlier contribution owners must prove these state transitions, not generic border/text. These are source-qualified restoration obligations; no assertion that current component APIs already implement them.

### BD-JT-26 — shared Config source has explicit non-atomic compatibility branches

Old `screens/config.rs`2345 and `screens/editor.rs`2230 lines fully read. TASK-053/JA-016–029 restoration includes global/workspace column differences, only configured role sections, foldable role headings, source-key rows/tombstones/undo, plain-only reveal, reference metadata inspector at width>=150, source-scoped browser and value-source Forms, exact changed/removed preview and pending/original split. Preserve main captured World save and qualified role identity; do not restore source's fragile completion routing1592–1605 merely to copy old ownership. Editor uniqueness validation is case-sensitive and excludes current workspace; workdir picker projects mount destinations and immediate host child directories, role default requires allowed role, disabling an account clears its workspace and role preferences, and preference requires active account. Source keyboard and pointer differ: Roles current-row click sends Space (allow toggle), not Enter (default); Accounts current-row click toggles active, unlike global Accounts rows. Proof must observe selected identity and each actual action, not universal current-row activation.

Three source quirks require explicit immutable-UX disposition before declaring validation/preservation universal: Config2199–2208 removes original environment row before checking duplicate target, so renaming A to existing B reports error but removes A from pending; scope move2026–2038 deliberately replaces a same-key destination row; numeric isolation1/2/3 at1309–1315 bypasses the running-isolated guard applied by `i`1295–1306. Main's atomic/guarded model progress must not be destroyed wholesale, but a generic new invariant cannot silently alter these visible source branches. Parent notified; adjudication remains open here. Suggested structural boundary is an explicit source-qualified app transition over retained typed pending state, with old/new-oracle assertions and approved disposition, not weakening reusable generic field validation. Counterexample tests must capture pending/original before and immediately after rejection, not only the error label or eventual cancel.

### BD-JT-27 — global account operations are not the reduced generic account list

Old `screens/accounts.rs`2625 lines fully read. TASK-052/JA-030–039 must preserve selected Account/Provider/Overview/Add scope, filter over name/provider/identity/status/plan, folding and fallback to provider on hidden selection, independent inspector reset/scroll, width<100 summary/drawer, action focus suppressing global letter shortcuts, per-scope refresh with160ms stagger (not Usage's120ms), disabled selected-account refresh versus enabled-only bulk refresh, last-good freshness and retry outcomes. Form source/provider disclosure, matching labels, optional endpoint, source fingerprint/reference duplicate detection,64-character name bound, material-level save gate, discovered-folder promotion, first registered provider default and provider/registry cleanup are actual transitions, not render metadata. Row `v` validation schedules900ms and plain-key revalidation deliberately cannot recover secret material; main retained provider models need the actual source UI dispatch rather than fabricated successful status.

Important source asymmetries: a duplicate display name is only a post-save warning893–975, not a rejection; discovered accounts expose only Refresh in visible action row1118–1133, while keyboard `v`1997–2000 has no mutation-allowed guard. Form Validate is synchronous2363–2378, whereas row Validate is scheduled1047–1061. Do not invent async Form validation or universal read-only event suppression from the hint text. Current-row global Account click2212–2221 selects only; same Add row opens, unlike Editor Accounts' toggle. Retain immutable source behavior unless explicitly adjudicated, and preserve main stable-key/model architecture when wiring it. Model consistency tests do not close real App dispatch, first-visible-frame, resize, hover/press/release or stale selected-account effects.

### BD-JT-28 — Capsule's actual mode routing differs from its priority comment

All2962 legacy Capsule lines read. Source `mode()`158–170 reports Dialog/Drag/Select/Prefix/Normal, but that is not the full input admission algorithm. `on_key`1872–1966 checks takeover, context/menu, F10/Ctrl+Q/palette, prefix, new prefix and split-resize before active-selection consumption; it does not suppress all keys while seam drag exists. `on_paste`2180–2194 goes directly to the focused simulated pane, removes CR/LF and has no length cap or takeover/menu/prefix/drag check. Shell `app.rs:489–502` blocks paste only for actual modal stack entries, not these Capsule modes. Menu Paste408–427 instead forwards only the first200 characters including CR/LF. A single generic “paste follows mode priority” rule changes source behavior. TASK-055/JA-050/051/054/056 must enumerate each route, preserving reusable runtime isolation through explicit app-owned compatibility only after exact disposition, not fake focus or a generic overlay passthrough.

Other concrete source contracts: prefix timeout2000ms; Ctrl+B then Ctrl+B1258–1263 returns Changed without forwarding despite its comment; `focus_pane_by_id`156 is empty, so seeded/Manager pane argument does not itself move daemon focus. Keyboard directional resize768–825 checks pane minimums and rolls back percent, while seam drag2116–2127 delegates SplitState directly. Drag-copy selects only pane IDs0..63; menu-copy/key-y/doubleclick/drag-release mutate `world.clipboard` without the shell's clipboard-generation increment used by Request::Copy. Preserve these distinctions in observers; do not infer host clipboard effects from status strings. Current branch typed pane/interaction models must stay, but isolated enum/state tests cannot replace these actual command paths. Existing BD-JT-13/19/20/22 supply main-side counterexamples and structural repair owners.

Capsule Usage1130–1158 projects unique accounts actually present across daemon panes, falling back to global defaults only when none; focused account selects its tab. Its `r`2634–2637/2666–2676 is an800ms local spinner only: it never schedules provider refresh or changes usage records. This differs intentionally from Accounts/Usage screen refresh. Arrow/h/l changes tab and resets scroll; pointer tab2651–2655 keeps scroll, and labels overflow by stopping at the first non-fitting item2729–2733. TASK-055 contribution and TASK-056 final application proof must retain both observable paths, not strengthen the read-only overlay into a service mutation. Export1019–1047 is simulated status only, rejects any `..` or `/etc` prefix, rejects trailing slash, and treats literal `cancel` specially; no actual host file action is authorized by its text.

### BD-JT-29 — shell restoration must retain real arbitration and event-specific ownership

All2662 legacy App lines read. Retain main's existing public Runtime, scoped lifecycle, monotonic admitted clock, typed identifiers and captured World jobs. Restore route-specific entry/exit and component composition rather than copying the old Screen trait, mutable RenderCtx or a second focus/hit registry. Source app505–683 orders minimum-size key guard, rituals, modal priority, host menu, editing-qualified global keys, screen handling and finally Tab traversal; the menu's action adapter754–813 routes to the same screen actions and blocks switching away from dirty editor/settings/prelude except their own exit path. TASK-051/052/053/054/055/056 need exact menu and direct-key equivalence where source actually provides it, not aliases inferred from labels.

Arbiter core is byte-identical across branches, but its consumers matter: source duplicate/unknown entry228–243 still enters Manager with distinct status; stopped-instance attach2040–2053 rejects; detach2098–2125 preserves running panes and selects the detached instance; last-instance exit2179–2231 distinguishes Outro, StillInside with6000ms roster, AlreadyEnded/Unknown with2500ms delayed quit. TASK-051/056 and JA-001–004/059/061–066 must observe domain, selected owner, timer and route alongside full frames. Main's retained arbiter unit correctness does not prove these restored consumers.

Source input asymmetries remain evidence, not automatically approved redesign: minimum-size guard exists only in on_key506–511, so tick/paste admission340–348 is not universally frozen; mouse Quit1822–1825 sets quit without the keyboard route's arbiter release1353–1356. Modal Form paste489–495 calls Form directly without shell `form_changed()` notification; key and click Changed events1427–1429/1860 do invoke it. Browser/Form/Choice outside-click is consumed1799; Picker/Op cancel1780–1783, typed facts stay1761–1764, custom dialogs opt in1792–1797. Exact reached states must be replayed before changing generalized task invariants. The safe architecture boundary is typed app transition/event routing plus reusable components, never label-driven fake actions, hidden focus grants or discarded main ownership improvements.

## Final coverage and handoff

137/137 source blobs read and independently identity-rechecked against the branch inventory:92 Jackin,45 TablePro. All44 removed legacy app files (35 Jackin,9 TablePro) equal their immutable-oracle counterparts byte-for-byte. Every ledger row now has a concrete retained/restored responsibility and task/finding mapping; none remains pending or inferred from a file count. Module exports, manifests, CLI/lib/main plumbing and supporting tests are included, not omitted as “non-UI”. Source matched-line pairing is recorded with exact equal/non-equal line counts; complete changed old lines were read independently after current files.

This report identifies29 bounded finding families, not29 exhaustive runtime tests. Existing task owner groups051–056 and058–063 remain the implementation route;057/064 are validation-only application closure, through shared component producers;067 owns measured performance and073 instrumentation qualification. Parent must incorporate missing finite variants/explicit source dispositions into those contracts and synchronize final traceability before claiming plan closure. BD-JT-26's non-atomic config branches and BD-JT-28/29's event-specific quirks are open adjudication inputs here, not permission to weaken generic runtime or pending-model guarantees. No application source/golden/AGENTS file or commit was changed. Only source-proven disposable oracle replays described above were executed; complete main test execution, full component parity, future golden equality and product performance remain unclaimed.
