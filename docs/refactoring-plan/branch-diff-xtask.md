# Whole-branch xtask review

Scope: all18 `xtask/` additions in `branch-diff-inventory.tsv`, main7b27732a8c3c131760ec3438f641cb3c11343a42 versus Holla2e2401393c47360741ebd321679de08982dca50a. Entire added source is the entire changed hunk set. Immutable product oracle remains02f5294bfdbf38004cc49130d0aff1d01f31434c. No builds, execution probes or source edits.

Coverage complete:18/18 full files;17727 source lines total, including main.rs1–11816 and every embedded test body. Every path is added, so its complete source is also its complete changed-hunk set. Per-file manifest records exact blobs, read mode and source-derived disposition/task mapping. This is source review, not a claim that any test was executed or any checker is independently qualified.

Completed small-file group preserves real isolated no-default-feature consumer builds/runs and transitive dependency traversal. The two fixtures construct Runtime and testing Harness and exercise key/chord types; they do not prove that application update/draw callbacks execute. TASK009/031/065/072 must retain the independent compiler/runtime capability boundary. Locked registry closures are separate from the main workspace feature union; both complete lockfile bodies are covered (second through full first plus every exact delta).

The doc and named-test allowlists are historical shrink-only mechanisms. Obsolete Caps/Fixture/Scene spellings are not a command to reintroduce retired APIs, and `capsule_pane_clone_4x2000` explicitly represents a removed architecture subject, not an unimplemented required clone. Current dispositions and qualified test identities/assertion bodies remain TASK007/008/068/071's authority. The unresolved Choice label-geometry name cannot close TASK018 merely through allowance.

`historical_additions.rs` correctly narrows its exception to the pinned499-capture/998-output installation, verifies1497 original inputs and fixed review/license/provenance hashes, rejects missing/extra/tampered outputs, and excludes any path already present at the comparison base. The independent history audit already verified those998 outputs and1716 preserved archive entries. Preserve this recovery integrity mechanism; it is not visual or immutable-oracle approval.

## BX-01: expected case is not joined to the receipt's declared case

`capture_wiring.rs:126–130` reads `<expected.recipe_id>-join.json` inside the expected-case loop, but `capture_contract.rs:334–336` validates only that `join.case` appears somewhere in the plan and matches its self-declared hash. Later provenance/artifact checks remain keyed to the loop's expected case. Concrete static counterexample: choose A and B for the same application at different sizes; change B's join to declare valid case A and recompute `case_sha256`, leaving B's path/artifact/run/provenance fields unchanged. The executable hash stays identical and all examined predicates remain satisfiable although observation B declares case A. Root enabling condition: membership in a finite plan substitutes for equality to the expected member being verified. TASK070/072 must require exact expected case identity/content equality before accepting any join; protected host index membership must bind the same case. No probe executed; this is a source-derived acceptance hole.

## BX-02: old offline manifest verification is not compiler execution authority

`capture_build.rs::build` genuinely launches a locked confined Cargo build, but `verify` later reads caller-held `cargo-messages.jsonl` and manifest, compares their recalculated hashes, and labels parsed entries actual compiler artifacts. A fully rewritten self-consistent transcript/manifest/binary/dep-info set has no independent launch authority in this layer. Source/input fingerprints and deny-default reads improve reproducibility but cannot establish that those bytes came from a compiler invocation. Preserve the build/input boundary and reject local undeclared build executables as currently scoped; TASK001/070/072 acceptance must use independently protected observer receipts, not candidate-rehashed logs. Constructor-only fixtures do not prove production draw/update either.

`app_inventory.rs` correctly pins exact required app/target identities, but its historical Holla132 namespace fixes794b095 and frame4000. That is explicitly separate from full current02-oracle obligations. `capture_wiring` correctly refuses unsupported Paper CLI capture instead of inventing `--theme`; current source-qualified production-view adapters need their own observed result. No old startup/suite count can close the entire four-app behavior matrix.

## BX-03: legacy parity traces and source fingerprints remain self-declared

Complete `parity.rs` review identifies independent gaps, not merely missing test counts:

- Lines1080–1091 accept any `keys:`/`mouse:`/`type:`/`resize:`/`wait:` suffix for the expected step kind, `observed=true`, and any64-hex state hash. Change expected `keys(Enter)` trace to `keys(Esc)` while keeping step index/kind and a synthetic digest; validation cannot distinguish the wrong action. Only Anchor payload equality is checked. TASK070 needs protected exact typed event payload and actual source-state observations, not a prefix or digest-shaped string.
- Lines1612–1615 parse trace/provenance directly into `serde_json::Value`, silently overwriting duplicate fields. Example: duplicate `observed:false,observed:true` survives as true even though capture_contract's strict parser rejects this class. Central strict parsing must serve all acceptance consumers; no legacy dispatch may bypass it.
- Lines1721–1772 discover source using `git ls-files --exclude-standard` and exclude generated `parity/**` except recipes. A production `include_bytes!` of an ignored file, or of `parity/payload`, remains an actual compiler input while its changed bytes are omitted. Root enabling condition: Git visibility/output naming substitutes for compiler input authority. Reuse the accepted complete-source/compiler-input/host frozen binding; do not preserve this weaker legacy fingerprint as a gate.
- Lines1332–1334 permit cursor x==width, while lines1364 and1404 count Unicode scalars rather than terminal cells; a preceding wide CJK glyph or combining mark gives a wrong reported coordinate. Lines1238–1269 compare serialized ANSI bytes and return the first mismatching recipe, not all canonical cell/cursor results. Preserve this as an archival byte diagnostic, not TASK001's qualified comparator/accounting result.
- The module's499-record local `baseline/before` archive is not current02-oracle source. Membership/nonempty checks at531–610 do not independently freeze its contents. A distinct reviewer string at1201–1219 cannot establish independent review. Existing TASK001/069/070/071/072 protected source and result authorities must replace those claims; preserve original archive evidence and recipe identities separately.

These are static source-derived counterexamples; no legacy recipe, compiler or acceptance probe was executed. No change to historical artifacts is authorized by finding their limitations.

## BX-04: capture command dispatch violates its own parser ABI

`main.rs:1846` retains the subcommand in `args`; lines1862–1864 pass that complete vector to three parsers expecting only operands. `capture-verify` expects four operands, `capture-build` two, and `capture-schema` three. A documented invocation therefore supplies one extra argument and fails usage before verification/build/schema work. `app-inventory` correctly slices `args[1..]` at1848. Root enabling condition: independently correct operand parsers have no end-to-end command-dispatch qualification. TASK070 must qualify actual executable argv for every operation, including positive complete invocations, rejection of extra/missing operands and recovery. This is a static arity contradiction, not an executed failure claim.

## BX-05: lexical and partial AST inventories can omit production obligations

`main.rs:1957–1971` drops traversal errors and turns failed reads into empty strings; `parse_files:6084–6088` silently drops parse failures. Several predicates then return success for no hits. Existing empty-root tests9006–9020 check zero aggregate files, not one omitted member among valid members. `is_cfg_test:3411–3418` treats any cfg text containing `test` as test-only; `cfg_test_only:4642–4654` similarly sees a test token without evaluating negation. Thus `#[cfg(not(test))]` production code is skipped by props/showcase scans. Root condition: heuristic discovery defines the accepted scope instead of an independently complete compiler/profile inventory. TASK007/071/072 must reject missing/read/parse members and bind production and test profiles separately; disabled cfg is not evidence of absence.

The string masker2058–2189 does not lex Rust character literals: a valid `let quote = '"';` can enter its double-quoted string mode and conceal following forbidden code. Single-line regexes also miss multiline signatures/calls, aliases and renamed raw-buffer receivers. For example `no_raw_background_parameter:5950–5968` requires `pub fn` and `bg: Color` on the same line; line wrapping preserves Rust semantics but removes its hit. `generic_component_copy_hits:4468–4488` has only four patterns and exempts every statement in rain.rs, not just app-owned rain paint. Preserve these as useful diagnostics, never as whole-source architecture proof. Current explicit custom-art exceptions and resolved production code-path mutants must decide acceptance; do not broaden bans to prohibit valid app art.

`declared_test_names:2867–2919` scans unqualified function spellings after literal `#[test]`, including cfg-disabled source, then collapses all targets/modules/profiles into a set. A same-named inert test can substitute for a required implementation; assertion weakening is invisible. `every_named_test_exists:3001–3064` does not execute those tests or enforce exact qualified inventory, and missing selected document headings can shrink its required set. TASK007/008/068/071 must retain source/assertion identity, compiled discovery, complete outcomes and protected allowance/closing-owner joins. Do not delete these historical tests merely because this particular inventory is incomplete.

## BX-06: syntactic reachability is not production phase or public API proof

Props calls lose their receiver/type/path at3494–3505; `resolve_calls:3563–3593` matches bare names within source scopes. `reaches:3596–3610` is syntactic and does not account for constant-false branches or different callback arguments. Two phase calls to the same helper with opposite disabled arguments satisfy the single-constructor graph while retaining draw/update disagreement. A dead same-named method can also lend false reachability. Keep the accepted intra-module constructor boundary, not a new cross-file helper architecture; TASK032–068/072 require source-qualified same receiver/configuration and actual phase observations for owned witnesses.

Showcase collects any path identifiers from a reachable function body5099–5105 and follows bare-name calls5212–5219. A body containing `let _: Option<Button> = None;` counts Button without calling its draw/update; a constant-false branch can contain the entire roster. Page dispatch4743–4754 collects match-arm patterns without proving the matched expression is the PageId argument or that the function is reached by App. The exact22 enum/list/nav/dispatch checks and named-page ownership are worth preserving, but not sufficient as runtime coverage. TASK032–039/073/072 must join immutable actual page identities, reached production methods and full component observations. Embedded tests9138–9442 reject comments/import-only/dead helpers/shared wrappers, but their positive fixtures mostly construct controls without drawing; that limits what they prove.

`collect_api:6850–7042` ignores visibility/reexport reachability and merges unqualified names; a private helper/member can satisfy a public-document reference. Rustdoc-based heading checks improve this, but component discovery7571–7631 includes only structs already carrying PARTS: removing PARTS from one component removes its documentation obligation while another component keeps the aggregate nonempty. Headings7640–7657 are checked, not their substantive answers. TASK068/072 require an independently frozen public roster and exact compiled facade identities, plus semantic per-heading proof. Preserve compiler rustdoc inspection and exact signature checks, while rejecting absent-member filtering as completeness.

## BX-07: dependency and legacy capture checks overstate their authority

`dependency_graph_is_exactly_the_declared_set:4007–4027` claims every path to backend internals crosses ratatui-crossterm, but tests only whether the entire inverted output contains that name. One allowed and one forbidden path can satisfy this predicate. `inverted_paths:4087–4089` also maps any failed Cargo command to an absent dependency. Root condition: a whole graph theorem reduced to substring existence and command failure reduced to absence. TASK009/065/072 must traverse resolved package-id edges and fail operational errors; direct set equality and backend-free real builds remain useful independent checks.

The legacy `capture-matrix` dispatch1861 selects `BIN_*`/target-debug paths and checks nonempty file/declared binary ownership530–717, not the confined build receipt. `capture_matrix_contract:1703–1753` checks old files and provenance without requiring the current source revision. Required shell fragments1608–1667 and1760–1831 can be supplied as comments while actual launch logic changes. Provenance1070 onward checks useful dimensions, artifact hashes and lifecycle fields, but does not itself establish protected execution/source authority. TASK001/070 must consume the protected observer's actual compiler, argv, source, lifecycle, cells and context-index bindings. Preserve staged executable/environment, no-shell argv, symlink/FIFO rejection, transactional rollback and concurrency tests10687–11816; fake tmux/converter fixtures establish transport safety only, not four-app visual parity.

## BX-08: legacy bless guard is not complete immutable parity/accounting

`diff_baseline:8025–8053` explicitly ignores removed keys; deleting every live baseline row therefore supplies no movement for this guard to refuse. `parse_baseline_entries:7983–8006` overwrites duplicate keys and accepts loosely shaped fields. `moved_claims:8292–8303` discards the new digest, and `claim_covers:8417–8419` compares only the key prefix; false old/new digest claims are not checked. `report_baseline_moves:8448–8481` also collapses keys across baseline files, allowing one claim to account for two distinct source-file identities. Historical §36.5 intentionally limited removal accounting; that history must remain quoted, but cannot authorize current whole-goal omissions. TASK001/007/008/069/071 need exact full identities and required-set equality, including removals/duplicates, with old/new full cells and independently accepted disposition rather than a free-form class label.

`bless_guard_base_from:8822–8835` refuses an absent base but accepts explicit `HEAD`; `resolve_rev` verifies existence and returns the mutable revision spelling rather than resolved commit. A caller-selected HEAD can again compare the tree with itself. TASK001/070/071 must bind the independently selected base/tree before candidate execution. The comments8423–8428 correctly say a final tree cannot establish classify-before-bless order; current protected receipts can establish that order and must remain the authority. Likewise ignoring performance ns7999/10395–10410 is an intentional old noise policy, not permission to skip current measured performance obligations. Preserve historical block markers, frozen-archive rejection and classification diagnostics; no previous green result or self-edited marker approves new oracle pixels.

## Review completion and handoff

All18 inventory paths have explicit preserve/continue/reject boundaries in the JSON ledger. All proposed repairs above belong to future production/checker tasks under independent qualification; none was implemented here. Source-only counterexamples distinguish a deficient individual predicate from a claim that every other check would also pass. No pre-existing PASS, fixture string, same-name inventory or archive hash was accepted as production behavior proof. No execution/probe/build/artifact decode performed during this review.
