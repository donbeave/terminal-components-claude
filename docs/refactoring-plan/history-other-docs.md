# Historical execution goals, handoff and improvements: direct-source review

This report complements the early/middle/late architecture histories and input review. It records direct Git source reads, not inherited report certification. Pins and topology are in `history.md`; exact parent/blob identities are in `history-revision-index.tsv`. Historical execution instructions have no present mutation authority. `PLANNING_GOAL.md` remains the current boundary.

## Coverage method

Continuation: `history-source-continuation.md` records the complete rediscovered JUNIE_PROMPT1/2 and JACKIN_REFERENCE blobs, all23 DESIGN edges, and source-clause/current-authority dispositions. Prompt duplicates are explicitly compared with earlier GOAL generations; original Jackin research is not mistaken for this repository's preview implementation. The revision index now contains281 edges across13 paths. Earlier counts below describe their original finite subsets, not a claim that linked-source discovery was already complete.

### JACKIN_GOAL research-only precursor (re-audit addition)

The independently rediscovered `JACKIN_GOAL.md` has three parent-relative edges, now included in the revision index: addition `d8f67460f70a410d24794eda0c04e12e2a2edcc9` (blob `5bed18785a8fef75855aa4b66a42d6e699ff4e7e`, 2,532 lines), full rewrite `f0c262173c74459e21774783c7f4b0ff7e4fe8de` (blob `a9fda52c26d0a071e320305f064323a996016902`, 640 lines), and deletion `1e6b2031d9594c01090e70d88a909f5630236ff9`. Both generations were read directly; the deletion is the exact previously read blob. These are research instructions for producing JACKIN_REFERENCE, not an extra library/API or preview implementation specification.

Disposition by substantive clause group:

| Clause group | Original → rewritten source | Present authority |
|---|---|---|
| One research document, no product changes or copying | Original §§1–3/35 → rewritten §§1–3/22 | Historical research-only deliverable. Does not require regenerating JACKIN_REFERENCE or authorize implementation now. |
| Source boundary and evidence | Original §§5–6/28/34 exact local SHA and local runtime evidence → rewritten §§1/3/4/17 remote-only, observed revision, explicit unavailable/UNKNOWN | Earlier local-source procedure superseded within that historical research task. Neither historical procedure replaces the current planning goal's immutable local oracle/main evidence. |
| Source-derived surface/data/interaction inventory | Original §§7–8/10–19/21–25/31–32 → rewritten §§5–15/19/21 | Research methodology and capability leads, not new operator behavior. Current Jackin source inventory and tasks051–057 retain every current oracle surface, transition, provider distinction and deterministic simulation boundary. Unimplemented original-product concepts cannot be introduced as shipped preview features. |
| Product semantics versus redesignable presentation | Original §§20/26/29 → rewritten §§10/16/18 | Describes a future initial design task. Its permission to redesign presentation is superseded by current no-divergence oracle parity. Current/shipped, planned and research-only distinctions remain useful evidence classifications. |
| Coordination, quality and stop condition | Original §§9/27/30/33–35 → rewritten §§5/17/20–22 | Completed historical research procedure; no current requirement for named tools, remote browsing, subagent count or new deliverable. Current PLANNING_GOAL governs. |

No new active task ID is fabricated for this retired research procedure. Its three indexed edges bind this explicit disposition; all current product claims still require the independently source-derived Jackin inventories. The earlier 248-edge completeness assertion omitted this differently named precursor; the corrected nine-path universe has 251 edges, not proof that every linked document history has been exhausted.

Read each initial generation completely, then every material parent-relative amendment. An unchanged blob is reused only by exact Git object identity. A deletion is reviewed against its already-read before blob; it does not erase product history. Renames are explicit: `80331448:JUNIE_PROMPT3.md` becomes `1e6b2031:GOAL.md` with substantive consolidation edits. The revision index includes both sides of that rename.

| Document | Direct sources and completeness | Obligation disposition |
| --- | --- | --- |
| GOAL.md: laboratory generation | Entire `4b857a07` (484 lines); deletion `74545e0f` removes that exact blob. | Original prototype target included stateful component catalogue, coherent visual grammar, keyboard/mouse/resize behavior and semantic themes. Original non-goal of stable public API is superseded by REFACTORING_GOAL. Product appearance comes from the current oracle, not the old prose or captures. |
| GOAL2.md: TablePro generation | Entire `65cb78d3` (1,595 lines); deletion `74545e0f` removes the exact blob. | Preserve the twelve product feature groups and deterministic database-preview boundary through the TablePro inventory. Original 31-step acceptance journey is a historical regression lead, not permission to replace oracle workflows. Database meaning stays in TablePro adapters. |
| JUNIE_PROMPT3.md / GOAL.md: Jackin generation | Entire `86f25734` (1,483 lines); full diffs `80331448`, `1e6b2031`, `5384c217`, `90c991aa`; deletions `7fbef957` and `49961e03` remove the same latest Jackin blob. | All thirteen areas and forty-step product journey are historical coverage leads. `80331448` explicitly separates the preview binary from the real Jackin CLI. `1e6b2031` consolidates progressive layout, stable collections, metadata, overflow, scoped destructive labels, state clarity and semantic tokens. `5384c217` makes 1Password account/vault/item/field references primary, masked plaintext fallback secondary; no secret material stored. `90c991aa` makes DESIGN authoritative for reusable visual/interaction conventions. Current oracle supersedes appearance; deterministic simulation and secret boundaries survive. |
| GOAL.md: refactoring pointer | Entire `2d81eec4` (30 lines); full `2a40616b` and `25ea92b0` diffs. | Historical pointer/routing, not an independent product contract. Strongest applicable gate equivalents are required, not blindly copied obsolete commands. Present planning goal supersedes old model names and execution mandate. |
| REFACTORING_GOAL.md | Entire `e48137f1` (§§1–30); full individual diffs `dce91d51`, `2d81eec4`, `25ea92b0`, `efada044`, `d4715f8e`. Merge `c3b51b96` equals the `d4715f8e` blob. | Capabilities and invariants remain accepted unless later architecture narrows them. `dce91d51` removes deleted prompt dependencies without dropping deterministic product behavior. `2d81eec4` adds routing and durable decision ledger. `25ea92b0` relaxes mandated research tools/references; no component capability removed. `efada044` labels the document historical and makes parity restoration active. `d4715f8e` changes role labels, not technical contracts. RG rows below supplement A and architecture-specific ledgers. |
| GOAL.md: parity generation | Entire current `7b27732a` (380 lines), replacement `efada044` endpoints and `d4715f8e` one-line subagent→agent diff. | Main architecture retained; four-app fidelity and independent source/visual review required. Old reference branches, limited accepted polish and old exception language are superseded by immutable September 10 oracle/no-divergence planning goal. |
| GOAL2.md: consolidation generation | Entire `db5c41a6` / current `7b27732a` (376 lines); merge copies are exact blob identity. | Preserve accepted integrated work, inventories, semantic union, truthful rejected/superseded decisions and source-bound proof. Consolidation/worktree/remote cleanup was a completed historical task, not current authority to delete anything. |
| HANDOFF_SLICE4_WAVE1.md | Entire `8b9d4d6d` (66 lines); complete added/changed paragraphs at `c9b765b8`, `6584b666`, `7d755752`, `a0e60b07`, `60fdede7`, `a489c0c8`, `54a7aa17`, `d4715f8e`; entire final main (201 lines). `246f7e40` mechanically verified exact `tui-next→junie-tui` and `tui_next→junie_tui` replacement, no other byte change. Other copied blobs reused exactly. | Interrupted WIP and old next-action ordering are superseded. Capsule chrome, focus/header/background/gutter/scroll behavior remain concrete product/regression obligations. The recorded 0/6→2/6 progress and final untested edits do not prove completed fidelity. |
| IMPROVEMENTS_PLAN.md | Entire `215990ef` (750 lines); complete `abb480ac` additions/diff (HP01–23 and common contracts); full `c9661f6c` diff; `64add0ea` old/new endpoint and every newly added tracker clause; entire final tracker. `812bad84` proven byte-exact after checkbox and status-string normalization; `3dbc73e2` copies its blob exactly. Read complete current `scope.md` and `shared-contracts.md` supporting split authority. | F01–23 preserve defects and explicit proof gaps; HP01–23 preserve simulated capability contracts separately from live execution. `c9661f6c` retires four generic framework proposals but retains concrete ownership. `64add0ea` explicitly defers unrelated applications, operational providers/persistence and all HP16; `812bad84` checks only Current slices. These old scope choices are inputs to current four-app parity planning, not automatic permission to ignore inherited bugs. |

All six non-architecture/state paths represented 47 changed parent edges before adding the renamed-source deletion edge. There are 34 distinct non-absent blobs across these paths (GOAL 9; GOAL2 2; REFACTORING_GOAL 6; handoff 10; Improvements 5; renamed Jackin 2). Counts identify review coverage only, not requirement completion.

## Original refactor obligations retained

`history-other-obligations.tsv` enumerates RG capabilities individually. Every RG row has a source section and disposition; accepted implementation details are supplied by A/HE/HM/HL architecture rows and the current component/application matrices. Original questions asking the architect to compare alternatives are not mandates to implement every alternative. Later accepted design decisions resolve them.

The original contract explicitly separates application data/effects, props, durable interaction state, controlled values, frame geometry, resolved theme and semantic actions. Rendering cannot secretly commit/cancel/validate, change selection/focus, reorder/mutate records, close overlays or emit effects. Dynamic keys, controlled-value synchronization, disabled/read-only distinction, secret handling, empty-area containment and meaningful non-color states are invariants even when no named test token appears beside them.

The original contract requires all customization layers: complete custom theme, partial semantic patch, family recipe, variant, subtree, instance, meaningful part, state, custom content and custom downstream component. A global second palette alone does not prove them. Public docs must match actual code and compile examples; framework/generic layers cannot depend on SQL, foreign keys or application models. A cosmetic builder wrapper, universal trait, virtual DOM, broad macro DSL or compatibility facade is not accepted architecture.

## Improvements decision precision

The initial source assigns **O01 real async ownership**, **O02 Widget/Theme/glyph abstractions**, **O03 generic keyed collections**, **O06 generic Container/modal manager**. The later prose lists framework categories in a different order; do not infer ID mapping from list position. O01's generic extraction is retired; task cancellation, generation/revision matching, stale-result rejection, failure inspection and shutdown remain concrete HP14/15/18/22 requirements. O04 matrix paste, O05 terminal extensions/OS clipboard and O07 new read-only field API remain conditional/unselected.

HP contract bodies preserve all named variants and boundary cases through the capability matrix, not one broad scenario per provider. The original 226 capability rows are not 226 live-provider deliverables: simulated representation and separately authorized operational gates were always distinct; `scope.md` later makes this distinction decisive for Current completion. Existing new-Holla context rings, aliases/pins/hide, PostgreSQL insights, SSH identity, cloning, specialist handoffs, dependency plans and session activities are separately protected H00 behavior, not proof of legacy-provider parity.

Safety contracts survive simulation: typed immutable target/action identity, exact displayed/effective cwd/argv/host, truthful outcome, no ID-prefix inferred effect, explicit cancellation and dependent-stage blocking, refreshed target-bound authorization, protected descendants, unknown-process refusal, Cancel default, explicit permanent mode, no dry-run target mutation, owned report through quit, separate estimated versus physical capacity. These do not authorize live commands or host changes.

## Late STATE source coverage and corrected claims

Directly read cumulative main `REFACTORING_STATE.md:1467–2327`, including integration checkpoints, visual FAIL, source/gate checkpoints, form security, port decisions, consolidation and final cleanup. Middle reviewer directly read `ecc13378:346–1669` and all fifteen corresponding additive edges; early-input reviewer owns the preceding ledger history. Late architecture reviewer independently read §73 and late semantic amendments.

For every non-merge STATE delta in `git log --all ecc13378..`, read the removed-line/hunk-position sweep, then compare every added line against current main with only explicit package/role-name normalization. All semantic additions are retained; remaining differences are directly read superseded checkpoint headings/counts and the corrections below. `246f7e40` is verified exact package-name-only replacement. Duplicate archive/cherry-pick branches have matching source changes; index preserves their individual parent/blob identities. Late review does not pretend to reread the untouched early body of every snapshot.

| Amendment | Decision / correction | Current consequence |
| --- | --- | --- |
| `08cabdf0` and archive equivalents; `37f9031e` / `612ff7af` | Reachable production page ownership, not imports, signatures, dead helper names or synthetic token mentions. Each component use must belong to its declared page. | Preserve executable anti-copy/page-mapping gates; scanner undercoverage remains a real gap, not discharged by old pass totals. |
| `11924199`/`c493a2cd`; `e45c3f5e`/`1852338e`; `d211a0b2`/`a10ff364`; `07bb7193` | Each authoritative checkpoint explicitly supersedes the prior status. Visual FAIL persists despite growing passing tests. | No old PASS paragraph can override later negative evidence. |
| `33eeb99e` / `03697429` | Missing disabled-pointer/Props-constructor §73 decision is supplied; missing public-API/visual findings still separately owed. | Preserve narrowly accepted §73 semantics and full incident history, not old blocker as a current API choice. |
| `91f0296a`, `7944fa17` | Form slot removal zeroizes secret drafts; source payload differs from later documentation checkpoints. Masked environment drafts persist only on Save, Debug is redacted. | Do not cite doc commit as implementation proof; retain secret ownership and cancellation semantics. |
| `15371443`, `2a0cf299` | Update conformance evidence and correct stale current-HEAD command/count language. | Source/test inventory must pin actual executable revision. |
| `ddc3a1aa`, `761f6d55`, `d4715f8e` | History/initial repair audit, completed Holla contract review, runtime proposal and accepted adjudicated ports recorded. | A proposal is not implementation; port decisions preserve underlying requirements even when one patch is rejected. |
| `b9bfd758`, corrected `1f2ea256` | CI failure count corrected 40→316/316 historical runs. Recipe244 failure is an anchor/run failure, not first byte mismatch. Triaged at `da38e52e`:60 exact/379 mismatch/60 run failure. Pre-merge main `af1e752e` reproduces TablePro startup-focus divergence. | Historical inherited red gate; current architecture does not excuse oracle drift. Never conflate mismatch, launch/anchor failure and infrastructure failure. |
| `1f2ea256` | `parity --approve` promotes exact matching replay evidence; it is not a per-recipe exception/waiver mechanism. Repinning oracle/deleting gate/blessing mismatch explicitly refused. | Historical broad-waiver suggestion is corrected, not a supported escape hatch. Current plan must retain hard oracle equality. |
| `e4a6a714`, `61b6f8f9` | Post-cleanup verification and final worktree/remote cleanup recorded. | Historical completion of consolidation scope, not proof of overall UI restoration and not current deletion authority. |

## Remaining limits

This history review certifies source coverage and decision extraction, not current implementation correctness. Existing A/F/HP rows need current code/test/evidence task mapping from the coordinated matrices. Original TablePro/Jackin product prose must resolve through the frozen oracle; do not resurrect obsolete UI layout from those documents. Material conflicts between inherited safety/architecture and exact visible oracle behavior need explicit plan rows, never silent exceptions.
