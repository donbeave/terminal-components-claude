# Full branch-delta review — components B

Source reading complete: all35 assigned whole-file deltas, including inline tests and comments. This is a planning/source audit, not execution of future component proofs or complete oracle qualification.

Branches: Holla `2e2401393c47360741ebd321679de08982dca50a`; main `7b27732a8c3c131760ec3438f641cb3c11343a42`. UI oracle remains `02f5294bfdbf38004cc49130d0aff1d01f31434c`, not the moving branch. All17 deleted legacy widget blobs in this partition were independently compared and equal the oracle blobs exactly. This is provenance evidence, not semantic completion.

Partition is exhaustive/disjoint against component-A: take the76 `partition=components` rows in `branch-diff-inventory.tsv`, then B matches path regex `editor|code|diff|grid|viewport|list|tree|table|picker|menu|completion|empty|props|tabs|segments|splitter|steps|status` (35 paths); A owns the41-row complement. Old splitter is B, new split is A; shared semantics coordinated. No production changes or baseline blessing.

The companion TSV records exact blobs, SHA-256, full-file line counts and **actual read coverage**. Added/deleted files are whole-file deltas; a function search does not count as reading that delta. All35 rows now have source-derived preserve/continue/restore/reject dispositions and responsible task mappings. Direct immutable-blob rereads confirmed every recorded SHA-256 and line count: Holla13116 lines; main29751 lines. No running count substitutes for the semantic notes below or each ledger row.

## B-STATUS-01 — two oracle drop policies collapsed into one

Source: oracle/Holla `src/widgets/segments.rs:87-101` versus `src/widgets/statusbar.rs:166-204`; main `crates/tui/src/components/status.rs:611-650`. The former removes the first tied left item and allows the final left item to disappear; the latter removes the later tied item and retains the strongest left survivor. Segment spacing is2, status spacing3. Current TASK-028 `trusted/source-witnesses.md:13` and `trusted/obligations.md:9,200` conflate both sources under last-survivor truncation.

Concrete source counterexample: left items `A`,`B`, equal priority5, no center/right and width5. Legacy segments needs8 cells initially, drops A and leaves B in5. Main merged StatusBar with gap2/edge1 needs6, drops B and leaves A. Width pressure can remove every segment, unlike StatusBar's retained-left invariant. The comment “rightmost on ties” in segments is not authoritative over its strict-less left loop.

Enabling condition: a shared component was defined with one hard-coded policy before enumerating both observable source contracts. Structural repair needs an explicit source-qualified shared layout/drop policy/configuration and distinct per-source geometry/action witnesses, never an app-local duplicate loop. Seven actual legacy consumers include Showcase chips209/218, TablePro app2289, Jackin app879/2516, Capsule1677 and Holla app3148. Coordinator notified; no repair authorized or implemented in this report yet.

Proposed narrow policy (not yet an accepted API decision): reuse StatusBar's borrowed items, typed identity, painter, slots and inline Meter. Add one shared finite layout/drop policy with explicit spacing/inset configuration: existing modern defaults remain unchanged; source segment configuration uses gap2, its per-item trailing-gap width budget, first-left tie removal, right-over-left cross-group tie and permits zero left survivors; source status configuration uses gap3/edge1, between-group spacing, later same-group tie removal, center/right/left equal-priority order and retained-left truncation. Caller background/surface and padded clickable geometry remain source-qualified. Do not implement a second segment painter or let app projections imitate dropping. TASK-028 owns algorithm/configuration/finite witnesses; app owners only choose the source-derived configuration and consume typed actions. Actual consumer paths are listed above; task joins include TASK-038 Showcase chips, TASK-040 Holla shell, TASK-051 Jackin shell, TASK-055 Capsule and TASK-058 TablePro shell, then TASK-039/050/057/064 family closures. Additional cross-page frame contributors must consume the same shell policy; their complete frame inventories remain authoritative.

## B-EMPTY-02 — inherited styles do not prove wrapped centered geometry

Oracle/Holla `src/widgets/empty.rs` wraps hint text at `width.saturating_sub(4).max(8)`, centers the complete title/blank/hint block vertically and truncates/centers each line. Main `components/empty.rs` measures wrapping at full width but paints a single hint row; `collection/empty.rs::draw_centered` paints title at the top and one detail row. At area20×9 with title `T` and hint `alpha beta gamma delta epsilon`, oracle wraps at16 into multiple hint rows; main does not paint that block. All three files were read completely.

TASK-027 W-027-04 proves actual inherited paint ownership and guards, but W-027-05's generic shared-layout preservation does not freeze this source geometry. Root condition is separate measure/painter layout plus inference of parity from correct style forwarding. Repair at TASK-027's already scoped shared helper/standalone component: explicit centered block, width-minus-four wrapping, exact title/hint truncation and error-prefix style witnesses, across actual source-supported allocations. Keep conditional TITLE/HELP/ICON paint credit; no unconditional icon requirement. No oracle blessing or app-local layout copy.

## B-MENU-03 — power-of-two tests conceal reverse modulo error

Oracle/Holla `src/widgets/menu.rs::step` uses signed `rem_euclid`; main `components/menu.rs::step`, `switch_menu` and closed MenuBar previous navigation use unsigned wrapping then modulo. On a64-bit target, previous from0 in a3-item enabled menu computes `usize::MAX % 3 == 0`, not oracle2. Four-item forward-only inline test cannot expose this. All1666 main and757 oracle lines were read, including those tests.

TASK-023's finite witnesses cover pointer phases, metadata and resizing, but no reverse non-power-of-two navigation witness. Add real 3/5-item previous-from-first cases for context, open bar and closed bar, mixed/all-disabled cases, with exact cursor/action and repaint observations; reject unsigned wrapping. Shared bounded cyclic-index arithmetic owns the repair, not three independently patched expressions. Preserve typed remaps and runtime layer ownership. This source finding is not yet an executed candidate test result.

## B-COMPLETION-04 — cross-row column maxima missing from popup proof

Oracle/Holla completion sizes from independently maximal label and detail lengths plus8; main `Completion::measured_size` maximizes each combined row instead. For labels/details `(20,0)` and `(1,20)`, at a screen/design supporting48 cells, oracle requests48 while main requests29. TASK-024 W-024-06 tests routing/splice only. Add exact first-frame/resize width and column alignment with longest label and longest detail on different rows; preserve editor-owned focus, alternate insert text and popup runtime placement. Source width policy belongs Completion, not app hardcoded popup rectangles. Lifecycle opening versus refreshing must also be source-qualified: oracle open resets cursor, modern request invalidates but can retain one; do not silently equate those operations.

## B-PICKER-05 — no keyless typed-query submission in shared action surface

Oracle/Holla `src/widgets/picker.rs:278-292` emits `Submit` when Enter has no eligible row but a nonblank searchable query. Actual Holla `src/bin/holla/app.rs:1245-1310` Files jump consumes Submit with selected=None, resolving the typed path directly. Main PickerAction/FilterListAction omit the variant and `FilterList::item_action` consumes absent/disabled/non-ready items. TASK-024's finite witnesses omit this action branch; the Holla consumer must not bypass component routing with a raw Enter shim or fabricate an item key.

Structural repair: a shared typed keyless submission outcome with exact query ownership and applicability, produced at TASK-024's same keyboard action path; app Files owner resolves domain path and success/error. Freeze empty/whitespace/nonblank query, no rows/all disabled/eligible rows, normal/Alt Enter and source readiness guards separately. Actual oracle permits its Submit fallback after eligibility fails; this is not authority to adopt deferred F01 proposals or to weaken modern hidden-query guards.

## B-GRID-06 — DataGrid gesture generalized over a different DataTable contract

TASK-022 `README.md:46` R-001 and `trusted/source-witnesses.md:13` W-022-01 state the DataGrid current/different-cell rule without a source-family qualifier. Oracle/Holla `src/widgets/table.rs:476-518` instead gives a completed click in `cell_nav=true` mode no activation for a current noneditable column; with `cell_nav=false`, any valid row click selects and activates its source row, including a different row. Oracle DataGrid `src/widgets/grid.rs:1334-1341` activates a current globally readonly cell and merely moves to a different cell. Main `components/grid.rs:2745-2780` currently uses press-move/double-click activation. All three bodies were read completely.

Concrete counterexamples: two-row DataTable with `cell_nav=false`, click row1 while cursor row0: oracle emits Activated(row1), whereas the universal plan says move-only. DataTable with `cell_nav=true`, noneditable current column: oracle emits no activation, whereas the universal plan requires readonly activation. Root condition is replacing source-class action policy with one component-name-wide rule. Shared Grid must provide explicit finite source-qualified gesture configuration, retaining its accepted default behavior and runtime-owned press/click transaction. Separate modern-default, DataGrid, DataTable-row and DataTable-cell proofs must assert exact action, selected/cursor identity, edit state and release-outside behavior. Do not change the previously repaired DataGrid partial-column routing contract or revive F04 unchecked mutation. App projections select policy; they do not suppress or synthesize generic cell actions. No repair made here.

## Additional source obligations to retain during integration

TASK-026 W-026-03 must explicitly include asymmetric adjacent runs, not only paired changes: main `append_changed_rows` indexes removed/added text without checking pair against the corresponding run count, so a remove1/add2/context sequence can read an Add as the second removed line and a following Context as a nonexistent addition. Freeze remove0/add2, remove2/add0, remove1/add2 and inverse, followed by context and another hunk; independently compare blank side, numbers, copied projected bytes and emphasis. Existing generic row source clauses intend this repair; the finite witness lacks these values.

TASK-020 source display Steps has no pointer activation when selectable=false, while the accepted main `Steps::new` constructor is explicitly ClickOnly and produces Moved/Activated. Preserve the modern constructor default and source display semantics through an explicit configured capability/presentation, not silent default replacement. Source defaults are selectable=false and numbered=true. A proposed interactive=false setting must preserve wheel/scrollbar while suppressing row controls, pointer/key actions and focused/hover flags; modern disabled is not equivalent because update ignores everything. Source selectable=true clicks only move; it has no Enter/Space activation, whereas modern navigable clicks/Enter and both constructors' double clicks activate. Thus interactive/numbered/meta builders alone do not resolve the action-policy difference. Pure borrowed metadata must preserve Some(empty) versus None: custom Some overrides every state, None defaults only queued/skipped/blocked. Source metadata threshold is mw>0 and avail>=mw+12, with right inset1 and label gap2; main uses every lifecycle label and a different threshold/inset. The source-specific presentation/action policy must explicitly cover these differences through the shared painter and keyed update path, with modern defaults unchanged. W-020-08/09/10 already bind source glyph/meta/numbering and display observations; component-A received exact source213–335 and main704–982 contracts. Main field/glyph/numbering/default-meta differences are not authorization to restore owned legacy models.

TASK-021 already explicitly requires keyboard selection/Esc, raw source copy, controls, revisioned mutation, retained reading/marks/caret and log-versus-prose policy. These are real restoration obligations, not demonstrated by current main tests: main viewport retains an evicted producer caret at(0,0), copies expanded tabs, trims all Unicode whitespace, and reserves gutter+bar unconditionally. Oracle clears an evicted caret, copies raw tabs/controls, trims ASCII spaces only, has no viewport gutter and conditionally reserves the bar; its cached two-width layout is not the ancient per-frame full reflow described in main's opening comment. TASK-008 must source-dispose those conflicting starting assertions; TASK-021 restores observed geometry without losing bounded cached projection. W-021-04 should freeze evicted-caret=None and top retained line/subrow; W-021-06 should freeze literal trailing tab/control/non-ASCII whitespace and ASCII-space-only trimming. Width/no-overflow versus overflow and focus-gutter absence need explicit source-qualified first-frame cells, not an invented guarantee that both source and main tests stay unchanged. No additional authority inferred from historical performance comments.

TASK-025 retains the shared TextEditorCore, borrowed Highlighter/Segmenter and immutable draw while restoring source modes. Whole-body comparison also finds missing source range indentation/dedentation, navigation-mode horizontal scroll/page scroll, manual wheel persistence until cursor movement/resize, running-versus-diagnostic marker precedence and compact find-footer suffix/count. Its existing broad clauses intend shared editor/output parity; W-025-03/04 currently lack exact vectors for these branches. Add document selection spanning two lines then Tab/BackTab; navigation Left versus editing Left; wheel off caret followed by unchanged frame then edit/resize; intersecting diagnostic/running marker and narrow active-find query. Expected document, caret, scroll and footer must come from exact source branches, with no full-document clone or per-span grapheme segmentation.

## Preservation/continuation boundaries

The35-row ledger is the per-path semantic coverage record. The retained modern architecture includes borrowed collection/model data; keyed row/column identity and ragged no-hook behavior; one CellUi/RowUi paint boundary; runtime capture/layer/scroll ownership; TreeIndex subtree splice and query/lineage/MAX protection; Viewport exact prefix/work probe; CodeEditor generation cache; controlled Picker/FilterList/PickerChain and editor-focused Completion; secret-safe Props; typed StatusBar items and inline Meter. Restore oracle observables through those owners. Reject legacy owned display/domain duplicates, index identity, mutable draw, per-frame document/cell cloning, local SQL/NULL comparators and app-local widget engines.

New split ownership was checked with component-A: TASK-012 owns modern `split.rs`; old splitter's mouse-only handle, source rule/heavy-drag glyph and caller background must map there, not TASK-014. TASK-020 owns list/nav/tree/tabs/steps; TASK-021 viewport; TASK-022 grid/table; TASK-023 menu; TASK-024 picker/completion; TASK-025 code; TASK-026 diff; TASK-027 empty/props; TASK-028 status/segments. TASK-031 consumes actual qualified production paint witnesses; this report is not a substitute receipt.

## Verification and limits

Read coverage:35/35 immutable source files, all whole added/deleted bodies; old Code431–860 was reread after an aggregated output truncation. Final metadata verification reloaded each immutable blob and checked all70 line-count/SHA fields, with zero mismatches; all17 legacy blobs separately equal oracle. This mechanical check proves inventory integrity only. No candidate Rust changes or test execution were performed during this branch partition pass. Existing narrow JT probes belong their separate reports and do not certify these35 files. New issues here remain planning findings until coordinator authorizes repairs and an independent reviewer validates the resulting contracts. Broader app/domain/tool/test partitions remain their assigned reviewers' responsibility.

## Authorized planning repair handoff

After the independent source audit above, the coordinator authorized this reviewer to author bounded task repairs. This section records author work, not independent validation of those repairs. TASK-021 through TASK-028 now bind D-BRANCH-SOURCE in README and trusted obligations plus concrete finite witness rows. TASK-042 binds W-042-JUMP-SUBMIT and the shared TASK-024 producer. TASK-020 remains component-A/root-owned. No production Rust, AGENTS, task dependencies, source-obligation/history canonical ledgers, baselines, global assembly or synchronization were changed.

Six findings are addressed in the owned task contracts: separate StatusBar/Segments policies; wrapped centered Empty/Error geometry; bounded reverse menu cycling; independent Completion column maxima; opt-in typed keyless Picker submission with real Files jump consumer; and four distinct Grid source/default gesture policies. Additional W-021-07, W-025-07/08 and W-026-05 make source-adjudicated conflicting starting tests and finite branches explicit. The actual oracle PickerStatus is Ready/Loading/Error only: Loading/Error have no eligible row but still reach searchable nonblank Submit; stale comments saying Enter is refused do not supersede that code. Modern Partial is separately qualified, not invented as an oracle state.

Only new production scope is TASK-025 text/editor.rs and text/buffer.rs for apply-owned multiline indentation and non-public buffer support, preserving source cursor/anchor rebasing and existing generic/sensitive behavior. Read-only graph analysis finds both paths owned only by TASK-008, TASK-013 and TASK-025; every pair is ordered, with TASK-013 an ancestor of TASK-025 through TASK-016. There is no reverse dependency. Existing TASK-042 ancestry already includes TASK-024 through TASK-041.

Validation: focused taskfmt lint passed all nine packages (021–028 and042), zero errors/warnings; git diff --check reported no errors (the planning tree remains untracked, so this is not a tracked-diff completeness proof). No future Rust tests or host proof gates executed. The coordinator must independently review the new contracts and their additional scope before freezing them.

Coordinator-owned integration still required: qualify any shared COMP:statusbar-segments final-survivor sentence as source StatusBar only; qualify generic Grid/DataTable gesture sentences by the four policies; add these finite witness identities to actual TASK-031/host required-set projection; propagate the bounded TASK-025 writable-source clause into the scope catalogue; retain exact TASK-008 assertion dispositions for viewport/code/diff conflicts. The source-obligations canonical/history ledger and decision-ledger were deliberately not edited. Additive public-policy authority to record centrally: single StatusBar finite policy/configuration with unchanged modern defaults; single Grid gesture policy with unchanged modern default; explicit Picker query-submit capability disabled by default. Completion independent semantic column measurement is a source geometry correction, not a separate app painter or authority to alter editor focus. No new historical acceptance is claimed.

### Changed task-file handoff inventory

| Path | Lines | SHA-256 |
| --- | --- | --- |
| refactoring-tasks/terminal-components/completion/021/README.md | 172 | be13bd2a4d6b290175c566aec5858be45a673de6aed3fa99043736c142d445c3 |
| refactoring-tasks/terminal-components/completion/021/trusted/obligations.md | 244 | c60e7ae5025c754a76007c15510a09ddce446737fe89e00e8e679fa0e69f0e08 |
| refactoring-tasks/terminal-components/completion/021/trusted/source-witnesses.md | 24 | 5246d99818385778f4fedb5a93a5c44a73c65cc16740851f29843856b260bc77 |
| refactoring-tasks/terminal-components/completion/022/README.md | 172 | 0c91cafa22855bb16c5a92c44eadb9b21942907b633140404326a20ded36c868 |
| refactoring-tasks/terminal-components/completion/022/trusted/obligations.md | 439 | a8df8fe5749a4375711d221823548da502e5381c17d4f4e9718e2b8c5e1b6b7b |
| refactoring-tasks/terminal-components/completion/022/trusted/source-witnesses.md | 26 | 554ae87e6ec6c571e947eba1d32b7168a25cf1dff02acb313e9b2b910fbc868c |
| refactoring-tasks/terminal-components/completion/023/README.md | 172 | 3e7a1804fefd7a4541541586c307596649b658ed964c1ec9e5c17918257ddc36 |
| refactoring-tasks/terminal-components/completion/023/trusted/obligations.md | 187 | 493b81c75efe20be155d5568ebca857347c39430a9bcb65f14b172c913686a87 |
| refactoring-tasks/terminal-components/completion/023/trusted/source-witnesses.md | 26 | 06bf79accfa1f72a8f1fb3e6ef0981908d8684c8756d521a3f34793f4940dfce |
| refactoring-tasks/terminal-components/completion/024/README.md | 174 | d4ca6725940df0052543e2dd679a12b8b90ecf062e9e0ca71bf28327c159d2c9 |
| refactoring-tasks/terminal-components/completion/024/trusted/obligations.md | 196 | 717f721f16ce164ccdb212c0b11717ecce1d721bd6bcfc2008e08bca017da335 |
| refactoring-tasks/terminal-components/completion/024/trusted/source-witnesses.md | 25 | f3eaad32970429670619985beb84a1cdec13154a0c70f3712c9551fe011ac10b |
| refactoring-tasks/terminal-components/completion/025/README.md | 175 | 154e595519985ec37b080112d9bc63f6fddfce289ef9ce019d8a39141e0ed93e |
| refactoring-tasks/terminal-components/completion/025/trusted/obligations.md | 103 | 7423324437ed8d7630922044fcfc8c3ff8b4717de6ec975de5254d32e7a13728 |
| refactoring-tasks/terminal-components/completion/025/trusted/source-witnesses.md | 25 | f3316f3dfd0456abea7696e5fbad4c4f43b1d7f0ffebf33cfc1e5b64eb0602d6 |
| refactoring-tasks/terminal-components/completion/026/README.md | 173 | e835ab28ab16761f4ccb8d52d5d6091446e5d2b00c5b7bef61a57688236c21da |
| refactoring-tasks/terminal-components/completion/026/trusted/obligations.md | 79 | eec68b37e5d4a008ec6a04f3c1413baf2dd64b20d993d1a01acedcb97cc1475b |
| refactoring-tasks/terminal-components/completion/026/trusted/source-witnesses.md | 22 | 66373298c4a6b8c9fe01ebdbb3d9178eb33c525784832653ed3d18ea6e401508 |
| refactoring-tasks/terminal-components/completion/027/README.md | 179 | 22647141076c4d8218bae91f2acb7dbdf405c7526cb993f53a2007cd728b750f |
| refactoring-tasks/terminal-components/completion/027/trusted/obligations.md | 202 | f7a52b410f440cb7aed998f70047e8193c5c40d076593b0b471f7ab5da39769d |
| refactoring-tasks/terminal-components/completion/027/trusted/source-witnesses.md | 24 | 6ef3e8993a186ff4bab8419786d81320e8437db81cb824944ea848a58868d63a |
| refactoring-tasks/terminal-components/completion/028/README.md | 178 | aa136f92e81d90db8cf70d64b68628c81567a3f5a41c7b72fb0f741731c7f3af |
| refactoring-tasks/terminal-components/completion/028/trusted/obligations.md | 278 | 2f5a62682ab00f3af2895c7be7834dc4fa886ef9be40daed5f09114e4ca61612 |
| refactoring-tasks/terminal-components/completion/028/trusted/source-witnesses.md | 23 | 453b626d7a5f7ada89659ee3787d6df2052a294aba7e07968705eb6d3d209928 |
| refactoring-tasks/terminal-components/completion/025/verify.toml | 60 | b61fbe88b1d20e71777bde24e93b19b501c48800e932c32389ec15f91fa96fb1 |
| refactoring-tasks/terminal-components/completion/042/README.md | 205 | 5c5d7ba66237dbaa2608d06b9d304b0ab967f2113eb210c9f6c17bbb3db79f71 |
| refactoring-tasks/terminal-components/completion/042/trusted/obligations.md | 157 | 08e1d85de5e90e6a9049c141a6735875d0e49cc63addd7e65dcf9f7901e2ed11 |

### Independent-review follow-up author repair

Foundation's independent rereview found surviving universal D-GESTURE-AUTHORITY wording, conflated global versus column/row edit eligibility, and missing exhaustive enum-consumer scope. The coordinator authorized bounded correction. TASK-022 now preserves the modern default and separate DataTable policies; DataGrid global editable=false activates, whereas global editable=true with a read_only column or pending-deleted row returns Consumed without action/edit/mutation. W-022-10 isolates these controls without active-editor, anchor or reference interception. Owned A58/HL64 source-obligation payloads are qualified; historical mechanism quotes remain intact. Root must update the central authority/projections from those corrected primary clauses.

TASK-024 now permits only the exhaustive no-op Submit match arm in apps/showcase/src/pages/pickers.rs:1037–1066, with capability off and unchanged result/detail/level/layer behavior. Read-only graph analysis finds owners008/024/038, every pair ordered. Capability-off keeps existing generic Ready/Loading/Error/Partial behavior; Partial remains consumed/no action even when source submission is enabled, because it has no oracle PickerStatus counterpart. Focused022/024 lints passed. These are author repairs awaiting Foundation's fresh rerecheck, not self-validated semantic closure. The prior27-file table is the first handoff snapshot; the following eight hashes supersede its matching entries and add the newly changed files.

| Path | Lines | SHA-256 |
| --- | --- | --- |
| refactoring-tasks/terminal-components/completion/022/README.md | 172 | cf2022b5300d732162d48dc4f2a2994b5d17df095529367bca3233d017aad2a2 |
| refactoring-tasks/terminal-components/completion/022/trusted/obligations.md | 439 | 982c6a3cf85f7c02e3f88abb444b8b39905be85b01f44474484c61d10da0a245 |
| refactoring-tasks/terminal-components/completion/022/trusted/source-witnesses.md | 27 | fc2a5d32ef47fc58f4a032448f4678e6f31fc9cbcf32f744983303b2904bdf30 |
| refactoring-tasks/terminal-components/completion/022/trusted/source-obligations.tsv | 42 | feb982351b80704b97291ab57ba27a74aef8980714f2bfcb9e15d230103e84d3 |
| refactoring-tasks/terminal-components/completion/024/README.md | 175 | 6acb68ec7ecd6b27d80603543a47d5a6bc040683893e6772b45fe6a4f2989e5e |
| refactoring-tasks/terminal-components/completion/024/trusted/obligations.md | 196 | 595d1d96425cb41aaf7118f7af40fb92edd99432fa6e711241bc93d513a766e2 |
| refactoring-tasks/terminal-components/completion/024/trusted/source-witnesses.md | 25 | 9b5569734923682e19c598fa76eda99766cecf65a285c29410815d20b899b994 |
| refactoring-tasks/terminal-components/completion/024/verify.toml | 60 | df9ca72f7c8512215ba9237992e786acf2f2c750d00edde22862dff95ea9cf09 |
