[package]
name = "tc-session-broker-feasibility"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
crossterm = "=0.29.0"
signal-hook = { version = "=0.3.18", default-features = false }

[lints.rust]
unsafe_code = "forbid"
