# ADJ-13 broker checker qualification

TASK-072 R-002 / AC-011 / CHK-013 must execute `broker-bootstrap-driver.py --runner /work/tools/refactor-proof/bin/tc-proof` from the host-frozen `broker-bootstrap` group. `--self-test` is only an observer/source-premise diagnostic; its output explicitly says `submitted_checker_qualified: false`. No premise run, existing 193-case corpus, or caller-owned lifecycle prototype substitutes for CHK-013.

The driver imports the unchanged protected source/actual/common transport. It builds `broker-bootstrap-observer.rs` using the pinned main source archive's xtask dependencies, extracts the exact executable artifact, and freezes complete `crates/tui/src/**/*.rs` source snapshots before submitted code runs. Each snapshot changes only its named fixture bytes. The candidate sees source bytes, their complete inventory, and the ADJ-13 policy; it never receives the private expected verdict or mutation name. The observer parses every inventoried Rust file with syn, including inactive and unreferenced source. Its raw records include file/module/item attributes, statics, storage types and nested generic arguments, type aliases, imports/reexports, bounded struct fields, function/method-local depth and macro token trees. Parse errors are data, not skipped files. The observer emits no checker verdict.

The submitted architecture operation uses the existing protected nonce/tree/compiler/observation protocol. Its observed bytes must match host-owned canonical JSON exactly. Every positive must return the exact complete observations; every negative must observe first and reject ARCHITECTURE with empty outputs and nonzero exit. The candidate's source digest, result status, or generated explanation alone proves nothing. Each negative is followed by a fresh valid-broker recovery; all invocations use one unchanged candidate executable hash. The instrument's own `--self-test` additionally executes ten broker-specific real-parser IPC controls: valid/recovery plus always-pass on a forbidden second singleton, always-reject on valid storage, zero observation, forged records with retained digest, forged records with recomputed digest, boolean exit substitution, malformed result JSON and duplicate result key. Each attack must reject for its exact intended guard and protected execution count. `--transport-self-test` runs only these ten controls for focused independent review; it also explicitly cannot qualify a submitted policy checker.

Eight positive controls preserve the complete pinned immutable source, qualified storage, split guards, inherited private module guard, equivalent cfg_attr, import renames, an equivalent storage type alias, and harmless comment/string mentions. Negatives cover additional same-file/other-file singleton, mutex/atomic and aliases, unresolved/cyclic types, Runtime/Theme/cache/extra flag/registration fields, public/crate visibility/accessor/reexport, wrong/missing/either cfg and cfg_attr leakage, unguarded broker fields, function/method/thread-local placement, macro-hidden globals, static mut/LazyLock/wrong lock, shadowed identities, wrong source path, and malformed referenced/unreferenced source. Exact names and bytes live in `cases()`, not duplicated hand-maintained counts here. Aliased storage resolving to the one allowed identity is positive; alias-hidden additional state is negative. Merely recognizing the final spelling `OnceLock` is insufficient.

Finite scope: this corpus supplies discriminating source-policy obligations, not a new general Rust compiler or whole-program mutable-state theorem. The future checker must reject unresolved claimed mutable-state syntax rather than accept it by omission. Existing immutable error/recipe data remains accepted; session.rs is never entirely exempted. Source parsing does not prove that the fixture is a runnable singleton implementation. The separate TASK-009 backend-free metadata/compile/symbol absence, initialization poison and first/second registration failure, retry without duplicate handlers, lease concurrency, cleanup and real SIGTSTP/PTY/deadline cases remain mandatory. This corpus requires no future TASK-009 receipt and makes no singleton lifecycle claim.

Freeze closure: the three new broker assets plus the complete existing ARCHITECTURE and RUNNER lists in `freeze-bootstrap-assets.py`, as the coordinator's new BROKER group. This includes source/actual/main drivers, parser and model/probe assets, main source archive, common runner, fixture app/worker, index/extensions and host sandbox observer. `runner-bootstrap-cases.tsv` is not imported by this broker path and is not an invented evidence-directory dependency. The coordinator owns the exact freeze helper and aggregate artifact manifest. Preserve existing architecture/style group bytes while adding this group. A missing transitive asset or unfinished independently reviewed checker qualification blocks dispatch; it is not optional.
