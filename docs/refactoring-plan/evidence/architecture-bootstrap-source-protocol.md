# Actual-source doc/API/dependency qualification

This planner-owned extension supplies TASK-072 CHK-006. It is an independent test instrument, not a production scanner, repaired TC workspace, baseline, or parity receipt. Freeze this protocol, architecture-bootstrap-source-driver.py, architecture-bootstrap-source-policy.py and architecture-bootstrap-source-observer.rs with every existing architecture, runner and host dependency and the exact pinned main archive. No TASK-073 product is needed. Source mutations occur only in private temporary copies.

Canonical candidate acceptance:

    python3 /task/trusted/architecture-bootstrap/architecture-bootstrap-source-driver.py --runner /work/tools/refactor-proof/bin/tc-proof

Preparation only:

    python3 -B docs/refactoring-plan/evidence/architecture-bootstrap-source-driver.py --self-test

## Actual public API and executable evidence

Cargo builds the actual pinned junie_tui and junie_tui_testing libraries with locked offline dependencies. Compiler identities, build argv, raw streams, source/lock identities and both library artifact digests are retained. An external observer request executes the pinned compiler on independently selected consumer bytes. It cannot select a mock implementation, candidate argv or guessed API inventory. Each specific compiler error is checked before the observation is released; missing tools/libraries and unrelated failures are preparation errors.

Use the shared main-helper toolchain contract: rustup resolves actual Cargo and rustc, not PATH launchers; the host freezes selected toolchain, absolute paths, byte hashes and verbose versions. Every library build, parser build, metadata query and external rustc invocation uses those exact paths and checks identity before and after execution. Cargo's RUSTC is fixed to the recorded executable and compiler wrappers are disabled. Missing toolchain resolution or changed identity is a preparation/observation failure, never an expected architecture negative.

Public Button controls require actual E0061 wrong arity, E0624 private method and E0432 missing import. Actual Grid consumers compile shared GridModel update and distinct mutable GridEditor update paths; shared editor misuse and a boolean editable capability fail with actual compiler diagnostics. Actual Harness/Scene/Fixture/Caps consumers qualify the testing crate separately, including wrong arity, private click_rect and an absent public API. The testing artifact digest and same-testing-family positive recoveries are mandatory.

Scoped consumer documents use ordinary Rust fences beneath both historical heading forms (## 18. and ## §18). Positives and wrong-arity negatives cover 18, 19, 20, 29 and one beyond the highest actual pinned section. Required ignore/no_run references still compile. These are qualification documents, not a claim that all illustrative production prose is already executable.

The allowlist corpus uses five exact tab-separated columns: kind, exact public symbol, consumer source, future owner and YYYYMMDD expiry. The protected context fixes the qualification date and permitted owner. A live exact missing API reference is a positive despite its honestly reported compiler failure. Duplicate, expired, missing kind/owner/expiry, malformed, zero-hit, satisfied, wildcard and wrong-source rows reject. Compiler failures are still measured; no allowance can turn absent execution into evidence or silence a whole type. This qualification grammar is not authority for candidate-selected production suppressions.

The real 04_family_recipe example is compiled and its existing the_more_specific_rule_wins test executes. The positive exercises actual style resolution; a private copy reverses the specificity assertion and must fail with the exact diagnostic. The actual executable is replayed through protected Rust transport. CHK-011 separately retains the actual external-author example and component/runtime probes.

## Real Cargo graph controls and scoped positives

Every graph is real cargo metadata over a private TC source-copy workspace. The independently constructed lock resolves before candidate execution; every later observation uses --locked --offline and frozen feature flags. A metadata error/cycle is not a successful negative. Extra bridge/dependency packages are real Cargo manifests and source files, not graph-shaped verdict objects. Complete stdout carries actual package IDs, versions, features, target kinds and edges.

Each profile selects the invariant being qualified. This prevents an incidental direct-dependency error from masking a missing transitive traversal. Every forbidden-name and version control has a same-policy positive; identity is package ID, not a name substring. A disconnected duplicate version is positive. Legal bridge-to-application reachability is positive; direct, transitive, build-only and testing-root-only forbidden application paths are negative. A permitted first root cannot suppress later restricted roots through a shared visited set. Separate controls cover direct smallvec/crossterm misuse, backend-only ownership, umbrella ratatui/widgets/macros, critical-section, palette, Unicode/bitflags version uniqueness and ratatui-core feature membership.

Current pinned main is not a clean positive for every historical dependency rule: actual ratatui-core 0.1.2 reaches palette, and the ordinary full workspace enables its default feature. These remain failures wherever those exact policies apply. Feature positives use a headless source-copy configuration with default features disabled and app crossterm requests removed; actual metadata must prove the expected feature set. Palette closure-predicate positives use a genuinely clean bridge root contrasted with a real reachable palette dependency. They do not assert the production core is clean. The future checker must share its production graph analysis and independently configured roots with this interface; fixture-name branches and a whole-main conformance receipt are prohibited.

## Actual Rust syntax and source inventory

The observer is compiled against the existing pinned xtask syn dependency. It parses actual supplied TC bytes and emits syntax facts plus complete observed text, not an expected category or checker verdict. It may read only its private subject/input/executable and system runtime files. Present files must match frozen bytes; absent files must yield NotFound, never an unrelated permission failure.

The protected source-policy cases require:

- All canonical examples 1–13 by exact source-owned name, each with a removal control; all four app manifests, missing app root, duplicate bin and incorrect publish policy.
- Actual 22 PageId::ALL identities in source order, with swapped/duplicate/omitted entries; PageId/constructor/module ownership, missing/shared/test-only page owners and omitted dispatch. The author helper and module root remain required files but are not invented PageId entries. Each of the 22 actual page modules has separate update/draw omission controls. These prove source ownership, not 22 runtime journeys.
- Malformed and empty existing actual app source; due Grid presence, generic multiline editable methods and inline GridCellActions syntax; actual library unsafe prohibition versus comment-only text and split unsafe tokens.
- Every actual public Button rustdoc heading, with a removal control per heading. Parsed doc attributes establish sections, not a substring elsewhere.
- Real todo! beside the scanner's own name versus a legitimate comment; actual legacy .owns/.locate/.child/scrollbar::id_for calls and the real legacy allowlist versus an exhausted allowlist and legal free owns helper. Blanket identifier bans are incorrect.
- One source-qualified gate joined across actual CHECKS tuple, test wrapper and authoritative §16.5 table, with deleted dispatch/wrapper/row, duplicate/extra dispatch and mismatched function controls. The complete current CHECKS name inventory is frozen. This focused join does not assert every unfinished main gate-table relationship already holds.

The executable finite manifest comprises public_cases, graph_cases, source-policy prepare and two actual example subjects. Freeze its measured case-kind, positive/negative and recovery counts from successful self-test evidence. These controls complement rather than replace CHK-011's existing Props, registry, PARTS, slots, nested ownership and real application runtime evidence.

## Source obligation mapping

| Obligation | Concrete qualification |
| --- | --- |
| A108, EARLY-AMEND-042 | Actual malformed/empty/missing source; CHK-011 retains configured Props and phase semantics |
| A109, EARLY-050, EARLY-AMEND-040 | Public compilation, real doc headings, strict suppressions and named example removals |
| A110, EARLY-AMEND-034, HM53 | Actual 22-identity/order/module/phase source inventory; CHK-011 retains live update/draw and paint-over discrimination |
| A47 | Existing CHK-011 receiver/helper/phase and duplicate-construction corpus, not inventory-only replacement |
| A93, HM21 | Real scoped Cargo path/version/feature controls, all-root/build traversal and actual TODO syntax |
| EARLY-AMEND-005, HM20 | Actual Grid syntax and external shared/editor compilation; actual unsafe syntax; CHK-011 registry invocation controls |
| EARLY-AMEND-012, RG47 | Actual legacy dispatch and real allowance exhaustion; public compiler boundaries and existing CHK-011 copy/ownership controls |
| HM14 | Live compiler-backed suppression and all stale/unused/duplicate/metadata negatives |
| HM25 | Every formerly invisible section, future section and both historical heading forms |
| HM26 | Actual CHECKS/function/wrapper/table join, exact registry names and missing/extra/duplicate/mismatch controls |
| HM28 | Every named example removal, public API signatures/privacy and actual specificity assertion execution |
| HM30 | Actual testing library Harness/Scene/Fixture/Caps public/private/arity/absence controls |

This mapping qualifies failure classes, not future production completion. Independent review must confirm the submitted dispatcher reaches shared production parser, compiler and graph analysis for frozen scopes. A finite public suite cannot exclude deliberately fixture-specific code alone.

## Context and protected transport

The CLI remains tc-proof architecture --context PATH. The protected tc-architecture-source-profile/v1 adds source role, exact directories and hashes, main/archive authority and scoped compiler, allowance, graph or inventory policies. It contains no case ID, expected category or mutation label. The exact Git tree binds source inventory and consumer bytes; it is distinct from main archive and immutable UI oracle identities.

One exact five-field tc-proof-runner-observe/v1 request executes the preselected compiler, Cargo query, source parser or Rust binary outside the candidate sandbox. The private observer owns argv, streams, exits and event memory. Pass requires exit zero, passed, null category and exact canonical-JSON observation bytes. Python value equality is insufficient: false, 0 and 0.0 must remain distinct. Reject requires nonzero exit, rejected, ARCHITECTURE and empty outputs. Both return exact private event digests; genuine digests cannot authorize altered output observations. Missing/replayed/forged events, omitted consumers, changed tool/library bytes and protected-file changes fail.

Cases are randomized; every negative is followed by a fresh positive for its policy and consumer family. Self-test executes every compiler/graph/parser/runtime premise plus real FD-bridge positive recovery and always-pass, zero-observer and forged-observation executables. Output-only boolean and float exit substitutions retain genuine private event digests and must still fail, each followed by recovery. These transport doubles are not a reference checker. Self-test never emits production acceptance or a completion receipt.
