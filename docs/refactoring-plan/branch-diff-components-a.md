# Whole-branch diff: components A

## Scope and actual read coverage

Compared holla `2e2401393c47360741ebd321679de08982dca50a` with main `7b27732a8c3c131760ec3438f641cb3c11343a42`; immutable UI oracle remains separately pinned at `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Main is implementation evidence, not authority to override accepted historical contracts.

This partition is the complement, within the 76-file components partition, of path regex `editor|code|diff|grid|viewport|list|tree|table|picker|menu|completion|empty|props|tabs|segments|splitter|steps|status`. Components B owns the matching 35 files; A owns exactly 41. All 41 complete old/new blobs were read, including tests and documentation: 15 deleted old files (4,630 lines) and 26 added files (25,474 lines), total 30,104 lines. There are no partial or search-only file credits. The old widget blobs in this partition equal the immutable oracle blobs; this was checked by a zero-diff restricted to these old widget sources, not assumed for either whole branch.

The companion [ledger](branch-diff-components-a.tsv) records every path, status, both exact Git blob identities, old/new line counts, full read range, semantic disposition and responsible tasks. Zero blob means absent on that side, not unread. A line-level audit means every line was inspected in its enclosing implementation/test contract; it does not falsely claim every possible input was executed.

## Dispositions

Continue the architectural transition: borrowed models, caller-owned stable state, one shared typed action/registration/style/geometry engine, sealed secret targets, pure draw and current-allocation layout facts. Preserve or restore source UI behavior through those owners. Reject resurrecting owned legacy widgets, app-local cloned painters/editors, render-side durable mutation, per-widget focus searches and stringly key dispatch.

No main test is authority merely because it passes. In particular Form's single-field test named “every visible field” cannot prove total validation; Select's changed behavior has explicit accepted history but still needs source-app reconciliation; a formatter callback can emit arbitrary fragments even when existing ASCII examples pass. Existing task acceptance rows and the independent 009–020 rereview remain binding, not replaced by this branch ledger.

### BA-FMT — streamed Display introduces false grapheme boundaries

- Location: main `crates/tui/src/collection/rowui.rs:256` (`label_fmt`) and its `CellWriter::write_str`; downstream default Display row rendering.
- Violation/reproducer: one `Display::fmt` calls `write_str("e")` then `write_str("\\u{301}")`. The same public List/Scene path paints the unsplit `é` correctly but loses the split suffix. Disposable public-consumer test `/tmp/branch-a-fmt.8HcjDQ`, `default_display_row_fragments_lose_combining_cluster`, passed by asserting the actual mismatch. The path dependency's rowui blob was checked against pinned main.
- Root condition: formatting fragment boundaries are incorrectly treated as Unicode boundaries. Fixing borrowed styled spans alone cannot fix arbitrary `fmt::Write` fragments.
- Structural repair owner: TASK-013 shared streaming logical-grapheme projection and TASK-015 actual RowUi/CellUi/default Display consumer. One original Display invocation; no complete formatted-line String/Vec, fixed cluster truncation, duplicated segmentation or global scratch. TASK-013 cannot depend on a future TASK-015 receipt; later TASK-031/069 joins consumers.
- Allocation disposition: accepted inline ASCII/CJK/combining zero-allocation and borrowed long-ZWJ visible-column bounds remain unchanged. The universal `label_fmt` documentation claim is broader than those proved domains. Locked ratatui Cell's symbol is private; public `set_symbol` constructs a replacement CompactString, while generic append is crate-private. Arbitrarily long transient fragments require retaining the incomplete cluster somewhere. The persisted seven-test [streaming qualifier](evidence/streaming-grapheme-bootstrap-20260911/README.md) now measures inline64/cluster-only overflow, cold/warm/fresh-owner allocations, lifetime high-water capacity, actual List resize frames and single formatting invocation; a compiled quadratic revalidation mutant is rejected. TASK-013/015 finite contracts and private FrameState owner scope are authored; root adjudication/independent review remains pending. No relaxed existing allocation promise is adopted.

### BA-SELECT — intentional generic change does not settle application parity

- Oracle `src/widgets/select.rs:83–126`: closed plain Up/Left and Down/Right commit selected index immediately, with Changed callback only when value differs. Open Up/k and Down/j move cursor; Enter/Space commit; Esc restores selected cursor; other unhandled keys including Tab are consumed (modified character exclusions apply first).
- Main `crates/tui/src/components/select.rs`: closed navigation changes cursor separately from controlled value; opening resets cursor from value. Popup is a nontrapping Popover; Tab changes focus and dismisses without opener restoration.
- Historical authority: closed cursor/value separation is explicitly required by the named test first added in `e8d053c9` §16 and main architecture line 1834. Main architecture §29.8 (6723–6792) explicitly replaced legacy unhandled-open-key swallowing with Tab focus-out dismissal. Preserve these generic defaults; do not label them accidental regressions.
- Actual source callers: Showcase `pages/chips.rs:53–61` sort/page-size/disabled engine; Holla `screens/review.rs:943` Choice arguments; Jackin `screens/accounts.rs:532` provider, `screens/config.rs:1700,1721,1838` schema/auth choices and `screens/editor.rs:178` dirty policy; TablePro `app.rs:1408–1409` filter column/operator (rebuilds at 1692/2072) and `connections.rs:143,165` engine/group.
- Proposed bounded repair: shared typed navigation result enabling source-qualified callers to commit the state-owned value key, plus an explicit shared open-key policy for those callers only. Form needs a private bridge because its SelectState is sealed. Keep one navigation engine and generic nontrapping default; no app-private raw key routing or inference that Radio ADJ-10 also adjudicates Select. Exact proposed API, existing focused-Tab binding precedence, source caller routing and Steps policies are recorded in [the bounded proposal](proposed-select-steps-policies.md); root selection is pending.

### Previously repaired obligations confirmed by complete files

- Input/textarea: preserve source edit-entry distinctions, invalid input commit/error semantics, TextArea manual scroll behavior and ADJ-09 programmatic paste versus runtime focus routing. Dynamic plain/secret transitions, pending errors, Clone/Debug, hidden/inactive fields and first inert-menu frame remain separately proved. CodeEditor active-find exception belongs TASK-025; this partition cannot certify it.
- Choice/Chip: ADJ-10 reserved mono geometry and typed navigation preserve controlled state. ADJ-11 source checked/removable/error/lead plus keyless clear/add and clipped overflow are structurally missing from main and remain implementation work, not “already compatible.”
- Form: main `submit_form:1175–1217` returns on first field failure; accepted F10/oracle evidence requires ADJ-12 all-visible declaration-order scan, all safe errors retained, first failure focus and cross-validation only after all fields pass. Source W-019-10 now distinguishes first/middle/last/multiple errors. Existing hidden slot retention, single `value_and_options` borrow, dynamic sensitivity classification and one outward action remain preservation obligations.
- Split/Panel/Scroll: logical source geometry and current-allocation facts must stay separate from clipped hit/paint geometry. Split belongs TASK-012 (TASK-008 override predecessor), not fade TASK-014. Source splitter glyphs, caller background and hover/pressed cases supplement logical seam tests. Panel/Brand/Button/TooSmall belong TASK-027; ScrollRegion/track/fade TASK-014.
- Collection scaffolding: stable keys and explicit invalidation stay; surviving disabled cursor is not universally erased. Row styles/column overrides preserve callback ownership and first-byte grapheme provenance. Interior reorder cannot be detected from length/first/last stamp alone; accepted invalidation obligations must remain executable.
- Hint/status/progress/help: keep effective live metadata and deterministic clocks/frames, exact fit/drop thresholds and source Warning disposition; decoration must not create focus stops. Borrowed Wizard slots/history remain caller-owned and require exact missing/disabled-history transitions.

## Cross-partition follow-ups, not false closure

Components B supplied fully read old splitter behavior, then A independently read all63 lines: only nonempty buffer-intersecting seam registers; full gap uses │/─ or pressed ┃/━, border strong on hover or press, caller background, drag Changed only when ratio changes. TASK-012 W-012-04 now binds exact cell geometry as well as model math.

Components B also identified TASK-020 source contracts outside this partition's blobs: Steps display mode must not silently replace current ClickOnly constructor semantics; source numbering and lifecycle/custom meta require a bounded explicit configuration. Tabs overflow must cover the first resulting frame, raw-label exact-fit widths and source indicator reservation, not only warmed scroll state. These need precise producer policy/finite witness closure under their actual owners; merely listing states does not resolve a public API choice.

## Evidence limits and handoff

This is a complete source/diff audit, not a claim of all component runtime parity. No TC production source, main/oracle worktree, canonical AGENTS, baseline or commit was changed. Disposable BA-FMT proves a concrete current defect, not the final replacement algorithm. Existing broker evidence proves its bounded lifecycle prototype only; it does not prove the new global-backend exception checker (ADJ-13 qualifier is separately owned by the closure agent). Outstanding new choices remain visibly open above until measured, adjudicated and independently reviewed.
