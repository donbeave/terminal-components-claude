# Independent adjudication and historical-prose projection rereview

2026-09-11. Bounded read-only review after the separate complete foundation branch diff. This review neither authors production fixes nor certifies future qualifiers. The verify-and-stop skill kept execution limited to the requested projection diagnostics; no additional product tests were created.

## Authority and exact scope

Read all 192 lines of architecture-adjudication.md, all four immutable F08a/F09/F16/F18 proposal documents, TASK-033's complete 152-line trusted obligations, the relevant canonical historical/source-payload rows and closure edges, and the complete 15-line independent JT replay report. Rechecked immutable TablePro app paste and Filter key branches, Jackin Inspect compact/advanced copy branches, and Showcase TextAreas construction/render/input plus Inputs fixed-layout/reference rendering. This is independent source verification; the coordinator's five actual replay tests are supporting evidence, not newly executed by this reviewer.

For ADJ-13, read the complete decision, TASK-009's fixed broker clauses and all nine finite witness rows, complete TASK-072/068 trusted contracts, the relevant README/verify scopes, and the previously fully reviewed pinned main session source. For TASK-073, read every finite attribution witness and all 23 lines of the timing contract, plus exact README and verifier writable scope. The remainder of TASK-009/073's historical copied prose was not newly exhaustively re-audited in this bounded pass.

Reviewed architecture-adjudication.md SHA256: 7e5caa090dc35764358f7d57d97f44ca6c188e67f2dc35823f31410014d94b63. Reviewed style-timing-contract.md SHA256: a48c04d0c75d81464c71965ca1a44e03e5f393add9c30d64b55f638c7dae6da9. Later changes require matching revalidation, not reuse of these identities as an unconditional approval.

## Findings and bounded conclusions

1. Scope-list mismatch: TASK-009 README.md:36–50 does not list xtask/src/main.rs, although README:16 explicitly authorizes its bounded rule18 integration and verify.toml:3 permits the file. This is not missing machine authorization, but conflicting inventories can mislead the future executor. Add the same narrowly bounded rule18-only path to the human scope list. The parent was notified; this reviewer did not edit shared task files.

2. No new source contradiction found in ADJ-09's F08a/F09/F16/F18 extension. Immutable TablePro app.rs:240–244 sends paste only to an editing lower Filter value, then consumes other Filter pastes; :1700–1715 handles both field keys, distinguishes Tab traversal from applying Enter. Immutable Jackin inspect.rs:250 and :343 discard the actual Diff event while retaining its Outcome. The unchanged clipboard/modal requirement must remain conditional on a genuinely nonempty emitted Copy, not be satisfied with an empty selection. TASK-060 is a bounded Filter contribution; TASK-063 closes the intact TablePro parent. TASK-056 owns JA-060's full Inspect path. The reported five replay tests do not establish canonical CLI/connection prefixes, full profiles or PTY coverage, and their report correctly retains those limits.

3. TASK-033 and historical F16/F18 dispositions correctly distinguish source app fixtures from modern generic APIs. Oracle TextAreas:37–39 uses the original Read-only transcript label with disabled(true), and :64 fixes rows to 13/1/10. Inputs:123 fixes its main reservation and :201–205 clips the remaining reference rows. The new requirement to preserve absent narrow-size geometry does not demand the deferred compact-switching/reveal redesign. TASK-032 owns shell allocation and TASK-039 final app closure; TASK-033 is the concrete page owner. Generic read-only navigation/copy remains separate, not authority to enable or relabel the oracle's disabled sample.

4. ADJ-13 now resolves the previously ambiguous global exception structurally: exact private OnceLock<Mutex<SignalBroker>>, effective Unix+crossterm cfg, bounded flags/lease/registration state, stable get_or_init storage initialization, fallible registration under a mutex, retained first registration and flag identity, typed poison/overlap failure, and no mode mutation before installation completes. It prohibits locking inside handlers or during terminal I/O/stop and distinguishes immutable constants from hidden mutable globals. The declared independent AST mutants cover aliases, visibility/path/cfg, extra globals and unresolved source. These are adequate explicit obligations, not proof they have executed. TASK-072's additional policy qualification and TASK-009's actual production singleton retry/concurrency/poison execution remain required; the caller-owned prototype cannot close them.

5. TASK-009 W00902 now rejects the precise main panic-cleanup gap: main restore_modes omits Show, so eventual leave is too late for the prior panic hook. The fixed witness requires actual terminal commands and termios at delegation, plus failed Show/output while later restoration still executes. This is materially stronger than a callback ordering log and retains partial-init/idempotent cleanup obligations.

6. TASK-073's scope now includes the actual extra binding owners: rowui, status, grid and ui/paint, plus Ui and optional testing export. The timing contract distinguishes real direct binding leaves from whole painting, requires complete census and one caller-owned testing-only seam, and keeps actual Instant quality separate from injected-clock arithmetic controls. No new scope gap was found in these named paths. Qualification remains pending; no performance ratio, true paint attribution, complete registry or singleton result is inferred from the contract alone.

## Independent history-prose synchronizer checks

Read all 77 physical lines of evidence/sync-history-prose.py (SHA256 a11322bb259661975dc13b25039aa4a6cf714572cb03beb8dbeae6c3eb36b685). It projects exactly six fields of each existing canonical HIST block and preserves additional authored prose and section order. It rejects duplicate/unknown IDs and missing/duplicate mapped fields. It does not infer task ownership from a prose block; ownership remains in the actual typed task/source mappings.

Executed the helper read-only under python3 -B and python3 -B -O: both reported 647 existing sections, zero changed fields, zero changed files, written=false. No helper write mode was invoked by this reviewer.

An independent in-memory diagnostic exercised 29 checks: valid identity; separate stale repair and second-pass idempotence for all six fields; missing and duplicate rejection for each field; duplicate/unknown ID rejection; and explicit demonstration of two nonmembership limits. A separate optimized interpreter run repeated 26 repair/idempotence/rejection checks. Both passed using explicit exceptions, not assert statements disabled by optimization. Authored prefix, nested authored subsection and trailing prose remained byte-identical in the tested LF string.

Important integration limit: deleting an entire HIST block, changing its heading from ### to ####, or removing an obligations file is not detected by this projection-only helper. A deleted/renamed block returned unchanged text with zero sections and zero changes in the independent diagnostic. This is consistent with its stated existing-block-only purpose, but a validator must not present this check as proof of complete expected section/file membership. Keep independently frozen expected path/block membership or an equivalent source-qualified structural invariant alongside field-drift checking. An empty set of discoverable obligations files can otherwise pass the projection loop. The helper's text I/O also assumes the repository's LF files; it does not establish arbitrary CRLF byte preservation.

No unresolved production defect is waived by these bounded conclusions. The requested independent qualifier executions remain the closure agent's responsibility. The only files authored by this reviewer in this phase are this report and the separate foundation branch report/ledger.

## Membership repair and freeze metadata follow-up

Root subsequently added fixed history-prose-membership.tsv (647 rows) and required obligations-file existence for every canonical task-index entry. Independent normal and optimized diagnostics now exercise the real synchronizer main and the independent validator historical_prose method. Deleting TASK-009's A89 block, demoting its heading from ### to ####, and deleting its required obligations file all reject in both implementations. Missing block/heading fails exact membership; missing file fails existence plus membership. An independently altered Requirement field repairs exactly once while every surrounding authored byte remains unchanged, then the second projection is idempotent. The previous existing-block-only integration gap is therefore repaired, provided the planner-owned task/membership tables remain frozen independently. Their own trusted mutation is not tested as if it were a candidate privilege.

Current read-only synchronization reports nine legitimate changed fields in TASK-021/025/026 and exits 1 after root's F20 authority update. This is correct drift detection, not a pass or a reason to revert the canonical update. Root owns the mechanical synchronization. Normal/-O mutation tests do not confuse these unrelated field errors with the specifically required missing-file/membership rejection.

The extended freeze helper contains exactly eleven style-timing assets and eighteen architecture assets, including source-policy/source-observer. A read-only dry run observed eleven not-yet-copied style assets and seven changed architecture assets. Unknown group rejects at argparse. Independent normal/-O controls reject a missing reviewed source, a nonregular destination, an unowned changed destination, an absolute path and a parent traversal; no write mode was invoked.

One additional metadata defect was reproduced in validate-plan.py:205–225: frozen_assets validates group spelling, safe existing paths and their digest, but not the exact allowed group/source/destination mapping. Replacing a valid row's source with its own existing destination passes; replacing its destination with its own existing source also passes because the bytes are identical. Unknown group does reject. The freeze helper's selected-group --check would reject the mapping drift, but the global validator must independently require complete expected mapping, not only hashes. Root was notified to share a read-only expected mapping expansion or freeze an independent exact row inventory. This report does not claim that pending repair has executed.

Reviewed follow-up hashes: sync-history-prose.py 8786de04933195ac3c5569a7edd21eff41225124615ddd1636ca876568c7e0df; history-prose-membership.tsv 606c0da97cdc1868c523dca00677d3220c866fc76992d19d9b1d76112ab5228a; validate-plan.py 38f6dee1bbaea15880b6895900392c4b1fec18f8d2d44790fff1c561949c0e32; freeze-bootstrap-assets.py c09cf93ffe5d444efb11427c1682cf93fa5d76610cc8188b4fb4833c71d68246.

## Compiler executable identity finding

At shared architecture-bootstrap-main-driver.py:166–172, compiler_identity hashes PATH cargo, while only rustc is resolved through rustup. The actual environment's cargo is /Users/donbeave/Library/Application Support/mbx/bin/cargo: a shell shim that reads mbx-target and invokes another executable. rustup which cargo resolves the actual selected /Users/donbeave/.rustup/toolchains/1.98.0-aarch64-apple-darwin/bin/cargo. Thus the recorded Cargo digest binds neither that selected executable nor even the entire wrapper dispatch chain. compile_execute:179 chooses PATH cargo independently and records cached identity after compilation, with no pre/post executable revalidation. The timing consumer pins RUSTC and release policy but invokes the same Cargo identity path, so its tool identity is incomplete too.

Structural repair must freeze actual selected Cargo and rustc absolute executable paths, hashes, versions and toolchain before invocation, use those exact paths, and revalidate around compiler/metadata calls. Keep one shared policy and propagate it through actual-source and timing consumers. Shared helper SHA256 at finding: 4a08097beacac8008c8c3a654a92620ee2613dcc52b2589ce3adb25c014aa412. No helper mutation occurred during this independent investigation: closure's source/transport/actual normal/optimized runs were still active, and the timing author stopped its incomplete optimized run to permit a later coordinated repair. Prior byte-type rejection results are distinct from missing compiler provenance; neither may be silently relabelled as the other.

## Coordinated compiler repair and final mapping rereview

After closure explicitly released all shared-helper processes, the parent authorized the structural compiler repair. The surgical-patch skill constrained edits to the responsible shared helper, source command boundary, their two protocols and a preparation-only regression script. Actual Cargo and rustc now resolve through one explicit rustup-selected toolchain before source-copy invocation. Their immutable records bind absolute executable paths, byte hashes, verbose versions and toolchain; returned records are copies. Validation checks the selected paths/toolchain and current bytes/versions before and after compiler invocation. Cargo receives the recorded RUSTC and empty compiler-wrapper settings. The source command boundary covers library builds, syn observer builds, all metadata queries and external rustc calls. The style owner separately wired the same validation API around release and featureless builds. The protected host now explicitly requires rustup resolution; absent resolution fails rather than claiming a launcher is the selected compiler. This is executable identity, not a new claim that every OS dynamic dependency is hashed.

Preparation-only compiler-identity-selftest.py passed 21 checks under both python3 -B and python3 -B -O. It executes both actual resolved tools for verbose-version observations; rejects altered record path/hash/version/toolchain, actual-byte changes, selected-path/toolchain changes, version changes and absent resolution; preserves frozen identity against caller mutation; checks pinned compiler environment; and exercises pre/post source-wrapper and shared build boundaries with injected post-invocation executable changes. The post-build mutation controls are host-function regressions, not substituted TC compilation or product behavior evidence. Full actual-source/actual-TC/transport and timing campaigns were handed back to independent reviewers and remain pending for these exact new hashes until their results arrive.

Stable repair hashes: architecture-bootstrap-main-driver.py fd6e1f240cd09367b8414041302fa926d6d939502577d2ff8f3269c9cde1cd49; architecture-bootstrap-source-driver.py b1a74b78f6a13d0ba2430e016aa25471314a31301b814adbec878428a38f9ed6; architecture-bootstrap-protocol.md 3fd781f55f1cdd0a1cc51857fa57e1754c55006dc30313a2324570e5e65ea0f7; architecture-bootstrap-source-protocol.md 3d8005b8b111cb061b300d64fea1bef08a8b2fb6b08d31f8ad076e8bab223e76; compiler-identity-selftest.py 034808f8046d80149d1571ef5831affddf47176dc871944628702d722dd0de28. The new selftest is preparation evidence, not a runtime imported asset; existing protected groups already include every changed runtime helper/protocol. No production TC file was changed.

Root independently repaired the asset-mapping validator to load only its trusted sibling freeze helper, expand the complete exact mapping and compare group/source/destination membership plus duplicate counts. This reviewer read the whole changed helper and changed validator method, then ran normal/-O controls over all 170 expected rows: a valid complete virtual projection passed; source relabelling, destination relabelling, row deletion, duplicate row, extra row and unknown group each rejected for membership/duplicate reasons. The two original identical-byte relabelling counterexamples now fail without relying on a digest mismatch. These isolated tests virtualized destination bytes to the exact real source bytes and do not claim pending task copies were actually frozen. Existing path/symlink/hash validation remains in place. Reviewed hashes: freeze-bootstrap-assets.py 3359c7e2a7a67479405276a32f1e7dc59fe50d9b2e7aef0d8642cfcacaee99e4; validate-plan.py 71b286d0fe93bec5bf7e01ff1e61b2e4d3b8e25eccd2cfc1a45fc7232cabf95b. Broker-group expansion remains the coordinator's separately pending dependency review, not covered by this 170-row result.
