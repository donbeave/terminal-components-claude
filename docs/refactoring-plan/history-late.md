# Late architecture history: direct source reconstruction

Scope: `COMPONENT_ARCHITECTURE.md` §§56–75, the §54 addendum, and the Meter/ScrollRegion amendments. Analysis reads Git objects; it does not assume that the planning checkout contains production files. Source checkpoint: `7b27732a8c3c131760ec3438f641cb3c11343a42`. Product acceptance belongs to `02f5294bfdbf38004cc49130d0aff1d01f31434c`, as required by `PLANNING_GOAL.md`. Historical visual approvals, including first-generation keys, cannot authorize differences from that oracle.

The companion [obligation ledger](history-late-obligations.tsv) separates contracts, rejected alternatives, supersession, evidence debt and present planning decisions. “Implemented” below describes source/history evidence, not a freshly executed test result. No production code, baseline, task graph, branch or checkout was changed for this review.

## Direct revision coverage

The common revision index records exact parent and whole-file before/after blobs. This review read the first relevant section versions and every distinct late-section delta. Its scope fingerprint is SHA-256 of the exact UTF-8 substring from `## §56 ` through EOF, including newline; 16 hex digits below are identification prefixes, not artifact security pins. Equal fingerprints establish identical late text even when earlier sections or state counts changed. The §54 addendum was read separately and in full at `ecc13378`.

| Fingerprint | Directly reviewed representative and blob | Changed meaning / equivalent edges |
|---|---|---|
| `8d6611eda86af981` | `3adb6efe:aed6f41d` edge; blob `dc03a38408ccd7d47eb0d3dab6113d6eb2074c52` | Original §56. |
| `892a3fbfb4f5da39` | `14bca4a3:bfcf5e44`; `8ee6f561ebd026143130dfcec52b12a04ad00b03` | Adds §§57–59. |
| `e82b7c0c6624b131` | `a1759b2a:f28a81e3`; `d9ab7d46c7490f3c028d844da3b9148d40fd43cc` | Adds §§60–72; read §§56–72 in full. |
| `4ca6a44a2b4b11f2` | `ecc13378:26913cc1`; `34602bce819eb2f88f9661b1ff3403fee8f9d3d3` | Adds §74; §73 deliberately absent. `cae32f88` late text identical; its earlier §16.5 capture gate addition read separately. |
| `2674a3d882cad05f` | `08cabdf0:b0c02082`; `d15ad05d0b51c6d4fb2483db013db2bc9ed22986` | §74 capture command exists, exact 96-cell axes. Identical late text in `0c6c45df`, `3296f55e`, `a573eba8`, `612ff7af`, `37f9031e`, `246f7e40`, `df83c5ed`, `9b311666`, `130b85a4`, `067869c7`. Reverts return to this contract; they do not reject capture reproducibility. |
| `05e8c1265c3c3f09` | `737ed7de:bdfda5df`; `84673d7952007475398287694982e79d7f6fa05b` | Explicit argv replaces ARGS string; owned run/session identity. Same late text at `2f20bea2`, `0defae22`, `ef15e28b`, `557331b6`, `ec3d760d`. `557331b6` atomic-publication amendment to Appendix A read separately. |
| `03d91924bcc29b87` | `e45fde1f`; `b6ddc1dd296e056792cb2c916adc8a91ee30cddf` compared directly with `84673d79` | Adds 480 artifact hash/byte/provenance checks but retains old ARGS wording. Same late text at `383af57c`, `8cfb810a`, `1f01b83f`, `87dae98d`. This branch variant is superseded by integration. |
| `a576b35dbfcc3933` | `e524ceaa:ec3d760d`; `aa986bd47b9ead3b5b67b2db413429d1fcc87fc7` | Merges explicit argv/identity with 480-artifact validation. Both parent-relative edges represented in common index. Same late text at `11924199`, both `aabe0b5f` edges, `c493a2cd`, `15ecde1e`, `0f018368`, `82158df5`, `e49de3f8`. |
| `4743325b48cf42d7` | `33eeb99e:a7e64c6b`; `ef5304a3c9fdbb678d0f6803257e359627cb542d` | Adds §73; duplicate branch landing `03697429:e49de3f8` has blob `181ebd1d0cb379a24111d0e2e61210de1c49612a` and identical late text. Same late text in `c936d51f`, `91f0296a`, `3f26ab14`, `cd70747d`, `43a147f0`, `15371443`, `2a0cf299`, `f01703a6`, `8514210c`. Earlier-section changes remain assigned to their respective historians. |
| `8cd9c8f27ea96a60` | `2ebe74c5:588423d2`; `ce5d17948fb1e54b6e05360dbdf8c4fff6b5240c` | Adds §75. Same late text at `2caff455`, `4afca16f`, `4a9dcab1`, `b1c8c447`, `934c92dc`, `2f139259`; their publication/theme/typing/feedback amendments to earlier sections were also read directly and sent to the early historian. |
| `3fbbeee3882238e7` | `d52ee38b:976cb45c`; `bcd85a487b0b0273c07338295a3a76a48192179f` | Adds Meter symbolic `RaisedSurface` proposal/implementation. |
| `3b03aaf1bc77b2ee` | `29ed8e5e:d52ee38b`; `ebcca7205c673b53312d77f9904314dbb8f14bd2` | Supersedes `RaisedSurface` with ordered resolved-color `ReferenceLift`; same late text at `577bf533`, `aacb7cb3`. Both earlier-section amendments read directly. |
| `37c0b508f793b1b9` | `30924105:7991eb0a`; `853f2ccae06c5de064654ef6ca97760a51cc28bf` | Amends §65: detailed gutters/header ICON allowed; global readiness still forbidden. |
| `bfd1bb679446e787` | `d4715f8e:f98c375d`; `b2da2ea84bd94458ebf7ea3a1dee04532a092ed1` | Parallel main branch still lacks integration §§75/Meter/Grid additions; substitutes read-only analyst wording. Difference from integration is branch topology, not a repeal. |
| `7ce1c12a81825950` | `da4b1364:f9ba0c3a`; `de3a8b215c05d4a2e4dbe6a3a6a7c5825f301881` | Adds ScrollRegion/NavList scrollbar visibility. |
| `0388e6f41336487f` | `c62ec8d3:f394c6a7`; `c57cb71fb1f41b54046004fe39618434d826f72e` | Meter prose no longer names nonexistent current `Theme::lift`. Same late text at `e94c1443`; earlier perf-test name change read directly. |
| `a7865863ab56ef27` | `c3b51b96:0191acbf`; `3b7885e63c7a61abf94b52590529868571fd4fa8` | Final merge preserves integration contracts and generic analyst wording. Same blob at `7b27732a`; both merge-parent edges exist in common index. Current §§73–75/Meter/ScrollRegion read in full. |

Coverage does not claim the whole 8,802-line architecture file was reread for every equal-tail edge. It proves exact equivalence within this historian's declared scope. No old history report was used as architectural authority.

## Review inputs and state amendments read directly

`a1759b2a:REFACTORING_STATE.md` final Slice-4F/H evidence was read: incomplete first-generation baselines, exact-key forced rendering, semantic item data, owner-correct bindings, caller-owned probes and outstanding visual closure are explicitly distinguished from passing component tests. `ecc13378`'s full architecture/state diff records the exact §54 addendum, §74, the independent visual FAIL superseding the prior PASS, and missing §73 decision. `33eeb99e`/`03697429` architecture/state additions then record §73 and retain its three real evidence gaps. Global subsequent checkpoint/count changes are assigned to the coordinating historian.

The current `REFACTORING_STATE.md` tail was read directly. It reports 60 exact historical recipes, 379 mismatches and 60 run failures, retains a red parity gate, and rejects five bypasses. Its then-current refusal to alter the merged product back to September 4 captures is a historical consolidation scope decision. It cannot override this planning goal's September 10 immutable Holla oracle. Its first startup-focus failure is concrete evidence of why corpus selection and source alignment must be explicit.

Direct supporting inputs:

- `docs/reviews/laneB-grid-contract.md` at main: accepted Q1–Q3; adapter owns sort, exactly five GridState readers, explicit focus-only registration. Later §61 changes schema/ragged representation, not these ownership rules.
- `docs/reviews/laneC-app-tick.md` at main: proposal permitted temporary status formatting allocation. Accepted `ecc13378` §54.6 deliberately tightens this to app-owned fixed buffers. The proposal's final-crate-name migration prerequisite is superseded by §47 timing; do not revive it. Its total RunId package includes Manager, Capsule and Cockpit and all scenario inputs.
- `docs/historical-regeneration-approval.md` at main: exact generator/manifest/input/review hashes, 998 absent-only renderings, twelve rejected mutations, explicit unknown original executable hash and glyph/modifier limitations. §75 is archive repair, never current UI acceptance.
- `efada044` REFACTORING_GOAL change: architecture remains a reference; active parity goal supersedes old slice order. Its historical allowance for small approved additions does not carry into the current task: current `PLANNING_GOAL.md` forbids visible drift from its pinned oracle.

The review-input history was checked with `git log --all -- <paths>`: both lane adjudication files have only their `3adb6efe` creation; the regeneration approval has only its `2ebe74c5` creation. Thus the directly read main blobs are also their original historical versions, not later summaries.

## Current adjudications the plan must settle

1. **Color capability ordering.** §74 leaves public `Ord` versus private rank unresolved. Main already has public `ColorLevel::narrow_to` (`crates/tui/src/theme/tokens.rs:644`) and no `Ord` derive (line 525). Preserve that existing public meet API and its exact 4×4 tests; record explicitly that adding a general public total order is rejected. This closes a stale question without inventing a new API.
2. **Dynamic disable during capture — current assessment corrected.** The historical §73 fresh-hit text leaves a gap, but implementation `715ee0777e20a09e0f373b07024076bdc742ef1d` already closes it. Pinned main `runtime.rs:1725` reconciles capture/press after successful publication through `capture::target_area`: disabled, absent, decorative, empty and layer-blocked targets cancel without retargeting or erasing independent timed feedback. The earlier assessment reading only direct delivery at `runtime.rs:1107` missed this boundary. `pointer_capture_eligibility.rs` supplies disable/re-enable, no Release/Click resurrection and dropped-frame regressions. Retain and verify this implemented policy; do not create duplicate implementation work. Current source was directly checked by historian and architecture adjudicator; the adjudicator owns fresh focused test execution.
3. **Disabled overlap proof.** Current AC1 (`runtime.rs:3122`) checks a disabled neighbor and subsequent enabled neighbor, not overlapping top/bottom regions. Add a real overlap runtime test under same-layer registration order and differing layers, proving no lower Press/Release/Click/DoubleClick/Drag/Secondary. Source inspection is useful but should not be chosen as a substitute for deterministic regression proof.
4. **Props construction guarantee.** Main `xtask/src/main.rs:3719` proves private constructor and intra-file call paths but does not reject receiver-bearing constructors. Keep the accepted private non-method contract, add AST receiver rejection with red witnesses, and state the existing intra-module boundary exactly. Do not call it a cross-file call-graph proof.
5. **Meter baseline provenance.** `c62ec8d3` changed seven of sixteen integers in `crates/tui/tests/meter_defaults.rs`, while architecture still says the sixteen-frame assertion remains unchanged pending independent review. Do not accept those current integers as product truth. Reconstruct canonical before/after cells from the immutable oracle with role/projection evidence and reconcile this stale claim explicitly.
6. **Capture matrix scope.** §74's 96-cell/three-app wording is obsolete for the four-app planning mission; later state claims 112 cells, which is also not the complete four-app 4×2×4 product. Define the required 128 base cells, then add scenario/state coverage from the application inventories. Preserve fail-closed presence/hash/provenance semantics; establish one declared inventory driving execution and checking.
7. **Visual-change authority.** §§60/62/63/70/71/72 and historical §20.10 allocations allowed particular visual corrections. Preserve the architectural ownership decisions; require exact pinned-oracle visible output. Historical ledger numbers or self-derived hashes do not grant new visual exceptions.

These are concrete reviewable planning dispositions, with historical versus proposed status explicit. Future executors must receive fixed decisions; they must not infer policy from a stale gap, a passing count, or a first-generation label.
