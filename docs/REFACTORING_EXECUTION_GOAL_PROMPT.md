# Superseded execution prompt

The old prompt described pre-migration work and is no longer an accurate
execution contract. Use the canonical parity-first goal in [`GOAL.md`](../GOAL.md).

It preserves the accepted architecture as reference, restores the historical
UI/TUI contract from `baseline/before/`, and defines the current gates.
