# TASK-027 concrete production witness obligations

Normative under R-001/R-002/R-003; adds required cases and never replaces the complete source-obligation or oracle inventory. The host freezes each row's source-qualified case identities, values, action sequence, observed fields and exact source function/range before dispatch. Main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; oracle pin is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Resolve actual source/test identities through this package's trusted family rows; a missing source branch is a failed setup, not applicability chosen by the executor. Historical proposals use their recorded authority disposition, not invented oracle output.

Each case executes real production update/draw against caller-owned inputs. CHK-004 compares independently captured oracle behavior where the source map assigns parity. CHK-006 proves ownership through independently qualified TASK-072 Rust witnesses; CHK-005 executes concrete regression and mutant outcomes. For every applicable public named slot, the host binds a row that actually paints it: observe real Id/Family/Variant/Part/Resolved, component versus caller-row provenance, source resolution/cell-write branch, changed intended cells, unchanged geometry/hits/focus and unrelated cells. Measurement-only queries cannot satisfy a painted-slot witness. A style-unreachable row must name the exact source guard and another reachable row; no generic all-states label or declaration-only union proves coverage. Run inherited CP-COMMON theme/capability/override and degenerate-size axes without weakening this concrete case set.

Mutants below are real isolated production-source changes independently applied/judged, never executor-authored booleans or expected-output rewrites. Every mutant must compile and reach the intended branch before its specific rejection counts; a build failure is not the behavioral witness. Draw twice without input, then change the named source fact: durable state stays fixed on repeat draw and only source-owned dependent observations change on update. Shared prerequisite defects return to the scoped owner rather than widening this package.

Source shorthand `M:foo.rs` means `crates/tui/src/components/foo.rs` at the pinned main; `M:keymap.rs` means `crates/tui/src/keymap.rs`, and explicit `ui/` or `collection/` subtrees are relative to `crates/tui/src/`. `O:` paths are at the pinned oracle. Line anchors identify audited starting branches, not candidate line numbers. App-composition rows retain the exact task-family source-qualified applicability and cannot invent a historical standalone widget.

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-027-01 button/brand | enabled/disabled/readiness button; key/completed click/press/outside; Brand static lockup vs click branch | exact response and geometry; static Brand has no invented focus stop; disabled action absent | shared activation branch ignores kind or disabled | M:button.rs update:280/draw:366; M:brand.rs update:228/draw:247; O:src/widgets/button.rs +brand.rs |
| W-027-02 panel | title/meta/badge/container focus with clipped/zero body and source mutation | body callback once including zero clip; no second decorative focus stop; title/meta/badge pixels follow source | skip empty callback or copy initial label | M:panel.rs draw:349/draw_badge:393; O:src/widgets/panel.rs |
| W-027-03 properties | Props and PropsList borrowed rich text, keyed reorder, wrapped value, selectable copy, secret locked/unlocked | current keyed public value copies source bytes; secret never exposed by public copy; source mutation changes cells | copy masked secret backing value or index target | M:props.rs PropsList::update:604/draw:725, Props::draw_rich:1165; O:src/widgets/props.rs |
| W-027-04 five-owner readiness | List/Grid/FilterList/Picker/Tree each Empty, Loading, Partial, Error; nonempty title and Some(detail); Loading/Partial nonempty spinner_frames; Error default/Set glyph; separate None(detail), empty spinner_frames and Error Clear controls | real inherited owner ID/provenance with truthful composed EMPTY family; TITLE paints every nonempty-title case, HELP only Some(detail), ICON only nonempty Loading/Partial frame or noncleared Error glyph; Empty ICON never paints and its query gets no paint credit; sentinels change intended cells only, geometry/hits stable | drop owner inheritance, ignore reachable ICON, claim Empty ICON paint, or count query-only sentinel | M:collection/empty.rs draw_inherited/paint_inherited:126–220, exact glyph/detail guards:153–213; List/Grid/FilterList/Picker/Tree real draw forwarding |
| W-027-05 standalone empty | standalone Empty and shared readiness helper with same text but distinct family owner | standalone and inherited owner records differ truthfully; shared layout policy preserved | replace shared helper with standalone-family paint | M:empty.rs draw:176; M:collection/empty.rs:126–220; O:src/widgets/empty.rs |
| W-027-06 too small | each minimum dimension−1/equal/+1, nonzero origin, zero clip; recovery to usable size | oracle four-line notice and exact title/secondary/muted/faint roles; no stale too-small capture after recovery | three-line notice or retained undersized state | M:too_small.rs draw:301; O:src/bin/showcase/app.rs:896 too-small notice |

## Branch-source finite cases

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-027-07 wrapped centered Empty/Error | Title T, hint alpha beta gamma delta epsilon at20x9, widths11/12/13, heights1/2/3/9, nonzero origin clipped at each edge; None hint, Some(empty), multiline/Unicode hint; ordinary and Error; paired shared inherited owner and standalone Empty | Same source wrap at max(width−4,8), complete-block vertical origin, title/blank/hint row positions, per-line truncation and centering; error prefix ! alone bold, title error tone, ordinary title muted and hint faint on caller background. Compare every clipped cell; standalone and inherited records retain their true owner. Loading/Partial retain separately qualified modern guards, not invented source output | paint only first hint line, measure full width but paint narrower, center each line vertically, force one row despite multiline hint, bold entire error title, or erase inherited owner | O:src/widgets/empty.rs:47–93; M:empty.rs measure/draw and collection/empty.rs draw_centered/paint_inherited |
