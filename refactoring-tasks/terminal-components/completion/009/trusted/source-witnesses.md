# TASK-009 finite source-qualified production witnesses

These case IDs are mandatory under R-001/R-002/R-003 and supplement, never replace, every accepted source-obligation row. Main authority is `7b27732a8c3c131760ec3438f641cb3c11343a42` (M); UI authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c` (O). Source paths and named source functions/tests below identify the starting branch; candidate line numbers are not authority. ADJ references bind the fixed architecture reconciliation in the shared adjudication. TASK-006 freezes the complete expanded case IDs, exact input bytes, enum values, checkpoints and independent expected observations before implementation dispatch; neither executor nor capture adapter chooses a subset.

## Finite expansion and checkpoints

- **P** means exact independently captured oracle cells/cursor/actions/visible state (CHK-002/004). **A** means the accepted architectural property and a real violating production mutation (CHK-005/006). A mixed row has both independent lanes, not a comparator derived from the candidate. Architecture-only APIs have no invented oracle widget.
- Every listed action is a separate ordered checkpoint: setup publication, action/update/settlement, first resulting draw, repeated draw without input, and the next changed-source publication. Compare the full allocation plus a one-cell sentinel surround, all hit/focus/cursor registrations, typed actions with stable owner/item/part, flow/invalidation and the row's named durable observations. Repeat draw must preserve durable state, callback counts and effects. App-consumer cases bind the app owner's exact full-frame scenario, but the producer task closes only its current component-owned direct behavior. Full application frames are diagnostic CHK-003 evidence with truthful immutable stage accounting until the owning app task is accepted, as README D-006 requires; no future app receipt or consumer source repair becomes this prerequisite's gate. Later app/031/069 closure joins those exact consumer cases. A component capture cannot certify an application frame.
- Render cases expand over Junie and Paper and TrueColor/ANSI256/ANSI16/Mono. Base allocations are (0,0,120,40), (0,0,40,10), (7,3,120,40), (7,3,40,10). The same source-applicable states additionally execute containment at widths {0,1,2,3,4,5,6,11,12}, heights {0,1,2,3,4,10,11,12}, and both origins. Degenerate containment is A unless the row explicitly identifies an oracle-supported tiny geometry; no unrenderable oracle allocation is silently blessed. Row-specific exact-fit and threshold sizes add to this set.
- State values are the explicitly listed alternatives and combinations in each row, not an arbitrary Cartesian product of unrelated flags. For any exposed paint part exercised by that row, expand Inherit, Clear, Set and an actual replacement slot at each declared override level; record exact Id/Family/Variant/Part/Resolved and whether cells came from component or caller row. The covered TextArea FIELD resolution-only exception applies solely to its named case. Disabled, read-only and inert registration behavior is observed separately from painted appearance.
- Event rows use all explicitly enumerated codes/modifier masks, not a representative mouse/key. Text rows use the listed literal strings and every UTF-8 split boundary; widths 0 through 120 are inclusive where specified. Collection keys A/B/C/D are distinct stable keys with identical visible labels where requested. Work rows count actual source reads, callbacks and allocations; a synthetic replacement loop cannot supply the counter.
- Each rejection in the observation column is an isolated real Rust mutation of the named source owner. It must compile and reach the branch before behavioral rejection counts. Examples include deleting the guard, routing through stale index, restoring the per-span loop or changing the exact arithmetic. A self-reported Boolean, source-string grep, case count or absent test identity is not execution proof. A missing source/branch mapping fails host preflight; no executor-selected applicability.

| Case | Source authority | Lane and axes | Concrete state/action | Exact observation and rejection |
| --- | --- | --- | --- | --- |
| W-009-01 session-stop | O:src/runtime.rs::TerminalSession/job_control;O:tests/terminal_suspend.rs | P+A; lifecycle | Idle session: SIGTSTP; fg at 100×30; SIGTSTP; fg at 80×24; quit; repeat session. | Exact launch termios while stopped and after quit; reverse mode cleanup and resumed geometry; 10-second safety bound. Reject input-dependent stopping and lost post-session default handling. |
| W-009-02 session-failure | M:crates/tui/src/runtime/session.rs::enter/leave/restore_modes;M:crates/tui/tests/terminal_cleanup.rs;O:src/runtime.rs:119–122/241–247 | P+A; lifecycle | Fail each setup/re-entry operation; call leave twice; hide cursor then trigger panic with a prior hook that observes captured actual terminal commands and termios at delegation; separately fail Show/output. | Every acquired mode and cursor visibility are restored through one shared cleanup path. Actual Show and mode cleanup are attempted before the prior hook runs once, not only later in unwinding leave; failed Show cannot skip later restoration, error/retry semantics remain. Reject omitted or leave-only Show and generic callback-log-only proof; failed output cannot publish. |
| W-009-03 service-clock | M:COMPONENT_ARCHITECTURE.md:658; fixed broker contract | A; clock | Fake now=100 ms; deadlines absent or 99/100/101/199/200/201 ms. With original deadline=101, admit 1000 inputs at timestamp100 then advance to101; never recreate the deadline from input count. Inject stop at inactive, lease-acquired/before first mode, setup-complete, active-wait, cleanup-start, modes-restored and inactive-again checkpoints. | Wait=min(remaining deadline,100 ms), with due/overdue work immediate and delivered once; input cannot reset or postpone101. Lifecycle-only wakes have zero Tick/Settle/draw/domain callbacks or simulation aging. At every broker checkpoint record lease, active/default flag, pending bit, acquired-mode mask and restoration order; no default stop with acquired modes, stale pending replay or post-session swallowed stop. |
| W-009-04 bootstrap | M:crates/tui/src/runtime.rs;M:crates/tui/tests/monotonic.rs | A; clock | Construct at epoch 0; first update/draw; equal and backward advance; resize; repeat draw. | One Bootstrap precedes input/draw without reading a clock. Equal advance is idempotent; backward advance rejects atomically; drawing cannot synthesize Tick. |
| W-009-05 feedback | M:crates/tui/src/runtime/feedback.rs;M:crates/tui/tests/feedback_clock.rs | A; clock | Elapsed activation at 0, observe 139/140 ms. Simulation starts at 2199, steps to 2200, then pauses while elapsed time advances 10000 ms. | Exact owner/part/expiry and Tick-then-Settle order. Simulation expiry never enters wall next_deadline; pause retains feedback; wrong-policy/backward synchronization rejects. |
| W-009-06 input-normalization | O:src/core/event.rs;M:crates/tui/src/event.rs | P+A; event | Enumerate terminal mouse kinds/buttons × modifier masks 0..7; both wheel axes; key Press/Repeat/Release. | Exact normalized code/modifiers/button/direction; key Release is dropped. Reject lost horizontal wheel or fabricated activation. |
| W-009-07 capability | M:crates/tui/src/runtime/session.rs;M:crates/tui/src/theme/tokens.rs;ADJ-03/17 | A; capability | All16 authored/detected level pairs under existing Detect APIs; TERM=dumb and FORCE_COLOR/CLICOLOR_FORCE/NO_COLOR/redirected-output presence combinations. | Authored capability never widens; existing Detect behavior remains unchanged. Only backend detection reads environment; pure Theme/Runtime/Scene constructors remain environment-independent. Do not reinterpret this lane as ambient clamping of Requested. |
| W-009-08 backend-boundary | M:Cargo.toml;M:crates/tui/Cargo.toml;M:Cargo.lock;fixed broker contract | A; build | Build backend-free, Unix crossterm and non-Unix profiles; attempt overlapping session leases. | signal-hook=0.3.18 is Unix optional/backend-only with default features off; unsafe denial remains. Overlap rejects before terminal mutation. |
| W-009-09 singleton-initialization | ADJ-13;M:runtime/session.rs::TerminalSession::enter;M:xtask/src/main.rs rule18 | A; lifecycle/source | From fresh process fail first registration, retry; separately succeed first/fail second, retry. Then concurrent two entry attempts, completed leave/new entry and poisoned registry-lock acquisition. Qualify exact allowed AST and each ADJ-13 forbidden mutable-global variant separately. | Exactly one persistent broker and one registration of each successful handler, same flag identity on retry, no modes/lease before complete install, one admitted concurrent session and typed rejection of the other, typed poison error; post-session default stop retained. Reject fresh inactive-handler accumulation, get_or_try_init MSRV dependency, whole-file exception and static Mutex/alias evasion. Existing immutable constants pass. No broker symbol or signal-hook dependency in isolated backend-free graphs. The protected qualifier's AST fixtures precede this task; actual production singleton execution belongs here, not to the caller-owned prototype. |
| W-009-10 explicit-color-policy | ADJ-17;M:runtime/session.rs::run_with_feedback_clock;M:theme/downgrade.rs::Theme::for_level;O:runtime.rs276/375–393/490 | P+A; capability/actual launch | All16 authored/requested level pairs through the real additive launch API with Elapsed and Simulation feedback; distinguish Detect and Requested(TrueColor) under NO_COLOR=1/COLORTERM=truecolor/TERM=xterm-256color and separately ANSI16 ambient. Repeat with already-Mono authored theme; serialize foreground/background/underline colors with bold/underline/reverse. | Actual semantic first-frame capability equals authored.narrow_to(requested), with independently expected resolved cells before backend suppression; Requested performs no ambient capability detection. Original APIs remain Detect. Rich authored TrueColor survives semantic selection under NO_COLOR, but an already-Mono theme stays Mono. Separately compare actual backend-normalized PTY cells/modifiers to the oracle: source suppression resets colors without dropping bold/underline/reverse, and semantic TrueColor does not mean emitted TrueColor. Check real cleanup and initialization/feedback invariants; reject second capability detection, forced color output, direct downgrade widening, guessed policy from theme capability, erased modifiers and a second driver. Pure enum parsing or candidate-reported capability is not sufficient. |
