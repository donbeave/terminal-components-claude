# Protected task inputs for TASK-005

Product: `oracle-tablepro`.

Expand TP-001–TP-080, including all ALL sizes and truecolor/256/16/mono/NO_COLOR profiles, Connections/form actions, Explorer/tabs/drawer, typed Grid/filter/Structure, SQL editor/results/Explain, six safety modes, history and every real overlay. Expand presets C/W/L/T/Q/QL by their actual startup/input sequences and preserve safety mode within Q actions.

Use the original in-memory connection/catalog/history/executor fixture and production handlers/rendering. Freeze alias-resolved control IDs and oracle coordinates, Ctrl+L replacement behavior, exact completion/query/error ticks, statement-at-cursor semantics, and 101/102 drawer threshold. Qualify Instant seam for140ms flash and five-second status without moving conditions or erasing text.

Execute all original35tests and exact SQL/model/application assertions. Capture every press checkpoint and intermediate result, target identity, pending SQL, selected mode, overlay/focus/cursor/scroll and ignored action. Repeat flattened traces in isolated fresh processes/worlds; keep private semantic and terminal-observable proofs separate.

Source requirements:

- [docs/refactoring-plan/tablepro.md](/work/docs/refactoring-plan/tablepro.md).
- [docs/refactoring-plan/tablepro-scenarios.tsv](/work/docs/refactoring-plan/tablepro-scenarios.tsv).
- [docs/refactoring-plan/verification.md](/work/docs/refactoring-plan/verification.md).

The host binds the source documents, canonical historical union and scenario files named above into the immutable campaign input manifest. Their exact scenario expansion algorithms and source-qualified tests are required membership, not a best-effort glob. Rejected/deferred source clauses retain their disposition. A task-local candidate manifest cannot override this document or the protected host context.
