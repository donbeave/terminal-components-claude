# TASK-010 finite source-qualified production witnesses

These case IDs are mandatory under R-001/R-002/R-003 and supplement, never replace, every accepted source-obligation row. Main authority is `7b27732a8c3c131760ec3438f641cb3c11343a42` (M); UI authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c` (O). Source paths and named source functions/tests below identify the starting branch; candidate line numbers are not authority. ADJ references bind the fixed architecture reconciliation in the shared adjudication. TASK-006 freezes the complete expanded case IDs, exact input bytes, enum values, checkpoints and independent expected observations before implementation dispatch; neither executor nor capture adapter chooses a subset.

## Finite expansion and checkpoints

- **P** means exact independently captured oracle cells/cursor/actions/visible state (CHK-002/004). **A** means the accepted architectural property and a real violating production mutation (CHK-005/006). A mixed row has both independent lanes, not a comparator derived from the candidate. Architecture-only APIs have no invented oracle widget.
- Every listed action is a separate ordered checkpoint: setup publication, action/update/settlement, first resulting draw, repeated draw without input, and the next changed-source publication. Compare the full allocation plus a one-cell sentinel surround, all hit/focus/cursor registrations, typed actions with stable owner/item/part, flow/invalidation and the row's named durable observations. Repeat draw must preserve durable state, callback counts and effects. App-consumer cases bind the app owner's exact full-frame scenario, but the producer task closes only its current component-owned direct behavior. Full application frames are diagnostic CHK-003 evidence with truthful immutable stage accounting until the owning app task is accepted, as README D-006 requires; no future app receipt or consumer source repair becomes this prerequisite's gate. Later app/031/069 closure joins those exact consumer cases. A component capture cannot certify an application frame.
- Render cases expand over Junie and Paper and TrueColor/ANSI256/ANSI16/Mono. Base allocations are (0,0,120,40), (0,0,40,10), (7,3,120,40), (7,3,40,10). The same source-applicable states additionally execute containment at widths {0,1,2,3,4,5,6,11,12}, heights {0,1,2,3,4,10,11,12}, and both origins. Degenerate containment is A unless the row explicitly identifies an oracle-supported tiny geometry; no unrenderable oracle allocation is silently blessed. Row-specific exact-fit and threshold sizes add to this set.
- State values are the explicitly listed alternatives and combinations in each row, not an arbitrary Cartesian product of unrelated flags. For any exposed paint part exercised by that row, expand Inherit, Clear, Set and an actual replacement slot at each declared override level; record exact Id/Family/Variant/Part/Resolved and whether cells came from component or caller row. The covered TextArea FIELD resolution-only exception applies solely to its named case. Disabled, read-only and inert registration behavior is observed separately from painted appearance.
- Event rows use all explicitly enumerated codes/modifier masks, not a representative mouse/key. Text rows use the listed literal strings and every UTF-8 split boundary; widths 0 through 120 are inclusive where specified. Collection keys A/B/C/D are distinct stable keys with identical visible labels where requested. Work rows count actual source reads, callbacks and allocations; a synthetic replacement loop cannot supply the counter.
- Each rejection in the observation column is an isolated real Rust mutation of the named source owner. It must compile and reach the branch before behavioral rejection counts. Examples include deleting the guard, routing through stale index, restoring the per-span loop or changing the exact arithmetic. A self-reported Boolean, source-string grep, case count or absent test identity is not execution proof. A missing source/branch mapping fails host preflight; no executor-selected applicability.

| Case | Source authority | Lane and axes | Concrete state/action | Exact observation and rejection |
| --- | --- | --- | --- | --- |
| W-010-01 published-eligibility | M:crates/tui/tests/pointer_capture_eligibility.rs;M:crates/tui/src/runtime.rs:1725 | A; runtime | Press A; successfully publish A disabled/absent/decorative/empty/layer-blocked; re-enable; Release; then a fresh gesture. | Old capture and press cancel at publication; no resurrected Release/Click; fresh Press works. Retain all 12 original eligibility identities. |
| W-010-02 aborted-eligibility | M:crates/tui/tests/pointer_publication.rs;M:crates/tui/tests/publication.rs | A; runtime | Press A; stage disabled/moved geometry but abort/drop the frame; release; later publish that geometry successfully. | Only successful publication changes routing/capture eligibility. Reject speculative geometry being used for input. |
| W-010-03 disabled-barrier | M:crates/tui/src/hit.rs;M:crates/tui/tests/pointer_capture_eligibility.rs | A; runtime | Disabled B exactly overlaps enabled A, first within one layer then across layers; primary/secondary click and wheel. | The disabled barrier remains real; neither owner receives leaked activation. Reject lower-target fallback. |
| W-010-04 gesture-phases | M:crates/tui/src/capture.rs;M:crates/tui/tests/activation_origin.rs | A; runtime | Press; inside/outside Release; Click/DoubleClick; DragStart/Drag/DragEnd; Secondary; disable during drag. | Exact ordered owner/part/phase/action sequence; Press does not become completed activation; terminal phases occur once. |
| W-010-05 focus-publication | M:crates/tui/tests/focus_restoration.rs;M:crates/tui/tests/focus_traversal.rs | A; runtime | Close nested modal; remove/disable its opener before publication; send retained key; Tab/BackTab with zero/one stops. | Restore only a live eligible published opener; retained physical key arrives once after settlement; no phantom FocusIn. |
| W-010-06 reference | M:crates/tui/src/ui/mod.rs;M:crates/tui/tests/conformance.rs | A; render | Draw live A and reference A/B with focus/hover/press overrides; reference callback attempts each registration type. | Only exact-target presentation is forced. Reference draw registers no hit/focus/cursor/scroll/binding and changes no durable state or effects. |
| W-010-07 typing-owner | M:crates/tui/tests/typing_owner.rs;M:crates/tui/tests/publication.rs | A; runtime | Two editors; switch route and resize before input; nested FocusOut commit and settlement. | Exactly one typing owner and hardware cursor. Reject stale geometry and physical-input redelivery during focus settlement. |
| W-010-08 hover-boundary | M:crates/tui/src/runtime.rs;M:crates/tui/src/hit.rs | A; runtime | Hover A; keyboard-focus B; boundary wheel A; press a ClickOnly target and release outside. | Hover observation stays truthful while keyboard presentation rules apply; boundary wheel causes no repaint/focus shift; outside release emits no click. |
| W-010-09 physical-pass-boundary | M:crates/tui/src/runtime.rs:1294–1298,1455–1472;M:crates/tui/src/intent.rs:433–449;branch BF14 | A; runtime | Use an app that drains its focused component on EVERY update before also handling cx.command. Send a raw key that returns Ignored without focus change and has a Bubble binding; separately send raw Esc followed by dismissal. Include a raw-consumed control and genuine focus/layer notifications. | One physical Key appears exactly once across the complete raw/follow-up pass sequence; each legitimate command/notification appears at its intended boundary. Reject run_update reuse of the frozen physical queue and a command-first test app that hides duplicate delivery. Preserve immutable iteration within one pass. |
| W-010-10 effective-chord-identity | M:crates/tui/src/keymap.rs:83,275,312,327,648;M:crates/tui/src/event.rs:132–139;branch BF13 | A; keymap | Publish Char('A') with NONE and SHIFT as conflicting same-scope bindings and as focused hints; repeat with Char('a') and non-character Up with NONE versus SHIFT. | Matching conflict detection and hint deduplication share one effective equivalence. Equivalent Char modifier forms cannot escape a conflict or produce duplicate hints; case and non-character modifiers remain distinct. Preserve documented structural Eq/Hash separately rather than globally lowercasing codes. |
