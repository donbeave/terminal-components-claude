# TASK-073 concrete attribution foundation witnesses

Normative R-001/R-002/R-003 under independently qualified TASK-072 CHK-006 and complete CHK-005 regression accounting. The main source pin `7b27732a8c3c131760ec3438f641cb3c11343a42` supplies `crates/tui/src/ui/mod.rs` StyledQuery/style resolution/scoped reborrows, `collection/rowui.rs` nested row/column callbacks, `crates/tui-testing/src/conformance` fixtures, and `crates/tui/tests/conformance.rs` subject invocations. This is not all-family repair or an oracle widget; TASK-031 owns final exact component closure.

The host binds each finite case below to actual production source ranges, concrete source/state values and fixture symbols before candidate dispatch. Observe real Id/Family/Variant/Part/Resolved plus explicit component-owned/caller-row-owned provenance; distinguish resolution from actual paint consumption. Expected ownership and field-to-cell relation comes from independent source witnesses, never from trusting a new candidate enum or treating added tuple fields as automatically truthful. Preserve existing public StyledQuery tuple field meaning/identity; if richer observations need a new record, do not change existing consumers into fake owner adapters.

| Case | Concrete state/action | Required observation | Reject real mutation |
| --- | --- | --- | --- |
| W-073-01 owned baseline | Real production component style/default/local patch/inherited reference, then real paint under same ID | All five legacy identity/resolution fields unchanged; richer source owner/provenance and paint sink agree | Alter real ID/family/variant, drop any resolved field or constant-fill state |
| W-073-02 same-ID composition | Parent with real child component sharing ID, ordinary child part before/after caller callback | Child-owned composition remains owned, not caller-row merely because nested | Label all nested calls caller-owned or rewrite child ID |
| W-073-03 row scope | RowUi default label then custom part, nested column/cell custom part and sibling default builtin | Only genuine caller customization has caller provenance; nested scope propagates and restores; actual label cells consume correct resolved style | Name-based ownership, lost nested scope or sibling leakage |
| W-073-04 unwind/empty scope | Callback returns normally, early-return path, unwind caught by harness; zero-area callback; then independent owner draw | Owner scope restored on every exit; no stale callback context; callback execution count matches real API | Manual reset missed on unwind/return or skip zero-area callback |
| W-073-05 measurement distinction | Same part first resolved to measure, then separately actually painted; another part resolved but never painted | Both real resolution records retained; only sink-bound consumer credited as painted; sentinel targets intended cells | Count every query as paint, suppress actual paint record or insert inert resolution |
| W-073-06 slot field relevance | Real public slot sentinel independently changes fg/bg/modifiers where sink supports them, with unrelated adjacent paint | Full resolved fields reach intended cells; unrelated geometry/hits/focus/cursor unchanged | Drop one field, store slot without consuming it or paint unrelated marker |
| W-073-07 registry generation | Independently parsed production subjects versus generated test invocation, including omitted historical subjects Probe/Dialog/Props/PropsList | One source of invocation for PARTS and degenerate-area tests; infrastructure can detect missing or dead subject before family repair | Remove invocation, register inert subject or retain unchecked extra list |
| W-073-08 instrumentation transparency | Same production fixture with observation disabled/enabled; two independent caller probe instances; repeated frame | Same cells/cursor/state/effects; no cross-instance counters or retained semantic-state contamination | Global counter, fake test painter or altered cache identity |
| W-073-09 style timing | All-call disabled/calibration/measured modes and concrete arithmetic/source mutants in style-timing-contract.md | Complete reachable entry census, honest durations and independent rejection; accepted seam reused by067 | Omit/double entry, foreign/constant interval, skipped resolver, inflated denominator, negative correction or unresolved error bound |

Actual paint ownership must have a scoped source witness at the responsible consuming branch; resolution logging alone cannot reveal a completely bypassed style path. Independent qualification therefore includes a real production mutant that bypasses attribution while still painting the same final cells, and a mutant whose extra query leaves those cells unchanged. Both must be rejected for their exact missing/false provenance reason even when snapshots match. Every mutant must compile and execute its named branch. Do not satisfy this contract by mutating a report, changing protected expected membership, installing a public patch_part hook, or claiming all-component PARTS equality before TASK-031.

The separate `style-timing-contract.md` is part of these same trusted obligations. Its actual production boundaries, calibration, field/cell transparency, upper/lower measurement bounds and negative controls are mandatory; a query-count-based estimated numerator is forbidden.

