# TASK-042 trusted obligations

## F20 source-owned preview find

Restore actual FilesPage preview find through shared matching, configured TextInput/HintBar and TextViewport marks, with source append/scalar-pop query grammar. Preserve exact empty/no-match, next/previous/wraparound, Unicode byte ranges, six-byte gutter marks, preview generation, reveal and resize. Escape clears query/matches/marks but does not clear unrelated selection, reset scroll or enable follow. Mode::Find paste updates the outer filename query before inner preview find, regardless of focus: map each CR/LF/tab to one space without trimming or collapsing. Browse paste appends text.trim() to an existing inner query without adding a focus guard; without a query it is consumed unchanged. Freeze recipients and nonrecipients through actual App routing. No inner-find-first normalization, new search widget, copied editor engine, Snapshot/trust/gate or Jackin Inspect search.

This file is part of the immutable task package, not candidate-writable configuration. It binds R-001/AC-001/CHK-004 and R-002/AC-002/CHK-006. Source rows are copied without changing their action or assertion content from the holla scenario register. Every copied field below is normative except its historical capture-status description, which is not a parity verdict.

## Branch-review source obligations

B-HO-09 Files branches in trusted/holla-branch-corrections.md are required under HO-PREVIEW-SELECT: files-browse-find-paste, files-mode-find-paste-outer and files-browse-paste-no-find. Preserve exact trim versus CR/LF/tab-to-space mapping, query recipients and unchanged preview; do not invent follow restoration. Holla owns outer-versus-inner query policy and append/scalar-pop grammar; shared configured TextInput/HintBar/matching/TextViewport own mechanics. Mode::Find paste reaches the outer query regardless of focus; Browse paste reaches an existing inner query without an added focus guard. Escape clears find, not an unrelated selection or follow state. The exact source supplement is `/task/trusted/holla-branch-corrections.md`; R-001 binds direct/PTY capture and comparison, while R-002 binds active shared ownership. TASK-040 uses its declared HO-SHELL-C04 semantic/process-lifetime contribution rather than a whole HO-MENUS pass.

## Observable outcome

### Typed keyless Files jump

Files jump explicitly enables TASK-024's typed keyless query-submission capability. Consume Submit with selected=None using the caller-owned query; never recover Enter from raw keys, synthesize a selected item, or perform path resolution in Picker. Preserve exact-path precedence over a selected suggestion, missing-path error retention and query-edit recovery through the existing virtual filesystem. The accepted TASK-024 receipt is already an ancestor through TASK-041; verify that ancestry rather than adding an application implementation of its missing behavior.

Source-bound W-042-JUMP-SUBMIT (R-001/AC-001/CHK-004, R-002/AC-002/CHK-006 and R-003/AC-003/CHK-005) extends the existing HO-HP04/HO-JUMP cases without deleting their native actions. At the immutable oracle's ParityBrowser state after the unchanged preceding hp04_browser_lists_previews_and_jumps_safely trajectory, invoke g, type ~/nowhere, Enter: no eligible suggestion must still emit Submit; selected=None reaches files_jump_event/jump_target, the modal remains with the exact source error and unchanged directory. Then Ctrl+U, type ~/work, Enter: modal closes, directory becomes the source work directory, and full frame contains Files › work and Jumped to ~/work. Retain the original exact-path-versus-selected-suggestion control and add normal/Alt Enter, blank-query and unavailable-row controls through real dispatch; record query, typed action, selected key/None, modal stack, directory/history, status and complete frame at every checkpoint. Source references: oracle 02f5294bfdbf38004cc49130d0aff1d01f31434c src/widgets/picker.rs:278–292, src/bin/holla/app.rs:1244–1338, screens/files.rs:1825–1861 and app_tests_parity.rs:737–755. Reject real compiling mutations that consume Submit, substitute the current row for None, close the modal on error, or add a raw-Enter bypass; CHK-006 must prove the real shared Picker action reaches the domain-owned resolver. Component direct fixtures remain TASK-024; this task proves intact application routing and frames, not a synthetic helper verdict.

Restore home indexing pending/partial/error/complete and Unicode search; exact resource paths/argv/outcomes; directory-before-file browser ordering, hidden toggles and parent/history identity; safe bounded normal/large/binary/control/invalid-text/symlink previews; jump suggestions/error/recovery; preview selection/find/copy/scroll. Context markers remain preview-only; no invented execution.

## Architecture and non-regression

Shared TextViewport, Picker, Menu and ScrollRegion own preview/find/selection, popup focus and scrolling. Holla virtual filesystem is domain state, not a real filesystem adapter; external-open/reveal/copy/analyze are exact simulated outcomes only.

All task-owned, prerequisite and previously closed scenarios must pass. Execute the complete required inventory without fail-fast and account for every actual result. Only exact unfinished future-owner failures in the immutable stage map may remain; missing execution, new failures, unexpected errors, changed classification or reopening a closed scenario fail. Whole-workspace build, MSRV compile, formatting, lint and compatible architecture checks must pass. A diagnostic failure is never relabelled a parity pass.

## Trust and expansion

The oracle commit is 02f5294bfdbf38004cc49130d0aff1d01f31434c; main architecture starts at 7b27732a8c3c131760ec3438f641cb3c11343a42. Accepted prerequisite receipts, taskfmt 52d9f1eb7721f409bc47beb9fced7997b5c13ede and the independently qualified proof executable are host-owned. Materialized actions, numeric oracle coordinates, scenario/checkpoint membership, palettes, clocks, fonts, profiles, semantic mappings and exact expected cells are sealed before this task starts. Candidate code cannot read expected artifacts or write the host catalog, receipts, refs, comparator or another run. The dispatcher/bootstrap is produced by TASK-001, runners by TASK-070, accounting by TASK-071 and architecture probes by TASK-072. These commands are prerequisite deliverables, not evidence that they already execute.

The host supplies the immutable full application expansion grammar and proof contract alongside this file. Expand all recorded sizes, colors, focus stops, targets, ticks, source-qualified test bodies and checkpoint boundaries exactly as the accepted baseline did. Preserve every original assertion in unchanged native ROUTE tests; separately freeze all resized assertion/coordinate mappings under `holla-route-expansion.md`. Fixed native rows are not claimed unchanged at other dimensions. Source-only helpers and SCAN/SWEEP membership are materialized before this task; the executor never expands against its own implementation. Capture full row-major schema-3 cells, blank/wide continuation cells, modifiers, cursor and semantic transitions after every listed checkpoint. Preview/state-only or exact-clock lanes remain honest; executable reachability and terminal lifecycle require an actual process and owned PTY. Both capture operations must produce an explicit complete result, including accepted non-applicability rather than omitted output.

Cross-cutting full-app rows may be closure-owned; earlier tasks still preserve the subset already closed in the host ledger. Final closure replays the union on one exact tree and requires zero application-unresolved identities. Future-owned diagnostic failures are reported as failures, never success.

## Exact scenario contracts

The following rows are complete primary ownership, not partial journeys. This task closes complete local Files/browser/preview/Jump journeys and HO-042-F/S contributions. It emits the exact source Reveal/Analyze request identity through real handlers, but complete Activity/Disk descendants close044/045. Do not port later renderers into file-menu handling.

### APP:HO-BASE-14

- **fixture:** parity-files
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** NEW(parity-files,Paused,40); DRAW; TICKS(10); DRAW; repeat fresh run; independently launch the same scenario/motion/frame through normal production CLI/lib/World constructor under holla-shell-construction-contract.md; capture source-qualified executable opening and cleanup
- **checkpoints:** initial full cells/cursor/focus/hits; unchanged after ticks; repeat byte identity; all four palettes; canonical scenario constructor genuinely succeeds; no ScenarioUnavailable or substitute world; full launch closes only at this parent owner
- **oracle_source:** src/bin/holla/scenario.rs:100; tools/holla_shots.sh:14
- **existing_evidence:** app_tests.rs::every_scenario_renders_at_every_size_without_panicking; app_tests_flows.rs::every_scenario_is_deterministic_at_a_frame
- **component_dependencies:** shell/theme/layout/fixture-specific
- **status:** planned-baseline; main parity fixture absent

### APP:HO-BASE-15

- **fixture:** parity-browser
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** NEW(parity-browser,Paused,40); DRAW; TICKS(10); DRAW; repeat fresh run; independently launch the same scenario/motion/frame through normal production CLI/lib/World constructor under holla-shell-construction-contract.md; capture source-qualified executable opening and cleanup
- **checkpoints:** initial full cells/cursor/focus/hits; unchanged after ticks; repeat byte identity; all four palettes; canonical scenario constructor genuinely succeeds; no ScenarioUnavailable or substitute world; full launch closes only at this parent owner
- **oracle_source:** src/bin/holla/scenario.rs:100; tools/holla_shots.sh:14
- **existing_evidence:** app_tests.rs::every_scenario_renders_at_every_size_without_panicking; app_tests_flows.rs::every_scenario_is_deterministic_at_a_frame
- **component_dependencies:** shell/theme/layout/fixture-specific
- **status:** planned-baseline; main parity fixture absent

### APP:HO-HP04

- **fixture:** exact pinned test fixture
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** ROUTE(src/bin/holla/app_tests_parity.rs::hp04_browser_lists_previews_and_jumps_safely); run the unchanged source-native seed at its original dimensions; separately materialize four-size oracle transcripts under holla-route-expansion.md with complete assertion/coordinate mapping
- **checkpoints:** all reached original assertions pass in the unchanged native seed; resized assertions/coordinates follow the protected source-qualified map; full checkpoints after every input draw and tick batch; fixture mutations recorded
- **oracle_source:** src/bin/holla/app_tests_parity.rs:658
- **existing_evidence:** measured test included in 143-pass binary suite; full-cell baseline still required
- **component_dependencies:** route-specific; see holla.md surface matrix
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

### APP:HO-ROUTE-21

- **fixture:** exact pinned test fixture
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** ROUTE(src/bin/holla/app_tests_rows.rs::i_b14_context_markers_and_recommendations_are_preview_only); run the unchanged source-native seed at its original dimensions; separately materialize four-size oracle transcripts under holla-route-expansion.md with complete assertion/coordinate mapping
- **checkpoints:** all reached original assertions pass in the unchanged native seed; resized assertions/coordinates follow the protected source-qualified map; full checkpoints after every input draw and tick batch; fixture mutations recorded
- **oracle_source:** src/bin/holla/app_tests_rows.rs:186
- **existing_evidence:** measured test included in 143-pass binary suite; full-cell baseline still required
- **component_dependencies:** route-specific; see holla.md surface matrix
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

### APP:HO-BROWSER

- **fixture:** parity-browser
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** ROUTE(app_tests_parity.rs::hp04_browser_lists_previews_and_jumps_safely); SWEEP browse list and preview
- **checkpoints:** loading/partial/error/empty/hidden/normal/large/binary/control/symlink previews; parent navigation restores row
- **oracle_source:** src/bin/holla/screens/files.rs:189,309
- **existing_evidence:** source-defined expansion; baseline not captured
- **component_dependencies:** TextViewport/ScrollState
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

### APP:HO-JUMP

- **fixture:** parity-browser
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** NEW(parity-browser,Reduced,0); TICKS(4); OPEN(Browse ~/work/site); g; TYPE(~/nowhere); Enter; Ctrl+U; TYPE(~/work); Enter; g; Esc
- **checkpoints:** error remains in same picker; successful path updates breadcrumb/list; cancelled jump restores focus; all intermediate frames
- **oracle_source:** src/bin/holla/screens/files.rs:606; app.rs:1247
- **existing_evidence:** source-defined expansion; baseline not captured
- **component_dependencies:** Picker/modal/focus
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

### APP:HO-PREVIEW-SELECT

- **fixture:** parity-browser
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** Expand holla-trace-corrections.md::HO-PREVIEW-SELECT named branches preview-copy-find-live, preview-selection-sweep, preview-ctrl-c-exit and preview-end-selection-noop; retain every source text preview and complete SWEEP; freeze each branch separately; separately expand holla-branch-corrections.md::HO-PREVIEW-SELECT branches files-browse-find-paste, files-mode-find-paste-outer and files-browse-paste-no-find
- **checkpoints:** exact nonempty selection and y clipboard payload; hello match/next/previous/wrap and no-match; independent Ctrl+C real process exit with no post-exit checkpoints; end-caret no-selection/no-copy retained; full frame/cursor/state and wrap offsets; Browse inner paste trims; ModeFind outer paste maps each CR/LF/tab to one space without trim/collapse and leaves loaded preview/find unchanged; no-find Browse paste consumed unchanged; Escape does not enable follow
- **oracle_source:** src/bin/holla/screens/files.rs:1163,1504; src/bin/holla/screens/files.rs:386,439,1329,1393
- **existing_evidence:** source-defined expansion; baseline not captured
- **component_dependencies:** TextViewport/selection/scroll
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

## Cross-route contribution parents

These complete parent rows are retained verbatim as required diagnostic transcripts. Only the exact frozen contribution IDs in `stage-contributions.tsv` must pass here; their later full parent owners remain unchanged by contribution success. Every parent assertion and unmasked full frame remains required at its complete owner.

### APP:HO-HP03

- **fixture:** exact pinned test fixture
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** ROUTE(src/bin/holla/app_tests_parity.rs::hp03_find_files_indexes_home_truthfully_and_resource_actions_are_exact); run the unchanged source-native seed at its original dimensions; separately materialize four-size oracle transcripts under holla-route-expansion.md with complete assertion/coordinate mapping
- **checkpoints:** all reached original assertions pass in the unchanged native seed; resized assertions/coordinates follow the protected source-qualified map; full checkpoints after every input draw and tick batch; fixture mutations recorded
- **oracle_source:** src/bin/holla/app_tests_parity.rs:580
- **existing_evidence:** measured test included in 143-pass binary suite; full-cell baseline still required
- **component_dependencies:** route-specific; see holla.md surface matrix
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

### APP:HO-ROUTE-20

- **fixture:** exact pinned test fixture
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** ROUTE(src/bin/holla/app_tests_rows.rs::i_f10_i_f12_file_actions_reveal_through_open_r_and_analyze_the_folder); run the unchanged source-native seed at its original dimensions; separately materialize four-size oracle transcripts under holla-route-expansion.md with complete assertion/coordinate mapping
- **checkpoints:** all reached original assertions pass in the unchanged native seed; resized assertions/coordinates follow the protected source-qualified map; full checkpoints after every input draw and tick batch; fixture mutations recorded
- **oracle_source:** src/bin/holla/app_tests_rows.rs:142
- **existing_evidence:** measured test included in 143-pass binary suite; full-cell baseline still required
- **component_dependencies:** route-specific; see holla.md surface matrix
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

### APP:HO-FILES-ACTIONS

- **fixture:** parity-files
- **sizes:** 80x24,100x30,120x40,160x50
- **actions:** ROUTE(app_tests_parity.rs::hp03_find_files_indexes_home_truthfully_and_resource_actions_are_exact); SWEEP file-action menu
- **checkpoints:** every menu action target stable; simulated argv/path/outcome; copy success/refusal; no real opener
- **oracle_source:** src/bin/holla/screens/files.rs:462
- **existing_evidence:** source-defined expansion; baseline not captured
- **component_dependencies:** ContextMenu/clipboard fixture
- **status:** planned-baseline; main migrated subset differs; full oracle replay required

## Correction boundary

If exact parity requires a shared-component fix, unsupported public extension, changed source authority or new trusted fixture, stop NEEDS_REPLAN and name the responsible owner. Do not duplicate mechanics inside this app or edit expected evidence. A completed task produces host-verified results and an accepted tree receipt, not a candidate-written approval.
