# TASK-025 concrete production witness obligations

Normative under R-001/R-002/R-003; adds required cases and never replaces the complete source-obligation or oracle inventory. The host freezes each row's source-qualified case identities, values, action sequence, observed fields and exact source function/range before dispatch. Main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; oracle pin is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Resolve actual source/test identities through this package's trusted family rows; a missing source branch is a failed setup, not applicability chosen by the executor. Historical proposals use their recorded authority disposition, not invented oracle output.

Each case executes real production update/draw against caller-owned inputs. CHK-004 compares independently captured oracle behavior where the source map assigns parity. CHK-006 proves ownership through independently qualified TASK-072 Rust witnesses; CHK-005 executes concrete regression and mutant outcomes. For every applicable public named slot, the host binds a row that actually paints it: observe real Id/Family/Variant/Part/Resolved, component versus caller-row provenance, source resolution/cell-write branch, changed intended cells, unchanged geometry/hits/focus and unrelated cells. Measurement-only queries cannot satisfy a painted-slot witness. A style-unreachable row must name the exact source guard and another reachable row; no generic all-states label or declaration-only union proves coverage. Run inherited CP-COMMON theme/capability/override and degenerate-size axes without weakening this concrete case set.

Mutants below are real isolated production-source changes independently applied/judged, never executor-authored booleans or expected-output rewrites. Every mutant must compile and reach the intended branch before its specific rejection counts; a build failure is not the behavioral witness. Draw twice without input, then change the named source fact: durable state stays fixed on repeat draw and only source-owned dependent observations change on update. Shared prerequisite defects return to the scoped owner rather than widening this package.

Source shorthand `M:foo.rs` means `crates/tui/src/components/foo.rs` at the pinned main; `M:keymap.rs` means `crates/tui/src/keymap.rs`, and explicit `ui/` or `collection/` subtrees are relative to `crates/tui/src/`. `O:` paths are at the pinned oracle. Line anchors identify audited starting branches, not candidate line numbers. App-composition rows retain the exact task-family source-qualified applicability and cannot invent a historical standalone widget.

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-025-01 readonly pointer | readonly first/repeated completed click at different source graphemes; drag selection; copy | caret/selection/copy change at exact pointer; document unchanged; press-only/release-outside do not edit | main pointer !read_only guard retained | M:code.rs update:1021–1114; O:src/widgets/code.rs:535 |
| W-025-02 editing focus | editable completed click vs keyboard focus only; disabled field; remapped edit command | click enters editing at pointer, focus alone does not; disabled never mutates; runtime publishes sole cursor | focus implies edit or component writes frame cursor | M:code.rs update:1021, draw:1264; O:src/widgets/code.rs |
| W-025-03 diagnostics/highlight | same revision warm draw; text revision change; diagnostic change without text change; width change | borrowed Highlighter/Segmenter cache invalidates only relevant revision/width; gutter/current line/diagnostics match live source | owned syntax clone or missing diagnostic revision | M:code.rs set_diagnostics:594/ensure:714, tests:1737–1855; O:src/widgets/code.rs |
| W-025-04 find/completion | remapped find/edit/completion; paste in find query; cancel find; tabs/CJK/combining spans; resize while scrolled | query and document targets remain distinct; copied document uses original bytes; shared completion splice and editor grammar | paste find query into document or duplicate completion engine | M:code.rs find_command:1235, update:1021, tests:1918–1960; O:src/widgets/code.rs |
| W-025-05 configured document paste | no active find query; same editable+editing source state through runtime Intent::Paste and configured helper; multiline/non-ASCII and limit boundary; readonly/disabled/nonediting controls | identical document/selection/caret/response/effects in eligible pair; no change in each ineligible document-only control; same TextEditorCore::apply paste branch | raw insert bypass, forged focus/Intent, omitted config guard or different limit branch | M:code.rs Intent::Paste:1070–1079/after_edit:641/update scroll tail:1099–1113; new helper preserves repaired production path |
| W-025-06 configured find-query paste | active find query with document(editing,readonly)=(false,false),(true,false),(false,true); runtime/helper paste combining acute sequence then Backspace; multiline query input; disabled control | query receives shared single-line normalized paste, matches refresh and match navigation occur, document bytes unchanged; exact query/caret/response/effects agree across both entry points; Backspace removes one query grapheme; disabled blocks | document eligibility checked before query target, document-only public helper, raw query append without normalization or stolen document paste | O:src/widgets/code.rs:609–631 and paste_and_backspace_edit_find_query_in_each_document_mode:1127–1140; M:code.rs update:1021/find_command:1235 |

## Branch-source finite cases

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-025-07 mode-specific indentation and scrolling | Editable document `a\n b\nc`, indent2, forward/reversed selection covering first two lines: Tab then BackTab; current-line/no-selection and tab_leaves controls; navigation Left/Right versus editing Left/Right; PageUp/PageDown; wheel vertically/horizontally away from caret, repeated pure draw, then cursor movement and resize | Tab yields `  a\n   b\nc`, BackTab restores `a\n b\nc`; exact source selection/caret rebasing and response after each indentation; tab_leaves commits/leaves rather than indenting. Navigation horizontal moves scroll by8, horizontal wheel by4 per delta; editing moves caret through shared grammar. Manual scroll persists through unchanged draw and only source cursor/resize path reveals caret; page behavior remains mode-specific | duplicate app indentation, navigation moves caret, unconditional caret reveal every frame, wheel changes document, or tab_leaves always indents | O:src/widgets/code.rs:341–448, after_edit:487–502, on_wheel:562–577 and manual-scroll tests:1151–1220; M:code.rs update/after_edit/ensure/draw |
| W-025-08 diagnostic precedence and find footer | Running range and diagnostic overlap same visible line; then diagnostic-only and running-only; active find with long query at widths7/8/9 and nonzero origin, zero/one/multiple matches, current match changed, narrow resize | Diagnostic owns marker slot over running marker; source line/text styles and current line remain exact. Footer left inset is1 iff width>2; available width>=8 shows `find `, otherwise no label. Count is empty for empty query/no matches, `no matches` for nonempty query/zero matches, otherwise current+1/total; reserve count_width+1 only when available>=label_width+count_width+4. Paint the longest whole-grapheme query suffix fitting query_room−1 and its insertion cursor, with no spill beyond allocation; each change updates cells without document mutation, cache contamination or owned syntax clone | running marker overwrites diagnostic, show query prefix instead of suffix, clip away count incorrectly, stale diagnostics revision, or write beyond component rect | O:src/widgets/code.rs:746–780, find footer:911–951 and narrow test:1222–1267; M:code.rs draw/cache/find command |
