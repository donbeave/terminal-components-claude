# Jackin branch-review interaction obligations — TASK-051

## Authority, expansion and evidence boundary

This normative source supplement binds BD-JT-10/15/17/18/21/29 families from `branch-diff-jackin-tablepro.md` for manager, prelude, shell and world routing owned primarily by TASK-051. Oracle authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c`; main `7b27732a8c3c131760ec3438f641cb3c11343a42` owns retained keyed architecture. It supplements existing primary rows without replacing native tests or enlarging the Jackin primary register.

TASK-004 must materialize every named branch below into distinct immutable action/checkpoint identities at 80×24, 100×30, 120×40 and 160×50. Source-selected labels, IDs and pointer coordinates are resolved once on the oracle and frozen. These obligations are source-inspected planning branches, not executed baseline evidence.

## JT-SHELL-ROUTING: event-specific shell admission — full owner TASK-051 (ADJ-21)

`DIRTY-EDITOR` opens Editor with unsaved pending config. `MANAGER-ENTRY` reaches Manager from the current route without fixture insertion.

1. **shell-menu-blocks-dirty-switch:** DIRTY-EDITOR; F10; attempt route to Settings. Require menu action adapter blocks leaving dirty editor except its own exit path; direct screen shortcut must not bypass.
2. **shell-mouse-quit-vs-keyboard-release:** DIRTY-EDITOR; mouse Quit. Require quit without keyboard route arbiter release sequence; observe domain/timer separately from keyboard Ctrl+Q path.
3. **shell-form-paste-without-form-changed:** open modal Form; paste. Require Form receives paste directly without shell `form_changed()` notification; key/click Changed paths still invoke it.

Sources: oracle `app.rs:505–683,754–813,489–495,1353–1356,1822–1825`; main discards ManagerOperation at `app.rs:3871–3875`. Retain captured ManagerActions reducer; wire live launch/editor/manager consumers.

## JT-WORKSPACE-TARGET: captured target authority — BD-JT-17

1. **manager-select-second-workspace-launch:** HardCases route; select workspace2; launch. Require captured target/account equals selection, not `world.workspaces.first()`.
2. **prelude-pending-workspace-bytes:** Prelude Choose with edited destination. Require actual pending workspace identity from source callback flow, not first fixture row.

Sources: oracle `prelude.rs:351–368`, `screens/mod.rs:145–164`; main `app.rs:1435,3441–3444`.

## JT-HARDCASES-PLANS: executable launch plans — BD-JT-21

Preserve disposable real-App replays for CredentialsLocked and BlockedSidecar without seeded plan mutation. Named branches must observe selected workspace/account and resulting plan stage.

Sources: oracle `screens/manager.rs:361–386,2253–2293`; main `app.rs:1393–1398`.

## JT-WORLD-POLICY; JT-ACCOUNT-PRECEDENCE; JT-SERVICE-MODALS; JT-HISTORICAL-PAINT; JT-RAIN-CHOREOGRAPHY; JT-MIGRATED-TESTS

Finite variants for these families remain bound through existing JA-* primary rows and sibling task supplements (052–056). This file names shell/routing/workspace/plan branches that TASK-051 alone must close before downstream consumers inherit target identity.

## Verification binding

R-001 binds all source outcomes to complete direct capture CHK-004 and applicable semantic capture CHK-006. R-002/AC-002/CHK-006 proves active configured shared controls. R-003/AC-003/CHK-005 retains all prior native/size identities and complete-parent stage ownership. TASK-057 repeats the Jackin union with zero unresolved entries from this supplement.
