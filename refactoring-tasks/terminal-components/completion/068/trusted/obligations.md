# TASK-068 protected closure obligations

ADJ-15/16 public documentation must preserve Select's default cursor-only/focus-traversal contract while explaining explicit Commit/ConsumeUnhandled and existing Chose/Form ownership, and preserve Steps new/navigable defaults while documenting passive/inspectable plus Compact/Rail. Document borrowed optional meta, order-independent explicit numbering/variant, RAIL8 after unchanged0–7 and ROW_NUMBER's existing identity. No new Select action or second Form engine; no passive-as-disabled substitution. Bind R-001/AC-001/CHK-006 to accepted018019020 receipts and exact public compile examples, not the rejected ValueRequests proposal or planning-only source text.

ADJ-14 requires documentation to distinguish borrowed-input allocation guarantees from arbitrary transient Display fragments. Explain one original formatting invocation, private per-Runtime pending-cluster capacity, cold/warm/fresh ownership and separately counted output allocations. Remove the universal label_fmt zero-allocation promise without weakening any inherited borrowed-input gate. Retained capacity follows lifetime high-water usage, not the most recent short draw; sensitive data remains masked before projection. Bind R-001/AC-001/CHK-006 to actual TASK-013/015 implementation receipts, not the disposable feasibility prototype.

ADJ-13 documents one explicit exception to the original no-process-global-core-state rule: private Unix crossterm SIGNAL_BROKER with OS registration/lease flags only. Preserve the original prohibition and historical source bytes, name exact scope and independent AST enforcement, and reject broader Runtime/Theme/model/cache globals or backend-free leakage. Explain that unchanged enter/run APIs select this bounded backend coordination instead of a new caller-owned host API. The caller-owned disposable fixture is feasibility evidence, not already executed singleton retry/concurrency proof. Bind R-001/AC-001/CHK-006 to TASK-009 and TASK-072's accepted receipts.

ADJ-12 requires explicit reconciliation of original F10 and public Form validation prose: all visible per-field checks run once in declaration order and retain every sensitivity-safe field error; the first failure controls focus/action; cross-validation runs only after all per-field checks pass. Explain the corrected early-return implementation without rewriting historical source evidence as if fail-fast were an accepted amendment. Bind this documentation/API consistency obligation to R-001/AC-001/CHK-006 alongside TASK-019's actual shared implementation proof.

These clauses are planner-owned acceptance inputs. Freeze this file and the exact mapped historical rows before dispatch. Candidate output cannot amend them.

## Source authority

ARCH:A01; ARCH:A02; ARCH:A25; ARCH:A29; ARCH:A30; ARCH:A32; HIST:A108; HIST:A109; accepted architecture §§16,18–20,37,42,48,73,74; architecture.md and architecture-adjudication.md.

The full historical clauses, remaining work and proof requirements are included in source-obligations.tsv, with their original ledger locations. Architecture and decision IDs are namespaced; HIST:A23 is not ARCH:A23.

## Exact closure clauses

1. Make doc-check cover the entire accepted document including §§18–20. Resolve exact current names and signatures, not only token presence. Parse errors, removed input, omitted sections and phantom API names fail.

2. Compile every real public example and required Rust block as an external-style consumer. Use stable keyed identity in the author example; insert/remove/reorder duplicate labels without changing logical focus or selection.

3. Validate public facade completeness against the accepted component registry and dispositions. No app imports private internals, testing-only implementation or legacy modules; no public API exists solely to appease a stale prose sketch.

4. Close the mono_pressed_choice_keeps_the_label_geometry deferral only with TASK-018's actual oracle-compatible geometry proof. Retire capsule_pane_clone_4x2000 as an obsolete name with exact replacement no-clone evidence, not a revived benchmark.

5. Resolve the 35 currently allowlisted doc references individually: current-target compilation/resolution or explicit immutable historical disposition. No blanket allowlist, suppressing an error, deleting a required document or claiming all references are obsolete.

6. Update architecture/state documents to the completed APIs, every decision in the host-frozen decision ledger and exact task receipts. Close expired Buttons/render packaging promises; preserve historical ledger provenance rather than rewriting past claims.

7. Run primary/MSRV/backend-free/example/rustdoc/compile-fail/format/Clippy/dependency gates with warnings denied. Prepare a reviewed final API baseline for future semver checking; do not publish a crate or run release automation.

8. Source review finds no material normal-path TODO, stub, dead duplicate implementation or unfinished migration exception. Word searches alone do not prove this; join every surviving requirement and production path with executed evidence.

## Check selection and evidence

The host binds the complete all-app and component required set to this task context; no candidate subset selector is permitted. Direct and PTY captures use their separately sealed oracle lanes. CHK-004 compares every required completed product checkpoint. CHK-006 additionally enforces the primary closure outcome; CHK-006 runs the exact applicable accepted architecture/API/performance checks, CHK-005 executes and reconciles the full test inventory, and CHK-007 requires all independently checked products and post-run integrity. TASK-070/071/072 implement and independently qualify those operations before dispatch.

The accepted task-stage command inventory records exact cargo targets, profiles and immutable relocation rules. TASK-066 may produce the explicitly scoped render-target relocation only after independent old/new identity comparison; downstream contexts use the host-accepted relocation receipt. A candidate edit to tools/test-inventory does not itself redefine that authority.

Documentation describes implemented accepted contracts; it cannot amend the product oracle or invent new public APIs to satisfy obsolete prose. A valid cross-crate API must be resolved by the gate, not removed merely because the old scanner cannot see it.
