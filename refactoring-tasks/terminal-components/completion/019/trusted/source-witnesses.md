# TASK-019 finite source-qualified production witnesses

These case IDs are mandatory under R-001/R-002/R-003 and supplement, never replace, every accepted source-obligation row. Main authority is `7b27732a8c3c131760ec3438f641cb3c11343a42` (M); UI authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c` (O). Source paths and named source functions/tests below identify the starting branch; candidate line numbers are not authority. ADJ references bind the fixed architecture reconciliation in the shared adjudication. TASK-006 freezes the complete expanded case IDs, exact input bytes, enum values, checkpoints and independent expected observations before implementation dispatch; neither executor nor capture adapter chooses a subset.

## Finite expansion and checkpoints

- **P** means exact independently captured oracle cells/cursor/actions/visible state (CHK-002/004). **A** means the accepted architectural property and a real violating production mutation (CHK-005/006). A mixed row has both independent lanes, not a comparator derived from the candidate. Architecture-only APIs have no invented oracle widget.
- Every listed action is a separate ordered checkpoint: setup publication, action/update/settlement, first resulting draw, repeated draw without input, and the next changed-source publication. Compare the full allocation plus a one-cell sentinel surround, all hit/focus/cursor registrations, typed actions with stable owner/item/part, flow/invalidation and the row's named durable observations. Repeat draw must preserve durable state, callback counts and effects. App-consumer cases bind the app owner's exact full-frame scenario, but the producer task closes only its current component-owned direct behavior. Full application frames are diagnostic CHK-003 evidence with truthful immutable stage accounting until the owning app task is accepted, as README D-006 requires; no future app receipt or consumer source repair becomes this prerequisite's gate. Later app/031/069 closure joins those exact consumer cases. A component capture cannot certify an application frame.
- Render cases expand over Junie and Paper and TrueColor/ANSI256/ANSI16/Mono. Base allocations are (0,0,120,40), (0,0,40,10), (7,3,120,40), (7,3,40,10). The same source-applicable states additionally execute containment at widths {0,1,2,3,4,5,6,11,12}, heights {0,1,2,3,4,10,11,12}, and both origins. Degenerate containment is A unless the row explicitly identifies an oracle-supported tiny geometry; no unrenderable oracle allocation is silently blessed. Row-specific exact-fit and threshold sizes add to this set.
- State values are the explicitly listed alternatives and combinations in each row, not an arbitrary Cartesian product of unrelated flags. For any exposed paint part exercised by that row, expand Inherit, Clear, Set and an actual replacement slot at each declared override level; record exact Id/Family/Variant/Part/Resolved and whether cells came from component or caller row. The covered TextArea FIELD resolution-only exception applies solely to its named case. Disabled, read-only and inert registration behavior is observed separately from painted appearance.
- Event rows use all explicitly enumerated codes/modifier masks, not a representative mouse/key. Text rows use the listed literal strings and every UTF-8 split boundary; widths 0 through 120 are inclusive where specified. Collection keys A/B/C/D are distinct stable keys with identical visible labels where requested. Work rows count actual source reads, callbacks and allocations; a synthetic replacement loop cannot supply the counter.
- Each rejection in the observation column is an isolated real Rust mutation of the named source owner. It must compile and reach the branch before behavioral rejection counts. Examples include deleting the guard, routing through stale index, restoring the per-span loop or changing the exact arithmetic. A self-reported Boolean, source-string grep, case count or absent test identity is not execution proof. A missing source/branch mapping fails host preflight; no executor-selected applicability.

| Case | Source authority | Lane and axes | Concrete state/action | Exact observation and rejection |
| --- | --- | --- | --- | --- |
| W-019-01 F01-props | M:crates/tui/src/components/form.rs::FieldKind;M:COMPONENT_ARCHITECTURE.md §15 | A; form | Build every closed FieldKind from borrowed props; rebuild configured fields. | Configuration never enters durable state; no provider execution, values bundle or public secret projection. |
| W-019-02 F02-order | M:crates/tui/src/components/form.rs | A; form | Declare A/B/C across sections/pairs; reorder C/A/B; Tab/BackTab. | Focus and submit validation follow declaration order, not coordinates/hash order; drafts stay with stable IDs. |
| W-019-03 F03-hidden | M:crates/tui/src/components/form.rs | A; form | Edit B; hide/show; remove; change type/sensitivity. | Hidden field has no geometry/hit/focus; draft retention is eligibility-specific; removal/type/sensitivity transitions clear as required. |
| W-019-04 F04-measure | M:crates/tui/src/components/form.rs;TASK012 measurecontract | A; form | Measure 1000 times at widths 0/1/40/120; reveal first invalid field after resize. | Height queries are pure; no callbacks or state changes; reveal uses current published geometry. |
| W-019-05 F05-actions | M:crates/tui/src/components/form.rs::FormAction | A; form | Each field kind produces change/commit/chooser/action/invalid outcomes. | Only stable IDs or typed keys leave through actions; no cloned values or raw secrets. |
| W-019-06 F06-draw | M:crates/tui/src/components/form.rs | A; form | Edit, draw twice, then deliver FocusOut update. | Drawing cannot commit; explicit FocusOut applies configured commit policy once. |
| W-019-07 F07-aggregate | M:crates/tui/src/components/form.rs | A; form | Two children produce actions and paint/layout invalidation in one update. | First action is retained; all invalidation merges. Reject losing later repaint/layout requirements. |
| W-019-08 F08-escape | M:crates/tui/src/components/form.rs;M:crates/tui/src/components/select.rs | A; form | Select is open inside Form/Dialog; Esc twice. | First Esc closes Select only; second reaches outer cancellation; no duplicate action. |
| W-019-09 F09-container | M:crates/tui/src/components/form.rs | A; form | Same Form bare, in Panel and in Dialog, at nonzero origin and empty allocation. | Same field engine and callback order; parent owns only chrome/layer behavior; no Form→Dialog dependency. |
| W-019-10 F10-validation | Original F10; ADJ-12; O:TablePro ConnForm::validate/render; M:crates/tui/src/components/form.rs:1175–1217 current divergence | A+P; form | Four fields A/B/C visible and D hidden. Separately invalidate A, B, C, A+C and A+B+C; then all-visible-valid/cross-invalid at C and all-visible-valid/cross-valid. Include plain and dynamically sensitive invalid fields. | Every field-failure case logs exactly [A,B,C], stores every corresponding error through the existing safe path, skips cross/submit and reveals/focuses the first invalid field only after the scan. Cross-invalid logs [A,B,C,cross]; successful logs [A,B,C,cross,submit]. Each begins with active commit once then stale-error clearing; D never validates. Public errors stay generic, private current-plain detail and every source-visible error cell match the oracle; sensitive details never escape. Reject early return, omitted/reordered/duplicate validator, lost later errors, last-error focus, cross-on-field-failure, omitted cross or premature submit. |
| W-019-11 F11-enter | M:crates/tui/src/components/form.rs::EnterPolicy | A; form | Enter on editing versus idle TextInput under SubmitsWhenIdle and Never. | First editing Enter commits without accidental submit; configured policy controls idle behavior; no duplicate key handling. |
| W-019-12 F12-dirty | M:crates/tui/src/components/form.rs | A; form | Cancel draft, commit text, immediate choice; mark clean; repeat an admitted equal-key navigation commit. | Cancel remains clean; committed value governs dirty state; exact callback sequence, including repeated-key admission; draw cannot set dirty. |
| W-019-13 F13-secret | M:crates/tui/src/components/form.rs;M:crates/tui/src/secret.rs | A; form | Secret invalid/server error; hide/remove/type change; inspect Debug and actions. | Redacted/cleared secret state; no public projection or clone; invalid secret message stays generic. |
| W-019-14 configured-bridge | M:crates/tui/src/components/form.rs:1124;M:crates/tui/src/components/choice.rs:982 | A; form | Default versus radio_navigation_commits(true); clamped Up with selected≠cursor; all inherited/configured disable combinations. | Default does not commit navigation; configured bridge consumes typed Navigated, including equal-key admission. One value_and_options borrow follows disabled query; no duplicate key/reconcile path. |
| W-019-15 select-existing-bridge | M:select.rs361–379/form.rs1121;ADJ-15 | A+P; form | LabelSelect default versus Commit/ConsumeUnhandled; values A/B/C; changed and clamped closed arrows; same/different open Enter; open and closed Tab/BackTab; inherited/configured disable and readonly; reorder options. | Existing Chose alone writes controlled usize and produces Committed exactly once when changed; clamp/same-value closure does not set dirty via a false callback. Policies survive private reconstruction; open Tab is consumed before traversal, closed Tab traverses. Radio repeated-key behavior remains distinct. Reject duplicate Form lookup, lost policy, fabricated Chose or wrong inherited guard. |
