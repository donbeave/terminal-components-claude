# Holla branch-review interaction obligations

## Authority, expansion and evidence boundary

This normative source supplement resolves B-HO-07/08/09 in `branch-diff-holla.md`. Oracle authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c`; Holla `2e2401393c47360741ebd321679de08982dca50a` has identical Holla source bytes. It supplements existing primary rows without replacing native tests, deleting assertions, changing full-parent ownership or enlarging the 135-row primary register.

TASK-003 must materialize every named branch below into distinct immutable action/checkpoint identities at 80×24, 100×30, 120×40 and 160×50, retaining each parent's palette and lane axes. Each input, draw and explicit tick batch has complete frame/cursor/state checkpoints. Source-selected labels, IDs and pointer coordinates are resolved once on the oracle and frozen; candidate label search, direct page replacement, fabricated response state and post-exit continuation are forbidden. Fixture preconditions are serialized before dispatch, never chosen by the candidate. Existing native tests retain their original dimensions and assertions; resized variants require the separate source-qualified mapping.

Quoted PASTE payloads use JSON escapes; decode each once into the indicated bytes, rather than typing literal backslashes.

These fourteen branches are source-inspected planning obligations, not executed or sealed baseline evidence. The sixteen separately measured preview/Args/overlay branches and rejected interpretations in `holla-trace-corrections.md` remain unchanged. No result from those sixteen branches qualifies the branches below.

## HO-ROUTE-10: alias and reset policy — full owner TASK-041

`RANKING-PRELUDE` constructs a fresh oracle RustDirty/Reduced frame0 World, advances discovery by four ticks, and records its actual host and cwd. Its native fixture setup uses the existing RankingMemory/UsageStore methods, not a replacement ranking algorithm: install aliases gp→git.pull and oldpush→git.push; pin git.push at cwd and at /other; hide git.push at both paths; add one current-time action use for git.push at each path and one global use, plus a learned query push→git.push. Add equivalent sentinel usage/hide/pin records for git.pull. Preserve every unrelated world field and record the complete initial memory. The /other path is a simulated scope key, not a real filesystem access. The fixture-seeded variants are direct/native evidence; they do not establish CLI reachability. Independently capture the ordinary event-only alias dialog route from the unchanged `alternatives_pin_alias_hide_and_why` test in the PTY lane.

`SELECT-ACTION(id)` uses the real Finder query and source-frozen list navigation to select the exact item ID, including a hidden item's search route. `ALIAS(text)` sends Alt+Enter, End, Up×3, Enter to open the alias prompt; Enter begins editing the focused field, Ctrl+L selects its complete existing value, TYPE(text) replaces it, and Enter commits and submits the prompt through its primary Save action. There is no extra post-dismissal Enter. Freeze every boundary; a direct call to set_alias cannot stand in for this route. `RESET` opens Alt+Enter, End, Up, Enter, selecting Reset ranking.

1. **alias-transfer-exact:** RANKING-PRELUDE; SELECT-ACTION(git.push); ALIAS(" gp "). Require trimmed gp assigned to git.push, no remaining gp owner on git.pull, removal of oldpush from git.push, and unchanged unrelated aliases. Require exact success status, prompt dismissal and resulting Finder identity/frame. An already-owned-alias error is wrong.
2. **alias-case-sensitive-ownership:** fresh RANKING-PRELUDE; SELECT-ACTION(git.push); ALIAS(" GP "). Require gp→git.pull and GP→git.push coexist, with oldpush removed. The storage conflict rule is exact-string comparison, not lowercase uniqueness; observe raw alias ownership separately from Finder's matching/ranking behavior.
3. **ranking-reset-all-paths:** fresh RANKING-PRELUDE; SELECT-ACTION(git.push); RESET. Require every git.push action-use and learned-query entry removed, all git.push hidden entries removed across both paths, both git.push pins preserved, and oldpush alias preserved. All git.pull sentinel records remain byte-equal. Require exact "Ranking reset for this row · pins and aliases kept" status and ensuing Finder/ranking state. Do not substitute main reset_at's path-local pin/hide/global-alias deletion.

Sources: `app.rs:1103–1118,2705–2724,2863–2897`; `domain/stack.rs:708–739`; `domain/usage.rs:105–143,219–224`; `app_tests_flows.rs:519–572`; shared `src/widgets/dialog.rs:222–250` prompt commit/submission. Retain typed command identity and encapsulated app-owned ranking policy. Shared TextInput/Dialog/Menu own editing and focus; do not resurrect public mutable old ranking structs as the implementation API.

## HO-MENUS: keyboard precedence — TASK-040 contribution, full parent TASK-049, final TASK-050

Each branch starts NEW(rust-dirty,Paused,40) at a normal-sized Here/Finder with no activity input owner and no modal. No domain command is invoked. The two Ctrl+C branches require actual PTY process lifetime evidence; direct quit state alone is insufficient.

1. **menu-ctrl-c-captured:** F10; Ctrl+C; Escape. At the chord, menu remains open with the same menu/cursor, no quit modal appears, quit=false and the process remains alive. Escape closes only MenuBar and restores source focus.
2. **menu-ctrl-q-captured:** F10; Ctrl+Q; Escape. Require the same capture/lifetime/focus facts; Ctrl+Q cannot open Quit while MenuBar owns the keyboard.
3. **menu-dismiss-ctrl-c-exit:** F10; Escape; Ctrl+C; terminate transcript. Require source quit=true, actual executable exit and terminal restoration, with no subsequent input checkpoint. This guards against globally suppressing Ctrl+C to pass the menu-open case.
4. **menu-dismiss-ctrl-q-confirm:** F10; Escape; Ctrl+Q; Escape. Require the real Quit modal, unchanged underlying Finder, cancellation and live process with quit=false.

Sources: `app.rs:507–565`, especially the early return at517–529; shared `src/widgets/menu.rs:442` modified-character rejection. Keep accepted App/Cx/Ui ownership. Application command policy must not execute global interrupt/quit before the active MenuBar branch; retained lifecycle polling still runs. True modal handling remains earlier. The separately required MenuBar paste passthrough is not keyboard fallthrough. HO-SHELL-C04 closes only these exact typed semantic/process-lifetime assertions under040 CHK-006, backed by actual executable capture; it never closes HO-MENUS frames or its later config/menu destinations.

## HO-PREVIEW-SELECT: Files paste recipient — full owner TASK-042

Reuse `BROWSER-TEXT(/Users/alex/work/site/index.html)` from `holla-trace-corrections.md`. It reaches the preview through the real list and focuses PREVIEW. `FIND-TEXT` starts a fresh parity-browser/Reduced/0, ticks four times, opens Browse ~/work/site, stays on LIST, presses / to enter outer Mode::Find, types index.html, selects the exact source file path with source-frozen list navigation, and presses Right to load/focus its preview. Freeze outer query, hits, selected path and inner preview identity separately.

1. **files-browse-find-paste:** BROWSER-TEXT; /; TYPE(he); PASTE("  llo\r\n\t "); Escape. The pasted payload contains actual CR/LF/tab. Require inner query hello, exact hello match and six-byte-gutter-adjusted marks; outer filename query is unchanged. Escape clears inner query/matches/marks, retains source focus/selection/scroll and does not enable follow.
2. **files-mode-find-paste-outer:** FIND-TEXT; /; TYPE(hello); PASTE(" \tZ\r\n "); Escape. Require outer query index.html followed by two spaces, Z and three spaces: CR, LF and tab each become one space, with no trimming or whitespace collapse. Inner query hello, its matches/marks and loaded preview content remain unchanged immediately after paste; the outer hit set/cursor refresh through source policy and may no longer select that preview path. Escape then clears only the inner find state. Do not rebuild the preview merely because outer selection changed.
3. **files-browse-paste-no-find:** fresh BROWSER-TEXT; PASTE(" hello "); require consumed input with unchanged query, preview content, selection and clipboard. Paste must not invent a find bar or edit read-only output.

Sources: `screens/files.rs:309–317,370–418,439–448,1163–1232,1329–1334,1393–1415`. Files query mutation resets find_at to0 for a nonempty query; empty-query handling clears marks without assigning a new index. Source Backspace removes one Unicode scalar, and query typing is append-oriented; composing shared TextInput must not add a different caret/editing grammar. Mode::Find routing precedes preview find regardless of focus; Browse routes to an existing inner query without an extra PREVIEW focus guard. Shared reusable controls own the editing mechanism, while these app policies select the command recipient.

## HO-OUTPUT-FIND: retained output find — full owner TASK-044

`BURST-DONE` starts parity-executor/Reduced/0; TICKS(6); RUN(Emit a burst) through the actual Finder; TICKS(40). Require at least4000 retained lines as in the unchanged native proof. `BURST-LIVE` uses the same fresh route but ticks twelve times after launch rather than forty. The exact numeric output/drop counts at these boundaries are frozen from the oracle; missing live output is a failed precondition, not permission to seed a replacement activity.

1. **output-find-paste-trim:** BURST-DONE; /; TYPE(line); PASTE("  44\r\n\t "); require query line44, not line 44: source trims only the pasted fragment and appends it without inserting a separator. Require no output mutation and exact no-match state; Escape; /; TYPE(line ); PASTE(" 44 "); require query line 44 with exact positive matches, marks and reveal. Keep query/index and underlying output as separate observations.
2. **output-find-escape-keeps-follow-paused:** BURST-DONE; End; /; TYPE(line 44); Enter; Escape; End. Require follow=true before find, follow=false on match reveal, query/matches/marks cleared by Escape but follow still false, then the explicit final End resumes follow. Escape does not clear an unrelated selection or jump to tail. The original next/previous/wrap/no-match native assertions remain required.
3. **output-find-live-coordinate-reconcile:** BURST-LIVE; /; TYPE(burst line); Enter; Enter; TICKS(4). Require append/retention reconciliation, unchanged query and no edit to underlying output. Before recomputation, identify the selected (visible-line index, byte range); keep its new match index only when that exact tuple survives, otherwise choose0. Source does not preserve a stable content-line identity through eviction. Capture exact dropped count, retained contents, current match and frame after each tick, including any changed-last-line or stream-filter replay in the original native inventory.
4. **output-find-empty-no-match:** BURST-DONE; /; Enter; Down; Up; TYPE(zzzz-no-output-match); Enter; Down; Up; Escape. Require empty/no-match inputs consumed without accidental activity action, no reveal without a match, and exact query/matches/marks/focus/follow states. Empty-query run_find clears marks but does not explicitly reset stored find_at.

Sources: `screens/activity.rs:98–224,322–401,467–470,533–563`; `domain/parity.rs:790–800`; `app_tests_proofs.rs:786–825`. Activity run_find preserves an existing exact (line, range) tuple even on query recomputation; the native test's changed-length query loses that tuple and therefore resets, not a universal new-query reset rule. Attached/stdin key ownership precedes find; paste enters stdin before find when input=true. Shared matching/TextViewport marks and editor operations remain reusable; app query policy does not authorize a new search widget, copied editor engine or search in Holla Snapshot/plan/trust facts or Jackin Inspect.

## Verification binding

R-001 binds all source outcomes to complete direct capture CHK-002, applicable PTY capture CHK-003 and exact oracle comparison CHK-004; normal app tasks use AC-001, while040 checks HO-SHELL-C04 semantic/process-lifetime facts under AC-001/CHK-006 from actual executable capture; its existing whole-frame and HO-CLI-PREVIEW process comparison remains AC-008/CHK-004. R-002/AC-002/CHK-006 proves active configured shared controls and absence of inert calls, application-local text mutation or global routing bypass. R-003/AC-003/CHK-005 retains all prior native/size identities and complete-parent stage ownership. TASK-050 repeats the full union with zero unresolved Holla entries. No branch may qualify from its name, static output, substituted model or the source inspection recorded here.
