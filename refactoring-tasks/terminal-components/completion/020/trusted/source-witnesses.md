# TASK-020 finite source-qualified production witnesses

These case IDs are mandatory under R-001/R-002/R-003 and supplement, never replace, every accepted source-obligation row. Main authority is `7b27732a8c3c131760ec3438f641cb3c11343a42` (M); UI authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c` (O). Source paths and named source functions/tests below identify the starting branch; candidate line numbers are not authority. ADJ references bind the fixed architecture reconciliation in the shared adjudication. TASK-006 freezes the complete expanded case IDs, exact input bytes, enum values, checkpoints and independent expected observations before implementation dispatch; neither executor nor capture adapter chooses a subset.

## Finite expansion and checkpoints

- **P** means exact independently captured oracle cells/cursor/actions/visible state (CHK-002/004). **A** means the accepted architectural property and a real violating production mutation (CHK-005/006). A mixed row has both independent lanes, not a comparator derived from the candidate. Architecture-only APIs have no invented oracle widget.
- Every listed action is a separate ordered checkpoint: setup publication, action/update/settlement, first resulting draw, repeated draw without input, and the next changed-source publication. Compare the full allocation plus a one-cell sentinel surround, all hit/focus/cursor registrations, typed actions with stable owner/item/part, flow/invalidation and the row's named durable observations. Repeat draw must preserve durable state, callback counts and effects. App-consumer cases bind the app owner's exact full-frame scenario, but the producer task closes only its current component-owned direct behavior. Full application frames are diagnostic CHK-003 evidence with truthful immutable stage accounting until the owning app task is accepted, as README D-006 requires; no future app receipt or consumer source repair becomes this prerequisite's gate. Later app/031/069 closure joins those exact consumer cases. A component capture cannot certify an application frame.
- Render cases expand over Junie and Paper and TrueColor/ANSI256/ANSI16/Mono. Base allocations are (0,0,120,40), (0,0,40,10), (7,3,120,40), (7,3,40,10). The same source-applicable states additionally execute containment at widths {0,1,2,3,4,5,6,11,12}, heights {0,1,2,3,4,10,11,12}, and both origins. Degenerate containment is A unless the row explicitly identifies an oracle-supported tiny geometry; no unrenderable oracle allocation is silently blessed. Row-specific exact-fit and threshold sizes add to this set.
- State values are the explicitly listed alternatives and combinations in each row, not an arbitrary Cartesian product of unrelated flags. For any exposed paint part exercised by that row, expand Inherit, Clear, Set and an actual replacement slot at each declared override level; record exact Id/Family/Variant/Part/Resolved and whether cells came from component or caller row. The covered TextArea FIELD resolution-only exception applies solely to its named case. Disabled, read-only and inert registration behavior is observed separately from painted appearance.
- Event rows use all explicitly enumerated codes/modifier masks, not a representative mouse/key. Text rows use the listed literal strings and every UTF-8 split boundary; widths 0 through 120 are inclusive where specified. Collection keys A/B/C/D are distinct stable keys with identical visible labels where requested. Work rows count actual source reads, callbacks and allocations; a synthetic replacement loop cannot supply the counter.
- Each rejection in the observation column is an isolated real Rust mutation of the named source owner. It must compile and reach the branch before behavioral rejection counts. Examples include deleting the guard, routing through stale index, restoring the per-span loop or changing the exact arithmetic. A self-reported Boolean, source-string grep, case count or absent test identity is not execution proof. A missing source/branch mapping fails host preflight; no executor-selected applicability.

| Case | Source authority | Lane and axes | Concrete state/action | Exact observation and rejection |
| --- | --- | --- | --- | --- |
| W-020-01 list-click | O:src/widgets/list.rs::on_click;M:crates/tui/tests/list_focused_click.rs | P+A; collection | Single/Multi List; current-row completed click twice; different row; disabled row; Press/outside Release. | Exact chosen/checked toggle and cursor/actions; disabled row consumes without activation; reject requiring a second click. |
| W-020-02 list-range | O:src/widgets/list.rs::move_cursor/on_key;M:crates/tui/tests/list_boundary.rs | P+A; collection | Multi A/B-disabled/C/D; ShiftDown/PageDown/Home/End; a twice; Ctrl/Alt character negatives. | Cursor follows source navigation even when it lands on disabled B; inclusive anchored selection and activation skip/deny disabled items without rewriting the cursor. Select-all toggles exactly; boundary flow is truthful; modified characters do not perform plain actions. |
| W-020-03 nav-actions | M:crates/tui/src/components/nav_list.rs;M:crates/tui/tests/nav_compact.rs | A; collection | Nav sections A/B with badges; focus cursor differs from current; Up/Down then Enter; compact/full resize. | Chose differs from EnterContent; only domain shell focuses content; exact compact sections, badges and hits. |
| W-020-04 nav-scroll | M:crates/tui/tests/nav_list_boundary.rs;M:crates/tui/tests/nav_list_scrollbar_visibility.rs | A; collection | First/last boundary wheel; hidden bar; current row at faded edge; narrow section header. | No boundary repaint/chaining/focus movement; current row remains protected; hidden bar reserves no column. |
| W-020-05 tree-lazy | O:src/widgets/tree.rs::toggle/set_children;M:crates/tui/tests/tree_branch_policy.rs | P+A; collection | Lazy empty Tree branch: Right/Enter/toggle click; busy then children arrive; collapse/reopen. | Exact typed expansion request and branch policy; busy clears on load; no duplicate request or stale index target. |
| W-020-06 tree-filter | O:src/widgets/tree.rs::flatten/set_filter;M:crates/tui/tests/tree_state_replacement.rs | P+A; collection | Collapsed parent with matching child; filter then clear; replace/remove current node. | Match+ancestor projection preserves saved expansion; clear restores it; live-key fallback, not stale display index. |
| W-020-07 tree-hit | M:crates/tui/tests/tree_selected_click.rs;M:crates/tui/tests/tree_presentation.rs | A; collection | Tree toggle versus row click; repeat selected row; disabled branch; duplicate-label siblings. | Exact branch/activation policy; hover is not activation; disabled row has no action; all hits stay in allocated rows. |
| W-020-08 steps-lifecycle | O:src/widgets/steps.rs::StepState/frontier/counts/render | P+A; collection | Six Steps states: Queued/Running/Done/Skipped/Failed/Blocked; Running→Failed; numbering on/off. | Exact glyphs, labels/meta, counts/frontier and spinner frames; Skipped is terminal but remains inspectable. |
| W-020-09 steps-navigation | O:src/widgets/steps.rs::on_key/on_click;M:crates/tui/src/components/steps.rs;ADJ-16 | P+A; collection | Source passive versus inspectable Steps; preserve separate main new/navigable; Up/Down/Home/End through Skipped/Blocked; click last; empty source. | Source passive has no row focus/action but wheel remains usable; inspectable moves without activation; generic constructors preserve existing semantics; cursor row is fade-protected. |
| W-020-10 steps-meta | O:src/widgets/steps.rs::render | P; render | Default queued/skipped/blocked meta versus custom meta; avail=mw+11/+12/+13. | Meta appears only when avail≥mw+12; label budget is avail−mw−2; exact right alignment/styles without geometry drift. |
| W-020-11 chip-events | O:src/widgets/chips.rs::on_key/on_click;M:crates/tui/src/components/chip.rs | P+A; collection | Checked/unchecked, removable/nonremovable chips; Enter/Space/Delete/Backspace/x/+/X; Ctrl/Alt character negatives. | Exact stable-key Activate/Toggle/Close and keyless Add/Clear; readonly/disabled block mutation; no fake item key. |
| W-020-12 chip-lead | O:src/widgets/chips.rs::render;O:src/bin/showcase/pages/chips.rs:47;O:src/bin/tablepro/tabs.rs:406 | P+A; collection | Lead match all↔match any click/hover; empty chips with Add; narrow/zero allocation. | Lead is not a cursor item and emits a keyless event for domain consumption. Exact source-qualified focus-registration behavior and lead padding/hover. |
| W-020-13 chip-fit | O:src/widgets/chips.rs::render;M:crates/tui/src/components/chip.rs | P; render | Label abc; removable/error/checked each true/false; widths fit−1/fit/fit+1; Add present/absent. | Whole chip width=label width+3+optional close 2, gap 1; first nonfit ends at ellipsis; Add fits only after items in nonreserved mode; close wins its cell hit. |
| W-020-14 tabs-planes | O:src/widgets/tabs.rs::render andinlineplane tests | P; render | General/Mounts/Roles: active Mounts, hover General, cursor Roles; quiet on/off; height 1/2. | Active plane is one lift with the only underline; inactive hover/cursor are two lifts, cursor bold; no row gutter; exact underline extent. |
| W-020-15 tabs-status | O:src/widgets/tabs.rs::tab_width/render | P; render | All dirty/busy/error combinations with prefix/suffix/closable; all 10 spinner frames. | One two-cell state budget; busy > error > dirty; exact muted prefix/suffix styles, close hit and width. |
| W-020-16 tabs-window | O:src/widgets/tabs.rs::on_key/remove/render;M:crates/tui/src/components/tabs.rs | P+A; collection | Empty/new; Left/Right/h/l/1..9/Enter/Space/x/Delete/n; first/middle/last overflow and fitting→overflow→fitting resize; raw-label exact-fit−1/exact-fit/exact-fit+1; remove before/current/last. | Exact Activated/Close/New and stable active/cursor fallback; source overflow reserves its full eight-cell indicator budget on the first resulting frame as well as subsequent frames, never a first-frame four-cell shortcut or clipped-ink width. Counts clamp99; modified characters never invoke plain actions. |
| W-020-17 stable-collections | M:crates/tui/src/components/{list,nav_list,tree,steps,chip,tabs}.rs;M:crates/tui/tests/perf_collections.rs | A; identity | Each family: A/B/C duplicate labels→C/A/B→insert D→remove held B between publication and input. | No stale-index activation/copy/reorder; retained keys keep identity; removed target cancels without lower retargeting. |
| W-020-18 collection-work | M:crates/tui/tests/perf_collections.rs;M:crates/tui/src/components/{tree,steps}.rs | A; work | 100000 rows with viewport 1/10/40; update one tree branch; advance one frontier; revision saturation. | Actual callbacks/work track visible or changed derived ranges; safe invalidation on saturation; no whole-model clone or hidden per-frame full scan. |
| W-020-19 steps-capabilities | O:steps.rs::on_key/on_click;M:steps.rs::update;ADJ-16 | P+A; runtime | new/navigable/passive/inspectable with Queued/Skipped/Blocked; first/last/clamped; Press/outside Release/Click/DoubleClick, Enter/Space, Up/k/Down/j/Home/g/End/G/PageUp/PageDown and all six modifier-bit subsets; wheel/thumb/track, disabled and inert. | Exact four-way focus/hit/binding/action matrix; passive scrolling survives with no row interaction; inspectable completed-click/navigation Moved only and no Press cursor mutation. Generic constructors remain byte/behavior compatible. Reject disabled-as-passive, generic activation in inspectable or all-modifier plain guard. |
| W-020-20 steps-rail-geometry | O:steps.rs213–350;ADJ-16 | P+A; render | Six states; ordinal indices0/8/9/98/99/998/999/99999; numbered true/false; all ten spinner frames; cursor != frontier; hover/press/focus, scrollbar present/absent. | Exact gutter/icon/ordinal/label positions and fixed3 advance, source lifecycle glyph and role/modifier cells; no extra frontier emphasis; cursor fade protection even passive. Raw source overflow tiny containment is A, not invented P. Reject ordinal cap, fixed-width numeric truncation, wrong lifecycle defaults or changed Compact cells. |
| W-020-21 steps-meta-policy | O:steps.rs213–350;ADJ-16 | P+A; render | Compact/Rail each state; meta None/Some(empty)/Some(abc); source threshold avail=mw+11/+12/+13; both origins; builder ordering presentation/numbered/variant in both orders. | None uses presentation defaults, empty suppresses; Rail inset1 and exact threshold/budget; explicit numbered and variant always win. Labels and callbacks remain borrowed and visible-only. Reject Option flattening, order-dependent override or app-local meta painting. |
| W-020-22 steps-recipe-identity | M:theme/recipe.rs18–155;M:measure.rs149;ADJ-16 | A; styling/API | Assert DEFAULT0..GHOST7 unchanged, RAIL8 once in ALL/name/raw, existing ROW_NUMBER35 in PARTS; default and explicit variants through every part and caller RowUi; all Inherit/Set/Clear/replacement slots at every override level. | Rail recipe affects Steps only; DEFAULT/other-family originals remain unchanged; explicit DEFAULT with Rail changes style not geometry. Public non-Clone/non-Display borrowed model compiles via custom row/meta. Reject hardcoded role/color, omitted effective variant, duplicate ALL member, renumbered part or lost props in type-changing builder. |
