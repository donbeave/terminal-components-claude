# Protected task inputs for TASK-002

Product: `oracle-showcase`.

Implement only the Showcase reference observation/clock adapter and source-only scenario expansion. Materialize every SC row and its finite size/color/focus/resize axis, including 368 default page frames, every live and disabled hit, Diff, General/Members/Environment settings, six Inputs controls, customer Grid pending/edit/SQL and timed form/button/output flows. Freeze exact coordinates, key counts, ordered checkpoints and semantic identities; candidate-derived selectors are forbidden.

Drive the original production App::handle/render and actual CLI --page/--color routes. Keep source-owned simulation ticks distinct from logical Instant deadlines. Prove clock adaptation at busy >=2200 ms, submit >1800 ms, flash >=140 ms and status >4000 ms without changing conditions or product state. Retain exact 31/32-row compact navigation, 99/100 inspector and 109/110 sidebar thresholds and minimum 72x20.

Run all original Showcase test identities from pinned source; preserve their assertions, instrument only allowed time/observation seams and show reverse-patch original-blob equality. Capture initial, every intermediate, press-before-release and ignored-input checkpoints. Replay every flattened action program afresh and require complete cell/cursor/state equality. Distinguish direct private semantics from terminal-observable PTY assertions.

Source requirements:

- [docs/refactoring-plan/showcase.md](/work/docs/refactoring-plan/showcase.md).
- [docs/refactoring-plan/showcase-scenarios.tsv](/work/docs/refactoring-plan/showcase-scenarios.tsv).
- [docs/refactoring-plan/verification.md](/work/docs/refactoring-plan/verification.md).

The host binds the source documents, canonical historical union and scenario files named above into the immutable campaign input manifest. Their exact scenario expansion algorithms and source-qualified tests are required membership, not a best-effort glob. Rejected/deferred source clauses retain their disposition. A task-local candidate manifest cannot override this document or the protected host context.
