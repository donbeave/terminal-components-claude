# TASK-022 concrete production witness obligations

Normative under R-001/R-002/R-003; adds required cases and never replaces the complete source-obligation or oracle inventory. The host freezes each row's source-qualified case identities, values, action sequence, observed fields and exact source function/range before dispatch. Main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; oracle pin is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Resolve actual source/test identities through this package's trusted family rows; a missing source branch is a failed setup, not applicability chosen by the executor. Historical proposals use their recorded authority disposition, not invented oracle output.

Each case executes real production update/draw against caller-owned inputs. CHK-004 compares independently captured oracle behavior where the source map assigns parity. CHK-006 proves ownership through independently qualified TASK-072 Rust witnesses; CHK-005 executes concrete regression and mutant outcomes. For every applicable public named slot, the host binds a row that actually paints it: observe real Id/Family/Variant/Part/Resolved, component versus caller-row provenance, source resolution/cell-write branch, changed intended cells, unchanged geometry/hits/focus and unrelated cells. Measurement-only queries cannot satisfy a painted-slot witness. A style-unreachable row must name the exact source guard and another reachable row; no generic all-states label or declaration-only union proves coverage. Run inherited CP-COMMON theme/capability/override and degenerate-size axes without weakening this concrete case set.

Mutants below are real isolated production-source changes independently applied/judged, never executor-authored booleans or expected-output rewrites. Every mutant must compile and reach the intended branch before its specific rejection counts; a build failure is not the behavioral witness. Draw twice without input, then change the named source fact: durable state stays fixed on repeat draw and only source-owned dependent observations change on update. Shared prerequisite defects return to the scoped owner rather than widening this package.

Source shorthand `M:foo.rs` means `crates/tui/src/components/foo.rs` at the pinned main; `M:keymap.rs` means `crates/tui/src/keymap.rs`, and explicit `ui/` or `collection/` subtrees are relative to `crates/tui/src/`. `O:` paths are at the pinned oracle. Line anchors identify audited starting branches, not candidate line numbers. App-composition rows retain the exact task-family source-qualified applicability and cannot invent a historical standalone widget.

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-022-01 DataGrid completed gestures | current cell vs different cell, range present vs absent, editable vs readonly; press-only and release-outside | DataGrid different cell moves; current unranged globally noneditable Grid activates; globally editable Grid invokes begin_edit, with read_only-column or pending-deleted-row controls Consumed/no action/no edit/no mutation; no press-only/outside activation; gesture metadata retained | universal single-move/double-activate rule or press activation | M:grid.rs navigate:2404, update:2817; O:src/widgets/grid.rs:1322–1345 |
| W-022-02 editor modes | inline, cycle, external, refused, locked and readonly target; active editor caret; invalid commit then navigation | only update_editable/GridEditor invokes mutation; refused/locked targets and validation rejection do not move or write; caret belongs runtime | readonly model write or commit-then-validate | M:grid.rs update_editable:2848, begin_edit:2881, drive_editor:2931, tests:5266–5359; O:src/widgets/grid.rs |
| W-022-03 ragged model | None cell at leading/interior/trailing position in each visible row | no decoration/action/editor hook for None; neighboring valid cells preserve their stable targets | pad with fake editable cell | M:grid.rs GridModel:316, draw_row:3348, tests:4671–4811 |
| W-022-04 identity reorder | publish press/key target then reorder model before action/release; row and cell selection modes | stable keyed target revalidated or canceled; never act on replacement index; generic sort/reference/fetch-more actions retain target | store visual index as authority | M:grid.rs row_index:2103, move_to:2297, tests:5197–5265; O:src/widgets/grid.rs |
| W-022-05 cell painting | header/gutter/row numbers, compact/normal density, widths at fit±1, both scrollbars/fades and custom cell sentinel | exact oracle cells and editor rectangle; sentinel affects intended real cells and keeps geometry/hits stable | resolve custom slot without consuming paint | M:grid.rs geometry:1751, draw_header:3140, draw_row:3348; O:src/widgets/grid.rs |
| W-022-06 capability closure | each separately identified entry in frozen22-capability TablePro map, with a generic non-database model and adapter journey | 22 distinct real action/model witnesses; SQL/NULL/default/pending/undo/comparator/permutation remain adapter-owned; no missing due stage or synthetic placeholder | delete any capability or import domain engine | M:grid.rs GridModel/GridEditor:316–395 and source-bound22-capability ledger; O:src/bin/tablepro/tabs.rs +src/widgets/grid.rs |
| W-022-07 deferred F04 | begin editing then change edit eligibility; source authority disposition inspected separately from ordinary readonly tests | CHK-001/R-004 records deferred-not-selected disposition; archive pinned direct-widget defect, preserve accepted main Grid entry-point/readonly guards, and do not restore unchecked mutation; proposed F04 is not an oracle gate | silently convert historical proposal into parity gate or restore legacy unchecked mutation | M:grid.rs update/update_editable:2817–2880; O:docs/improvements/f04-reconcile-datagrid-read-only-transitions-before-mutation.md; O:src/widgets/grid.rs paste:1417/commit:593 |
| W-022-08 registered partial cell is not necessarily routable | Exact oracle TablePro orders first-row notes fixture at80x24,100x30,120x40,160x50: real Grid focus then Ctrl+Home and eight Right inputs, registered notes coordinate, completed click, Enter, paste; paired fully routable currency current/different/repeated clicks | TASK-022's direct generic fixture proves registered/topmost notes geometry versus the source-ineligible click guard, unchanged Grid edit state after the click, keyed external-edit request on Enter via accepted GridAction::EditRequested for EditIntent::External, and no Grid model mutation on paste; at120x40 source rectangle=(95,9,17,1). Fully routable current currency click edits and different currency click only moves. Actual viewer modal, pending edits and complete TablePro frames belong TASK-060's TP-037 contribution and TASK-063's intact parent, without a Dialog/app prerequisite for TASK-022. Source clip/routing guard belongs reusable Grid, not an app-side suppression shim | Treat every registered cell rectangle as activatable, extend click lookup past source-visible column interval, or globally suppress current-cell clicks to hide the partial case | O:src/widgets/grid.rs owns/locate:1213–1235, same-current activation:1334–1341, long-text viewer:570–578; O:src/bin/tablepro/db.rs orders columns:489–503 and deterministic notes:1010; direct generic Grid fixture may mirror these public column/source values without importing TablePro domain code |

## Branch-source finite cases

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-022-09 four source gesture policies | Two keyed rows and two columns, editable versus readonly column, current/different target; modern default, DataGrid, DataTable row mode and DataTable cell mode; complete Press/Release/Click plus DoubleClick, press-only/outside, reorder between phases and active-editor valid/invalid commit | Default retains accepted main gesture behavior. DataGrid different target moves; current unranged globally noneditable Grid activates, while globally editable Grid invokes begin_edit and refuses read_only columns or pending-deleted rows with Consumed/no action/no edit/no mutation. DataTable row mode selects source row and activates even when clicked row differs; DataTable cell mode current readonly produces no activation, current editable starts edit, different target moves. Preserve source editor-caret/failed-commit result and keyed selection; compare exact response, edit target and rendered cursor. Press cannot reclassify a different target as already-current; outside release never activates | universal DataGrid policy for DataTable, press-induced current-cell activation, app suppression/synthesis, display-index action after reorder, or readonly mutation | O:src/widgets/table.rs:476–518; O:src/widgets/grid.rs:1322–1345; M:grid.rs:2745–2780 and shared navigate/update_editable |
| W-022-10 DataGrid global versus cell refusal | DataGrid policy, fully routable already-current nonreference cell, no active edit and no range anchor. Separate fixtures: globally editable with eligible column/row, read_only column only, or pending-deleted row only; then globally noneditable with each same column/row condition. Real completed click, with press-only/outside and different-cell controls | Global editable=true and column read_only or row pending-deleted returns Consumed with no action, editor or model mutation. Global editable=false emits Activated(source_row) even when a per-column/row condition would refuse begin_edit, because that branch does not invoke begin_edit. Eligible globally editable cases retain separately proven inline/cycle/external results. Different cell moves without activation; no inference from generic read-only labels. Use generic adapter eligibility facts, not a database engine inside Grid | collapse global/cell eligibility into one readonly bit, emit Activated when begin_edit refuses, bypass pending-delete eligibility, or apply this DataGrid policy to modern-default/DataTable controls | O:src/widgets/grid.rs begin_edit:542–578, completed click:1322–1345; M:grid.rs shared gesture/update_editable/GridEditor path |
