# TASK-032 trusted obligations

This file is part of the immutable task package, not candidate-writable configuration. It binds shell contributions to R-001/AC-001/CHK-006, complete owned frame/process subsets to R-001/AC-008/CHK-004, and architecture to R-002/AC-002/CHK-006. Source rows are copied without changing their action or assertion content from the showcase scenario register. Every copied action/source field below is a normative capture input. Whole-frame parent-scenario equality remains app-closure-owned; this shell task owns only the separate finite contributions and listed whole process contracts, not all parent assertions. Historical capture-status descriptions are not parity verdicts.

## Shell contribution ownership

CLI source rows SC-SHELL-CLI-GRAMMAR and SC-SHELL-CLI-COLOR-ENV are parent-owned by TASK-039. This task restores their actual parser and accepted TASK-009 ADJ-17 explicit launch seam, closes only source-qualified C08/C09 observations and complete pre-frame process subsets F02/F03, and captures every successful launch frame/full terminal output as unmasked future-owner diagnostics. No stdout cropping or page-region masking converts a successful launch into full parity. Preserve existing generic Detect entry points and caller-authored theme limits; CLI Requested avoids the second ambient detection without host-environment mutation or duplicate runtime. Existing test assertions change only through accepted TASK-008 disposition.

This task closes only the exact shell contributions in `trusted/contributions.tsv`. The parent scenario transcripts still generate complete, unmasked frame/cursor/state artifacts and every frame mismatch remains an explicitly reported future-owned failure under the sealed stage map. A passing contribution never closes its parent scenario or claims any cell region equals the oracle. Complete parent-scenario equality is owned by TASK-039 after the page repair slices. The independent architecture check proves the exact source-qualified shell assertions, and architecture requires every exact contribution observation/assertion to pass. No executor-selected fields, checkpoints, masks, selectors or assertion subsets are allowed.

## Observable outcome

Restore the live 23-entry shell route table including Diff; cursor movement versus choose versus enter-content, navigation return without quit, keyboard-before-global editing, persisted page state, header Help/Inspector, status and focus restoration. Sidebar changes at terminal heights 31/32, widths 109/110; inspector at widths 99/100; minimum is 72×20. Flash clears at ≥140ms and status only after 4000ms. Only shell routing and geometry/state contribution proof is in scope, plus the explicitly approved minimal real public DiffView route; no permission to repair other initial page compositions is granted.

## Architecture and non-regression

Remove shell compatibility paint-over; Brand, NavList, StatusBar, HintBar and runtime layers must jointly own their painted geometry and real hit/focus state. Never introduce a second sidebar or footer renderer. Preserve Moment/advance_to; Tick does not advance time.

All task-owned, prerequisite and previously closed scenarios must pass. Execute the complete required inventory without fail-fast and account for every actual result. Only exact unfinished future-owner failures in the immutable stage map may remain; missing execution, new failures, unexpected errors, changed classification or reopening a closed scenario fail. Whole-workspace build, MSRV compile, formatting, lint and compatible architecture checks must pass. A diagnostic failure is never relabelled a parity pass.

## Trust and expansion

The oracle commit is 02f5294bfdbf38004cc49130d0aff1d01f31434c; main architecture starts at 7b27732a8c3c131760ec3438f641cb3c11343a42. Accepted prerequisite receipts, taskfmt 52d9f1eb7721f409bc47beb9fced7997b5c13ede and the independently qualified proof executable are host-owned. Materialized actions, numeric oracle coordinates, scenario/checkpoint membership, palettes, clocks, fonts, profiles, semantic mappings and exact expected cells are sealed before this task starts. Candidate code cannot read expected artifacts or write the host catalog, receipts, refs, comparator or another run. The dispatcher/bootstrap is produced by TASK-001, runners by TASK-070, accounting by TASK-071 and architecture probes by TASK-072. These commands are prerequisite deliverables, not evidence that they already execute.

The host supplies the immutable full application expansion grammar and proof contract alongside this file. Expand all recorded sizes, colors, focus stops, targets, ticks, source-qualified test bodies and checkpoint boundaries exactly as the accepted baseline did. Preserve every original assertion in ROUTE seeds. Source-only helpers and SCAN/SWEEP membership are materialized before this task; the executor never expands against its own implementation. Capture full row-major schema-3 cells, blank/wide continuation cells, modifiers, cursor and semantic transitions after every listed checkpoint. Preview/state-only or exact-clock lanes remain honest; executable reachability and terminal lifecycle require an actual process and owned PTY. Both capture operations must produce an explicit complete result, including accepted non-applicability rather than omitted output.

Cross-cutting full-app rows may be closure-owned; earlier tasks still preserve the subset already closed in the host ledger. Final closure replays the union on one exact tree and requires zero application-unresolved identities. Future-owned diagnostic failures are reported as failures, never success.

## Whole-frame comparison subset

`trusted/frame-contributions.tsv` fixes a nonempty subset of complete checkpoints. These compare every cell and cursor field, never rectangles or cropped pixels. Broader parent frames still execute and retain their actual mismatch reports under the immutable future-owner stage map. A subset pass closes only its frame-contribution identity, never the whole parent scenario. HO-CLI-PREVIEW in TASK-040 additionally compares every exact stdout/stderr/exit record. All required frame and shell contributions must pass again at whole-app closure.

## Exact scenario contracts

### APP:SC-SHELL-NAV

- **page:** overview
- **sizes:** 80x24,100x30,120x40,160x50
- **action_checkpoints:** Down; Enter; Right; Esc; 0; End; Enter; ]; [; Home; PageDown; PageUp; g; G; fresh-run j;k;l;Space
- **required_observable_proof:** Cursor vs active page; enter content vs remain navigation; wrapping; all23 pages including Diff reachable; Esc never quits
- **source_refs:** O:app.rs:445;O:app_tests.rs:536
- **components:** NavList,FocusRing

### APP:SC-SHELL-HITS

- **page:** overview
- **sizes:** 80x24,100x30,120x40,160x50,100x31,100x32
- **action_checkpoints:** Fresh-run each visible sidebar row: hover; Down; Up; fresh-run Down(row),Up(other row); click headings/gaps/clipped positions; wheel(nav,+1); End; at80x24 drag(nav scrollbar,top,bottom); at100x30/100x31/100x32/120x40/160x50 assert shell scrollbar absent and no scrollbar hit
- **required_observable_proof:** Frozen O coordinates select matching page; no heading/gap activation; wheel does not move cursor/focus; reveal last page; exact edge fade; compact23 rows and expanded28 rows determine explicit overflow/fit branches
- **source_refs:** O:app.rs:953;O:app.rs:1222
- **components:** NavList,scrollbar,fade,HitRegistry

### APP:SC-SHELL-HELP

- **page:** buttons
- **sizes:** 80x24,100x30,120x40,160x50
- **action_checkpoints:** focus(Run task); ?; Tick; Tab; BackTab; wheel(background,+1); click(background Run task); paste(abc); Esc; click(header Help); Enter
- **required_observable_proof:** Focus barrier/restoration; dim frame; no background effects; dialog hints replace page hints
- **source_refs:** O:app.rs:424,584,610;O:app_tests.rs:454
- **components:** Dialog,overlay,focus,runtime

### APP:SC-SHELL-INSPECTOR

- **page:** buttons
- **sizes:** 80x24,99x30,100x30,109x30,110x30,120x40,160x50
- **action_checkpoints:** i; hover(Run task); Down(Run task); Up(Run task); Tab; move pointer; click(header Inspector)
- **required_observable_proof:** Inspector threshold30 columns; mode/focus/hover/press/mouse/key/stop/tick/color fields; header capability clipping
- **source_refs:** O:app.rs:849,1061
- **components:** Panel,layout,interaction

### APP:SC-SHELL-MINIMUM

- **page:** buttons
- **sizes:** 71x20,72x19,72x20,80x24
- **action_checkpoints:** resize71x20; Tab; resize72x19; resize72x20; resize80x24; click(Run task); fresh undersize q; fresh edit Ctrl+C
- **required_observable_proof:** Exact reduced notice and recovery; no stale controls; exit codes and terminal restoration
- **source_refs:** O:app.rs:813,902;O:app_tests.rs:172,181,552
- **components:** runtime,layout

### APP:SC-SHELL-STATUS

- **page:** buttons
- **sizes:** 120x40
- **action_checkpoints:** click(Run task) at t0; time139ms;time140ms;time4000ms+Tick;time4000ms+1ns without Tick;Tick; fresh click(Run task) t0;click(Preview) t1000ms;time5000ms+Tick;time5000ms+1ns without Tick;Tick
- **required_observable_proof:** Flash active before140ms and absent at140ms on redraw; stored status alive at exactly4000ms Tick and after clock-only expiry redraw; explicit next Tick expires it; replacement resets expiry to5000ms; no duplicate footer hints
- **source_refs:** O:app.rs:386,1129
- **components:** clock,KeyHints,Button

### APP:SC-SHELL-PERSIST

- **page:** inputs
- **sizes:** 120x40
- **action_checkpoints:** focus(Project name);Enter;End;type(-persist);Enter;];[;assert committed value retained and navigation focused;focus(Project name);Enter;type(q[]?i0);Esc;fresh focus(Project name);Enter;Ctrl+C
- **required_observable_proof:** Committed data persists route roundtrip while focus returns to navigation; explicit edit re-entry shields global printable shortcuts as text; Ctrl+C still quits
- **source_refs:** O:app.rs:365,512;O:pages/inputs.rs:210
- **components:** routing,TextInput

### APP:SC-SHELL-CLI-GRAMMAR

- **page:** all23
- **sizes:** 71x20,80x24,100x30,120x40,160x50
- **action_checkpoints:** Fresh real processes: each --color/-c with each truecolor,24bit,256,16,none,mono; each --page/-p with every23 NAV label as original label,source-ASCII-normalized label,ASCII-uppercase original label,and exact !- prefix plus _東京 suffix around normalized label; both help flags; unknown --unknown ignored; --theme paper ignored; each color/page flag missing or empty; invalid color invalid,TRUECOLOR,ansi256,ansi16; invalid page invalid,editor,grid,chips,editable; ordered repeated colors truecolor then mono and reverse, pages overview then diff and reverse, every long/short flag pairing; invalid before help; help before invalid; --page --help and -p -h; capture startup then real q for each successful launch
- **required_observable_proof:** Exact source stdout/stderr/exit before terminal entry; last valid option wins; exact23 route and capability semantics; terminal entry/restoration; complete unmasked startup/final frames for successful launches at all sizes; ignored theme has no Paper effect; normalization removes non-ASCII-alphanumeric characters without extra slugs
- **source_refs:** O:src/bin/showcase/main.rs:17-72;O:app.rs:185-199
- **components:** CLI,runtime,NavList,theme

### APP:SC-SHELL-CLI-COLOR-ENV

- **page:** overview
- **sizes:** 71x20,120x40
- **action_checkpoints:** Clear NO_COLOR,COLORTERM,TERM,CLICOLOR_FORCE,FORCE_COLOR then fresh each env case: clear;NO_COLOR=empty;NO_COLOR=0;NO_COLOR=1;NO_COLOR=raw0xff;COLORTERM=truecolor;COLORTERM=24bit;COLORTERM=TRUECOLOR;COLORTERM=unknown;COLORTERM=raw0xff;TERM=xterm-256color;TERM=ghostty;TERM=kitty;TERM=dumb;TERM=empty;TERM=raw0xff;CLICOLOR_FORCE=0;CLICOLOR_FORCE=1;FORCE_COLOR=0;FORCE_COLOR=1;NO_COLOR=1+COLORTERM=truecolor+TERM=xterm-256color+CLICOLOR_FORCE=1;NO_COLOR=empty+COLORTERM=truecolor+TERM=xterm-256color;COLORTERM=24bit+TERM=dumb;COLORTERM=unknown+TERM=ghostty; cross each case with no color flag and each --color/-c value truecolor,24bit,256,16,none,mono; fresh PTY launch then q; repeat in a fresh Unix session without controlling TTY and all standard handles redirected pipes and retain actual source setup failure/no-frame outcome
- **required_observable_proof:** Source default detection and explicit CLI override exact; nonempty raw NO_COLOR affects only default; Unicode COLORTERM then TERM precedence; FORCE variables ignored by source default; no second ambient narrowing of explicit request; distinguish semantic theme capability from backend color suppression and preserve modifiers; exact direct and backend-normalized PTY frames/capability/process exit/restoration; redirected failure is source evidence not fabricated frame; raw-byte env applicability sealed by host
- **source_refs:** O:src/bin/showcase/main.rs:18-30,64-72;O:src/theme.rs:30-45;O:src/runtime.rs:276,375-391
- **components:** CLI,runtime,theme,terminal-capability

## Correction boundary

If exact parity requires a shared-component fix, unsupported public extension, changed source authority or new trusted fixture, stop NEEDS_REPLAN and name the responsible owner. Do not duplicate mechanics inside this app or edit expected evidence. A completed task produces host-verified results and an accepted tree receipt, not a candidate-written approval.
