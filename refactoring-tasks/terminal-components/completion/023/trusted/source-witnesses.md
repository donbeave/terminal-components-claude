# TASK-023 concrete production witness obligations

Normative under R-001/R-002/R-003; adds required cases and never replaces the complete source-obligation or oracle inventory. The host freezes each row's source-qualified case identities, values, action sequence, observed fields and exact source function/range before dispatch. Main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; oracle pin is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Resolve actual source/test identities through this package's trusted family rows; a missing source branch is a failed setup, not applicability chosen by the executor. Historical proposals use their recorded authority disposition, not invented oracle output.

Each case executes real production update/draw against caller-owned inputs. CHK-004 compares independently captured oracle behavior where the source map assigns parity. CHK-006 proves ownership through independently qualified TASK-072 Rust witnesses; CHK-005 executes concrete regression and mutant outcomes. For every applicable public named slot, the host binds a row that actually paints it: observe real Id/Family/Variant/Part/Resolved, component versus caller-row provenance, source resolution/cell-write branch, changed intended cells, unchanged geometry/hits/focus and unrelated cells. Measurement-only queries cannot satisfy a painted-slot witness. A style-unreachable row must name the exact source guard and another reachable row; no generic all-states label or declaration-only union proves coverage. Run inherited CP-COMMON theme/capability/override and degenerate-size axes without weakening this concrete case set.

Mutants below are real isolated production-source changes independently applied/judged, never executor-authored booleans or expected-output rewrites. Every mutant must compile and reach the intended branch before its specific rejection counts; a build failure is not the behavioral witness. Draw twice without input, then change the named source fact: durable state stays fixed on repeat draw and only source-owned dependent observations change on update. Shared prerequisite defects return to the scoped owner rather than widening this package.

Source shorthand `M:foo.rs` means `crates/tui/src/components/foo.rs` at the pinned main; `M:keymap.rs` means `crates/tui/src/keymap.rs`, and explicit `ui/` or `collection/` subtrees are relative to `crates/tui/src/`. `O:` paths are at the pinned oracle. Line anchors identify audited starting branches, not candidate line numbers. App-composition rows retain the exact task-family source-qualified applicability and cannot invent a historical standalone widget.

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-023-01 dialog body | prompt, confirm, info, error, destructive acknowledgement and facts; ordinary and zero-area body | content determines geometry; body executes once and returns bare R; actual draft changes next frame/eligibility; facts are borrowed live values | static copied prompt or callback executed twice | M:dialog.rs update:540, draw:752; O:src/widgets/dialog.rs |
| W-023-02 modal lifecycle | nested Esc, outside click, secondary click, paste over editor; opener present then vanished before close | runtime owns capture/trap/backdrop and focus restoration; source-authorized app routing exceptions remain app-owned; vanished opener never receives stale focus | second component trap or unconditional stale restore | M:dialog.rs update:540; M:menu.rs update:665; O:src/widgets/dialog.rs +src/widgets/menu.rs |
| W-023-03 menu pointer phases | enabled/disabled row under Move, Press, Release, Drag; sibling submenu crossing | only enabled Move changes hover cursor; disabled item never acts; exact source submenu bounds and transition sequence | treat any pointer event as hover | M:menu.rs update:665, MenuBar::update:1098; O:src/widgets/menu.rs |
| W-023-04 menu action metadata | remap, remove, hidden, duplicate and inactive owner, then invoke key and menu on same action | same effective owner/action identity and eligibility; no decorative chord surviving removal | independent menu binding cache | M:menu.rs update:665, draw_row:803; M:keymap.rs effective bindings; O:src/widgets/menu.rs |
| W-023-05 resize/reopen | title/dropdown/submenu at each terminal edge; narrow height; close/reopen after source change | clipped real bounds/hits and focus match oracle; no stale popup rectangle or durable draw mutation | reuse previous-frame popup geometry | M:menu.rs measured_size:516, draw:712, MenuBar::draw:1179; O:src/widgets/menu.rs |
| W-023-06 exact reference child | two separately configured fixtures: no-prompt Dialog with action 0 disabled and actions 1/2 enabled, then prompt Dialog with the same action siblings; render baseline, exact focused/hovered/pressed target, and inert semantic/default reference; use existing central Ui::reference, never component-local propagation | no-prompt runtime reference targets action 1 (first enabled), not disabled action 0, sibling 2 or Dialog root; prompt focus reference targets its real input, while editing comes from real input state rather than injected EDITING; SELECTED/editing without a corresponding semantic source changes no child; only FOCUSED/FOCUS_VISIBLE/HOVERED/PRESSED may be injected into the exact owner/part; root chrome, sibling cells, label/close geometry and content stay unchanged; complete callback subtree emits no controls/parts/layout/cursor/bindings/layers; repeated draw preserves caller state | fixture hardcodes action index 0; broadcast runtime flags to root/all actions; prompt target remains an action; invent semantic EDITING/SELECTED flags; callback registers a nested control despite reference scope | a1759b2a21adeac08222f2438243033fabd77512:COMPONENT_ARCHITECTURE.md:4045 item30 and §12.1; M:dialog.rs draw:752-916; pinned-main crates/tui/tests/conformance.rs:738-819 and render_components.rs:447-506 currently use first-action fixtures, so leading-disabled and prompt branches must be separately exercised, not inferred |

W-023-06 is an accepted architecture/reference-rendering proof, not permission to reproduce the historical 28 baseline movements or bless an oracle. TASK-023 owns fixture target selection and Dialog child/chrome observations; TASK-010 owns the existing central reference mechanism and whole-subtree suppression. TASK-031 consumes this concrete case in its conformance registry after TASK-023; TASK-023 has no future TASK-031 receipt prerequisite. A shared runtime defect returns to TASK-010 rather than adding a Dialog-local reference implementation.

## Branch-source finite cases

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-023-07 reverse non-power-of-two cycle | Three and five enabled items, cursor0 then Previous/Up; forward return; context menu, open MenuBar previous menu and closed MenuBar previous title. Repeat with trailing/interior disabled items, one enabled, all disabled and empty; remapped command | Enabled len3 previous0 reaches2, len5 reaches4, forward returns0; each mode skips unavailable targets with bounded termination, no unavailable activation, exact source open/close response and real highlighted row/title. All-disabled/empty produce no invented target. Shared index operation serves all three paths | unsigned wrapping_sub(1)%len, special-case only len3, fix context but leave bar paths, loop forever over disabled rows, or activate disabled target | O:src/widgets/menu.rs signed rem_euclid step and menu switching; M:menu.rs step/switch_menu/MenuBar::update closed previous branches |
