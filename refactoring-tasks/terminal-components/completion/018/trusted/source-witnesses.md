# TASK-018 finite source-qualified production witnesses

These case IDs are mandatory under R-001/R-002/R-003 and supplement, never replace, every accepted source-obligation row. Main authority is `7b27732a8c3c131760ec3438f641cb3c11343a42` (M); UI authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c` (O). Source paths and named source functions/tests below identify the starting branch; candidate line numbers are not authority. ADJ references bind the fixed architecture reconciliation in the shared adjudication. TASK-006 freezes the complete expanded case IDs, exact input bytes, enum values, checkpoints and independent expected observations before implementation dispatch; neither executor nor capture adapter chooses a subset.

## Finite expansion and checkpoints

- **P** means exact independently captured oracle cells/cursor/actions/visible state (CHK-002/004). **A** means the accepted architectural property and a real violating production mutation (CHK-005/006). A mixed row has both independent lanes, not a comparator derived from the candidate. Architecture-only APIs have no invented oracle widget.
- Every listed action is a separate ordered checkpoint: setup publication, action/update/settlement, first resulting draw, repeated draw without input, and the next changed-source publication. Compare the full allocation plus a one-cell sentinel surround, all hit/focus/cursor registrations, typed actions with stable owner/item/part, flow/invalidation and the row's named durable observations. Repeat draw must preserve durable state, callback counts and effects. App-consumer cases bind the app owner's exact full-frame scenario, but the producer task closes only its current component-owned direct behavior. Full application frames are diagnostic CHK-003 evidence with truthful immutable stage accounting until the owning app task is accepted, as README D-006 requires; no future app receipt or consumer source repair becomes this prerequisite's gate. Later app/031/069 closure joins those exact consumer cases. A component capture cannot certify an application frame.
- Render cases expand over Junie and Paper and TrueColor/ANSI256/ANSI16/Mono. Base allocations are (0,0,120,40), (0,0,40,10), (7,3,120,40), (7,3,40,10). The same source-applicable states additionally execute containment at widths {0,1,2,3,4,5,6,11,12}, heights {0,1,2,3,4,10,11,12}, and both origins. Degenerate containment is A unless the row explicitly identifies an oracle-supported tiny geometry; no unrenderable oracle allocation is silently blessed. Row-specific exact-fit and threshold sizes add to this set.
- State values are the explicitly listed alternatives and combinations in each row, not an arbitrary Cartesian product of unrelated flags. For any exposed paint part exercised by that row, expand Inherit, Clear, Set and an actual replacement slot at each declared override level; record exact Id/Family/Variant/Part/Resolved and whether cells came from component or caller row. The covered TextArea FIELD resolution-only exception applies solely to its named case. Disabled, read-only and inert registration behavior is observed separately from painted appearance.
- Event rows use all explicitly enumerated codes/modifier masks, not a representative mouse/key. Text rows use the listed literal strings and every UTF-8 split boundary; widths 0 through 120 are inclusive where specified. Collection keys A/B/C/D are distinct stable keys with identical visible labels where requested. Work rows count actual source reads, callbacks and allocations; a synthetic replacement loop cannot supply the counter.
- Each rejection in the observation column is an isolated real Rust mutation of the named source owner. It must compile and reach the branch before behavioral rejection counts. Examples include deleting the guard, routing through stale index, restoring the per-span loop or changing the exact arithmetic. A self-reported Boolean, source-string grep, case count or absent test identity is not execution proof. A missing source/branch mapping fails host preflight; no executor-selected applicability.

| Case | Source authority | Lane and axes | Concrete state/action | Exact observation and rejection |
| --- | --- | --- | --- | --- |
| W-018-01 choice-cells | O:src/widgets/choice.rs::render;ADJ-10 | P; render | Checkbox/Toggle true/false and Radio chosen 0/1; widths 0..6 and 40; focus/hover/press/disabled. | Exact gutter x, marker x+1, label x+5; Checkbox budget w−6, Radio/Toggle w−5. Compact marker below width 4; no in-run brackets. |
| W-018-02 toggle-trailing | O:src/widgets/choice.rs::Toggle::render | P; render | Label abc, on/off, widths offset+2/+3/+4; pressed Mono. | State text appears at 6+label width only if offset+3<width; label geometry and budget remain unchanged. |
| W-018-03 radio-typed | M:crates/tui/src/components/choice.rs:1162;ADJ-10 | A; runtime | Generic caller has selected B/cursor A; clamped Up then Down; caller ignores or commits Navigated. | Navigated(A/B) is distinct from Chose. Ignoring preserves controlled value; oracle caller commits. No cursor/value ownership collapse. |
| W-018-04 radio-oracle | O:src/widgets/choice.rs:151;ADJ-10 | P+A; runtime | Oracle-configured caller: Up/Down/j/k at first/middle/last; Space/Enter; Press/outside Release then completed click. | Every admitted arrow selects cursor, including clamped arrows; activation emits Chose; pointer Press does not commit. |
| W-018-05 radio-reconcile | M:crates/tui/src/components/choice.rs;M:crates/tui/tests/conformance.rs | A; identity | A/B/C duplicate labels; external value change/reorder/removal/empty source; readonly, configured and inherited disable. | Reconcile/seed emits no Navigated; stable cursor survives unless removed; blocked input does not change value. Compile-test exhaustive API compatibility. |
| W-018-06 select-disclosure | O:src/widgets/select.rs;M:crates/tui/src/components/select.rs | P+A; render | Select closed→open→Esc, then open→Tab; chosen differs from cursor; FocusOut, disabled and tiny popup. | Distinct closed/open marker at FIELD.right−2; no false FIELD SELECTED; one chosen popup item; exact dismiss/focus behavior. |
| W-018-07 slot-clear | M:crates/tui/src/components/choice.rs;M:crates/tui/src/components/select.rs | A; render | Marker Inherit/Set/Clear and actual readiness slot replacement at all four capabilities. | Clear reserves blank geometry; actual replacement slot differs from single-glyph override; labels and hits remain unchanged. |
| W-018-08 select-policy-defaults | M:select.rs::move_cursor/layer;M:architecture1834 and §29.8;ADJ-15 | A; runtime | Default versus Commit/ConsumeUnhandled with A/B/C; chosen B, cursor A; closed Up/Down/Home/PageDown/k/j; open Tab/BackTab then external FocusOut. | Default preserves cursor-only extended navigation and Tab dismissal; configured closed arrow derives from value, commits once; configured open Tab remains in Select. Real mutation changing default or using cursor instead of value fails. |
| W-018-09 select-commit-counts | O:select.rs70–128/on_click;ADJ-15 | P+A; runtime | Commit: closed first Up/last Down, middle Down; open cursor same/different then Enter/Space; body Press/outside Release/completed click; empty closed Down and open Enter/Space. | Changed clamped/equal-choice has no Chose/domain callback; changed value has exactly one real-key Chose; choice closes and empty choice closes without fabricated key. Pointer Press never commits. Reject unconditional Chose, empty-list early-return leaving open, or synthetic index. |
| W-018-10 select-open-owner | O:select.rs::on_key;M:runtime.rs832;ADJ-15 | P+A; runtime | Both open-key policies; Tab/BackTab while closed/open, open→disabled/read-only/inert, external focus-away, Esc. | Only admitted open ConsumeUnhandled publishes focused traversal bindings; external focus-out still dismisses without restoring opener. Observe binding identities, focus, popup, flow and both controls' states. Reject stale binding, global trap, fake focus or default Tab capture. |
| W-018-11 select-live-options | M:select.rs::update_in_form/index_of;ADJ-15 | A; identity | Borrowed keyed A/B/C same labels; value B/cursor A; reorder C/A/B, remove B, empty then repopulate; no-input reconcile versus following arrow; inherited/configured disable pairs. | No reconciliation callback/stale key commit. Both policies survive type-changing builders and inherited-disabled reconstruction. Existing value reconciliation is retained; only admitted key commits. No new action variant or second Form engine. |
| W-018-12 select-exact-grammar | O:select.rs70–128;ADJ-15 | P+A; runtime | Closed/open × Cursor/Commit × FocusTraversal/ConsumeUnhandled; each Up/Down/Left/Right/Home/End/PageUp/PageDown/Enter/Esc/Tab/BackTab and chars k,j,g,G,space,z with every subset of SHIFT/CONTROL/ALT/SUPER/HYPER/META. | Source-policy code/modifier admission, exact cursor/value/flow/actions and open state follow actual source guards; generic source-inapplicable lanes preserve main defaults. Ctrl/Alt chars ignored; no broad non-character modifier rejection. Reject common-table substitution or plain-Tab-only publication. |
