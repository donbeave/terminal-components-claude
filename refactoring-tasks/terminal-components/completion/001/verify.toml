schema = "verify/v2"
task_id = "TASK-001"
writable_paths = ["tools/refactor-proof/src"]
forbidden_paths = ["refactoring-tasks","docs/refactoring-plan","shots", "snapshots","baseline"]

# TASK-001 is retained as a historical contract, but its host lifecycle was
# retired. This gate intentionally fails closed so no verifier run can create
# an accepted qualification receipt for the removed product.
[[checks]]
id = "CHK-001"
phase = "precondition"
argv = ["python3", "-c", "raise SystemExit(78)"]
expected = { exit = 0 }
requirements = ["R-004"]
acceptance = ["AC-004"]

[[checks]]
id = "CHK-004"
phase = "focused"
argv = ["python3","refactoring-tasks/terminal-components/completion/001/trusted/proof-bootstrap/proof-comparator-bootstrap.py","--runner","tools/refactor-proof/bin/tc-proof"]
expected = { exit = 0 }
requirements = ["R-001"]
acceptance = ["AC-001"]

[[checks]]
id = "CHK-005"
phase = "regression"
argv = ["python3", "-c", "raise SystemExit(78)"]
expected = { exit = 0 }
requirements = ["R-003"]
acceptance = ["AC-003"]

[[checks]]
id = "CHK-006"
phase = "lint"
argv = ["python3", "-c", "raise SystemExit(78)"]
expected = { exit = 0 }
requirements = ["R-002"]
acceptance = ["AC-002"]

[[checks]]
id = "CHK-007"
phase = "gate"
argv = ["python3", "-c", "raise SystemExit(78)"]
expected = { exit = 0 }
requirements = ["R-005"]
acceptance = ["AC-005"]
