# TASK-028 concrete production witness obligations

Normative under R-001/R-002/R-003; adds required cases and never replaces the complete source-obligation or oracle inventory. The host freezes each row's source-qualified case identities, values, action sequence, observed fields and exact source function/range before dispatch. Main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; oracle pin is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Resolve actual source/test identities through this package's trusted family rows; a missing source branch is a failed setup, not applicability chosen by the executor. Historical proposals use their recorded authority disposition, not invented oracle output.

Each case executes real production update/draw against caller-owned inputs. CHK-004 compares independently captured oracle behavior where the source map assigns parity. CHK-006 proves ownership through independently qualified TASK-072 Rust witnesses; CHK-005 executes concrete regression and mutant outcomes. For every applicable public named slot, the host binds a row that actually paints it: observe real Id/Family/Variant/Part/Resolved, component versus caller-row provenance, source resolution/cell-write branch, changed intended cells, unchanged geometry/hits/focus and unrelated cells. Measurement-only queries cannot satisfy a painted-slot witness. A style-unreachable row must name the exact source guard and another reachable row; no generic all-states label or declaration-only union proves coverage. Run inherited CP-COMMON theme/capability/override and degenerate-size axes without weakening this concrete case set.

Mutants below are real isolated production-source changes independently applied/judged, never executor-authored booleans or expected-output rewrites. Every mutant must compile and reach the intended branch before its specific rejection counts; a build failure is not the behavioral witness. Draw twice without input, then change the named source fact: durable state stays fixed on repeat draw and only source-owned dependent observations change on update. Shared prerequisite defects return to the scoped owner rather than widening this package.

Source shorthand `M:foo.rs` means `crates/tui/src/components/foo.rs` at the pinned main; `M:keymap.rs` means `crates/tui/src/keymap.rs`, and explicit `ui/` or `collection/` subtrees are relative to `crates/tui/src/`. `O:` paths are at the pinned oracle. Line anchors identify audited starting branches, not candidate line numbers. App-composition rows retain the exact task-family source-qualified applicability and cannot invent a historical standalone widget.

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-028-01 StatusBar geometry | source StatusBar left/right/center groups; width enough, one-drop threshold and last-survivor truncation; separate W-028-06 Segments policy | StatusBar declaration priority/drop order and final ellipsis; exact gap3/edge1 and center cells; never apply retained-left rule to Segments | sort by width, distribute padding differently or conflate source policies | M:status.rs survivors:611/draw:831; O:src/widgets/statusbar.rs; Segments separately W-028-06 |
| W-028-02 status hover | two keyed labels with blank gap, unkeyed label and key input; Move across each | only current keyed label lifts; blank/unkeyed/key clears; no neighbor geometry or action change | bar-wide hover or stale hover on blank | M:status.rs update:652/item_style:684; O:src/widgets/statusbar.rs +segments.rs |
| W-028-03 hint/keyhint | action/chord priority overflow; modifier fragments and multi-key chord at clipping boundary | exact intended overflow marker/fragments; displayed action remains same effective handled action | truncate raw bytes or show stale chord | M:hintbar.rs draw:406, keyhint.rs draw:289; O:src/widgets/hintbar.rs +keyhint.rs |
| W-028-04 binding ownership | active/inactive/modal/edit owner; remap/remove/hidden/latent/duplicate chords; snapshot revision change and unchanged warm read | UiCore/keymap single effective owner/action metadata consumed through Cx; hidden visibility distinct from enabled routing; structural changes invalidate cache and warm fixed-buffer projection allocates zero | hint-only map, missing revision bump or stale focus-owner cache | M:keymap.rs FocusedHintKey/FocusedHints::derive:628–675; M:ui/mod.rs UiCore; M:ui/cx.rs component_chord:313 |
| W-028-05 binding serialization | 073 attribution/timing installed,010 focus lifecycle,017 layers, then028 metadata repair;030 reads accepted result | new boundary writes preserve real source identities, callback provenance, modal capture and timing census | recreate UiCore snapshot or bypass073 style probe | M:ui/mod.rs UiCore scoped publication; M:ui/cx.rs effective lookup; M:keymap.rs FocusedHints |

## Branch-source finite cases

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-028-06 distinct strip policies | Real StatusBar modern default, source StatusBar gap3/edge1 and source Segments gap2/edge1; left A/B each priority5 at width5, single left A at width4; equal-priority left/center/right and two tied right items; keyed/unkeyed labels at nonzero origin and widths around fit | Segments width5 drops A and retains B; width4 single A drops entirely. Retained-left policies retain A in the tied-left case and truncate rather than remove the final left survivor. Source StatusBar cross-group ties remove center then right then left; Segments removes right before left, first tied left and last tied right. Exact source cells, padding, background and padded keyed hit rectangles agree; modern constructor behavior remains unchanged | apply retained-left invariant to Segments, remove later tied left Segment, count clickable padding in source Segments survival budget, merge source gaps, or app-local survivor loop | O:src/widgets/segments.rs:60–153 and src/widgets/statusbar.rs:137–255; M:status.rs:578–650 and draw:831 |
