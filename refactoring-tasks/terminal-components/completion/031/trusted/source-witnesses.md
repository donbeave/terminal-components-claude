# TASK-031 concrete registry closure witnesses

Normative R-001/R-002/R-003 obligations, not a declaration that an unexecuted census has passed. The foundation comes from accepted TASK-073; complete family repair receipts come from TASK-009–030, including their trusted/source-witnesses.md when present and every exact source-obligation row. Required main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; preserve oracle-owned behavior at `02f5294bfdbf38004cc49130d0aff1d01f31434c`. These are architecture/regression cases, CHK-006/CHK-005, not invented registry oracle frames.

Before dispatch the host independently inventories actual public component registrations, production PARTS declarations, public Overrides named slot promises and the concrete source guards reaching their resolution and painted-cell sinks. Each registry subject must have a source-qualified witness manifest: unique state ID, production draw/update symbol and source range, guard/field values, caller source identity, action sequence, area/clip, theme/capability/override axis, real expected owner/provenance and actual observable/check. Guards with conjunctions require a case reaching the conjunction, not independent booleans that never reach paint together. Each style-reachable guard outcome needs a reachable production fixture; a structurally unreachable or inapplicable combination needs independently reviewed source-qualified exclusion. No candidate flag, generic all-states label, unparsed method-name list or branch counter seals this census. The independent manifest is frozen in protected TASK-072 context; the candidate implements production fixtures, not the judge. Removing a source path cannot remove its already-frozen obligation.

For every PARTS member the source-to-resolution relation and actual relevant fixture trace must agree, including owner ID, family, variant, part and full Resolved fields. The exact union excludes caller-owned custom rows but includes same-ID child composition; measurement-only queries remain resolution evidence only and never establish a painted slot. For every public named slot independently parse the actual promise and bind a concrete case whose sentinel changes the intended painted cells, preserving geometry/hits/focus/cursor and unrelated neighbors. Each slot must be tested separately, against inherited, local and suppressed-reference combinations where source-reachable. A public field with no actual sink fails instead of disappearing from the expected set.

W-031-10/11 are also normative R-006/AC-006/CHK-005 obligations. Freeze actual test-only lock/critical-section phase observations before dispatch: hold A after acquisition, observe B's acquisition attempt and bounded waiting outside fresh-read/mutate/commit, release A, then require A commit/unlock before B acquisition/read/commit. An unlocked mutant is rejected by simultaneous admission or bad phase order even if its final union accidentally survives; do not deadlock the correct implementation by requiring both to reach an inside-lock barrier. Test existing hardlinked destinations and parent aliases as well as symlinks; reject `nlink > 1` where supported, or require equivalent independently established host filesystem identity. Unsupported/unprovable atomic directory/rename or unique-file identity fails closed, never becomes a silent skipped portability case. The host freezes these supported-filesystem premises and exact protected phase observations independently of candidate-authored output.

| Case | Concrete state/action | Required observation | Reject real mutation |
| --- | --- | --- | --- |
| W-031-01 registry census | Existing conformance subjects plus independently discovered Probe, Dialog, Props, PropsList and every remaining public family | One generated subject source drives both degenerate-area and exact PARTS invocations; parsed source membership equals invoked membership | Drop any invocation, add a dead registry entry, or keep a second manual whitelist |
| W-031-02 finite family states | Every sealed family case from009–030; add independently discovered missing guards/field combinations before dispatch | Each required source-qualified guard actually reaches its real update/draw sink; future or impossible case cannot silently count as covered | Delete one state case while leaving family invocation and broad CP-COMMON label |
| W-031-03 same-ID child | Parent draws actual child with same ID and a part belonging to composed public contract, before/inside/after a caller row callback | Real identity preserved; child component-owned resolution counts toward composed owner contract and callback scope restores | Exclude all nested parts or fabricate a different child ID |
| W-031-04 caller row | List, Tree and Grid caller RowUi/CellUi custom part, nested columns, same spelling as a builtin part, then sibling builtin paint | Caller part excluded only by truthful provenance, not name; sibling/builtin part included; actual custom cells still paint | Classify by part spelling, leak caller scope into sibling or omit builtin owner |
| W-031-05 declared/unowned parts | Each independently parsed PARTS member under its frozen reachable state; one extra owned production resolution | Exact set equality, both missing and extra diagnostics name subject/state/source | Introduce undeclared owned part or declared unreachable part |
| W-031-06 public slot paint | Every actual documented named slot individually with sentinel on its frozen real sink state; matching measuring-only resolution control | Exact public-promised set equals actual paint-changing set; unrelated cells and all hit/geometry/cursor observations unchanged | Ignore one public slot, consume it only in measurement or paint a decorative sentinel elsewhere |
| W-031-07 readiness | List/Grid/FilterList/Picker/Tree each Empty/Loading/Partial/Error; source guards from collection/empty.rs:153–213: TITLE nonempty, HELP Some(detail), ICON nonempty Loading/Partial frame or noncleared Error glyph; negative controls Empty ICON, None(detail), empty frames and Error Clear | Real inherited owner ID/provenance and truthful composed EMPTY family; exact reachable sentinel cell changes; source-unpainted controls receive no slot-paint credit despite their resolution queries | Omit one owner/state, ignore reachable ICON, falsely credit Empty ICON, or substitute unrelated standalone ownership |
| W-031-08 degenerate geometry | Every registry subject at zero width, zero height, one-cell extent and nonzero clipped origin, with callback-bearing subjects | No outside cells/hits, underflow or panic; required callbacks run once; real eligible parts retained in separate reachable states | Remove a subject from degenerate sweep or skip zero-area callback |
| W-031-09 reference and repeat draw | Reachable inherited reference plus explicit local slot; repeated identical draw then caller source change | Suppression order and exact full resolved fields correct; no durable draw mutation; source change changes dependent real cells | RGB-role inference, cumulative dimming or stale source snapshot |
| W-031-10 actual cross-process publication | Main `crates/tui-testing/src/digest.rs:455–479,703–814`: two actual helper subprocesses each signal readiness before a common parent release; each concurrently publishes its disjoint 24-key half to one fresh temporary file. Independently force both processes to load the old cache before release. Then order same-key commits from A and B, followed by A's unrelated-key write | Both real processes overlap in lifetime/work, both exit successfully, exact 48-key union and values survive; the ordered latest same-key commit remains after the stale-cache process writes its unrelated key. Independently observed lock acquisition/commit order establishes the same-key expectation; no timing-only assumption | Remove shared lock while preserving rename; shorten lock to write only; merge with cached `entry.or_insert` so old values replace fresh disk values; run workers sequentially. Each mutant must reach the relevant transaction and fail the intended union/value/overlap check, not merely compile or time out |
| W-031-11 lock and publication failures | Actual helper against isolated temp destinations: held lock with bounded timeout; crash-left lock; symlink destination/lock alias; unreadable/malformed committed file; forced failure before rename; subsequent valid publication after explicit host cleanup with every writer stopped | Fail closed without stealing/removing another owner's lock, discarding prior committed bytes, blessing tracked files or promoting partial output. Own pre-rename temporary resources cleaned; successful recovery preserves old entries and adds only the requested update. Existing Baseline API callers remain source-compatible | Automatically reclaim stale/PID/age lock; continue unlocked after timeout; replace invalid disk with cache; delete a foreign temp/lock; allow aliases to acquire independent locks for one file |

Independent qualification applies each violating mutation to real Rust, requires successful build and the intended branch before counting its specified failure, and restores the exact base between mutants. A report-only truth table, mutant compile failure or expected-set edit is not rejection proof. Missing production authority is returned to the named prerequisite; TASK-031's testing scope cannot repair arbitrary family sources.
