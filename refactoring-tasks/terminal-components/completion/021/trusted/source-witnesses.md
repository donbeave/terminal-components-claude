# TASK-021 concrete production witness obligations

Normative under R-001/R-002/R-003; adds required cases and never replaces the complete source-obligation or oracle inventory. The host freezes each row's source-qualified case identities, values, action sequence, observed fields and exact source function/range before dispatch. Main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; oracle pin is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Resolve actual source/test identities through this package's trusted family rows; a missing source branch is a failed setup, not applicability chosen by the executor. Historical proposals use their recorded authority disposition, not invented oracle output.

Each case executes real production update/draw against caller-owned inputs. CHK-004 compares independently captured oracle behavior where the source map assigns parity. CHK-006 proves ownership through independently qualified TASK-072 Rust witnesses; CHK-005 executes concrete regression and mutant outcomes. For every applicable public named slot, the host binds a row that actually paints it: observe real Id/Family/Variant/Part/Resolved, component versus caller-row provenance, source resolution/cell-write branch, changed intended cells, unchanged geometry/hits/focus and unrelated cells. Measurement-only queries cannot satisfy a painted-slot witness. A style-unreachable row must name the exact source guard and another reachable row; no generic all-states label or declaration-only union proves coverage. Run inherited CP-COMMON theme/capability/override and degenerate-size axes without weakening this concrete case set.

Mutants below are real isolated production-source changes independently applied/judged, never executor-authored booleans or expected-output rewrites. Every mutant must compile and reach the intended branch before its specific rejection counts; a build failure is not the behavioral witness. Draw twice without input, then change the named source fact: durable state stays fixed on repeat draw and only source-owned dependent observations change on update. Shared prerequisite defects return to the scoped owner rather than widening this package.

Source shorthand `M:foo.rs` means `crates/tui/src/components/foo.rs` at the pinned main; `M:keymap.rs` means `crates/tui/src/keymap.rs`, and explicit `ui/` or `collection/` subtrees are relative to `crates/tui/src/`. `O:` paths are at the pinned oracle. Line anchors identify audited starting branches, not candidate line numbers. App-composition rows retain the exact task-family source-qualified applicability and cannot invent a historical standalone widget.

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-021-01 borrowed revision | append one line; replace last; front-evict one; wholesale replace; same-length replace; saturated generation, each on same source handle | actual source ranges/copy and rendered cells update; cold/warm/append/reflow counters remain distinct; append visits suffix only and warm visits no source index | length-only cache key, ignored revision or full warm scan | M:viewport.rs ViewportLayout::ensure:578, tests:1936–2170; O:src/widgets/viewport.rs |
| W-021-02 selection | forward and reverse Shift-arrow/Home/End; Ctrl/Alt+Shift word/document; matching mouse drag; Esc | same ordered source endpoints and original copied bytes; Esc clears selection without content mutation | independent keyboard grammar or visual-column-as-byte selection | M:viewport.rs command:1317, handle_text_pointer:1235; O:src/widgets/viewport.rs keyboard_selection tests |
| W-021-03 split grapheme | LineRef runs split e plus combining acute, a ZWJ sequence and a wide CJK glyph at wrap/clip; differing first/later run styles | one whole-line grapheme geometry from013; first-byte style; continuation clipping does not paint half glyph or split hit target | per-run segmentation or last-run style | M:viewport.rs walk_run/walk_line:441–486, rows:1487; O:src/widgets/viewport.rs |
| W-021-04 retention | paused log append; selection/caret/mark before and across front eviction; active drag while evicting; width shrink/grow | retained source identity, cleared fully evicted selection/producer caret and source-clipped partially evicted endpoints; top retained source line/subrow preserved; current marks/hits correspond to visible source, not stale row indexes | cached visual-row identity or cloned history capsule | M:viewport.rs retained:792, copy_from:832; O:src/widgets/viewport.rs eviction/marks tests |
| W-021-05 follow policy | log off-tail then tail reached separately by End, wheel, track and thumb; prose same actions | log resumes follow only at tail; prose remains nonfollowing even after End; producer caret never becomes scroll command | shared unconditional End-to-follow branch | M:viewport.rs command:1317, tests:2185–2210; O:src/widgets/panel.rs scroll_panel_tests |
| W-021-06 raw copy | select a source line containing literal tab, control byte and combining sequence, after wrapping and reversed selection | copy returns selected original source bytes with only source-specified trailing ASCII-space trimming; trailing tab/control/non-ASCII whitespace survives; no diff-projection tab expansion | copy painted/expanded cells instead of source | M:viewport.rs copy_from:832, copy tests:2172/2367; O:src/widgets/viewport.rs |

## Branch-source finite cases

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-021-07 source geometry and disposition | Source viewport with no overflow, exact-fit content, then one-row overflow; widths around wrap boundary and nonzero clipped origin; producer caret on first retained line then evict it; source selected suffixes ending ASCII space, tab, control and non-ASCII whitespace | No source viewport gutter; scrollbar column reserved only when source overflow requires it, with exact first-frame/reflow cells and source-row/subrow anchor. Evicted producer caret becomes None rather than line0. Copy trims source-specified ASCII trailing spaces only, preserving selected trailing tabs/control/non-ASCII whitespace; original bytes remain distinct from Diff projection. Warm redraw does not scan whole source. TASK-008 exact test identities explicitly dispose contradictory main expectations before execution | retain gutter unconditionally, always reserve scrollbar, retain evicted caret at0, trim_end Unicode whitespace, expand copy tabs, or drop conflicting test without protected source disposition | O:src/widgets/viewport.rs reconcile:542–585, selected_text:1029, render:1420–1508, producer_caret_follows_eviction_and_replacement:1885; M:viewport.rs retained/copy_from/draw and conflicting inline tests |
