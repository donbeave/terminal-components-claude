# TASK-066 protected closure obligations

These clauses are planner-owned acceptance inputs. Freeze this file and the exact mapped historical rows before dispatch. Candidate output cannot amend them.

## Source authority

ARCH:A28; ARCH:A29; DEC:ADJ-07; HIST:A113; HIST:A115; main COMPONENT_ARCHITECTURE.md:2054,3934; tools/test-inventory/README.md:69 and required.json; TASK-007/TASK-008 sealed identity and disposition receipts.

The full historical clauses, remaining work and proof requirements are included in source-obligations.tsv, with their original ledger locations. Architecture and decision IDs are namespaced; HIST:A23 is not ARCH:A23.

## Exact closure clauses

1. Reconcile all 3211 source-qualified historical inventory entries plus the later immutable oracle inventory. Every entry has executed target/profile/test identity or a previously accepted, evidence-backed supersession with replacement proof. No unresolved entry remains.

2. Move render_components assertions into the single retained render target without dropping cases, changing baseline keys or silently replacing assertion bodies. Keep original historical files as immutable archived source evidence.

3. Update CI and every owned command reference atomically with the target relocation. No post-consolidation gate invokes the removed --test render_components target. Derive execution commands through the independently accepted relocation product, never an executor-editable skip list.

4. Require complete unfiltered execution with no fail-fast suppression. Inject a dropped test, duplicate identity, renamed case without relocation, omitted feature profile, changed old assertion and fabricated listing; the independent accounting gate rejects each.

5. Confirm live Buttons page and application assertions were already migrated by TASK-033; do not recreate an alternative Buttons application. Preserve any legitimate public-consumer example separately.

6. All four app suites and the full required oracle corpus remain green; the allowed unresolved migration set is empty at this closure.

## Check selection and evidence

The host binds the complete all-app and component required set to this task context; no candidate subset selector is permitted. Direct and PTY captures use their separately sealed oracle lanes. CHK-004 compares every required completed product checkpoint. CHK-005 additionally enforces the primary closure outcome; CHK-006 runs the exact applicable accepted architecture/API/performance checks, CHK-005 executes and reconciles the full test inventory, and CHK-007 requires all independently checked products and post-run integrity. TASK-070/071/072 implement and independently qualify those operations before dispatch.

The accepted task-stage command inventory records exact cargo targets, profiles and immutable relocation rules. TASK-066 may produce the explicitly scoped render-target relocation only after independent old/new identity comparison; downstream contexts use the host-accepted relocation receipt. A candidate edit to tools/test-inventory does not itself redefine that authority.

This task is the explicit reviewed relocation producer, not general permission to rewrite tests. The host validates old and new assertion/source identities independently before accepting its relocation receipt. Historical archive bytes and expected oracle evidence remain protected.
