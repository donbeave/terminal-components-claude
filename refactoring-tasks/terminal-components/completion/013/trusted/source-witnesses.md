# TASK-013 finite source-qualified production witnesses

These case IDs are mandatory under R-001/R-002/R-003 and supplement, never replace, every accepted source-obligation row. Main authority is `7b27732a8c3c131760ec3438f641cb3c11343a42` (M); UI authority is `02f5294bfdbf38004cc49130d0aff1d01f31434c` (O). Source paths and named source functions/tests below identify the starting branch; candidate line numbers are not authority. ADJ references bind the fixed architecture reconciliation in the shared adjudication. TASK-006 freezes the complete expanded case IDs, exact input bytes, enum values, checkpoints and independent expected observations before implementation dispatch; neither executor nor capture adapter chooses a subset.

## Finite expansion and checkpoints

- **P** means exact independently captured oracle cells/cursor/actions/visible state (CHK-002/004). **A** means the accepted architectural property and a real violating production mutation (CHK-005/006). A mixed row has both independent lanes, not a comparator derived from the candidate. Architecture-only APIs have no invented oracle widget.
- Every listed action is a separate ordered checkpoint: setup publication, action/update/settlement, first resulting draw, repeated draw without input, and the next changed-source publication. Compare the full allocation plus a one-cell sentinel surround, all hit/focus/cursor registrations, typed actions with stable owner/item/part, flow/invalidation and the row's named durable observations. Repeat draw must preserve durable state, callback counts and effects. App-consumer cases bind the app owner's exact full-frame scenario, but the producer task closes only its current component-owned direct behavior. Full application frames are diagnostic CHK-003 evidence with truthful immutable stage accounting until the owning app task is accepted, as README D-006 requires; no future app receipt or consumer source repair becomes this prerequisite's gate. Later app/031/069 closure joins those exact consumer cases. A component capture cannot certify an application frame.
- Render cases expand over Junie and Paper and TrueColor/ANSI256/ANSI16/Mono. Base allocations are (0,0,120,40), (0,0,40,10), (7,3,120,40), (7,3,40,10). The same source-applicable states additionally execute containment at widths {0,1,2,3,4,5,6,11,12}, heights {0,1,2,3,4,10,11,12}, and both origins. Degenerate containment is A unless the row explicitly identifies an oracle-supported tiny geometry; no unrenderable oracle allocation is silently blessed. Row-specific exact-fit and threshold sizes add to this set.
- State values are the explicitly listed alternatives and combinations in each row, not an arbitrary Cartesian product of unrelated flags. For any exposed paint part exercised by that row, expand Inherit, Clear, Set and an actual replacement slot at each declared override level; record exact Id/Family/Variant/Part/Resolved and whether cells came from component or caller row. The covered TextArea FIELD resolution-only exception applies solely to its named case. Disabled, read-only and inert registration behavior is observed separately from painted appearance.
- Event rows use all explicitly enumerated codes/modifier masks, not a representative mouse/key. Text rows use the listed literal strings and every UTF-8 split boundary; widths 0 through 120 are inclusive where specified. Collection keys A/B/C/D are distinct stable keys with identical visible labels where requested. Work rows count actual source reads, callbacks and allocations; a synthetic replacement loop cannot supply the counter.
- Each rejection in the observation column is an isolated real Rust mutation of the named source owner. It must compile and reach the branch before behavioral rejection counts. Examples include deleting the guard, routing through stale index, restoring the per-span loop or changing the exact arithmetic. A self-reported Boolean, source-string grep, case count or absent test identity is not execution proof. A missing source/branch mapping fails host preflight; no executor-selected applicability.

| Case | Source authority | Lane and axes | Concrete state/action | Exact observation and rejection |
| --- | --- | --- | --- | --- |
| W-013-01 split-cluster | M:crates/tui/src/ui/paint.rs:140;O:src/widgets/viewport.rs | P+A; text | Split e+U0301, woman+ZWJ+laptop and US regional indicators at every UTF-8 boundary; insert empty fragments and distinct styles. | Split and unsplit cells/byte offsets/width agree; first-byte fragment owns style. Reject the old per-span loop and lost combining suffix. |
| W-013-02 width-authority | M:crates/tui/src/text/measure.rs;M:COMPONENT_ARCHITECTURE.md:4995 | A; text | ASCII, e+acute, CJK, emoji and halfwidth katakana with U+FF9E/U+FF9F; widths 0..120. | Every layout/wrap/hit/cursor path uses ratatui CellWidth; reject alternate unicode-width measurements or CJK switching. |
| W-013-03 wide-shadow | M:crates/tui/src/ui/paint.rs::paint_graphemes | A; render | Paint two-cell CJK, then overwrite either cell with ASCII; clip each half at a nonzero origin. | Exact wide-cell continuation reset and untouched neighbors; no orphan continuation or half glyph. |
| W-013-04 tabs-newlines | O:src/widgets/viewport.rs;O:src/ui/text.rs8–15;M:crates/tui/src/text | P+A; text | Literal a\tb and aa\tb, CRLF, empty and trailing-newline lines; offsets 0/1/3/4 and widths 0/1/3/4/5/6. | Source viewport expands each tab to four spaces independent of column: b starts at column5/6, not a next-four-column stop. Preserve original byte/copy mapping. Any accepted generic tab-stop policy has a separate A lane and explicit source fixed-four caller policy; no conflated oracle baseline. CRLF adds no phantom grapheme. |
| W-013-05 truncate-wrap | M:crates/tui/src/text;O:src/ui/text.rs | P+A; text | Each listed text value at every width 0..120; truncate with/without ellipsis and wrap. | No split grapheme; exact width, line breaks and ellipsis; no 0/1-cell overflow; repeated calls agree. |
| W-013-06 borrow-work | M:crates/tui/src/text;fixed cross-fragment contract | A; work | Borrow a 100000-line source, visible windows 1/10/40; one cluster spans fragments and contains 256 combining marks. | No whole-document clone; work tracks visible input and actual cluster length; scratch allocations are counted, not hidden behind a false fixed cluster bound. |
| W-013-07 public-painter-route | M:crates/tui/src/ui/paint.rs | A; text | Feed the same split cluster/styles through the shared projection and public Ui spans, then repeat the draw. | TASK-013 closes the actual public Ui painter path: a tested but unused helper fails. RowUi and viewport LineRef migration are separately mandatory TASK-015/021 cases joined by later TASK-031/069; their future receipts and consumer repairs are not TASK-013 prerequisites. |
| W-013-08 streaming-boundaries | M:crates/tui/src/collection/rowui.rs:256;branch BA-FMT;fixed streaming contract | A; text | Shared private formatted Ui seam: one Display writes e/acute, family-ZWJ, RI pairs, CJK, CRLF and Indic conjunct at every UTF-8 split including empty writes; a+256/4096 acute+z; widths0..120 and nonzero origins. | Exact unsplit cluster bytes/width/first-byte provenance, clipping and shadow reset; no suffix backfill after a nonfitting wide cluster. One original formatting call, never a whole-row String or capped cluster. Actual015 consumer receipt remains later-owned. |
| W-013-09 streaming-storage | M:crates/tui/src/ui/mod.rs::FrameState;fixed streaming contract;planning qualifier streaming-grapheme-bootstrap-20260911 | A; work and cleanup | Cold Runtime, same-owner second draw, fresh Runtime, resize and two independent runtimes; inline corpus then 256/4096 marks; formatter returns Err after prefix, panics after prefix, or writes huge suffix after output fills. | Actual scratch/output/total allocation scopes satisfy fixed ceilings separately; no growing-prefix UTF-8 rescan; formatter effects continue after clipping without continued projection. Prefix survives returned Err; unwind clears content and restores reusable capacity only. Reject global scratch, stale bytes in Debug/output, two passes, fresh owner falsely warm, quadratic revalidation and output allocation subtraction. |
| W-013-10 normalization-entrypoints | O:src/core/text.rs:15;M:crates/tui/src/text/buffer.rs:83,135;branch BF01 | P+A; text | Constructor, set_text, character insertion and paste receive empty, a LF b, a CR LF b, a CR b and mixed CR/LF text under single-line and multiline policy; ordinary and sensitive storage. | One shared normalization invariant governs every mutation entrance; exact oracle grammar is frozen independently, not inferred from insertion-only main tests. Single-line storage never retains forbidden line breaks; multiline normalization does not manufacture double breaks. Sensitivity-safe replacement/redaction stays intact. |
| W-013-11 post-edit-graphemes | O:src/core/text.rs87–91,200–237,409–448;M:crates/tui/src/text/buffer.rs207;branch BF02 | A; text | Set selection endpoints inside e+acute and family-ZWJ at every byte and scalar boundary in both directions; insert a combining mark or ZWJ that merges existing clusters, then move/delete/replace selection. Include already aligned ASCII controls and sensitive drafts. | Requested select_range endpoints both floor to preceding whole-grapheme boundaries. After an atomic edit, cursor ceils and retained anchor floors against the NEW full string, where that operation retains an anchor; replacement clears anchor. Exact rebased endpoints and subsequent whole-cluster deletion are frozen from the source policy, not merely UTF-8 validity. Reject scalar-only snapping, deletion-before-insertion repair that changes replacement position, or omitted post-edit repair. Modern sensitivity/redaction remain; no illegal-offset oracle app trajectory is invented. |
| W-013-12 word-grammar | O:src/core/text.rs;M:crates/tui/src/text/buffer.rs:243;M:crates/tui/src/text/measure.rs:39;branch BF03 | P+A; text | Ctrl-word movement and deletion from each boundary in snake_case, e+acute adjacent to punctuation and ASCII words separated by repeated spaces; selection both directions. | Preserve source grapheme/alphanumeric endpoints through one shared word walk. Reject underscore-as-word or combining-scalar splitting where those change the source trajectory; no app raw-key editing implementation. |
| W-013-13 replacement-outcome | M:crates/tui/src/text/buffer.rs:400–435;branch BF04 | A; edit outcome | Select nonempty text and apply empty or fully filtered paste; repeat with empty selection, ordinary/sensitive buffers and accepted single-line filter. Observe state before/after and typed edit outcome. | Selection deletion is a real mutation even when inserted length is zero; unchanged buffers do not receive fabricated changes. Report the whole replacement transaction. TASK-016 separately proves idle TextInput begin-edit versus TextArea idle no-op, so no universal Changed-on-empty-paste shortcut is accepted. |
| W-013-14 fuzzy-coordinate-map | O:src/ui/text.rs::SearchText/fuzzy;M:crates/tui/src/text/fuzzy.rs7–27;branch BF05 | P+A; matching | Label ΟΣ with query ος; combining-scalar query within e+acute; ASCII controls; candidates whose byte offsets and grapheme ordinals differ. Separately use a-b, a b, a_b, a.b with query b under Word and Identifier. | Preserve whole-string lowercase/scalar matching and source byte-based subsequence penalty, projected to public original-grapheme ordinal highlights. Preserve accepted FuzzyBoundary::Word default (space/hyphen/underscore/dot bonus); Identifier uses only underscore/dot and is the exact oracle fuzzy boundary lane. Source-qualified callers explicitly select Identifier rather than changing generic default. Reject per-grapheme lowercase, ordinal-based byte penalty, lost boundary policy or coordinate API reverted to bytes. |
| W-013-15 overwide-wrap | O:src/ui/text.rs;M:crates/tui/src/text/measure.rs:174;branch BF06 | P+A; wrap | Wrap 日 alone and beside ASCII at widths0/1/2/3; include newline and empty inputs. Compare emitted rows and wrapped_rows from the same policy. | Source width1 日 yields one ellipsis row, not an empty row followed by an overwide glyph. Count and emitted rows agree; zero width remains safe. Reject emission before testing fit in an empty row and preserve one shared traversal. |
