# Jackin and TablePro flow contribution contract

This planner-owned contract repairs PARITY-FINAL-02 and PARITY-FINAL-03. It applies to TASK-051–064 and oracle baseline producers TASK-004/005. The oracle is commit 02f5294bfdbf38004cc49130d0aff1d01f31434c. All original parent scenario events, source-native assertions, variants and complete frames remain required. A contribution never replaces a parent or makes an unexecuted route reachable.

## Complete ownership and nonempty stage proof

[app-flow-stage-audit.tsv](app-flow-stage-audit.tsv) audits all 150 parent scenarios, including presets, nested modals, test replay bodies and final routes. The primary owner is no earlier than the last required application producer. TASK-057/064 rerun the entire app and TASK-069 the complete campaign. Every bounded earlier producer retains its existing local primary scenarios and the exact additional identities in the two contribution tables. A preservation intersection can protect previous work but is not new-behavior acceptance and cannot satisfy these contribution identities.

[app-flow-frame-contributions.tsv](app-flow-frame-contributions.tsv) selects a nonempty finite set of complete checkpoints per contribution, with all parent finite axes. It compares every cell, styled continuation, modifier, dimension and cursor, not a crop or pixel mask. A source-route predicate is evaluated only while capturing the immutable oracle and converted into fixed checkpoint IDs. The candidate receives no predicate or adaptive selector. Empty selections, unresolved labels, duplicated/missing/extra IDs and altered source axes fail baseline preparation and preflight.

[app-flow-contributions.tsv](app-flow-contributions.tsv) separately fixes exact semantic keys, action/assertion boundaries and assertions. TASK-072-qualified architecture checks these as R-002/AC-002/CHK-006; this state projection does not assert any cell equality. The baseline schema fixes each key's type and complete nested value shape from real production state; unknown/missing fields and candidate-derived identity rules fail. These are independent actual-handler observations, not surrogate-model execution or invented success flags.

Frame contributions bind R-001/AC-001/CHK-004, alongside the unchanged complete-primary frame requirement. Semantic contributions bind R-002/AC-002/CHK-006. Direct/PTY captures retain their own declared lane membership. Closed contribution identities are monotonic. Parent closure requires all original checkpoints, all original source-native assertions and every associated contribution on the same frozen tree.

## Explicit direct starting-state fixtures

TablePro T means W then Ctrl+O, literal orders, Enter. Its quick-switcher route is owned by TASK-063. Merely skipping these inputs would be invalid: early Grid/safety code cannot claim their UI reachability. Therefore the named post-preset-T contributions are separate direct-lane state fixtures, not partial runs of the full parent.

TASK-005 runs the intact oracle parent prefix through the exact named checkpoint, records complete cells/cursor and all production state consumed by subsequent draw/update, and freezes the state schema and source-derived constructor recipe. For T the source starts with App::new and App::connect at src/bin/tablepro/app.rs:145,176. App::connect constructs Workbench and immediately calls new_query at :181; W and completed Connections therefore have Query1, not an empty Workbench. Preserve that query, query counter, focus and status/time before the source picker result opens the table through Workbench::open_table at workbench.rs:428. Merely calling Workbench::new at :99 would be an invalid seed. Query construction, where needed, uses the real Workbench::new_query at :470. Existing production constructors/domain methods populate models; the reviewed adapter restores only the captured fixture state, time, focus and navigation identities. It may not copy rendering, key dispatch, SQL logic, filtering or mutation algorithms. The candidate recipe uses the accepted corresponding production constructors/model identities, not oracle implementation bytes.

Qualify each seed independently: a fresh seeded oracle must equal the intact-prefix oracle checkpoint in its complete frame, cursor and full source-defined continuation state; then both execute the exact same frozen suffix and match at every suffix checkpoint. Include mutations of selected object/tab, query counter, pending edits, history, focus/edit owner, scroll, popup stack and time, requiring a mismatch. Reject a seed whose omitted field changes any future action result. The state map includes every original field read by the suffix, established by source audit, not only the public keys in the semantic contribution table. Adapter seam/source hashes and field mapping are immutable accepted inputs; no executor may add a new injection field or omit a mismatch.

The named TP-037 fragments separately seed the original post-T state for foreign-key navigation and the post-Ctrl+O/events/Enter state for JSON viewing. TP-079 seeds after the original switcher-opened exact object for every catalog table/view variant. TP-077 uses distinct post-T, post-help-open and post-help-Esc states; the help-open seed proves only actual paste isolation semantically, never help cell parity before TASK-063. Each seed is derived and independently replay-qualified at every declared size/palette and is recorded as fixture_kind=source-derived-direct-seed. No seeded contribution receives a PTY or CLI-reachability badge.

The original parent always executes its complete original prefix and suffix in all declared direct/PTY lanes, including quick-switcher/help frames and all original assertions. Those remain failed diagnostics until their complete primary owner passes. TASK-063 must replay the actual T/switcher/help inputs rather than consume the seeded shortcuts. App/final closure reruns both the full parent and the independent contribution fixtures.

### Empty-Workbench Explorer contributions

TASK-059's TP-017/018/019 contributions are explicitly distinct direct fixtures. Derive their seed from the intact TP-026 cp(empty) checkpoint after every tab has been closed; do not pretend W starts empty. Independently prove the reconstructed seed equals that full source checkpoint/state. Then execute the exact focus-cycle or Explorer inputs listed by TP-017/018/019 using real handlers. Resolve the seeded oracle's own full focus cycle and pointer coordinates into a separate immutable trace; original W coordinates/focus counts are not silently reused or rewritten. These fixtures prove empty-Workbench focus, tree/filter/lazy/schema and layout now. All original W parent inputs/frames and its Query1 focus stops still close at TASK-061. TP-026 cp(empty) remains an additional nonempty intact-parent whole-frame contribution for TASK-059.

### Frozen seed state coverage

The source App state boundary is app.rs:116–143: theme, screen, connections, workbench, history, focus, ring, hits, hover, pressed, hover_suppressed, modal, size, tick, status, flash, quit, saved_focus, too_small, committing, scope and switcher_targets. The Workbench boundary is workbench.rs:67–91: connection, catalog, schema, explorer, explorer_filter, explorer_visible, maximized, strip, tabs, active, query_counter, pending_loads, open_objects, current_object, pending_run and closed. Freeze these exact fields and their complete reachable state for the named checkpoint; nested component state follows the pinned concrete source types. Reconstructed render caches/ring/hits are permitted only after proving complete resulting geometry/state equality; they cannot be omitted from qualification. Constructor-built source callbacks/formatters remain real production functions, never serialized surrogate implementations. This closed source-type traversal fixes the finite schema; unknown future fields require a separately reviewed schema revision, not a candidate omission.

## Protected producer and qualification handoff

Copy these three contract/table files and the complete source-stage audit into TASK-004/005 trusted inputs before dispatch; each producer consumes only its app's rows. TASK-070 implements/qualifies exact membership, source-derived seed boundaries and lane honesty, TASK-071 accounts for separate parent/frame/semantic identities, and TASK-072 qualifies actual-state extraction/assertions. Missing source field coverage, an empty required set, an unqualified seed or unavailable production constructor is a preparation failure, not permission to relax an implementation task.

The host protects fixture schema, recipe, stage ownership, required identities, oracle artifacts and comparator together. Tests must reject replacing intact parent execution with a seed, asserting PTY reachability from direct state, cropping an excluded future surface, removing an original source assertion, closing a parent after contribution-only success, or accepting an empty preservation intersection as a contribution. A valid seeded contribution and a fully intact parent both pass independently after all their owners finish.
