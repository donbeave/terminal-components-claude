# Accounting check context templates (TASK-071 authority)

Frozen partial contexts merged by `tc-proof-host prepare` into `/run/tc-proof/contexts/CHK-NNN.json`. See `accounting-mode-bindings.tsv` and `proof-contract.md` §136–144.
