# Accounting check context templates (TASK-071 authority)

Frozen partial contexts are materialized by the verifier subagent into the external `$RUN_DIR/contexts/CHK-NNN.json` directory. See `accounting-mode-bindings.tsv` and `proof-contract.md` §136–144.
