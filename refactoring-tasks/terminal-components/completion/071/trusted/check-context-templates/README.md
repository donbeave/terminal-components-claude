# Accounting check context templates (TASK-071 authority)

Host-local partial contexts are assembled by the verifier subagent under
`$RUN_DIR/contexts/CHK-NNN.json`. Standalone taskfmt only validates the task
package and verifier evidence. See `accounting-mode-bindings.tsv` and
`proof-contract.md` §136–144.
