# Protected task inputs for TASK-006

Product: `oracle-complete`.

## Finite source-witness input binding

The host seals all twenty-four `refactoring-tasks/terminal-components/completion/{009,010,011,012,013,014,015,016,017,018,019,020,021,022,023,024,025,026,027,028,029,030,031,073}/trusted/source-witnesses.md` files, their fixed shared contract sections in `trusted/obligations.md`, and TASK-073's `trusted/style-timing-contract.md` as exact immutable campaign inputs before baseline expansion. The brace expression here is an explicit enumerated path set, not candidate-selected discovery. Missing, duplicated, stale or unbound files fail required-set construction. Each finite witness identity is retained with its source, direct/PTY/architecture applicability, fixed expansion axes, owning stage, action checkpoints and independent observations. The 009–020 rows supplement rather than replace the other component witnesses, public PARTS/slot census, historical union and all 54 component rows.

ADJ-12's total ordered Form validation witness supersedes the short-lived proposed fail-fast witness, not the original source requirement. Seal its exact all-visible-callback and per-field-error observations; preserve oracle connection multi-error frames separately from the modern architectural API case. Cross-validation remains conditional on all field checks succeeding.

Oracle parity cases use actual old production source; architectural-only API, tiny containment, safe-session implementation and style-timing cases stay separately typed obligations for their future implementation owners. TASK-006 freezes their membership and predicates, not fabricated old frames, candidate timing or a future TASK-073 receipt. No successor evidence is a prerequisite to this baseline task. Shared paint witnesses close first at their actual responsible layer; RowUi/viewport/app trajectories remain staged with later owners and join at final closure. The exact ADJ-04 covered TextArea FIELD resolution exception, ADJ-09 source-specific paste, ADJ-10 controlled choice policy and ADJ-11 generic-versus-source strip policies remain distinct. CHK-002/006 reject omitted witnesses, switched applicability, altered axes or missing source/census bindings; CHK-004 accepts only independently captured oracle observations for actual parity lanes.

Classify all 54 component rows and expand every oracle-renderable row into actual production widget/view fixtures, preserving the common states and CP-specific corpus. `testing-registry` is architecture-only, not an oracle widget: CHK-002 must retain an explicit non-frame disposition that binds its attribution/conformance obligations to TASK-073 and TASK-031. Do not create a screenshot, direct-capture or PTY identity for that row. It remains required in the complete obligation union; the explicit architecture disposition is not an omission. Cover default/focus/hover/pressed/selected/disabled/readiness, all source-supported capability levels, tiny/nonzero origins, exact resize thresholds, scroll/fades, Unicode/selection/cursor, overlays and semantic identities. Explicit evidence-backed non-applicability replaces impossible states; absence never does.

Use original widget code and separately reviewed oracle observation adapters; keep architecture-only Paper/API tests distinct from oracle-Junie's product frames. Components introduced only by the new architecture are proven through matching oracle compositions, with an explicit old-to-new semantic mapping and architecture fixture; do not invent an old component screenshot.

Resolve accepted oracle-showcase/holla/jackin/tablepro receipts, verify all source/tool/adapter/required-set pins, and join every namespace without duplicate/missing/checkpoint IDs. Require two independent component captures and repeat equality plus exact union coverage of every APP and COMP obligation. Include fade heights3/4/11/12,55%/80% rounding, non-RGB DIM, protected rows/cursor, styled-wide continuation and all retained-output mutation cases.

Source requirements:

- [docs/refactoring-plan/components.md](/work/docs/refactoring-plan/components.md).
- [docs/refactoring-plan/component-parity.tsv](/work/docs/refactoring-plan/component-parity.tsv).
- [docs/refactoring-plan/parity-synthesis.md](/work/docs/refactoring-plan/parity-synthesis.md).
- [docs/refactoring-plan/verification.md](/work/docs/refactoring-plan/verification.md).

The host binds the source documents, canonical historical union and scenario files named above into the immutable campaign input manifest. Their exact scenario expansion algorithms and source-qualified tests are required membership, not a best-effort glob. Rejected/deferred source clauses retain their disposition. A task-local candidate manifest cannot override this document or the protected host context.
