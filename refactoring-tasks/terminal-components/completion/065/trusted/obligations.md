# TASK-065 protected closure obligations

These clauses are planner-owned acceptance inputs. Freeze this file and the exact mapped historical rows before dispatch. Candidate output cannot amend them.

## Source authority

ARCH:A23; DEC:ADJ-02; HIST:A108; HIST:A110; accepted architecture §§16,47,48,73; main xtask/src/main.rs:3443,3596,3613,3666,3768,4464.

The full historical clauses, remaining work and proof requirements are included in source-obligations.tsv, with their original ledger locations. Architecture and decision IDs are namespaced; HIST:A23 is not ARCH:A23.

## Exact closure clauses

1. Reject renamed generic painters, unreachable component calls, live calls whose pixels are overpainted, and inert reference controls presented as editable. Exercise actual production update/draw roots for all four apps, not a source-token whitelist.

2. Reject private methods with self, &self, &mut self or typed self as configured-props constructors. Accept private free functions and associated functions without receivers. Require the same controlled/disabled configuration in both phases.

3. Negative fixtures cover duplicate same-ID construction, phase-specific values, absent source roots, parse errors and unresolved claimed reachability. Preserve the accepted dynamic-ID, unconfigured and test-fixture exemptions, each with a positive witness.

   This configured-props proof is explicitly intra-module/source-scan scoped, as accepted at pinned architecture lines 2193 and 8577. Receiver rejection does not grant authority for whole-program or cross-file call-graph analysis. Report the cross-file limitation honestly; unresolved claimed reachability inside the declared source boundary still fails. The reconciled HIST:A47 source row must preserve this same boundary before dispatch.

4. Compile valid external author examples and preserve legitimate Jackin rain/product art. Candidate-authored scanner output is checked against the protected independent mutants; neither a renamed copy nor an always-success scanner may pass.

5. All 361 application scenarios remain exact, all 54 reusable families retain their completed proof, and all already closed architecture contracts remain closed.

## Check selection and evidence

The host binds the complete all-app and component required set to this task context; no candidate subset selector is permitted. Direct and PTY captures use their separately sealed oracle lanes. CHK-004 compares every required completed product checkpoint. CHK-006 additionally enforces the primary closure outcome; CHK-006 runs the exact applicable accepted architecture/API/performance checks, CHK-005 executes and reconciles the full test inventory, and CHK-007 requires all independently checked products and post-run integrity. TASK-070/071/072 implement and independently qualify those operations before dispatch.

The accepted task-stage command inventory records exact cargo targets, profiles and immutable relocation rules. TASK-066 may produce the explicitly scoped render-target relocation only after independent old/new identity comparison; downstream contexts use the host-accepted relocation receipt. A candidate edit to tools/test-inventory does not itself redefine that authority.

This task strengthens enforcement and closes remaining production adoption; it does not authorize a new renderer or copying oracle widgets into applications. If the final application tree still needs a functional repair outside scope, return the failure to its owning task and reverify the dependency.

## HAR-020 finite accepted extraction dispositions

Source qualification is complete for this table: `7b27732a8c3c131760ec3438f641cb3c11343a42:COMPONENT_ARCHITECTURE.md:3927-3954`, accepted after `REFACTORING_STATE.md` at `a156054d` accepted I/J1–J13. The original `e81ca17b:docs/audit/app-audit.md:324-350` calls these proposals; that historical label does not leave current disposition open. Preserve source target vocabulary below as lineage; later accepted exact APIs, central `Ui::reference`, and the immutable application oracle govern implementation. In particular, #22's original broad request-bus proposal was narrowed explicitly, not adopted wholesale.

The rightmost column is a required future proof, not a claim that the source table or this audit executed production. CHK-006 must bind each numbered source item to actual responsible production symbols, caller paths, and an independently qualified ownership witness; CHK-005 runs its concrete behavior/negative cases and CHK-004 checks applicable completed app oracle checkpoints. All23 must remain present; a generic source-row PASS, declaration-only family list, or count cannot discharge them. TASK-065 verifies integrated ownership and scopes its existing parsed-boundary changes; missing component/app implementation returns to its existing responsible prerequisite owner, not an unbounded closure rewrite. No new library product protocol or oracle behavior is authorized.

| # | Source control | Accepted disposition | Source target(s) | Source target file(s) | Required production-owner proof (future execution) |
| --- | --- | --- | --- | --- | --- |
| 1 | `NavList` + `NavItem` (`showcase/pages/sidebars.rs:16-165`) | **move** | `NavList<'a,T,K,R>`, `NavListState` | `«tui»/components/nav_list.rs` | Trace real NavList rows to the shared collection owner; section/collapse/badge/disabled/current behavior and keyed actions; reject private registry/locate loops. |
| 2 | Shell nav sidebar (`showcase/app.rs:868-926, 461-492, 696-698`) | **compose** | uses #1 | `«showcase»/app.rs` | Actual shell consumes #1; sidebar navigation and mouse checkpoints use its regions/actions; reject a parallel sidebar key table or reverse-ID scan. |
| 3 | `static_field` (`showcase/pages/inputs.rs:65-106`) | **compose** | `TextInput` + `Field` inside `Ui::reference(Some(exact_target), …)` | `«showcase»/pages/inputs.rs` | Real TextInput/Field draw inside exact central reference scope; semantic state stays caller-owned, fake cursor/underline painter absent; unchanged oracle cells. |
| 4 | Button state matrix (`showcase/pages/buttons.rs:143-176`) | **compose** | `Button` × `Variant` × `ReferenceTarget` fixtures | `«showcase»/pages/buttons.rs` | Real Button variants/reference fixtures in the actual Showcase page; both prototype halves have explicit retained test lineage; reject example-only completion. |
| 5 | Showcase footer hint row (`showcase/app.rs:1018-1077`) | **compose** | `HintBar` + `HintLayer` | `«showcase»/app.rs` | Real HintBar derives effective metadata and preserves width/drop/badge/status semantics; reject handwritten shortcut painting. |
| 6 | `layout::{caption,rows,columns}` (`showcase/pages/mod.rs:120-168`) | **move** | `layout::rows`, `layout::responsive_columns`, `Part::HELP` caption style | `«tui»/layout.rs` | Real shared row/responsive-column allocation and HELP caption style; narrow/zero geometry matches oracle; reject copied app arithmetic. |
| 7 | State inspector panel (`showcase/app.rs:948-1012`) | **compose** | `Props` + `Runtime::diagnostics()`/`ring()`/`focus()` | `«showcase»/app.rs` | Real Props plus supported Runtime diagnostics/ring/focus readers; no app full-registry area scans; displayed inspection facts remain truthful. |
| 8 | `FilterEditor` (`tablepro/app.rs:99-109, 1368-1433, 1648-1736, 2051-2086, 2337-2468`) | **compose** | `Dialog` + `Form` + `Field<Select>`/`Field<TextInput>` | `«tablepro»/filter_editor.rs` | Real Dialog/Form/configured fields own filter hits/Tab/editor painting; FilterOp/SQL remain TablePro; upper Filter paste remains oracle consumed unchanged, not historical F08a proposed fix. |
| 9 | Status-segment priority dropper (`tablepro/tabs.rs:794-838`) | **compose** | `StatusBar` | `«tablepro»/tabs.rs` | Real StatusBar owns priority removal and narrow survivors; no second app while-remove-lowest-priority loop. |
| 10 | Plan-tree metric columns (`tablepro/tabs.rs:1774-1852`) | **move** | `Tree` `.row(…)` with `RowUi::columns` | `«tui»/components/tree.rs` + `«tablepro»/tabs.rs` | Real Tree row closure/RowUi columns owns metrics placement; app supplies metric data; no read-back-background paint-over. |
| 11 | TablePro identity strip (`tablepro/app.rs:2189-2282`) | **compose** | `StatusBar` with clickable `StatusItem` ids | `«tablepro»/app.rs` | Real StatusBar keyed items own visible strip hits/hover/actions; app retains connection/safety/scope/help consequences. |
| 12 | `modal_frame` (`jackin/screens/modals.rs:36-96`) | **move** | `LayerSpec` + `Panel`/`Frame` chrome | `«tui»/layer.rs`, `«tui»/components/panel.rs` | Real LayerSpec and Panel/Frame supply chrome/backdrop/geometry once; no app modal_frame barrier/dim copy; derived hints retain product text. |
| 13 | `FileBrowser` (`jackin/screens/modals.rs:117-563`) | **compose** | `Form` + `List` + `Field<Checkbox>` + `Dialog` | `«jackin»/screens/file_browser.rs` | Real Form/List/Checkbox/Dialog compose the browser; filesystem/Git lookup and source validation remain app-owned; no duplicate generic Tab/capture machine. |
| 14 | `ChoiceDialog` (`jackin/screens/modals.rs:569-783`) | **move** | `Dialog::choice(...)` | `«tui»/components/dialog.rs` | Accepted composed Dialog choice convenience plus RadioGroup/actions and Wizard stepper; real action eligibility/focus and oracle body geometry; exact later public API wins over obsolete type spelling. |
| 15 | `FormDialog` + `FormField` + `FieldKindW` (`jackin/screens/modals.rs:787-1541`) | **move** | `Form<'a>`, `FormState`, `Field<C>`, `FormAction` | `«tui»/components/form.rs` | Real Form/Field drives ordered visibility/drafts/error/action/scroll/clipping and Select layer; three former form engines do not survive as private alternatives. |
| 16 | `OpFlow` (`jackin/screens/modals.rs:1546-1943`) | **compose** | `PickerChain` (J8) | `«tui»/components/picker_chain.rs` + `«jackin»/screens/op_flow.rs` | Real PickerChain owns stage/back/retry/breadcrumb mechanics; app owns account/vault/item/field data and async effects; completed source-qualified trajectories preserve both. |
| 17 | `InfoDialog` (`jackin/screens/modals.rs:1947-2280`) | **move** | `Dialog::facts(...)` + `Props` + a scrollable detail slot | `«tui»/components/dialog.rs` | Real facts/Props/scroll detail through open Dialog body; no closed facts enum or post-construction button surgery; actual copy handling stays app-oracle-specific. |
| 18 | `HelpOverlay` (`jackin/screens/modals.rs:2284-2425`) | **move** | `HelpOverlay<'a>`, `HelpOverlayState` | `«tui»/components/help.rs` | Real HelpOverlay consumes binding metadata for multi-column round-robin/scroll/scope; source app content and visible spacing remain exact. |
| 19 | Host menu bar + `run_host_menu` (`jackin/app.rs:699-813`) | **split** | item list stays domain; `MenuItem.action: ActionKey` + `.chord(Chord)` move to the library | `«tui»/components/menu.rs` + `«jackin»/app.rs` | Real typed MenuItem ActionKey/chord and key path resolve same effective metadata; item list/product routing remains Jackin; key synthesis rejected. |
| 20 | Master-detail + draggable seam (`manager.rs:114`, `accounts.rs:96`, `inspect.rs:111`, `showcase/pages/terminal.rs:104`) | **move** | `SplitPane` | `«tui»/components/split.rs` | Real SplitPane owns seam geometry/capture and narrow collapse; app owns master/detail content; duplicate seam-container reconstruction rejected. |
| 21 | "Terminal too small" screen (`showcase/app.rs:802`, `tablepro/app.rs:2121`, `jackin/app.rs:2283`) | **move** | `TooSmall<'a>` | `«tui»/components/too_small.rs` | Real TooSmall owns generic minimum-size notice; each app retains its exact copy and reachability; duplicate generic minimum-screen painter rejected. |
| 22 | `PageCtx` / `Cx` request bus (`showcase/pages/mod.rs:74-98`, `tablepro/app.rs:51-84`, `jackin/screens/mod.rs:180-227`) | **move (partially)** | `Cx` (focus, layers, capture, repaint, intents, area) in the library; the `Request`/`Go`/`Status` payload stays per-app | `«tui»/ui/cx.rs` + `«showcase»/pages/mod.rs`, `«tablepro»/app.rs`, `«jackin»/screens/mod.rs` | Only generic Cx focus/layer/capture/repaint/intent/area services move to library; Request/Go/Status payload enums and domain interpretation remain per app. Reject both app-owned generic focus/hit engines and library-owned route/product request enums. |
| 23 | `InspectChanges` diff modal (`jackin/screens/inspect.rs:61-89`) | **keep domain** | composes `Tree` + `DiffView` + `SplitPane` | `«jackin»/screens/inspect.rs` | Inspect keeps compact/advanced mode and region focus domain-owned while Tree/DiffView/SplitPane own generic mechanics. Preserve actual oracle copy-effect handling, including F09 discarded copy event and retained modal; no proposed product-effect fix. |
