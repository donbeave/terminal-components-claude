# TASK-024 concrete production witness obligations

Normative under R-001/R-002/R-003; adds required cases and never replaces the complete source-obligation or oracle inventory. The host freezes each row's source-qualified case identities, values, action sequence, observed fields and exact source function/range before dispatch. Main source pin is `7b27732a8c3c131760ec3438f641cb3c11343a42`; oracle pin is `02f5294bfdbf38004cc49130d0aff1d01f31434c`. Resolve actual source/test identities through this package's trusted family rows; a missing source branch is a failed setup, not applicability chosen by the executor. Historical proposals use their recorded authority disposition, not invented oracle output.

Each case executes real production update/draw against caller-owned inputs. CHK-004 compares independently captured oracle behavior where the source map assigns parity. CHK-006 proves ownership through independently qualified TASK-072 Rust witnesses; CHK-005 executes concrete regression and mutant outcomes. For every applicable public named slot, the host binds a row that actually paints it: observe real Id/Family/Variant/Part/Resolved, component versus caller-row provenance, source resolution/cell-write branch, changed intended cells, unchanged geometry/hits/focus and unrelated cells. Measurement-only queries cannot satisfy a painted-slot witness. A style-unreachable row must name the exact source guard and another reachable row; no generic all-states label or declaration-only union proves coverage. Run inherited CP-COMMON theme/capability/override and degenerate-size axes without weakening this concrete case set.

Mutants below are real isolated production-source changes independently applied/judged, never executor-authored booleans or expected-output rewrites. Every mutant must compile and reach the intended branch before its specific rejection counts; a build failure is not the behavioral witness. Draw twice without input, then change the named source fact: durable state stays fixed on repeat draw and only source-owned dependent observations change on update. Shared prerequisite defects return to the scoped owner rather than widening this package.

Source shorthand `M:foo.rs` means `crates/tui/src/components/foo.rs` at the pinned main; `M:keymap.rs` means `crates/tui/src/keymap.rs`, and explicit `ui/` or `collection/` subtrees are relative to `crates/tui/src/`. `O:` paths are at the pinned oracle. Line anchors identify audited starting branches, not candidate line numbers. App-composition rows retain the exact task-family source-qualified applicability and cannot invent a historical standalone widget.

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-024-01 semantic rows | non-Clone/non-Display source; distinct AsItem vs custom RowFn text; searchable and nonsearchable rows | query uses semantic AsItem and returns original source index/key; renderer cannot redefine identity | filter rendered label or require owned clone | M:picker.rs AsItem:119; M:filter_list.rs update:720/draw_rows:792; O:src/widgets/picker.rs |
| W-024-02 query paste | ASCII, combining, CJK query; paste then backspace one grapheme; narrow resize | original-grapheme match indexes and exact query bytes/caret; row paint and semantic match agree | match normalized byte offsets as original indexes | M:filter_list.rs update:720; M:picker.rs update:624; O:src/widgets/picker.rs |
| W-024-03 readiness | FilterList and Picker separately Empty/Loading/Partial/Error: nonempty title, Some(detail), nonempty Loading/Partial spinner frames, default/Set Error glyph; separately None(detail), empty frames, Error Clear and Empty-without-icon | actual inherited owner ID/provenance and truthful composed EMPTY family; TITLE/HELP/ICON sentinel credit only at their real reachable sinks; Empty ICON and other source-unpainted controls get no paint credit; projected selection valid and empty never manufactures an item | measure-only inherited style, falsely credited Empty ICON or selectable placeholder | M:filter_list.rs draw:880; M:picker.rs draw:692; M:collection/empty.rs:126–220, exact glyph/detail guards:153–213 |
| W-024-04 palette identity | available/unavailable/destructive action; reorder source between publication and completed activation | stable target and unavailable guard survive; destructive typed action unchanged | invoke replacement index or decorative unavailable state | M:picker.rs update:624; O:src/widgets/picker.rs |
| W-024-05 picker chain | advance, back, stage error, retry; retained query and breadcrumb at each checkpoint | caller-owned stage/data/job identity retained; exact prior query restored; no component domain work | discard query or retain stale stage row | M:picker_chain.rs back:108/update:229/draw_readiness:421; applicable app-stage oracle source in trusted family row |
| W-024-06 completion routing | popup open: Arrow/Tab/Enter/Esc/remap/paste; insert text supplied vs empty fallback; cancel | exact editor splice bytes/caret; empty insert text falls back to label under source contract; cancel leaves editor unchanged; runtime owns popup capture/placement | second editor grammar or synthetic semantic selection | M:completion.rs CompletionController:162/update_for:541; O:src/widgets/completion.rs |

## Branch-source finite cases

| Case | Concrete state/action | Required observation | Reject real mutation | Audited source witness |
| --- | --- | --- | --- | --- |
| W-024-07 independent completion width | Two semantic rows with display widths(label,detail)=(20,0),(1,20), then reversed declaration order; same-row maxima control; screen supports48 cells, first open and resize, terminal edge anchor, below/above placement and overflow rows; reopen versus same-open refresh | Source desired width48 for cross-row maxima before clipping, not29; exact detail visibility, column cells, hit rectangles, scrollbar reservation and editor focus. Reordering cannot change measured width. Reopen follows source reset while refresh retains only source-authorized state; wheel survives pure draw. RowFn cannot redefine semantic measurement | maximize per-row sum, fix test with app rectangle, retain stale cached size, steal editor focus, or reset wheel in paint | O:src/widgets/completion.rs:156–230 and open lifecycle; M:completion.rs measured_size/draw/update_for |
| W-024-08 typed keyless query submission | Query-submission capability off/on; searchable versus nonsearchable; no rows/all-disabled rows/current eligible row; query empty, whitespace and nonblank; Enter and Alt+Enter; separate oracle Ready-with-no-rows, Loading and Error fixtures, plus modern Partial/disabled/hidden-query controls with explicit non-oracle applicability | Default/off retains existing generic Ready/Loading/Error/Partial behavior. Enabled source-searchable nonblank no-eligible case emits exactly one typed Submit with no item key/index and unchanged caller-owned query; both Enter variants take same keyless branch. Eligible rows retain keyed normal/alternate actions; blank/nonsearchable/disabled controls emit no Submit. Oracle eligible() admits rows only in Ready: Loading/Error with searchable nonblank query therefore take keyless Submit despite their stale Enter-refused comments; no keyed row activates. Modern Partial is consumed with no action for both capability states; no historical PickerStatus counterpart authorizes Submit. The additive enum compiles through Showcase's exhaustive no-op Submit arm without enabling capability or changing result/detail/level/layer state; deferred F01 remains unselected | consume absent item before source fallback, fabricate index0, emit both Submit and keyed action, synthesize raw Enter in app, or leak hidden query | O:src/widgets/picker.rs:278–292 and eligible; M:picker.rs PickerAction/update and filter_list.rs FilterListAction/item_action; TASK-042 Files jump consumer |
