# Protected task inputs for TASK-003

Product: `oracle-holla`.

Implement the Holla reference adapter and expand every HO-BASE, HP, ROUTE and explicit shell/finder/files/review/activity/disk/cleanup/snapshot/lifecycle row. Preserve all finite snapshot kinds and emitted parameters, every HP01–15/17–23 simulated capability, and all recorded route prefix expansions. Capture complete page/tab/modal/focus/hit/edit/selection/scroll/argv/cwd/host/world state at each declared checkpoint.

Use original App/World/Clock with 80 ms tick ordinals and simulated filesystem/provider/outcome/usage/trust/log stores. Expand OPEN/RUN/SETTLE/ROUTE/SWEEP through exact pinned helper source and bounded oracle event traces. Distinguish private state direct observations from PTY terminal effects. True modals capture paste; F10 MenuBar preserves source passthrough to focused Args, including auto-begin for an enabled idle field through the shared configured field operation. Preserve the separate editing/idle branches, typing-hot query, Ctrl+G, Ctrl+Up/Down, Alt+Enter/Alt+digit, and query-before-scope Escape.

Execute every original Holla test assertion, all 143 binary tests as source-qualified inventory and owned PTY lifecycle/color/flood gates. Repeat all numeric traces in fresh worlds with identical complete frames and semantic records. Include pointer edges/down-up/outside/capture, 72x20 threshold, body-width108 finder split, output retention, two-stage gates, secret stdin and cleanup settlement.

Source requirements:

- [docs/refactoring-plan/holla.md](/work/docs/refactoring-plan/holla.md).
- [docs/refactoring-plan/holla-scenarios.tsv](/work/docs/refactoring-plan/holla-scenarios.tsv).
- [holla-trace-corrections.md](/task/trusted/holla-trace-corrections.md): all sixteen named source-correct branches are normative; four preview, four Args and eight editing/idle overlay branches.
- [docs/refactoring-plan/verification.md](/work/docs/refactoring-plan/verification.md).

The host binds the source documents, canonical historical union and scenario files named above into the immutable campaign input manifest. Their exact scenario expansion algorithms and source-qualified tests are required membership, not a best-effort glob. Rejected/deferred source clauses retain their disposition. A task-local candidate manifest cannot override this document or the protected host context.


## Native, resized and staged baseline products

R-001/AC-001/CHK-004 requires the complete products in `holla-route-expansion.md`, `holla-route-sites.tsv`, `holla-stage-audit.tsv`, `holla-stage-contract.md` and `stage-contributions.tsv`. The site inventory is a protected starting inventory, not permission to omit additional reachable assertion/coordinate helper sites. R-002/AC-002/CHK-006 requires independently qualified source-only mapping authority. R-003/AC-003/CHK-005 preserves every unchanged native source assertion at its actual dimensions and requires the real fixed-footer and pointer qualifier plus its rejection controls. No fixed-row assertion is falsely claimed evaluated unchanged at another height. Resized captures retain complete cells/cursor/state and use a separate exhaustive protected native-to-resized map. The host seals exact numeric nonempty early contribution checkpoints and state schemas; a contribution never closes a parent. Full parent assertions, events and unmasked frames remain required at the audited later primary owner.
