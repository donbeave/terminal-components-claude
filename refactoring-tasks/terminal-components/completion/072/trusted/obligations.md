# Protected TASK-072 operation boundary

ADJ-13 additionally requires independent qualification of the exact backend-only signal-broker exception before TASK-009 dispatch. Parse the whole core source, effective cfg/module/visibility and type aliases; admit only private SIGNAL_BROKER: OnceLock<Mutex<SignalBroker>> in runtime/session.rs under cfg(all(unix, feature="crossterm")), whose fields are only inactive/pending Arc<AtomicBool>, lease bit and two optional signal registration IDs/install phase. A read-only constant-data positive stays accepted. Required independent negatives: second singleton; renamed static Mutex/AtomicBool; alias-wrapped mutable state; Runtime/Theme/cache field; public visibility/accessor; wrong path; missing Unix or feature guard; function-local/thread-local variant; malformed or unresolved claimed source. A whole-file allowlist or the old regex's failure to see static Mutex cannot pass. Test exact source mutation contrasts and positive recovery, plus isolated backend-free dependency/symbol absence. The future TASK-009 production scanner integration consumes this qualified policy; do not require its future receipt or claim the caller-owned lifecycle prototype proves singleton behavior. Existing source-bootstrap and style-timing qualifier commands and artifacts remain unchanged by this clause; a new required policy case needs independently frozen qualification before dispatch, not an assumed prior PASS.

The exact frozen `holla-trace-corrections.md` additionally binds all sixteen named preview, raw Args and idle/editing overlay-paste branches. Original HO parent identities remain intact. Source Ctrl+C process exit, input raw-kind versus parsed fallback, F10 pass-through and true-modal isolation are distinct required observations; no generic modal-first-paste claim may replace them. These source corrections are independently captured by TASK-003 and remain immutable required membership through source-runner, accounting, architecture qualification and final closure.

Product: `architecture-verifier`. Independent qualifier: group072 of runner-bootstrap-driver.py.

CHK-011 additionally invokes the independently frozen architecture-bootstrap driver and protocol. The Rust model and actual pinned production corpus are both mandatory, with positive controls, exact mutation contrasts, real compiler/runtime evidence and external protected-observer transport. The corpus must remain independent of the future TASK-073 API. Its source archive and every transitive fixture/tool identity are bound by the host before execution.

CHK-006 additionally invokes architecture-bootstrap-source-driver.py under architecture-bootstrap-source-protocol.md. Actual public-TC compiler witnesses qualify private/missing/wrong-arity references, both CHECKS heading forms including later/future sections and ignored Rust fences. Real locked Cargo metadata qualifies all dependency roots and direct/transitive forbidden application paths. Exact covered controls and mandatory independent production source review are listed by that protocol; a shared check ID is not evidence that an unlisted control ran.

CHK-012 invokes the separately protected style-timing-bootstrap corpus. Its actual pinned source paths, whole Showcase frame denominator, three interleaved timing modes, complete entry census, raw measurement authority and negative controls qualify TASK-073's future seam independently. It neither consumes that future receipt nor claims TASK-067's final performance threshold has passed.

The protected app-flow, Holla stage/route and shell contribution contracts/tables bind exact semantic contributions to actual production handlers and protected observation schemas. They do not authorize test-only business logic, copied rendering, invented initial states, or frame comparison by a semantic assertion checker. Actual source-owned setup must qualify direct seeds before their observations can count.

Implement architecture from proof-contract.md using immutable per-task source obligations and qualified test/source-check profiles. Qualify whole-source parsing, missing/empty roots, forbidden dependency/public API paths, receiver-bearing Props helpers, copied/renamed painters, inert/dead component calls and paint-over witnesses against independent fixtures. Preserve legitimate external author components and application-owned art. Distinguish attributed PARTS resolution, documented painted-slot equality and source registry completeness; TASK-073 supplies production observation infrastructure and TASK-031 closes the full real registry.

Separate the independent verifier implementation/fixtures from later production scanner and component repairs. Stage-specific known unfinished architecture obligations retain exact owners and truthful failure records; they are never silently passed. In-scope already completed invariants and the task's own fixtures must pass. A narrow syntax scan cannot claim whole-program reachability or API semantics; pair explicit scan scope with executed production phase/action tests.

Pass every group072 independent fixture and recovery positive, including malformed syntax, empty claimed root, omitted registry member, subset-only PARTS acceptance, false component-versus-row attribution, ignored slot, self/borrowed/typed receiver forms, unreferenced Props helper, duplicate same-ID construction and live paint-over. Reject fixture/allowlist/profile substitution and source/task/tree mismatch. Preserve existing runner/accounting/comparator behavior when adding dispatch.

Every exact source clause in source-obligations.tsv remains binding under its mapped requirement. The independent driver, fixture app, worker and observer are immutable judge inputs, never candidate implementation or successful product evidence.
